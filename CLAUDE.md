# CLAUDE.md — постоянный контекст проекта repracer

## Что строим

Мультитенантный SaaS, который для продавца:

1. **Синхронизирует остатки** из внешнего источника между каналами продаж. Мы **не владелец остатка**:
   общий пул + буфер на канал.
2. **Автоматически меняет цены** по стратегиям, не опускаясь ниже обязательного абсолютного `min_price`.
3. **Ведёт историю наших цен по каждому каналу** для Omnibus и §11 PAngV.

## Для кого

Профессиональные продавцы (SMB и mid-market) на нескольких маркетплейсах. Пользователь может состоять в
нескольких тенантах, например агентство.

Вне скоупа: создание листингов, выполнение заказов, бухгалтерия, хранение PII.

## Статус

- **Шаг 29: живой прогон через консоль, разделение CI, комиссия от продавца** [Р-136…Р-138] — всё, к чему прикасается продавец, проверяется живым прогоном ЧЕРЕЗ КОНСОЛЬ, а не вызовом хранилища [Р-136]: `apps/console/test/console-live.pg.test.ts` поднимает НАСТОЯЩИЙ HTTP-сервер стенда и ходит по нему как браузер на каталоге целевого клиента (10 000 предложений); у каждой операции утверждается время и размер ответа (предел экрана — 10 с и 8 МБ). Прогон нашёл семь операций, не выдержавших объёма: экран комплаенса — 24,8 с и 8,7 МБ (глубину истории Omnibus спрашивал у базы по разу на предложение), список товаров — 10,2 МБ, лента — 2,3 МБ, ответ на запись стратегии — 3,8 МБ, применение импорта — 11,7 с, а массовая правка границ и стратегия на каталог отказывали молча (пределы 500 и 200 не назывались). Исправлено договором экранов: страницы на сервере с итогами по всему каталогу, дорогие вычисления только для показанного, ответ на запись — страница, названные пределы, выборка предпросмотра стратегии [Р-125] и поиск предложения. Ревью шага нашло главное: первая редакция прогона звала обработчик напрямую, мимо HTTP-слоя, и не видела, что правка каталога и стратегия отвечают **413** (список 10 000 идентификаторов — 381 КБ); HTTP-слой вынесен в `createStandServer`, «весь каталог» стал выбором `all: true`, который раскрывает сервер ([docs/evidence/step29-console-live.md](docs/evidence/step29-console-live.md)); CI разделён на быстрый (каждый коммит: типы, юнит-тесты, PostgreSQL) и полный (слияние в main и по запросу: мутации [Р-95], брокер, ClickHouse, развёртывание), задания параллельные, отложенные тесты названы поимённо с причиной в `scripts/test-scopes.mjs` [Р-137]; комиссия из файла продавца — свой источник `SELLER_DECLARED` (0106), не смешивается с тарифной таблицей [Р-32], пол маржи считается по САМОЙ ДОРОГОЙ действующей оценке (сравнение по ставке было неверным: «0 % + 5 €» дороже «10 %» на цене ниже 50 €), экран границ показывает обе с источниками [Р-138]; кодировка файла — по BOM и содержимому, Windows-1252 разбирается, при неуверенности продавца спрашивают (OQ-200 закрыт); гардрейл уровня тенанта попал в окно массовой правки (0106, задача D); OQ-197 закрыт; ревью — 23 находки, 17 исправлено (в том числе: «выбрать всё» означало «выбрать страницу», выбор не сбрасывался при листании, доказательство Omnibus было недоступно для 98 % каталога, неполные оценки комиссии помечались использованными, страница за концом списка показывала «10001–10000»), 6 вынесены в OQ-201…OQ-205 ([ревью](docs/evidence/step29-review.md)); отдельный прогон по тавтологиям нашёл 15 слабых утверждений, 4 исправлены, остальные — OQ-204; **три живых прогона имели скрытым входом время суток** — закрытие суток даёт час после полуночи витрины, а секции журнала снимков идут по суткам UTC: тесты краснели час в сутки и каждый ночной прогон CI, теперь их окна привязаны к часу; CI [35412198609](https://github.com/mehmetkerim00/repracer/actions/runs/35412198609): быстрый прогон — 14 м 39 с (526 тестов), полный — 29 м 25 с (532 из 532 тестов, мутаций 347 из 347, три развёртывания ответили на `/healthz`) ([docs/evidence/step29-mutation.md](docs/evidence/step29-mutation.md)); [ADR-0029](docs/adr/0029-console-live-run-and-split-ci.md).
- **Шаг 28: массовый импорт себестоимости, повтор внутренних работ, проверенное развёртывание** [Р-133…Р-135] — импорт себестоимости как часть онбординга (0103): `packages/cost-import` читает CSV и XLSX без новых зависимостей (XLSX — ZIP с XML, `node:zlib`, с пределами распаковки), предлагает колонки по заголовкам и значениям и **даёт исправить выбор**, показывает предпросмотр с отпечатком и отдельно — непримененные строки с причиной у каждой группы; применение целиком, второй фактор обязателен [Р-134, Р-135]; живой прогон [Р-130]: 10 000 строк с 15 % ошибок — 8571 строка на 8571 оффер, 1430 не сопоставились по шести причинам, 1429 офферов остаются без себестоимости, 11 секунд ([docs/evidence/step28-cost-import.md](docs/evidence/step28-cost-import.md)); риск 17 (открыт с шага 21) закрыт двумя защитами — импорт неделим (строки живут только в транзакции своего пакета) и окно массовой правки (больше пяти предложений за десять минут без второго фактора — отказ), у версий цены и себестоимости признак `created_with_mfa`, ставит его база; OQ-144 закрыт; Р-133 — вид повтора работы в базе (0104): у внутренних работ пауза растёт от минуты до часа, OQ-193 закрыт; внешняя отметка у диспетчера и приёмника [Р-127], OQ-194 закрыт; секрет — только из файла, значение переменной окружения принимается лишь в режиме стенда; **развёртывания впервые запущены** (задача D): первый запуск показал, что не стартует ни один из трёх процессов — выключатель отметки не доходил до контейнера, каталог секретов был недоступен процессу, приёмнику не хватало файла ([docs/evidence/step28-deployment.md](docs/evidence/step28-deployment.md)); ревью шага — 24 находки, 21 исправлена (в том числе: риск 17 был объявлён закрытым преждевременно — у версии себестоимости не было стража «время версии — время транзакции», и окно обходилось датой задним числом; гардрейл уровня тенанта менял пол маржи у всех офферов без второго фактора; импорт больше ~2 180 строк не доходил до сервера; `10.505` и комиссия `0,15` читались молча и неверно; книга XLSX разворачивалась из 306 КБ в 300 МБ; три тавтологии в тестах), 3 вынесены в OQ-198…OQ-200 ([ревью](docs/evidence/step28-review.md)); CI [35385427369](https://github.com/mehmetkerim00/repracer/actions/runs/35385427369): 512 из 512 тестов, мутаций 342 из 342 ([docs/evidence/step28-mutation.md](docs/evidence/step28-mutation.md)); [ADR-0028](docs/adr/0028-cost-import-internal-retry-deployment.md).
- **Шаг 35: путь «только остатки», экраны остатков, рост данных** [Р-152…Р-154] — ПЕРВЫМ сделан Р-154, потому что без него остального было бы не видно: состояние консоли читало ВСЕ решения тенанта на каждый запрос любого экрана (19 запросов без предела), и демо-стенд жил часы вместо суток. Теперь состояние ограничено КАТАЛОГОМ, а потоки — решения, намерения, история записей, отклонённые снимки, аудит — читаются узкими методами хранилища: страница решений и её итог, одно решение по идентификатору, статистика только показанных строк, окно вмешательств (решения не «без изменения»), страница ленты со счётчиками групп, последние события аудита, счётчики списка миров одним агрегатом; под каждый запрос — индекс (0117), и частичный индекс вмешательств отсекает девять десятых буфера решений ещё в индексе; **демо-стенд прожил виртуальные СУТКИ**: 143 900 решений, 3476 подтверждённых каналом записей цены, 0 провалов работ, 32 м 53 с настоящего времени, и после них самый медленный экран — 1,4 с при пределе 10 с, самый тяжёлый ответ — 99 КБ при пределе 8 МБ ([docs/evidence/step35-demo-day.md](docs/evidence/step35-demo-day.md)); OQ-214 закрыт, OQ-215 тоже (прогон суток — своим заданием CI); **путь «только остатки»** [Р-152]: онбординг предлагает два пути, выбор — намерение продавца (единственное, что добавилось к хранимому), шаги остатков выводятся из данных теми же правилами (источник «есть», когда ОТДАЛ остаток; синхронизация «включена» там, где есть единица записи QUANTITY), права разошлись — остатки ведёт тот, кто ведёт каталог, и менеджер остатков цен не касается; база под остатки была готова с шага 2 (0007, 0020) и два года ждала конвейера — написаны `packages/stock-sync` (правило публикуемого количества: общий пул минус буфер канала [Р-6]), `PgStockStore`, Inbound API (`/inbound/v1/stock`, ключ показывается один раз, в базе — префикс и SHA-256) и вид задания `STOCK_IMPORT`; живой прогон через консоль как браузер: **девять действий продавца и 2,7–3,1 секунды** от пустого пути до записи остатка, ПОДТВЕРЖДЁННОЙ каналом (до исправления — 11,6–24 с: сразу после загрузки каталога у таблиц нет статистики, и каталожный запрос состояния консоли шёл тройным вложенным циклом, 8 млн проходов на 200 предложениях, — первая запись продавца отвечала до 11,6 с), без единого ввода себестоимости и без единого упоминания стратегий (утверждается буквально — по всему ответу экрана) ([docs/evidence/step35-stock-path.md](docs/evidence/step35-stock-path.md)); **экраны остатков** [Р-153] показывают по каналу три РАЗНЫХ числа рядом — посчитанное к публикации, отправленное и подтверждённое обратным чтением, — расхождения «у нас / в канале» отдельным списком, а ловушки каналов (остаток Amazon EU на весь регион [Р-1], Kaufland общий на `id_offer` [Р-35]) названы ДО первой записи из возможности канала; на каталоге целевого клиента включение синхронизации 10 000 единиц оказалось 33-секундным запросом — стало заданием [Р-139], а список расхождений с 5,7 с упал до 0,93 с (сперва кандидаты по водяным знакам, подробности — только по ним); у симулятора появился СПРОС (заказы, отгрузки, отмены) и работа планировщика `order-lines`: заказ канала становится резервацией [Р-25], отгрузка списывает пул движением базы, уменьшение доступного уходит в канал записью — впервые проверено, что резервации и пулы шага 2 работают вместе; OQ-216 закрыт (обход предложений ждёт до `retryAt`, если он внутри срока вызова); найден дефект схемы, живший с шага 8: проверка «цена равна решению» стояла ОДНИМ выражением с `field = 'PRICE'`, и с ШЕСТОГО исполнения в сессии PL/pgSQL берёт общий план — роль остатков [Р-105] упиралась в право читать `price_decision` на шестой записи количества (смоук вставлял пять); открыто: резервации Inbound API некому подтверждать (OQ-217), возврат не возвращает остаток (OQ-218), записи остатка идут по одной (OQ-219), заказ, списанный каналом самим, виден только со следующей записи (OQ-220); ревью шага — 23 находки (3 критичных, 10 существенных, 10 мелких), исправлены все: страница решений по предложению отвечала 500 ВСЕГДА, столбец «последнее изменение» стал пустым у всех, Inbound API писал в тенанта мира, а не ключа, расхождения остатка имели ТРИ определения и ни одного положительного утверждения, функция освобождения резерваций по сроку существовала с шага 2 и её не звал никто; семь исправлений первой редакции остались без тестов, и дописанный тест нашёл изъян в исправлении ([ревью](docs/evidence/step35-review.md)); прогон суток нашёл ещё три дефекта — опрос конкурентов проходил таблицу движений целиком на каждый товар (индекс по товару: сутки с 2,5 часов до 33 минут), чтение заказов роняло такт на бюджете канала (OQ-216 в новой форме — правило ожидания вынесено в порт), а провалившийся такт уносил своё окно и терял заказы навсегда; мутаций локально 405 из 405 своей проверкой ([docs/evidence/step35-mutation.md](docs/evidence/step35-mutation.md)); CI [35774371932](https://github.com/mehmetkerim00/repracer/actions/runs/35774371932) — **зелёный целиком**: быстрый 16 м (595 тестов), стражи 5 м, полный 35 м (601 тест, мутаций 405 из 405), замер сжатия 5 м и новое задание суток 49 м (24 виртуальных часа, 143 900 решений, 0 провалов работ, экраны 0,04–0,68 с); [ADR-0034](docs/adr/0034-stock-path-screens-and-bounded-console-state.md).
- **Шаг 34: онбординг и демо-тенант** [Р-149…Р-151] — онбординг стал направляемым путём: шесть шагов (тенант → канал → себестоимость → границы → стратегия → включение), состояние каждого ВЫВОДИТСЯ из данных функцией базы `tenant_data.onboarding_status` (0115), а не ставится галочкой; хранится только сужение набора — пропустить себестоимость нельзя [Р-131], можно сузить путь до предложений, у которых она готова; включение набора — задание `REPRICING_ENABLE` со своим правом `ENABLE_REPRICING` [Р-143]: оператор включает движок, хотя цен не правит, а право вида — одно на создание и отмену и без ветки «иначе» (правило схемы 14е, 0116); канал без доступов — состояние `AWAITING_ACCESS` с обязательным перечнем того, чего ждёт, кодами из списка [Р-150], операция без канала отвечает `409 NO_CHANNEL`, а не 500; демо — клиентский тенант с признаком `tenant.demo`, заданным при создании, на симуляторе шага 21: 200 предложений, ровно три конкурента (дрейф, подрезчик, волны), метка в списке миров, на каждом экране и в ФАЙЛАХ — её ставит единственный путь выгрузки [Р-151]; **онбординг прошёл целиком живым прогоном как браузер** ([docs/evidence/step34-onboarding-live.md](docs/evidence/step34-onboarding-live.md)): от пустого пути до первого решения с объяснением — 9000 решений и 762 подтверждённые каналом записи цены за два виртуальных часа, пять шагов «почему эта цена» без единого «нет данных»; споткнулся восемь раз, пять — дефекты продукта: шаг себестоимости был зелёным, а включение отказало всем 150 (критерий шага ≠ критерий включения — теперь равносильность утверждает тест на четырёх случаях), отказы включения были видны только в базе, список решений — 5,8 МБ одним ответом, экран остановки без канала — 500, часы мира в прошлом делали ввезённую себестоимость «ещё не действующей»; пустой тенант — одиннадцать экранов отвечают пустыми списками с итогом ноль и объяснением на обоих языках (задача D); история git с путями разработчика записана принятым риском 35, не переписана; **ключ healthchecks.io в задании шага подставлен не был** — письма при остановке трёх процессов по-прежнему не проверены ни разу (OQ-188, OQ-198); ревью — 13 находок, 12 исправлены, 1 частично ([ревью](docs/evidence/step34-review.md)): шаг заявлял больше, чем делал, — «своё право включения» было недостижимо через консоль, отказанные предложения не рисовал ни один компонент, признак демо в базе никто не читал, демо-режим стенда не запускался ни разу, правило 14е пропускало ровно тот случай, ради которого написано; исправление «числа утверждаются, а не печатаются» нашло провал работы планировщика, который был в выводе прогона с первой редакции и которого никто не читал (OQ-216: обход предложений проваливается на клиентском бюджете адаптера вместо ожидания); открыто: регистрации тенанта и подключения канала из консоли нет (OQ-213), экраны читают все решения тенанта на каждый запрос — демо-стенд пригоден часы, не сутки (OQ-214); мутационная проверка — 397 из 397 своей проверкой ([docs/evidence/step34-mutation.md](docs/evidence/step34-mutation.md)), причём первый прогон после исправлений по ревью дал 1 непойманную: положительный контроль, добавленный против беззубого правила, сам был беззубым (`bad || 'текст'` — «malformed array literal») и зеленел на словах внутри побочной ошибки; первый прогон CI [35667111458](https://github.com/mehmetkerim00/repracer/actions/runs/35667111458) — полный, стражи и замер сжатия зелёные, быстрый красный одним тестом: экран пути демо-тенанта на раннере 10,98 с при пределе 10, потому что ждал всех решений тенанта ради шести счётчиков (OQ-214 в действии) — отвязан, 0,003–0,028 с; второй прогон CI [35693968021](https://github.com/mehmetkerim00/repracer/actions/runs/35693968021) — **зелёный целиком**: быстрый 17 м 33 с (571 тест), стражи 5 м 21 с, полный 36 м 23 с (мутации, брокер, ClickHouse, запуск трёх развёртываний); [ADR-0033](docs/adr/0033-guided-onboarding-demo-tenant-awaiting-access.md).
- **Шаг 33: внешний контроль, уборка отложенного, чистка публичного репозитория** [Р-148] — репозиторий публичный, и логи доказательств больше не несут путей машины разработчика: из шести логов убрано 406 строк с домашним каталогом и рабочим каталогом сессии, правило смотрит ВЕСЬ репозиторий и знает `~/`, `/var/folders`, адреса частных сетей; путь раннера сборки разрешён намеренно. Решение относится к рабочему дереву, а не к истории git — прежние коммиты эти пути сохраняют, вычистить можно только перезаписью всех 101 коммита, и это отдельное решение владельца; **OQ-183 закрыт правилом**: свойство «внешний контроль покрывает все процессы» держалось перечислением трёх, и четвёртый мог появиться молчащим — теперь правило берёт точку входа из compose самого развёртывания; **аккаунт внешнего сервиса завести не удалось** (регистрация идёт через письмо на почту владельца), поэтому письмо при остановке процесса не проверялось ни разу и заявить, что Р-127 работает, нельзя: проверено, что процесс ОТПРАВЛЯЕТ отметку, а не что её отсутствие кем-то замечается — подготовлен `scripts/setup-heartbeats.mjs` (после ключа одна команда, факты API из снимка страницы, секрет через stdin) ([docs/evidence/step33-heartbeat-live.md](docs/evidence/step33-heartbeat-live.md)); **OQ-204 закрыт**: мета-проверки «у каждого механизма есть утверждение» считали ЗАРЕГИСТРИРОВАННОЕ ИМЯ, а не выполненное утверждение — пустой блок проходил их так же, как проверяющий; `jobOf` отдавал заглушку `{runs:0,failed:0}`, и «провалов не было» было истинно для работы, которая ни разу не запускалась; плюс `every` на пустом списке, длина строки вместо содержания, ранги из собственного результата, «принимаются оба исхода»; **OQ-196 сужен**: за отказом «нужен второй фактор» стояли ДВА правила, и продавец, честно поправивший шестое предложение подряд, читал совет про операцию, которой не делал, — у окна свой код, текст DE/EN и проверка настоящим отказом базы; само число (5 за 10 минут) остаётся названным ограничением продукта; OQ-211 и OQ-197 проверены — закрыты шагами 32 и 29; **реестр принятых рисков пересмотрен впервые с шага 19** ([docs/evidence/step33-risk-review.md](docs/evidence/step33-risk-review.md)): 39 пунктов по коду — закрылись 3 (22, 29, 31), устарел 1, сужен 1, остаются 30, переписано 10 формулировок (пункт, описывающий несуществующий механизм, хуже отсутствующего; худший — риск 24, который при беглом чтении схемы выглядит закрытым); ревью шага — 11 находок, все закрыты, в том числе: новый код `MFA_REQUIRED_WINDOW` давал неверный текст в двух случаях из трёх (тот же дефект, что чинил OQ-196, в новой форме), ветка не была проверена ни одним тестом, `report.lies === LIES` — тавтология, добавленная задачей про тавтологии, правило отметки обещало «каждый процесс», а смотрело `services/` ([ревью](docs/evidence/step33-review.md)); новое правило «ссылка из документации ведёт на существующий файл» — на этом же шаге три документа стали вести в никуда; CI [35479462995](https://github.com/mehmetkerim00/repracer/actions/runs/35479462995): **зелёный целиком** — быстрый 15 м 25 с, стражи 4 м 46 с, полный 32 м 37 с (мутации, брокер, ClickHouse, запуск трёх развёртываний).
- **Шаг 32: единый путь выгрузки, находки закрываются правилами** [Р-145…Р-147] — шаг 31 починил сборку файла для доказательства Omnibus и В ТОМ ЖЕ ШАГЕ собрал её заново, хуже, в новом обработчике: когда два обработчика решают одну задачу по-разному, второй решает её хуже. Теперь выгрузка имеет ОДИН путь [Р-145]: обработчик ОБЪЯВЛЯЕТ файл (имя, заголовок, строки) и не может собрать байты — хранилище выгрузки не входит в его тип (`Omit<PricingStore, 'saveBulkJobArtifact'>`); нарезку на части, отдачу цикла событий, контрольную сумму и имя файла делает исполнитель. Три способа удержать это: тип, структурное правило «файл собирается ровно в одном месте» (смотрит весь код репозитория, четыре контроля) и БАЗА — у таблицы файлов задания не было НИ ОДНОЙ защиты, теперь их три (0113): сумму считает база по содержимому, файл кладётся только к своему идущему заданию и только у вида из `security.file_producing_job_kinds()`; проверка формата суммы удалена как дубль [Р-104]. Правило нашло третий живой путь (отчёт об импорте собирался целиком и синхронно) и мёртвый четвёртый (`priceFeedCsv` не звали ниоткуда) — оба жили на момент, когда шаг 31 объявил вопрос закрытым; Р-146: разобраны все 59 находок ревью шагов 29–31 ([docs/evidence/step32-rules.md](docs/evidence/step32-rules.md)) — 8 закрыты правилами, и два правила нашли живой дефект при написании (разбор периода отчёта через `Number(...)`; три решения стояли в таблице CLAUDE.md дважды), 30 правилом не выражаются, 8 названы кандидатами поимённо; Р-147: итог задания не входит в список заданий — проверяется поведением; право на отмену ЧУЖОГО задания стало правом на его ВИД операции (перечисление полное по типу); OQ-211 закрыт как **неподтвердившаяся находка**: читающую копию стража границ написали и удалили — случай недостижим, правка умеет только ЗАДАТЬ границу, но не снять её, а ветка, которую нечем провалить, — тавтология [Р-94]; ревью шага — 12 находок, 9 исправлены в коде, 3 в формулировках: шаг чинил удвоение расширения в имени файла и В ТОМ ЖЕ МЕСТЕ внёс обратную ошибку (отчёт по `kosten.xlsx` получил бы имя `.xlsx` с CSV внутри) — теперь расширение ставится по виду содержимого, а имя файла впервые утверждается тестом; проверка Р-147 зеленела бы и без защиты; правило схемы 14д сверяло ТЕКСТ определения CHECK вместо поведения [Р-93] и пропускало опечатку с подчёркиванием — проба переписана на вставку в копию таблицы; зритель мог отменять чужой экран различий и срывать правку каталога владельца — право стало правом на ВИД операции и закреплено стражем в базе, а не только консолью ([ревью](docs/evidence/step32-review.md)); локально: быстрая область **558 из 558** тестов, мутаций **385 из 385** поймано своей проверкой, 78 строк каталога ([docs/evidence/step32-mutation.md](docs/evidence/step32-mutation.md)); CI [35472100270](https://github.com/mehmetkerim00/repracer/actions/runs/35472100270) — **зелёный целиком**, полный 31 м 29 с; прогнан на шаге 33, после перевода репозитория в публичные (OQ-212 закрыт); [ADR-0032](docs/adr/0032-one-export-path-and-findings-closed-by-rules.md).
- **Шаг 31: загрузка файлов как в браузере, устойчивость заданий** [Р-142…Р-144] — «через консоль» значит «как браузер» [Р-142]: перенос байтов файла вынесен из работы с окном (`fetchFile`), и живой прогон зовёт ИМЕННО его — заголовок, которого не послала бы страница, он послать не может; рядом с каждым файлом проверка «переход по ссылке без заголовка не отдаёт файл» (401), и структурное правило «ни один экран не ведёт ссылкой на адрес API» с четырьмя положительными контролями, включая `window.open` и `location.href`. Файловых операций было ОДНА (доказательство Omnibus, и до ревью шага 30 она не работала), стало три: добавлены отчёт об импорте (каждая несопоставленная строка с причиной — экран показывал пять примеров на причину при 1430 несопоставленных в замере шага 28) и выгрузка ленты цен за период (экран отдаёт страницу не больше 200 записей); содержимое файла ленты проверяется на мире стенда, где записи в канал есть ([docs/evidence/step31-file-delivery.md](docs/evidence/step31-file-delivery.md)); второй фактор — свойство ВИДА операции [Р-143]: `security.second_factor_present` требует совпадения вида, живой аренды и второго фактора при создании, виды «ничего не меняющих» заданий названы одним списком в базе и в коде, а правило проверки схемы (0112) не даёт поставить такой вид в список стража — дыру шага 30 находит правило, а не случайная мутация; записи задания помечаются подтверждёнными тем же правилом, по которому база пропускает запись; правка ОДНОГО предложения второго фактора не требует [Р-144] — проверено настоящим входом одним паролем (OQ-209 закрыт); **правка границ каталога 44,2 → 5,9 с**: экран различий перестал вставлять версии и откатывать их (считается подстановкой значения — `effective_*_price_with`), стражи массового изменения считают один раз на ОПЕРАТОР, а не на строку (0110), применение ссылается на задание экрана различий и применяет его набор; попутно найдено, что сборка файла в 27 МБ занимает поток целиком и продление аренды не успевает — выгрузка доказательства не заканчивалась никогда; OQ-207 закрыт (отмена ждущего задания с автором и аудитом, предел очереди у тенанта и у каждого участника), OQ-209 и OQ-210 закрыты, OQ-208 сужен (удаление по сроку требует подтверждённого экспорта, которого у заданий нет); ревью — 16 находок, 13 исправлены (в том числе: выгрузка ленты собирала файл синхронно и квадратично — тот же дефект, что шаг только что чинил; её живая проверка была тавтологией 0 === 0; отменить чужое задание мог участник без права менять цены; итог задания с 10 000 правок уходил в СПИСОК заданий; история заданий не была отрисована нигде, и уйдя с экрана продавец терял готовый файл), 2 вынесены в OQ-211 ([ревью](docs/evidence/step31-review.md)); CI [35455587737](https://github.com/mehmetkerim00/repracer/actions/runs/35455587737): быстрый прогон, стражи и замер сжатия зелёные — на раннере CI применение границ каталога **9,3 с** против 122,4 с шага 30; полный прогон шага состоялся на шаге 33, после перевода репозитория в публичные: [35470509850](https://github.com/mehmetkerim00/repracer/actions/runs/35470509850) — **зелёный целиком**, полный 31 м 25 с (мутации, брокер, ClickHouse, развёртывания). До этого он дважды не выходил: сперва краснел на мутационной проверке (каталог снимал страж старым текстом — исправлено, [docs/evidence/step31-mutation.md](docs/evidence/step31-mutation.md)), затем GitHub не запускал задания вовсе — «recent account payments have failed» (OQ-212); [ADR-0031](docs/adr/0031-browser-truthful-file-delivery-and-job-scoped-second-factor.md).
- **Шаг 30: массовые операции фоном, уборка отложенного** [Р-139…Р-141] — четыре массовые операции продавца (применение импорта себестоимости, правка границ каталога, назначение стратегии каталогу, выгрузка доказательной истории) и пятая, предпросмотр стратегии, перестали быть синхронным запросом: запрос СОЗДАЁТ задание, работу делает отдельный процесс (`packages/bulk-jobs`, `apps/console/server/bulk-worker.ts`), экран показывает ход [Р-139]; состояние задания в базе (0108), поэтому перезагрузка страницы ничего не теряет; применение — одна транзакция [Р-134], аренда продлевается, пока идёт работа, а упавший процесс возвращает задание в очередь; второй фактор предъявляет ЧЕЛОВЕК при создании задания, и стражи массового изменения принимают его через `security.second_factor_present(kinds)` — сверяются вид задания, живая аренда и второй фактор при создании; роли разделены [Р-90]: создаёт задание административная роль (автор и аудит), ведёт его новая роль `repracer_bulk_worker`, иначе каждый тик хода был бы строкой аудита; живой прогон 10 000 предложений: нажатие ≤ 0,99 с (было до 3,3 с), работа заданий 2,5–44,2 с, процесс, убитый ПОСРЕДИ открытой транзакции применения, не оставляет в базе ничего, экран говорит это словами и задание доходит до конца со второй попытки ([docs/evidence/step30-bulk-jobs-live.md](docs/evidence/step30-bulk-jobs-live.md)); «выбрать всё» означает весь каталог, а не страницу, выбор строк сбрасывается при листании и показан числом [Р-140]; пол по большей комиссии считается по результату на цене [Р-141]; OQ-201 и OQ-202 закрыты вместе с их причиной — предпросмотр стратегии идёт по ПОЛНОМУ каталогу (10 000 решений за 31 с), выгрузка доказательства — файлом в базе (300 000 строк, 27 МБ), предела строк больше нет; OQ-199 закрыт замером (файл в 200 000 строк разбирается за 0,72 с при пределе экрана 10 с); OQ-203 сужен, OQ-205 и OQ-206 закрыты: мутационная проверка КРИТИЧНЫХ защит (14 строк, 77 мутаций) идёт отдельным заданием CI на каждый коммит; ревью — 20 находок, 16 исправлены (в том числе: файл доказательства нельзя было скачать — ссылка не несёт токен; аренда не продлевалась во время подготовки; её потеря проходила молча; правка ОДНОГО предложения стала требовать второго фактора; всё, что писало задание, помечалось как сделанное без второго фактора), 4 вынесены (OQ-207…OQ-210) ([ревью](docs/evidence/step30-review.md)); мутационная проверка нашла настоящую дыру: страж широкого гардрейла принимал ЛЮБОЕ выполняющееся задание, в том числе выгрузку доказательства, и открывал изменение пола маржи всего тенанта; CI [35468802623](https://github.com/mehmetkerim00/repracer/actions/runs/35468802623) — **зелёный целиком** (прогнан после перевода репозитория в публичные, шаг 33): быстрый 15 м 37 с, стражи 4 м 28 с, полный 33 м 4 с с мутациями, брокером, ClickHouse и запуском трёх развёртываний; прежний полный прогон краснел на правке границ каталога (122,4 с при пределе 120) и до развёртываний не доходил. Локально на шаге: быстрая область 539 из 539 тестов, мутаций 371 из 371 ([docs/evidence/step30-mutation.md](docs/evidence/step30-mutation.md)); [ADR-0030](docs/adr/0030-bulk-operations-as-background-jobs.md).
- **Шаг 27: живой режим для остального фона, себестоимость как условие репрайсинга** [Р-130…Р-132] — живым прогоном проверяются все фоновые механизмы: диспетчер записей, ретранслятор outbox, приёмник уведомлений, выгрузка в ClickHouse и удаление по сроку работают вместе три виртуальных часа на отдельной базе с моделями канала, очереди SQS и ClickHouse; у каждого механизма своё утверждение [Р-130]; прогон показал ограничение выгрузки: провал не повторяется внутри суток — прямое следствие Р-132 (OQ-193); ревью того же шага нашло, что первая редакция прогона проверяла только путь отказа выгрузки, а два её утверждения были тавтологиями — исправлено: отказ слоя точечный, успех утверждается числом строк, непонятые моделью запросы утверждаются отдельно ([docs/evidence/step27-live-background.md](docs/evidence/step27-live-background.md)); без объявленной себестоимости репрайсинг не включается — проверяет база (0098), причина `COST_REQUIRED` с указанием, чего нет, экран показывает требование без кнопки «всё равно включить» [Р-131]; в тестовых данных 76 из 101 включённого оффера под новым правилом репрайсинг не включат ([docs/evidence/step27-cost-required.md](docs/evidence/step27-cost-required.md)); OQ-186 закрыт; провалившаяся работа повторяется с растущей паузой (минимум — период работы, предел — сутки) [Р-132]; риск 34 и OQ-192 закрыты (0101): подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29] — своя таблица системных поправок, итог в `price_daily_effective`, сутки без цен становятся пустыми; OQ-190 закрыт: точки входа и развёртывания диспетчера (`deploy/worker`) и приёмника (`deploy/notification-receiver`), общий слой процессов `packages/service-runtime`; правило «таблица тенанта названа в очистке тенанта» расширено на все таблицы тенанта и канала (0102) и нашло три потери (0100); ревью — 19 находок: 16 исправлено (в том числе отмена флага `dispatchInline`: путь решения отправляет свою запись сам, а проверка схемы сверяла имя таблицы подстрокой), 2 вынесены владельцу (цена Р-132 и секрет значением переменной), 1 отпала; CI [35336919500](https://github.com/mehmetkerim00/repracer/actions/runs/35336919500): 493 из 493 тестов, мутаций 311 из 311 ([docs/evidence/step27-mutation.md](docs/evidence/step27-mutation.md)); [ревью](docs/evidence/step27-review.md); [ADR-0027](docs/adr/0027-live-background-cost-required-backoff.md).
- **Шаг 26: живой режим как способ проверки, развёртывание планировщика, внешний контроль** [Р-127…Р-129] — периодические работы проверяются прогоном процесса, а не вызовом функций: цикл `runScheduler` работает виртуальные сутки на PostgreSQL с симулятором Kaufland (два аккаунта) и моделью порта Amazon, между тактами — приёмник уведомлений и диспетчер; утверждаются числа запросов к каналу и строки базы, каждая из девяти работ каталога обязана иметь утверждение [Р-128]; первый прогон нашёл шесть дефектов продукта (новый товар и товар с нашим Buy Box застревали в холодном ярусе — 2 опроса в сутки вместо 720; 20 из 40 офферов Amazon не сверялись ни разу — 30 с против лимита 30,3 с и сдвиг окна при отказе; остановка витрины без ENGINE-товаров не снималась; ложное отставание при регистрации работы; повтор провала каждую секунду) и два дефекта модели Amazon ([docs/evidence/step26-live-mode.md](docs/evidence/step26-live-mode.md)); развёртывание `deploy/scheduler` и точка входа `services/scheduler/src/main.ts` — один экземпляр, перезапуск, секреты файлами, метрики и `/healthz` [Р-129]; отметка во внешнем сервисе каждый такт, пропуск отметки — письмо владельцу [Р-127] (аккаунта сервиса нет — OQ-188, [docs/evidence/step26-external-control.md](docs/evidence/step26-external-control.md)); риск 31 закрыт (сроки по часам базы, уровень отставания в базе, 0094), OQ-180 закрыт (окно Omnibus и суточная свёртка по времени применения цены, 0096), OQ-182 сужен (учётная запись оператора платформы и сверка разбора с ClickHouse, 0095); ревью — 13 находок, все исправлены, кроме риска 34 (цена закрытых суток остаётся в сутках принятия, OQ-192); правило «таблица тенанта названа в очистке тенанта» нашло вторую потерю — `price_stop` (шаг 12); CI [35272864293](https://github.com/mehmetkerim00/repracer/actions/runs/35272864293): 478 из 478 тестов, мутаций 296 из 296 ([docs/evidence/step26-mutation.md](docs/evidence/step26-mutation.md)); [ревью](docs/evidence/step26-review.md), [ADR-0026](docs/adr/0026-live-mode-checks-scheduler-deployment-external-control.md).
- **Шаг 25: планировщик периодических работ, честность комплаенс-модуля, выборка бэктеста** [Р-124…Р-126] — процесс-планировщик `services/scheduler` (0090): 9 работ (4 глобальные, до 5 на аккаунт), аренда в базе с продлением, перехват и освобождение чужой аренды отклоняет база, пропущенные сутки выгрузки — по очереди, отставшие и провалившиеся сутки — повтором из отставания, алерты отставания, провала и принудительного удаления, журнал запусков; остановка планировщика на сутки историю снимков не теряет — тест на PostgreSQL и ClickHouse, история теряется только при провале выгрузки 14 суток подряд, с алертами ([docs/evidence/step25-scheduler.md](docs/evidence/step25-scheduler.md)); Omnibus показывает глубину истории «видим N дней, проверка достоверна / неполная», модуль не гарантирует соответствие, цены мимо нас в окне — `EXTERNAL_CHANGES`, истории цен у Kaufland и Amazon нет — полнота с подключения (0091) [Р-124]; неприменённая цена не входит в свёртку и окно (риск 28 сужен); бэктест — выборка 200 из 10 000 товаров (60 % выручки) за 5 минут, полный каталог — фоновое задание партиями [Р-125] ([docs/benchmarks/step25-backtest-sample.md](docs/benchmarks/step25-backtest-sample.md)); разбор пропущенных снимков человеком отдельной ролью (0092, OQ-181); снимки Amazon, AWS и eBay в `vendor/` были вне git с шага 22 — добавлены; ревью — 10 находок: 9 исправлены (в том числе срок опроса истекал у всех аккаунтов кроме первого, ярусы не работали, страж отметки неприменённой цены, аренда без продления), 1 частично, остаток — риск 31 и OQ-182; исправления нашли ещё два дефекта (движения в памяти не находились, алерт удаления называл секции OID); CI [35248728141](https://github.com/mehmetkerim00/repracer/actions/runs/35248728141): 458 из 458 тестов, мутаций 274 из 274 ([docs/evidence/step25-mutation.md](docs/evidence/step25-mutation.md)); [ревью](docs/evidence/step25-review.md), [ADR-0025](docs/adr/0025-scheduler-honest-compliance-sampled-backtest.md).
- **Шаг 24: сверка уведомлений опросом, история снимков конкурентов в ClickHouse, комплаенс Omnibus** [Р-121…Р-123] — опрос — обязательный дублирующий контур уведомлений: расхождение с принятым состоянием — проверка в транзакции снимка, вердикт «задержка» / «подозрение на потерю» ставит база (0088), CRITICAL-алерт; Kaufland — сверка ярусного опроса по Buy Box, Amazon — `getCompetitiveSummary` по кругу только для сверки (AMZ_C11, A-15); на модели Kaufland без потерь — 0 ложных тревог, полная потеря подписки — 46 из 46, ложные тревоги при debounce дольше срока ([docs/evidence/step24-notification-loss.md](docs/evidence/step24-notification-loss.md)); снимки — журнал в транзакции снимка (0086) и выгрузка суток в ClickHouse с вердиктом и доставкой: Kaufland 66 байт на снимок (×7,7), Amazon 97 (×8,6), типичный клиент ~12 ГБ в год на Kaufland ([docs/benchmarks/step24-competitor-snapshot-volume.md](docs/benchmarks/step24-competitor-snapshot-volume.md)); бэктест — 26,4 млн снимков за 40 минут, 11 тыс./с, результат шага 21 совпал ([docs/benchmarks/step24-backtest-catalog.md](docs/benchmarks/step24-backtest-catalog.md)); Omnibus — наименьшая цена 30 суток витрины в базе и в коде (0087), отказ базы при нарушении, начало не раньше текущих суток, консоль: проверка до объявления, отчёт с повторной проверкой, доказательная история CSV; OQ-169…173 закрыты (0085); расписания периодических работ нет (OQ-174); ревью — 14 находок: 13 исправлены (в том числе объявление задним числом, ложные тревоги по б/у, сверка вне транзакции снимка), 1 — риск 28; CI [35223561793](https://github.com/mehmetkerim00/repracer/actions/runs/35223561793): 441 из 441 тестов, мутаций 240 из 240 ([docs/evidence/step24-mutation.md](docs/evidence/step24-mutation.md)); [ревью](docs/evidence/step24-review.md), [ADR-0024](docs/adr/0024-reconciliation-snapshot-history-omnibus.md).
- **Шаг 23: приём уведомлений, третий вид остановки, консоль** [Р-118…Р-120] — недоверие каналу (`channel_distrust`, 0082): все цены и порог цены канала, ставит система, снимает только владелец или администратор со вторым фактором и заметкой, отдельно от `pricing_halt` [Р-51] и `price_stop` [Р-69]; снятие выборкой на Amazon неприменимо — свойство канала `MANUAL_ONLY` [Р-119]; чужое ценообразование — при обнаружении оффера, при записи и сверке, стратегию и включение отклоняет база [Р-120]; доступность стратегии в базе, одна идентичность оффера (OQ-165, OQ-166); приёмник уведомлений Amazon `packages/amazon-notifications` (SQS, SigV4, дедупликация по `NotificationId`, порядок по `EventTime`, тишина, опоздание, повторы; маршрут продавца — функцией базы, 0083), `PRICING_HEALTH` — на стенде, живой очереди нет (OQ-167); консоль — три вида остановки, стратегии с именами и новой версией, список офферов с Automate Pricing, правка границ по праву, лента цен с фильтрами и страницами на сервере; CI нашёл причины `CHANNEL_DISTRUSTED` и `PRICING_STOPPED` вне ограничения ClickHouse (080); ревью — 17 находок шага: 13 исправлены (в том числе страж мешал выключить репрайсинг, наблюдения при записи не писались, обход стража при создании единицы), 4 — риски 22–24 и OQ-173; вне шага — kill switch не держит CHANNEL_MIN_PRICE (OQ-172); CI [35201010758](https://github.com/mehmetkerim00/repracer/actions/runs/35201010758): 416 из 416 тестов, мутаций 204 из 204 ([docs/evidence/step23-mutation.md](docs/evidence/step23-mutation.md)); [ревью](docs/evidence/step23-review.md), [ADR-0023](docs/adr/0023-channel-distrust-notification-receiver-console.md).
- **Шаг 22: адаптер Amazon по снимку спецификации, чужой репрайсер, сверка базы цены** [Р-114…Р-117] — `packages/amazon-client` (LWA, SP-API) и `packages/amazon-adapter`: цена и остаток через `patchListingsItem`, остаток — аккаунт + регион + SKU, запись асинхронна (ACCEPTED, подтверждение обратным чтением), ограничитель на двух уровнях (пара и приложение), неподтверждённое — AMZ_C01…C10; не пишутся никогда `automated_pricing_merchandising_rule_plan` и границы цены канала [Р-114]; привязка к правилу ценообразования — единица BLOCKED и при записи, и при сверке [Р-115]; цена покупателя ≠ отправленной на ставку НДС — системная остановка витрины на ВСЕ цены, только ручное снятие (0080, OQ-162) [Р-116]; отчёт «пол удержал цену N раз» [Р-117]; контрактный стенд Amazon — 33 сценария (память) и 18 пути решения на PostgreSQL ([docs/evidence/step22-amazon-adapter.md](docs/evidence/step22-amazon-adapter.md)); мутационная проверка 170 из 170 ([docs/evidence/step22-mutation.md](docs/evidence/step22-mutation.md)); ревью шага 22 — 13 находок: 10 исправлены (в том числе сверка базы цены на первой отправке — вариант K-12 симулятора теперь ловится), 3 — риски 15, 19, 20; CI [35169269203](https://github.com/mehmetkerim00/repracer/actions/runs/35169269203): 384 из 384 тестов, мутаций 171 из 171; найдено: Р-52 на Amazon недостижим (OQ-164), код ошибки принятой записи не доходил до причины HELD (исправлено); ревью шага 21 — находки 1–5 и 7 исправлены, 6 — стенд, 8 — риск 17 ([ревью шага 21](docs/evidence/step21-review.md)); [ревью шага 22](docs/evidence/step22-review.md), [ADR-0022](docs/adr/0022-amazon-adapter-channel-repricer-price-basis-halt.md).
- **Шаг 21: симулятор каналов, бэктест, остаток интерфейса, Р-111 в схеме** [Р-111…Р-113] — симулятор на контрактном стенде: модель Kaufland с состоянием вместо записанных обменов и модель Amazon на уровне порта; открытые вопросы K-04…K-17 и A-01…A-08 — параметры модели, варианты сценариев с находками ([docs/evidence/step21-simulator.md](docs/evidence/step21-simulator.md)); симулятор нашёл и шаг исправил два дефекта: стенд в памяти отпускал принятую без применения запись, путь решения блокировал единицу без алерта; не исправлено — потеря `buy_box_changed` не догоняется, нетто `listing_price` (K-12) не обнаруживается (риски 18–19); бэктест на истории через настоящий путь решения, окно 18 мес, на синтетике ([docs/benchmarks/step21-backtest-synthetic.md](docs/benchmarks/step21-backtest-synthetic.md)) — реальной истории нет, писателя `competitor_snapshot` нет (OQ-156); консоль: превью стратегии до сохранения, массовая правка границ с экраном различий, лента цен, отчёт об опасных изменениях; поле собственного пола цены канала у Amazon отклоняет БД (0077), массовая правка границ — только со вторым фактором в БД (0078, OQ-144); CI [35159204622](https://github.com/mehmetkerim00/repracer/actions/runs/35159204622): 317 из 317 тестов, мутаций 166 из 166 ([docs/evidence/step21-mutation-final.md](docs/evidence/step21-mutation-final.md)); **ревью шага не выполнено** — агент ревьюера остановлен лимитом сессии; [ADR-0021](docs/adr/0021-channel-simulator-backtest-console-edits.md).
- **Шаг 20: возврат к каналам — брокер и ClickHouse в CI, снимки спецификаций, Р-109** [Р-108…Р-110] — первый полностью зелёный CI ([35146073530](https://github.com/mehmetkerim00/repracer/actions/runs/35146073530): 275 из 275 тестов, мутационная проверка 157 из 157); порядок записей за брокером проверен впервые: с ключом 0 перестановок, отклонения `OUT_OF_ORDER` — только повторные доставки, негативный контроль без ключа находит 133–196 перестановок ([docs/evidence/step20-broker-order.md](docs/evidence/step20-broker-order.md)); шаг 19 обрабатывал ноль сообщений — журнал теста был в чужой базе; DDL ClickHouse применён впервые, выгрузка из PostgreSQL проверена (найдены и исправлены: порядок `CODEC`/`DEFAULT`, NO_OP не выгружался из-за прав представления, двойной счёт агрегата при повторе — 070); сжатие замерено: ~62–70 ГБ на клиента за 18 мес при доле NO_OP как в генераторе шага 14 (88–95 ГБ без неё) против оценки 50–100 ([docs/benchmarks/step20-clickhouse-compression.md](docs/benchmarks/step20-clickhouse-compression.md)); элемент согласия и старт миграции eBay — по последней проверке ≤ 24 ч, не из будущего, с тем же вердиктом и поимённо принятыми потерями (0074, риск 12б закрыт; первая редакция обходилась — ревью шага 20); новая защита без строки каталога мутаций — красная сборка (базовый список 725, `scripts/db/check-catalog-coverage.mjs`); снимок Amazon SP-API — 14 из 26 ячеек Amazon с [док], вопросы A-01…A-08; снимок eBay не сохранён — документация отвечает 403, вопросы E-01…E-06 (channel-capabilities §9); [ревью](docs/evidence/step20-review.md), [ADR-0020](docs/adr/0020-infrastructure-in-ci-spec-snapshots-new-protections-catalogued.md).
- **Шаг 19: честный раннер мутаций, без исключений, последний шаг линии безопасности** [Р-104…Р-107] — мутация поймана только своей проверкой с результатом «защиты нет»: смоук — «отказа не было» по метке целиком, тест — точная метка утверждения и фактический результат (сообщение утверждения попадает в ошибку при любом провале — первая редакция шага этим ошибалась, ревью шага 19); первый честный прогон — не поймано 22 из 124 ([docs/evidence/step19-mutation-honest-first.md](docs/evidence/step19-mutation-honest-first.md)), после исправления раннера — не поймано 2 из 148 ([docs/evidence/step19-mutation-runner-fixed.md](docs/evidence/step19-mutation-runner-fixed.md)), итог — **0 из 148** ([docs/evidence/step19-mutation-final.md](docs/evidence/step19-mutation-final.md)), только для каталога: 82 триггера и 487 CHECK вне каталога ([docs/evidence/step19-uncatalogued-protections.txt](docs/evidence/step19-uncatalogued-protections.txt)); защиты-дубли удалены, а не оставлены исключениями [Р-104]; находки 1–7 ревью шага 18 закрыты (0072) — на таблицах административной записи; элементы и отзыв согласия eBay, ручной intent только от человека [Р-107], системная остановка и наблюдения выборки не от человека, удаление привязок отдельной ролью, привязка только в READ COMMITTED, скаляры в слепке; `svc_stock` пишет в канал только QUANTITY [Р-105]; восстановлены SECURITY DEFINER, сброшенные `CREATE OR REPLACE`; проверка схемы 0073; **CI впервые запущен: красный, 2 из 269 тестов — порядок за брокером (ни одного сообщения не обработано)** ([docs/evidence/step19-ci-first-run.md](docs/evidence/step19-ci-first-run.md)); незакрытое, включая обход «только из триггера» временным триггером и обход остановки свободным `rule_code`, — [docs/accepted-risks.md](docs/accepted-risks.md) [Р-106]; [ревью](docs/evidence/step19-review.md), [ADR-0019](docs/adr/0019-honest-mutation-runner-no-exceptions-quantity-only-stock.md).
- **Шаг 18: мутационная проверка своей проверкой, права по столбцам, роль остатков** [Р-99…Р-103] — мутация поймана только своей проверкой с причиной (первый прогон по этому правилу: не поймано 17 из 98 вместо заявленных «1 из 83», [docs/evidence/step18-mutation-first.md](docs/evidence/step18-mutation-first.md); итог шага — 5 из 124, но это нижняя граница: ревью шага 18 показало, что для смоук-проверок отказ по другой причине засчитывался своей поимкой, [docs/evidence/step18-mutation-after.md](docs/evidence/step18-mutation-after.md), [ревью](docs/evidence/step18-review.md)); причина обязательна во всех `expect_fail`, проверки, отказывавшие чужой защитой, переписаны; страж административной записи — по столбцам и с ролью участника, согласие eBay — владелец от своего имени со вторым фактором, путь решения — только рабочие столбцы и только блокировка единицы (0068); гонка привязок, роль `svc_stock`, срок привязок (0069); виды значений у каждого поля слепка (0070); у каждой из 32 append-only таблиц есть строка в смоук-мире; проверка схемы 0071; [ADR-0018](docs/adr/0018-own-check-mutations-column-rights-stock-role.md). **Первый прогон CI не выполнен** (у токена gh нет права `workflow`, OQ-146).
- **Шаг 17: мутационная проверка защит, список разрешённого, административные операции от человека** [Р-94…Р-98] — тесты защит проверяют причину отказа, а не факт (`expect_fail(метка, запрос, причина)`); мутационная проверка схемы снимает защиты по одной и требует падения заявленной проверки (`scripts/db/mutation-check.mjs`, каталог `tests/db/mutations.mjs`, [замер до исправлений](docs/evidence/step17-mutation-before.md)); права пути решения — список разрешённого (`security.decision_path_allowed_privileges()`, 0062; согласия eBay, Smart Pricing, границы, себестоимость, стратегии — административной роли), блокировка товара и проверка остановки по выборке — функциями базы (0063, 0065); административная запись — только при пользователе сессии и вся в `audit_event` (0066); перепривязка входа — приглашением владельца с отзывом прежней привязки; создание тенанта не присоединяет существующих пользователей мимо приглашения; слепок проверяется по видам значений; проверка схемы 0067 сверяет права в обе стороны; [ADR-0017](docs/adr/0017-mutation-checked-guards-allow-list-human-admin.md). **Первый прогон CI не выполнен** (у токена gh нет права `workflow`, OQ-146).
- **Шаг 16: защита защиты, разделение ролей БД, сохранность работы** [Р-89…Р-93] — каждый `*.test.ts` репозитория входит в сборку (`scripts/check-test-inclusion.mjs`, шаг CI; вне сборки было 4 файла, 17 тестов шага 15); роли подключения разделены: у пути решения нет вставки аудита, членств, тенантов, пользователей, остановок человеком, функций входа, пользователь сессии и второй фактор принимаются только у административного сервиса (0058, [ADR-0016](docs/adr/0016-role-separation-build-completeness.md)); подрез стратегии — вне вечного хранения, 18 месяцев после замены версии, проверка слепка fail-closed (0059); повтор при неподтверждённом поясе витрины завершается с причиной, обход диспетчера не падает (0057); проверка схемы 0061 — поведение вместо имён (перевод нашёл неработавшую с шага 12 проверку валюты сумм в БД, исправлено 0060); утраченные вопросы помечены; код — в приватном удалённом хранилище, **первый прогон CI не выполнен** (у токена gh нет права `workflow`, OQ-146).
- **Шаг 15: главный инвариант восстановлен, CI без пропусков, находки ревью** [Р-83…Р-88] — перед каждой отправкой цены БД вычисляет пол заново, включая пол маржи по текущим себестоимости, курсу и НДС (0051; тест падал до исправления — [docs/evidence](docs/evidence/)); тесты не пропускаются, CI на чистой PostgreSQL ([.github/workflows/ci.yml](.github/workflows/ci.yml), `scripts/test-all.mjs`, смоук-тесты схемы [tests/db](tests/db/)); производные от цены конкурента вне вечного ядра [Р-85]; сжатие ядра в PostgreSQL отменено [Р-86]; привязка identity только приглашением, второй фактор для снятия остановки тенанта и смены роли [Р-88]; журнал проверок остановок, JWKS, режим стенда, бюджет при повторе, дефекты диспетчера; миграции 0051–0056; [ADR-0015](docs/adr/0015-dispatch-floor-recheck-ci-invitations.md). CI в GitHub не запускался (OQ-146).
- **Шаг 14: вход у поставщика, самодостаточное и меньшее объяснение, ретроспективное ревью** [Р-78…Р-82] — собственные пароли и сессии удалены: вход, MFA, сброс и приглашения — у внешнего поставщика identity (ZITADEL Cloud EU, [ADR-0013](docs/adr/0013-external-identity-provider.md)); у нас — сопоставление `(issuer, subject)` с членствами, автор действия с ценами — пользователь сессии (сверяет БД); слепок `r80.1` без копий столбцов, архив ядра тенанта — со справочниками и проверкой развёртывания без базы [Р-79]; код причины NO_OP в агрегате ClickHouse [Р-81]; сжатие jsonb в секциях включено, но PostgreSQL не сжимает строки короче ~2 КБ (OQ-140); миграции 0048–0050; [замер](docs/benchmarks/step14-explanation-size.md), [ADR-0014](docs/adr/0014-self-contained-compact-explanation.md). Поставщик и ClickHouse не подключались: нет сети.
- **Шаг 13 завершён: размер слепка, аудит остановок, рабочий вход** [Р-74…Р-77] — слепок объяснения только у CHANGED и REJECTED_BY_GATE, у NO_OP — код причины; повторяющиеся части — в неизменяемом справочнике `platform.explanation_ruleset` и в версиях стратегий (формат `r74.1`, замер — [docs/benchmarks/step13-explanation-size.md](docs/benchmarks/step13-explanation-size.md)); остановки и системные остановки — в `audit_event` триггерами; стратегия хранится независимо от режима, ENGINE без стратегии невозможен; вход по email и паролю (отменён Р-78 на шаге 14); права администратора и менеджера цен; миграции 0044–0047; [ADR-0012](docs/adr/0012-explanation-dictionary-audit-identity.md).
- **Шаг 12: объяснимость на рабочих данных, остановки и роли** [Р-68…Р-73] — решение хранит неизменяемый слепок объяснения без данных канала (вечно, с ядром), ссылка на снимок — 18 мес; экран «почему эта цена» на PostgreSQL показывает все пять шагов и совпадает с памятью (`tests/contract/src/console.pg.test.ts`); kill switch человеком (`price_stop`) останавливает все цены, системная остановка — только цены из данных конкурентов; остановка тенанта — объект; роли владелец и оператор проверяет БД; суммы с валютой, тексты — словарь DE/EN; «опасное» — отклонение от границы > 10 %; вход стенда синтетическими пользователями; миграции 0042–0043; [ADR-0011](docs/adr/0011-explanation-stops-roles.md).
- **Шаг 11: интерфейс на данных стенда** [Р-67] — консоль продавца [apps/console](apps/console/) (React, Vite): товары с явными статусами, «почему эта цена» — 7 шагов одного решения, отклонённые решения, границы с разбивкой пола до цента, остановка тенанта и витрины в два нажатия; модели экранов [packages/console-model](packages/console-model/), пробелы в данных — находки на экране; [ADR-0010](docs/adr/0010-seller-console-on-stand.md). Пояс — по витрине, для США не подставляется, бюджет правок — только по подтверждённому поясу [Р-65]; клиент Kafka — confluent-kafka-javascript [Р-66]; ручная остановка с автором и заметкой; миграции 0040–0041.
- **Шаг 10: диспетчер записей, брокер, ClickHouse, валюты и регионы** — диспетчер записей [Р-64] ([packages/write-dispatcher](packages/write-dispatcher/)): запись из очереди не теряется (до исправления терялись последние цены: в пяти прогонах шага 10 — 19–35 из 40; контрольный тест утверждает, что потеря есть), вытеснение — с причиной, неизвестный итог — сверка обратным чтением; курсы ЕЦБ и курс в решении [Р-61] ([packages/fx-rates](packages/fx-rates/)), граница суток витрины [Р-62], регион — место клиента [Р-60], якорь EAN между валютами [Р-63]; миграции 0036–0039. Брокер (Redpanda, [packages/broker](packages/broker/), [services/pricing-worker](services/pricing-worker/)) и выгрузка в ClickHouse ([packages/analytics-export](packages/analytics-export/)) написаны, **на стенде не прогонялись**: Docker не скачивает образы (OQ-114).
- **Шаг 9 завершён: узкие места пути решения и две рыночные зоны** — оценка цены за 2–3 транзакции [Р-59] (контекст — один запрос, снимок и решение — одна транзакция), проекция окна сдвига, валюта и подсказка канала в проекции снимка, параметры причины отказа в `price_decision`; валюты EUR и USD, налоговые режимы НДС и sales tax в схеме и расчёте маржи [Р-57, Р-58]; справочник витрин; миграции 0034–0035; замер — [docs/benchmarks/step9-decision-path.md](docs/benchmarks/step9-decision-path.md) (пик 185 оценок/с выдерживается).
- **Шаг 8 завершён: путь решения на реальной PostgreSQL** — хранилище [packages/pricing-store-pg](packages/pricing-store-pg/) (intent, решение и запись — одна транзакция с закреплённой версией границ [Р-54]), якоря проверки входов [Р-49, Р-50], остановка только цен из данных конкурентов с автоматическим выходом [Р-51, Р-52], НДС на товаре [Р-53]; миграции 0032–0033; сценарии стенда прогоняются на памяти и на PostgreSQL (до шага 15 — только при заданной `REPRACER_PG_URL`, без неё молча пропускались; с шага 15 обязательно, CI [Р-84]); нагрузочный замер — [docs/benchmarks/step8-decision-path.md](docs/benchmarks/step8-decision-path.md).
- **Шаг 7 завершён: путь решения о цене** — проверка входов ([packages/input-sanity](packages/input-sanity/)), движок стратегий ([packages/strategy-engine](packages/strategy-engine/)), Price Gate ([packages/price-gate](packages/price-gate/)), сквозной путь ([packages/pricing-pipeline](packages/pricing-pipeline/)) на стенде без сети; миграции 0030–0031, [ADR-0008](docs/adr/0008-price-decision-path.md).
- Шаг 6: адаптер Kaufland ([packages/kaufland-adapter](packages/kaufland-adapter/), правила KFL_C01…C19) и стенд [tests/contract](tests/contract/). Реальный канал не подключался: нет регистрации партнёра (OQ-75).
- Стек — TypeScript (Node ≥ 22), npm workspaces [Р-33]; зависимости времени выполнения — `pg` (шаг 8), `@confluentinc/kafka-javascript` 1.10.1 (брокер, Р-66), `react` и `react-dom` 19.3.0 (интерфейс, Р-67); dev: `typescript`, `openapi-typescript`, `@types/node`, `vite` 8.3.0, `@types/react`, `@types/react-dom`
- Зависимости не устанавливаем, пока шаг явно этого не требует.

## Принятые решения (окончательные) — [docs/decisions.md](docs/decisions.md)

| № | Коротко |
|---|---|
| Р-1 | Amazon EU: остаток MFN — одно значение на SKU в регионе, last write wins; цена — по маркетплейсу |
| Р-2 | eBay: только Inventory API; миграция необратима, **автомиграция запрещена**; 250 правок листинга/день |
| Р-3 | Наши цены — данные тенанта; прочитанное из SP-API — Amazon Information ≤ 18 мес |
| Р-4 | PII не запрашиваем, restricted-роли не берём |
| Р-5 | Floor = максимум; абсолютный `min_price` обязателен |
| Р-6 | Не владелец остатка; общий пул + буфер на канал |
| Р-7 | Каналы по порядку: Kaufland → Amazon → eBay → Otto; в Release 1.0 — первые три [Р-56] |
| Р-8 | p95: Amazon < 60 с, Kaufland < 2 мин, eBay/Otto < 5 мин |
| Р-9 | Пользователь в нескольких тенантах |
| Р-10 | Модели на данных SP-API не обучаем вообще |
| Р-11 | Цель — Release 1.0; выпуск только когда готовы 3 канала Release 1.0 [Р-56] (критерии — channel-capabilities §6) |
| Р-12 | Kaufland `minimum_price` не пишем никогда (включает Smart Pricing); режим тенанта Smart Pricing выключает наш движок; смешение запрещено |
| Р-13 | ~~Конкуренты Kaufland — `competitorsComparer`~~ → исправлено Р-36 |
| Р-14 | Kaufland: единица записи цены — аккаунт + витрина + unit; остаток — исправлено Р-35 |
| Р-15 | Остаток: внутренний пул / Inbound API / зеркало ERP; в Release 1.0 — первые два |
| Р-16 | Изоляция: общая схема + RLS + `tenant_id` везде + партиции по тенанту; раздельные базы — только EU/US; регион — по месту клиента [Р-60] |
| Р-17 | Ответ SP-API на запись — Amazon Information; бессрочно — только id записи, статус, время |
| Р-18 | `min_price` и `max_price` [Р-43] — только уровни товара и единицы записи |
| Р-19 | Бюджет eBay 250/день — на листинг, все попытки |
| Р-20 | Три слоя: PostgreSQL — только данные текущего решения; ClickHouse — наблюдения, снимки, завершённые записи; архив Parquet — префикс на тенанта |
| Р-21 | Суточная свёртка наших цен — вечно, доказательство Omnibus; сырьё — 90 дней, затем архив |
| Р-22 | Инварианты, RLS, append-only — только в PostgreSQL; **решение о цене не читает ClickHouse** |
| Р-23 | Изоляция тенантов в ClickHouse и архиве — только через слой, подставляющий `tenant_id`; никаких общих выгрузок |
| Р-24 | Брокер Kafka-совместимый, ключ `write_scope_id`; шина не нарушает порядок внутри единицы записи |
| Р-25 | Резервация `CREATED → CONFIRMED_BY_SOURCE → CONSUMED \| RELEASED`; подтверждение — вызовом источника с внешним id заказа; TTL 24 ч |
| Р-26 | Германия и Австрия — ограничение только Kaufland [Р-56]; мультивалютность активна [Р-57]; согласия eBay хранятся 3 года после закрытия |
| Р-27 | `price_intent`: `CHANGED` и `REJECTED_BY_GATE` — целиком всегда; `NO_OP` — 7 дней, затем почасовой агрегат |
| Р-28 | Буфер PostgreSQL: intent 3 дня, decision 30 дней, `observed_price_daily` 45 дней |
| Р-29 | Суточная свёртка неизменяема; исправление — строка-поправка, итог — представление |
| Р-30 | Подтверждённая резервация старше 14 дней не освобождается, а поднимает алерт |
| Р-31 | **Адаптер не выбирает тенанта:** тенант приходит в сообщении и сверяется с `channel_account`; несовпадение — отказ и алерт |
| Р-32 | Комиссии каналов — версионируемые тарифные таблицы в репозитории, не API |
| Р-33 | TypeScript для сервисов и фронтенда, React; второй язык — только через ADR |
| Р-34 | Ретранслятор outbox — собственный поллер с advisory lock; Debezium не используем |
| Р-35 | Kaufland: остаток — аккаунт + `id_offer` (общий для витрин); цена — аккаунт + витрина + unit |
| Р-36 | Конкуренты Kaufland: `buy_box_changed` + `GET /buybox` — основной источник; `competitors-comparer` — только сверка (ранний доступ — Р-45) |
| Р-37 | Область записи остатка шире области цены — проверять первым при подключении канала (ADR-0002) |
| Р-38 | Ядро `price_intent` — вечно, входы с данными канала — 18 мес; окно бэктеста ≤ 18 мес |
| Р-39 | Стратегия объявляет требуемую полноту конкурентных данных; недоступна там, где канал её не даёт (ADR-0007) |
| Р-40 | Вебхук: один адрес на аккаунт с ротируемым токеном в пути; подпись проверяется всегда, токен — не аутентификация |
| Р-41 | Smart Pricing не включается ни у кого до ответа поддержки; гипотеза нейтрализации — только тест на тестовом аккаунте |
| Р-42 | Проверка правдоподобия входов до движка: ×N от нашей цены, вне истории 30 дней, массовый сдвиг — остановка канала, ровно ×100/×0.01 — отдельная тревога |
| Р-43 | `max_price` обязателен наравне с `min_price`; без обоих репрайсинг не включается; уровни товара и единицы записи |
| Р-44 | Gate проверяет обе границы; отклонённое по верхней хранится как по нижней — `REJECTED_BY_GATE` |
| Р-45 | Kaufland `buy_box_changed` — ранний доступ у аккаунт-менеджера; до получения Р-36 не активна |
| Р-46 | Адаптер работает с уведомлениями с данными и без; выбор — по факту доставки |
| Р-47 | Опрос конкурентов ярусный по волатильности; целевая задержка — горячий ярус, длинный хвост — раз в сутки (ограничение продукта) |
| Р-48 | Потеря подписки: автовосстановление и обязательный догон опросом; `fallback_email` — наш ящик с мониторингом |
| Р-49 | Якоря проверки входов: себестоимость → согласованность снимка → тот же EAN на другом канале → история; наша цена — только предупреждение |
| Р-50 | Наш сбой против рынка — по разбросу коэффициента сдвига: почти нулевой — остановка; разброс или один продавец — рынок |
| Р-51 | Остановка канала блокирует только цены из данных конкурентов; фиксированная, маржинальная и ручная цены работают |
| Р-52 | Остановка снимается автоматически по свежей выборке после окна, с записью в журнал; ручное снятие — с заметкой, без лимита попыток |
| Р-53 | Ставка НДС — на товаре; по умолчанию DE 19 %, AT 20 %; категории не сопоставляем; цены витрин ЕС — брутто до K-12 (режим НДС, Р-58) |
| Р-54 | Intent, решение и запись цены — одна транзакция; смена версии границ — откат и пересчёт |
| Р-55 | Устаревший снимок — отказ с предупреждением; правка продавца в кабинете канала — DivergenceCase, а не отказ снимка |
| Р-56 | Release 1.0 — Kaufland, Amazon, eBay; Amazon и eBay — ЕС и США; Otto и Walmart — после первых клиентов, по спросу |
| Р-57 | Мультивалютность — до адаптера Amazon; валюта — свойство единицы записи; EUR и USD наравне |
| Р-58 | Налог — режим расчёта цены: НДС (ЕС, цена брутто) и sales tax (США, цена нетто, налог при покупке); влияет на маржу и пол |
| Р-59 | Одна оценка цены — не больше 3 транзакций |
| Р-60 | Регион хранения — место клиента, не витрины; тенант в одной базе, витрины любые; перенос между регионами — отдельная процедура |
| Р-61 | Себестоимость — в валюте возникновения; перевод при расчёте по дневному курсу ЕЦБ на момент решения; курс хранится в решении |
| Р-62 | Граница суток истории цен — часовой пояс витрины из справочника, не константа Europe/Berlin (уточнено Р-65) |
| Р-63 | Якорь «тот же EAN» при разной валюте — перевод по курсу или явный отказ с причиной; молчаливый пропуск запрещён |
| Р-64 | Записи в очереди отправляет диспетчер по `write_scope_id`; запись не остаётся PENDING бесследно — отправлена или вытеснена с причиной |
| Р-65 | Часовой пояс — свойство витрины со статусом проверки; общего значения для США нет; Amazon US и eBay US — (проверить) по отдельности; бюджет правок — только по подтверждённому поясу |
| Р-66 | Клиент Kafka — confluent-kafka-javascript, не kafkajs |
| Р-67 | Интерфейс строится до подключения каналов; источник данных — стенд и фикстуры |
| Р-68 | Решение хранит неизменяемый слепок объяснения без данных канала — вечно с ядром; ссылка на полный снимок — 18 мес |
| Р-69 | Kill switch (человек) — все изменения цен; остановка канала (система) — только цены из данных конкурентов |
| Р-70 | Остановка тенанта — самостоятельный объект; аккаунт, подключённый при ней, не работает |
| Р-71 | Каждый параметр с суммой несёт валюту; интерфейс валюту не домысливает |
| Р-72 | Движок отдаёт коды и параметры; тексты — словарь интерфейса DE и EN |
| Р-73 | «Опасное изменение» — отклонённое Gate с отклонением от границы > 10 %; остальное — «скорректировано» |
| Р-74 | Слепок объяснения: CHANGED и REJECTED_BY_GATE — полный, вечно; NO_OP — слепка нет, только код причины |
| Р-75 | Повторяющиеся части слепка (наборы правил, профиль Gate, параметры стратегий) — в справочниках; слепок ссылается |
| Р-76 | Остановки цен пишутся в `audit_event`, а не только в `price_stop` |
| Р-77 | Тип стратегии доступен там, где решается о предупреждении; косвенный вывод по минимальной марже запрещён |
| Р-78 | Вход, MFA, сброс пароля, приглашения — у внешнего поставщика identity; у нас только сопоставление с членствами и ролями |
| Р-79 | Справочник правил и версии стратегий архивируются вместе с ядром intent; объяснение самодостаточно после закрытия тенанта |
| Р-80 | Ядро меньше: слепок без данных, хранимых столбцами, и сжатие значения в секциях |
| Р-81 | Почасовой агрегат NO_OP содержит код причины |
| Р-82 | Исправление обоснования Р-74: NO_OP никогда не был в вечном ядре; решение — ради горячего буфера |
| Р-83 | Перед КАЖДОЙ отправкой цены границы вычисляются заново, включая пол маржи; проверка на пути PostgreSQL; несовпадение — запись не уходит, алерт |
| Р-84 | Пропуск теста — провал сборки; CI гоняет всё на чистой PostgreSQL |
| Р-85 | Вечный слепок не позволяет восстановить данные канала; производные величины к вечному хранению не допускаются |
| Р-86 | Сжатие ядра внутри PostgreSQL отменено; ядро — окно ~2 месяца, архив сжат gzip |
| Р-87 | Parquet пишет ClickHouse своим экспортом; новая библиотека не вводится |
| Р-88 | MFA — для снятия остановки тенанта, массового импорта себестоимости и границ, смены ролей; привязка identity — только приглашением |
| Р-89 | Тест, не входящий в сборку, считается несуществующим; проверка полноты сборки — часть CI |
| Р-90 | Роли подключения разделены: путь решения без аудита, членств, признаков второго фактора и ролей; административная роль — только административному сервису |
| Р-91 | Подрез стратегии в вечном ядре не хранится: тип и ссылка на версию; величина — в таблице с 18-месячным сроком |
| Р-92 | Утраченные тексты открытых вопросов не восстанавливаются по памяти; помечаются с шагом возникновения |
| Р-93 | Правило проверки схемы проверяет поведение, а не имена функций и ключей, либо не существует |
| Р-94 | Тест защиты проверяет ПРИЧИНУ отказа; зеленеющий без своей защиты тест не существует |
| Р-95 | Мутационная проверка схемы (снять защиту → упасть должна заявленная проверка) — часть сборки и CI; с шага 29 — в полном прогоне [Р-137] |
| Р-96 | У пути решения только то, что нужно для цены, — списком разрешённого; остальное административной роли |
| Р-97 | Компрометация административного сервиса = полная компрометация; административные операции — только по действию человека, всё в аудите |
| Р-98 | Перепривязка внешнего входа — только новым приглашением владельца; автоматической нет |
| Р-99 | Мутация поймана только СВОЕЙ проверкой строки каталога с её причиной; падение соседней — непоймана |
| Р-100 | Права по столбцам, где у таблицы рабочие и административные поля; на всю таблицу — только если все столбцы рабочие |
| Р-101 | Согласие на миграцию eBay — только владелец, от своего имени, со вторым фактором |
| Р-102 | Роль svc_stock — только остатки и резервации, без цен и без аудита |
| Р-103 | У каждой append-only таблицы есть строка в смоук-мире |
| Р-104 | Исключений в мутационной проверке нет: защита, пойманная только соседней, получает свою проверку или удаляется как дубль |
| Р-105 | `svc_stock` пишет в `channel_write` только поле QUANTITY — право ограничено видом поля |
| Р-106 | Шаг 19 — последний шаг линии безопасности; незакрытое — принятые риски ([docs/accepted-risks.md](docs/accepted-risks.md)) |
| Р-107 | Ручной intent вставляет только человек; путь решения не создаёт «ручную цену владельца» |
| Р-108 | Каталог мутаций задним числом не расширяется; каждая НОВАЯ защита — в каталоге при создании (проверка в CI) |
| Р-109 | Элемент согласия eBay сверяется с вердиктом и свежестью предполётной проверки до первой строки кода миграции |
| Р-110 | Линия безопасности закрыта; новые находки ревьюера вне шага — в принятые риски или открытые вопросы |
| Р-111 | Amazon `minimum_seller_allowed_price` не пишется НИКОГДА (как Kaufland `minimum_price`, Р-12) — запрет, закреплён в БД; причина исправлена Р-114 |
| Р-112 | Снимок спецификации — условие адаптера; адаптер eBay заблокирован до ключей разработчика |
| Р-113 | Симулятор — на контрактном стенде шага 6: тот же формат сценариев, канал с состоянием вместо записанных ответов |
| Р-114 | Исправление Р-111: границы `minimum/maximum_seller_allowed_price` репрайсер не включают, но создают второй набор границ — не пишутся; `automated_pricing_merchandising_rule_plan` включает репрайсер — не пишется никогда |
| Р-115 | Правило автоматического ценообразования у оффера — наш движок для единицы не работает, продавцу предупреждение; состояние — `getListingsItem` |
| Р-116 | Обратное чтение сверяет применённую цену с отправленной; расхождение на ставку налога — неверная база цены, остановка канала |
| Р-117 | Отчёт границ — «пол удержал цену N раз, без него вы продали бы на X дешевле»: удержания стратегии, а не отклонения Gate |
| Р-118 | Третий вид остановки — недоверие каналу: все цены, включая фиксированные и маржинальные; ставит система, снимает только человек; не расширение Р-51 и не kill switch |
| Р-119 | Автоматическое снятие остановки выборкой [Р-52] на Amazon неприменимо — свойство канала (`MANUAL_ONLY`), не пробел |
| Р-120 | Чужой репрайсер проверяется при обнаружении оффера; продавец видит офферы с Automate Pricing до назначения стратегии |
| Р-121 | Ярусный опрос — обязательный дублирующий контур уведомлений: расхождение опроса без уведомления до срока — подозрение на потерю с алертом |
| Р-122 | Снимки конкурентов — в ClickHouse с первого дня живого канала; история задним числом не добирается |
| Р-123 | Omnibus: прежняя цена скидки ≤ наименьшей цены оффера за 30 суток витрины, по каждому каналу; на суточной свёртке [Р-21] |
| Р-124 | Комплаенс-модуль не гарантирует соответствие: проверяет по известной истории и показывает её глубину; полнота — с подключения, если канал не отдаёт историю цен |
| Р-125 | Бэктест по умолчанию — выборка товаров с основной выручкой; полный каталог — фоновое задание |
| Р-126 | Периодические работы запускает отдельный процесс-планировщик; без него история снимков пропадает |
| Р-127 | Работоспособность планировщика контролируется извне: отметка во внешнем сервисе, пропуск отметки — письмо владельцу |
| Р-128 | Периодические механизмы проверяются прогоном в живом режиме: утверждается наблюдаемое поведение за период, а не вызовы функций |
| Р-129 | Развёртывание планировщика — часть репозитория: один экземпляр, перезапуск, конфигурация, метрики |
| Р-130 | Живым прогоном проверяется весь фон: диспетчер, приёмник, ретранслятор, выгрузка, удаление по сроку — утверждается наблюдаемое |
| Р-131 | Без объявленной себестоимости репрайсинг не включается; требование проверяет база и показывает экран |
| Р-132 | Провалившаяся фоновая работа повторяется с растущей паузой (минимум — период работы, предел — сутки) |
| Р-133 | Уточнение Р-132: правило «пауза не меньше периода» — только для работ, ходящих в каналы; внутренние повторяются от минуты до часа |
| Р-134 | Массовый импорт себестоимости (CSV, XLSX) — часть онбординга: подсказки колонок, предпросмотр, список несопоставленного, применение целиком |
| Р-135 | Массовый импорт себестоимости и границ — только со вторым фактором; разбиение на отдельные транзакции его не обходит |
| Р-136 | Расширение Р-130: всё, к чему прикасается продавец, проверяется живым прогоном ЧЕРЕЗ КОНСОЛЬ, а не вызовом хранилища |
| Р-137 | CI делится на быстрый (каждый коммит: типы, юнит-тесты, PostgreSQL) и полный (слияние в main: мутации, брокер, ClickHouse, развёртывание); задания параллельные |
| Р-138 | Импорт не переводит валюты и не угадывает единицы; комиссия продавца — источник «от продавца», не смешивается с тарифом, при расхождении видны оба числа |
| Р-139 | Массовые операции продавца выполняются фоновым заданием с видимым ходом, а не синхронным запросом |
| Р-140 | «Выбрать всё» — весь каталог по текущему фильтру, а не страница; выбор строк сбрасывается при листании и показан числом |
| Р-141 | Пол по большей комиссии выбирается по РЕЗУЛЬТАТУ на цене, а не по сравнению ставок |
| Р-142 | Уточнение Р-136: «через консоль» значит «как браузер» — переход по ссылке, скачивание и новая вкладка идут без Authorization |
| Р-143 | Второй фактор относится к виду операции: задание, которому он не нужен, не открывает операций, которым он нужен, и его записи не подтверждены |
| Р-144 | Рутинная операция не наследует требования массовой: правка одного предложения второго фактора не требует |
| Р-145 | Выгрузка файла имеет ОДИН обязательный путь: тип не даёт написать вторую сборку, правило ловит обход типа, база проверяет свойства результата |
| Р-146 | Находка ревью или мутации закрывается правилом там, где это возможно, а не только исправлением |
| Р-147 | Итог массового задания не входит в список заданий: список — состояние и ход, итог запрашивается отдельно |
| Р-148 | Логи и доказательства не содержат путей машины разработчика, имён пользователей и внутренних адресов; репозиторий публичный |
| Р-149 | Онбординг — направляемый путь: шаги завершаются проверяемым состоянием, выведенным из данных; прогресс в базе; себестоимость не пропускается, набор можно сузить |
| Р-150 | Канал без доступов — честное состояние «ожидает доступа» с перечнем того, чего не хватает, а не ошибка и не пустота |
| Р-151 | Демо-тенант на симуляторе: данные синтетические, путь настоящий; помечен везде, где показываются деньги |
| Р-152 | Онбординг предлагает два пути: «остатки» и «остатки + репрайсинг»; путь остатков не требует себестоимости, границ и стратегий |
| Р-153 | Экраны остатков: по товару — физический, резервации, доступно; по каналу — отправлено и подтверждено; ловушки каналов названы до записи |
| Р-154 | Экран не читает все решения тенанта ради счётчиков: счётчики — агрегатом базы, списки — страницами |

## Каналы

| Канал | API | Release 1.0 [Р-56] | Аутентификация |
|---|---|---|---|
| Kaufland | Seller API v2 (2.44.0), HMAC, ключи продавца + технологического партнёра | витрины `de`, `at` — только DE/AT [Р-26, Р-35]; EUR, брутто | Client/Secret key, HMAC (проверить) |
| Amazon | SP-API | ЕС (amazon.de) и США (amazon.com); идентификаторы и режимы витрин — (проверить) | LWA OAuth |
| eBay | **Inventory API** | EBAY_DE и EBAY_US (проверить) | OAuth2 user token |
| Otto Market | Otto Market API | После первых клиентов, по спросу | OAuth2 client credentials |
| Walmart | (проверить) | После первых клиентов, по спросу | (проверить) |

Возможности каналов по полям — [docs/channel-capabilities.md](docs/channel-capabilities.md).

## Жёсткие ограничения

### Amazon
- `getCompetitiveSummary`: **0.033 req/sec, burst 1** → поллинг конкурентов невозможен, только нотификации.
- **Запись остатка меняет остаток SKU во всех маркетплейсах EU сразу** [Р-1], даже если мы работаем только с DE.
- DPP: не-PII из SP-API — **≤ 18 месяцев**; логи — **≥ 12 месяцев**; PII не храним [Р-4].
- AUP: не агрегировать данные разных продавцов; строгая изоляция тенантов; модели на данных SP-API не обучаем [Р-10].
- **Не пишем никогда** [Р-111, Р-114]: `automated_pricing_merchandising_rule_plan` (включает репрайсер Amazon), `minimum_seller_allowed_price` и `maximum_seller_allowed_price` (второй набор границ: цена вне них — отказ Amazon; наши границы — единственные). Поле порта отклоняет БД (0077), тело запроса — контрактный стенд.
- **Чужой репрайсер** [Р-115, Р-120]: при обнаружении оффера (`searchListingsItems` с атрибутами), перед записью и при обратном чтении; привязка к правилу — единица BLOCKED, стратегию не назначит база (0082). **База цены** [Р-116, Р-118]: цена покупателя ≠ отправленной ровно на ставку НДС — недоверие каналу, все цены витрины, снятие только человеком со вторым фактором (0082).
- **Уведомления** `ANY_OFFER_CHANGED` и `PRICING_HEALTH` — только очередь SQS (стандартная: без порядка, с повторами, без подписи); приёмник `packages/amazon-notifications` — дедупликация по `NotificationId`, порядок по `EventTime`, потеря уведомления обнаруживается только сверкой по кругу [Р-121] (риск 21). Автоматического снятия остановки выборкой нет [Р-119].
- `patchListingsItem` — 5 rps на пару «продавец × приложение» и 500 rps на приложение, срабатывает первый порог: ограничитель на двух уровнях (AMZ_C01). Запись асинхронна: ACCEPTED ≠ применено, подтверждение — обратное чтение. Неподтверждённое — AMZ_C01…C10 (channel-capabilities §11).

### eBay
- **Адаптер заблокирован до снимка спецификации** [Р-112]: документация отвечает 403, снимок требует ключей разработчика (E-01).
- Низкие дефолтные лимиты до Application Growth Check; часть лимитов — на приложение, общая для всех тенантов.
- **Миграция листинга необратима** (Revise/Relist/Verify перестают работать; потеря Best Offer, требований к
  покупателям, благотворительности, шаблона). Аукционы не мигрируются. Только после предполётной проверки и
  согласия `OWNER` — [docs/onboarding-ebay-migration.md](docs/onboarding-ebay-migration.md).
- **250 правок листинга в календарный день** — бюджет на листинг, резерв под остаток.

### Kaufland
- **`minimum_price` не пишем никогда** — запись включает Smart Pricing Kaufland [Р-12]. Исключение — явный режим тенанта Smart Pricing на единице записи: наш движок для неё выключен, пишется только `minimum_price` не ниже нашего `min_price`.
- **Smart Pricing до ответа поддержки не включается ни у кого [Р-41]**; `minimum_price` > 0 у unit — CRITICAL-алерт.
- Единица записи цены — аккаунт + витрина + unit; остатка — аккаунт + `id_offer`, общий для витрин [Р-35]. Конкуренты — `buy_box_changed` + `GET /buybox`, `competitors-comparer` — сверка [Р-36].
- Лимит — 111 запросов в секунду на продавца на все эндпоинты; `units/bulk` — до 150 unit одной витрины.
- `buy_box_changed` — ранний доступ [Р-45]; до выдачи конкуренты — опрос `/buybox` по ярусам волатильности [Р-47]; уведомления с данными и без [Р-46]; потерянная подписка — восстановление и догон опросом [Р-48].
- Вебхук: один адрес на аккаунт с токеном в пути; подпись проверяется всегда [Р-40]. Всё неподтверждённое документацией — консервативное правило с кодом и вопросом K-nn (channel-capabilities §7–8).

### Otto Market
- ~10 req/sec; OAuth2 client credentials, токен 60 минут → централизованное обновление.

### ЕС
- История цен по каждому каналу/маркетплейсу (Omnibus / §11 PAngV).
- Цены витрин брутто, НДС внутри цены — режим `VAT_INCLUDED` [Р-58].

### США
- Цены витрин нетто, sales tax добавляется при покупке — режим `SALES_TAX_EXCLUDED` [Р-58]; валюта USD [Р-57].
- Граница суток витрин США не задана: amazon.com и EBAY_US — (проверить) по отдельности; до подтверждения записи с бюджетом правок не создаются, дни не закрываются, сырьё цен не удаляется [Р-65]; себестоимость в EUR переводится по курсу ЕЦБ [Р-61]; данные тенанта — в базе региона клиента, не витрины [Р-60].

## Архитектурные инварианты (кратко; полностью — [docs/domain-model.md §4](docs/domain-model.md))

1. **Граница тенанта.** Один запрос — один тенант, выбранный через `Membership`. `tenant_id` — из контекста сессии.
2. **Обе границы вне движка стратегий** [Р-43, Р-44]: Price Gate, БД при вставке решения, БД при создании записи и перед КАЖДОЙ отправкой — пол (min_price и пол маржи) вычисляется заново по текущим себестоимости, комиссии, курсу и НДС [Р-83]; выход за границу — отказ, не округление. **Входы проверяются до движка** [Р-42]: испорченный снимок — REJECT, массовый сдвиг — остановка витрины до снятия человеком.
3. **Единица записи — `WriteScope` на поле**, а не оффер ([ADR-0002](docs/adr/0002-write-scope-per-field.md)).
   Версии, идемпотентность, схлопывание, упорядочивание, наблюдение и история цен — по `write_scope_id`.
4. **Наши цены:** сырьё `price_history` — 90 дней в PostgreSQL, затем архив; **суточная свёртка `price_daily` — вечно** [Р-21]. Прочитанное из каналов — ClickHouse, ≤ 18 мес.
5. **«Отправили» ≠ «видим»:** `ObservedChannelState` отдельно; реакция — только через `DivergencePolicy`.
   Уменьшение опубликованного остатка никогда не блокируется.
6. **Fail-closed.** Неподтверждённые лимиты каналов → консервативная политика Dispatcher.
7. **Деньги** — целые minor units + валюта + база; валюта, база и налоговый режим — атрибуты единицы записи, сверяются со справочником витрины; EUR и USD наравне, конвертации нет [Р-57, Р-58].
8. **Изоляция в БД** — общая схема, `tenant_id` везде, RLS `ENABLE`+`FORCE`, составные FK `(tenant_id, …)`, без `BYPASSRLS` ([ADR-0003](docs/adr/0003-tenant-isolation.md)).
9. **Три слоя хранения** ([ADR-0004](docs/adr/0004-storage-tiering.md), [docs/data-retention.md](docs/data-retention.md)): данные каналов ≤ 18 мес в любом слое и не попадают в архив; данные тенанта удаляются из PostgreSQL по времени только после подтверждённого экспорта.
10. **Решение о цене читает только PostgreSQL** [Р-22]: всё нужное ему — горячие проекции (`competitor_state`, `observed_price_daily`, `observed_channel_state`).
11. **Шина:** ключ `write_scope_id`, retry-топиков для `scope.*` нет, ошибка блокирует единицу записи ([ADR-0005](docs/adr/0005-message-broker.md)).
12. **Запись не теряется** [Р-64]: одна запись в полёте на единицу; ждущую запись отправляет диспетчер, событие о ней ставится в той же транзакции; завершение без отправки — только с причиной в БД ([ADR-0009](docs/adr/0009-write-dispatcher-and-broker.md)).
13. **Три остановки** [Р-69, Р-70, Р-118]: остановка человеком (`price_stop`) — все цены, тенант — объект без аккаунта; системная (`pricing_halt`) — только цены из данных конкурентов, снятие выборкой там, где канал её даёт [Р-52, Р-119]; недоверие каналу (`channel_distrust`) — все цены и порог цены канала, ставит система, снимает только человек со вторым фактором; права — роли в БД.
14. **Объяснение без данных канала** [Р-68, Р-72, Р-74, Р-75]: слепок — у CHANGED и REJECTED_BY_GATE (у NO_OP — код причины); ключи параметров класса `CHANNEL` в нём отклоняет БД; суммы — с валютой [Р-71]; повторяющиеся части — ссылками на неизменяемый справочник и версию стратегии; столбцы строки в слепке не повторяются [Р-80]; архив ядра несёт справочники и объясняет себя без базы [Р-79]; производные от цены конкурента, в том числе подрез стратегии, в вечное ядро не попадают, проверка ключей слепка — fail-closed [Р-85, Р-91].
15. **Вход не наш, аудит наш** [Р-78, Р-76]: паролей и сессий в системе нет — пользователь по токену поставщика и `(issuer, subject)`, роль — из `Membership` при каждом запросе; автор остановки и снятия — пользователь сессии (сверяет БД); остановка цен без события `audit_event` не записывается; привязка identity — только приёмом приглашения, снятие остановки тенанта, ручное снятие системной остановки и смена роли — со вторым фактором [Р-88]; роли подключения разделены: путь решения не пишет аудит, членства, тенантов и остановки человеком, пользователь сессии и второй фактор принимаются только у административного сервиса, который база заявить за себя не мешает [Р-90, ADR-0016].
16. **Тест не пропускается, не выпадает из сборки и проверяет причину** [Р-84, Р-89, Р-94, Р-95]: тест без базы или брокера падает; сборка с пропущенным тестом или с файлом теста вне скрипта сборки — красная (`scripts/test-all.mjs`, `scripts/check-test-inclusion.mjs`, CI).

## Правила работы в репозитории

- Существенное решение → ADR в `docs/adr/NNNN-kebab-case.md`; решение владельца продукта → `docs/decisions.md`.
- Новая таблица → миграция в `migrations/`: `tenant_id`, `security.register_table(...)`, строка в `maintenance.retention_policy`, `security.grant_retention(...)`; последняя проверка схемы (сейчас `0121`) должна проходить; правило проверки — поведение, не имена [Р-93], тест защиты проверяет причину отказа [Р-94], новая защита — строка в каталоге мутационной проверки при создании, иначе красная сборка [Р-95, Р-108]; тесты — `scripts/db/prepare.sh` и `node scripts/test-all.mjs`, пропуск теста и файл теста вне скрипта сборки — красная сборка [Р-84, Р-89]. Правила — [migrations/README.md](migrations/README.md).
- Индекс — только под конкретный запрос, с комментарием над индексом.
- Таблица ClickHouse: `tenant_id` первым в ключе сортировки, TTL ≤ 18 мес, политика строк `tenant_isolation`; `099_verify.sql` должен возвращать 0 строк.
- Никаких запросов к ClickHouse или архиву из пути решения о цене; никаких выгрузок, объединяющих тенантов.
- Адаптер канала реализует `ChannelAdapter` ([ADR-0006](docs/adr/0006-channel-port.md)): описывает канал данными, классифицирует ошибки, проверяет тенанта; не хранит версии и не решает о повторах.
- Факты об API канала — только из снимка спецификации в `vendor/` и страниц документации со ссылкой; неясное — вопрос в поддержку (`channel-capabilities.md` §7).
- Сгенерированный код (`src/generated/`) не редактируется; перегенерация — из конкретного снимка с проверкой SHA-256.
- Инвариант, который можно выразить ограничением или триггером, закрепляется в БД, а не только в приложении.
- **Цифры лимитов и поведение API не выдумывать.** Неподтверждённое — «(проверить)» с планом проверки в `channel-capabilities.md`.
- Никаких реальных данных продавцов в тестах, фикстурах, логах отладки и внешних инструментах (включая LLM).
- Поведение адаптера меняется вместе со сценарием в `tests/contract/fixtures/`. Фикстуры — синтетические по документации или записанные рекордером, обезличенные и прошедшие рецензию (`provenance.reviewedBy`); запись с реального аккаунта хранится вне репозитория до рецензии.
- Никакой логики, отчётов или метрик, объединяющих бизнес-данные нескольких тенантов (в том числе детекторов сдвига рынка: окно Р-42 — внутри тенанта).
- Новая причина решения или правило проверки входов — код и схема параметров в `packages/pricing-model/src/reasons.ts` (вид, класс источника; сумма — с валютой [Р-71]); тексты — в словаре `packages/console-model/src/i18n` на немецком и английском [Р-72]; новый ключ класса `CHANNEL` — и в `security.channel_param_keys()`. Изменение порогов Р-42 — только с оценкой ложных срабатываний.
- Нет кода, мигрирующего листинги eBay без `MigrationConsent`.
- Никогда не логировать секреты, токены, данные покупателей.
- Документация — на русском; идентификаторы — на английском.
- Неизвестное — в [docs/open-questions.md](docs/open-questions.md).

## Карта документации

- [README.md](README.md) — обзор
- [docs/decisions.md](docs/decisions.md) — принятые решения Р-1…Р-154
- [docs/accepted-risks.md](docs/accepted-risks.md) — принятые риски после линии безопасности [Р-106]
- [docs/domain-model.md](docs/domain-model.md) — доменная модель v0.15 и инварианты
- [docs/data-retention.md](docs/data-retention.md) — три слоя, сроки, удаление, закрытие тенанта
- [docs/tenant-isolation-analytics.md](docs/tenant-isolation-analytics.md) — изоляция тенантов в ClickHouse, архиве, брокере
- [schemas/clickhouse/](schemas/clickhouse/) — DDL аналитического слоя
- [packages/channel-port](packages/channel-port/) — порт `ChannelAdapter`; [packages/kaufland-client](packages/kaufland-client/) — клиент Kaufland; [packages/kaufland-adapter](packages/kaufland-adapter/) — адаптер Kaufland; [packages/amazon-client](packages/amazon-client/) — клиент SP-API (LWA); [packages/amazon-adapter](packages/amazon-adapter/) — адаптер Amazon по снимку спецификации; [packages/amazon-notifications](packages/amazon-notifications/) — приёмник уведомлений Amazon из SQS (SigV4, дедупликация, порядок, потеря)
- [packages/pricing-model](packages/pricing-model/), [input-sanity](packages/input-sanity/), [strategy-engine](packages/strategy-engine/), [price-gate](packages/price-gate/), [pricing-pipeline](packages/pricing-pipeline/) — путь решения о цене
- [tests/contract](tests/contract/) — стенд контрактных тестов record/replay, симулятор каналов (открытые вопросы — параметры модели) и бэктест на истории [Р-113, Р-38]; демо-мир на симуляторе [Р-151] — `tests/contract/src/live/demo-world.ts`, стенд поднимает его по `REPRACER_DEMO=on`
- [apps/console](apps/console/) — консоль продавца на данных стенда [Р-67]; [packages/console-model](packages/console-model/) — модели экранов, словарь DE/EN, объяснимость причин, разбивка пола
- [packages/pricing-store-pg](packages/pricing-store-pg/) — хранилище пути решения на PostgreSQL, тесты БД, нагрузочный замер ([docs/benchmarks/](docs/benchmarks/))
- [packages/write-dispatcher](packages/write-dispatcher/) — диспетчер записей; [packages/broker](packages/broker/) — брокер и ретранслятор outbox; [services/pricing-worker](services/pricing-worker/) — путь решения за брокером
- [packages/identity](packages/identity/) — проверка токена поставщика identity, JWKS, сопоставление с членствами, приглашения [Р-78, Р-88]
- [tests/db](tests/db/) — смоук-тесты схемы; [scripts](scripts/) — подготовка базы, запуск всех тестов без пропусков и проверка полноты сборки [Р-84, Р-89]; [docs/evidence](docs/evidence/) — логи доказательств
- [packages/fx-rates](packages/fx-rates/) — курсы ЕЦБ; [packages/analytics-export](packages/analytics-export/) — выгрузка в ClickHouse, самодостаточный архив ядра [Р-79] и замер сжатия; [infra/local](infra/local/) — локальные Redpanda и ClickHouse
- [docs/tenant-region-transfer.md](docs/tenant-region-transfer.md) — перенос тенанта между регионами
- [vendor/kaufland/](vendor/kaufland/), [vendor/amazon/](vendor/amazon/) — снимки спецификаций каналов с контрольными суммами; [vendor/aws/](vendor/aws/) — страницы SQS и SigV4 приёмника уведомлений (адреса и SHA-256); [vendor/ebay/](vendor/ebay/) — почему снимка eBay нет; [vendor/healthchecks/](vendor/healthchecks/) — страницы внешнего сервиса контроля планировщика [Р-127]
- [migrations/](migrations/) — DDL PostgreSQL
- [deploy/scheduler/](deploy/scheduler/) — развёртывание процесса-планировщика [Р-129]: compose с закреплёнными версиями, конфигурация, секреты, метрики и проверка работоспособности; [deploy/worker/](deploy/worker/) — путь решения за брокером (диспетчер записей и ретранслятор outbox), [deploy/notification-receiver/](deploy/notification-receiver/) — приёмник уведомлений Amazon [OQ-190]; [deploy/ci/](deploy/ci/) — надстройки, которыми CI поднимает все три развёртывания и ждёт `/healthz` (шаг 28, D)
- [packages/cost-import](packages/cost-import/) — разбор выгрузок продавца (CSV, XLSX) и предпросмотр массового импорта себестоимости [Р-134]
- [packages/stock-sync](packages/stock-sync/) — синхронизация остатков [Р-6, Р-152, Р-153]: правило публикуемого количества (общий пул минус буфер канала), порт хранилища остатков, конвейер «заказы канала → резервации → пересчёт → записи», разбор файла остатков продавца; на PostgreSQL — `PgStockStore` ([packages/pricing-store-pg/src/stock.ts](packages/pricing-store-pg/src/stock.ts))
- [packages/bulk-jobs](packages/bulk-jobs/) — фоновый исполнитель массовых операций [Р-139] и ЕДИНСТВЕННЫЙ путь выгрузки файла [Р-145] (обработчик объявляет строки, байты делает исполнитель): аренда с продлением, ход отдельной транзакцией, применение целиком или никак, возобновление после падения процесса; обработчики видов заданий, включая включение репрайсинга набора [Р-149]. Точка входа процесса — [apps/console/server/bulk-worker.ts](apps/console/server/bulk-worker.ts)
- [packages/service-runtime](packages/service-runtime/) — общий слой процессов: конфигурация с секретами из файлов, учётные данные каналов, каталог аккаунтов, журнал JSON, `/healthz` и метрики
- [docs/channel-capabilities.md](docs/channel-capabilities.md) — матрица возможностей каналов и план проверки
- [docs/onboarding-ebay-migration.md](docs/onboarding-ebay-migration.md) — предполётная проверка и миграция eBay
- [docs/adr/](docs/adr/) — архитектурные решения
- [docs/open-questions.md](docs/open-questions.md) — открытые вопросы (S14 — блокируют шаг 14)
