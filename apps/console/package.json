{
  "name": "@repracer/console",
  "version": "0.1.0",
  "private": true,
  "description": "Seller console (Р-67, step 14): React screens over stand worlds — sign-in through the identity provider token (Р-78; a local simulator on the stand), roles from memberships on every request, products, why this price, stopped changes, bounds, kill switch with audit log. Data comes from the contract stand, not from channels.",
  "type": "module",
  "dependencies": {
    "@repracer/console-model": "0.1.0",
    "@repracer/contract-tests": "0.1.0",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "@repracer/identity": "0.1.0"
  },
  "scripts": {
    "stand": "node --experimental-strip-types --disable-warning=ExperimentalWarning server/stand-server.ts",
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/console.test.ts test/console.pg.test.ts"
  },
  "devDependencies": {
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "vite": "8.3.0"
  }
}
