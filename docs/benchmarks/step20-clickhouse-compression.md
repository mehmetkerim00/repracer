# Шаг 20: сжатие ClickHouse против оценки шага 4

Дата: 2026-09-16. Первый прогон DDL `schemas/clickhouse` и первый замер на настоящем ClickHouse. До шага 20 слой ни разу не
поднимался (OQ-114).

## Стенд

- **Среда:** GitHub Actions `ubuntu-24.04`; прогон [35140682931](https://github.com/mehmetkerim00/repracer/actions/runs/35140682931) — временный workflow `infra-debug` ветки `step20-infra`. Тот же замер с тем же генератором затем выполняется заданием `clickhouse-compression` основного `ci.yml`.
- **ClickHouse 26.8.4.11** (docker). Таблицы — копии `repracer_analytics` (`CREATE TABLE … AS`): те же движок, кодеки, ключ сортировки, партиции.
- **PostgreSQL 16** — копии таблиц `LIKE … INCLUDING INDEXES`.
- **Скрипт:** [bench/compression.bench.ts](../../packages/analytics-export/bench/compression.bench.ts).
- **Итог прогона:** [results/step20-clickhouse-compression.json](results/step20-clickhouse-compression.json).
- **Объём:** 1 000 000 синтетических строк на таблицу ClickHouse, 200 000 — на таблицу PostgreSQL. Мир: 20 тенантов по 2 500 единиц записи, 30 дней.

Генератор написан на шаге 10 и до шага 20 ни разу не запускался. На первых прогонах вскрылись три дефекта:

1. Случайное блуждание цены уходило ниже пола. Ограничение ClickHouse `floor_respected` отклонило вставку.
2. У решения не было столбцов шагов 12–14 (`trigger_type` NOT NULL).
3. В контейнере PostgreSQL не хватило `/dev/shm`.

После исправлений измерено то, что описано выше.

Сам DDL ClickHouse на первом прогоне тоже упал: в `050_step10.sql` было `CODEC(...) DEFAULT`, а ClickHouse требует
`DEFAULT ... CODEC(...)`. Кроме того, `infra/local/apply-clickhouse.mjs` не применял `060_step14.sql`. Полный прогон тестов в CI нашёл
ещё два дефекта:
- роль вставки не могла вставлять NO_OP: представление почасового агрегата выполнялось её правами, а чтения у неё нет;
- повтор выгрузки неизменного дня удваивал счётчики агрегата: у таблицы агрегата не было окна дедупликации.

Оба исправлены в `070_step20.sql`. Теперь применяются все файлы с 001 по 070, а `099_verify.sql` (правила 1–8) возвращает 0 строк.

## Результат

Строк на клиента за 18 месяцев — из ADR-0004 (оценка шага 4, не замер). Байт на строку — `bytes_on_disk` активных частей после
`OPTIMIZE FINAL`.

| Таблица | CH, байт/строку | Сжатие CH | PG, байт/строку | PG/CH | Строк за 18 мес | Замер, ГБ | Оценка шага 4, ГБ |
|---|---|---|---|---|---|---|---|
| `price_decision` | 76,6 | ×11,0 | 1 924 | ×25 | 245 млн | **18,8** | 11–22 |
| `price_intent` | 108,6 | ×7,0 | 1 226 | ×11 | 245 млн | **26,6** | 11–22 |
| `channel_write_completed` | 86,7 | ×2,5 | 352 | ×4,1 | 186 млн | **16,1** | 5–11 |
| `competitor_snapshot` | 88,3 | ×5,2 | — | — | 106 млн | **9,4** | 8–16 |
| `channel_observation` | 32,6 | ×3,6 | — | — | 255 млн | **8,3** | 6–13 |
| **Измерено** | | | | | | **79,2** | **41–84** |
| `channel_write_response` | не замерялось | | | | 190 млн | — | 8–15 |
| `fee_actual` | не замерялось | | | | мало | — | < 1 |

**Итог в модели строк шага 4:** 79,2 ГБ по измеренным таблицам, ~88–95 ГБ с незамеренными.

### Поправка ревью шага 20: NO_OP в ClickHouse не идёт в `price_intent` и `price_decision`

Шаг 4 считал 245 млн строк `price_intent` и столько же `price_decision` за 18 месяцев — это все оценки, включая NO_OP. После
Р-27 и Р-74 NO_OP в ClickHouse устроен иначе: сырьё хранится 8 дней в `price_intent_noop`, дальше остаётся только почасовой агрегат,
а NO_OP-решения не выгружаются вовсе (`exporter.ts`). Поэтому строка «79,2 ГБ» — верхняя оценка «все оценки как CHANGED».

Пересчёт с долей классов из генератора шага 14 (CHANGED 31,4 %, REJECTED_BY_GATE 11,9 %, NO_OP 56,7 %; тоже синтетика):

| Таблица | Строк за 18 мес | ГБ |
|---|---|---|
| `price_intent` (CHANGED + REJECTED_BY_GATE) | 106 млн | 11,5 |
| `price_decision` (то же) | 106 млн | 8,1 |
| `price_intent_noop` (8 дней сырья) | ~2 млн | 0,2 |
| `channel_write_completed`, `competitor_snapshot`, `channel_observation` | как выше | 33,8 |
| **Измерено** | | **53,6** |
| + незамеренные (`channel_write_response` 8–15, `fee_actual` < 1, почасовой агрегат) | | ~8–16 |
| **Итого на клиента** | | **~62–70 ГБ** |

**Итог:** ~62–70 ГБ — середина оценки шага 4 (50–100), а не её верхняя граница. Верхняя граница — 88–95 ГБ, если NO_OP окажется
редким.

## Что расходится с оценкой

- **`price_intent` — 26,6 ГБ при оценке 11–22.** Больше всего места занимают столбец `inputs` (JSON, 31 байт на строку после
  сжатия), `source_event_id` (18) и идентификатор (16).
- **`channel_write_completed` — 16,1 ГБ при оценке 5–11.** Сжатие всего ×2,5: строка состоит в основном из случайных UUID. Столбцы
  `price_decision_id` и `channel_write_id` дают по 16 байт на строку, и эти байты не сжимаются.
- **Случайные UUID** (v4) дают по 16 байт на строку и не сжимаются ни в одной таблице. Это свойство модели, а не генератора: в
  работе идентификаторы такие же.
- **Сжатие решения ×11** — выше оценки «5–15× от PostgreSQL» в части самого ClickHouse. Относительно строки PostgreSQL с индексами
  решение меньше в 25 раз.

## Пересчёт стоимости на клиента

Ставка из ADR-0004 — 0,12 € за ГБ горячего SSD в месяц. Ставка — предположение шага 4, цены поставщика не проверены (OQ-67).

| | Оценка шага 4 | Шаг 20 |
|---|---|---|
| ClickHouse, € в месяц на клиента | 6–12 | **~7,4–8,3** (с поправкой NO_OP); без поправки 10,6–11,4 |
| PostgreSQL, € в месяц на клиента (год 1) | ~1,6 | не пересчитывалось |
| Итого хранилище, € в месяц на клиента | ~8–14 | **~9–10** (без поправки NO_OP ~12–13) |
| 100 клиентов, ClickHouse | 5–10 ТБ | **~6,2–7,0 ТБ** (без поправки 8,8–9,5) |

## Ограничения замера

- **Строки синтетические.** Распределения заданы генератором: 80 % одобренных решений, 10 % с курсом ЕЦБ, 1–10 конкурентов.
  Реальные значения повторяются иначе.
- **Объём.** 1 млн строк в одной таблице — это части размером в десятки мегабайт. На сотнях миллионов строк части сливаются
  крупнее, и сжатие обычно не хуже. Экстраполяция линейная.
- **Число строк на клиента** взято из предположений шага 4 (230 тыс. записей цены в сутки и т. д.), не из данных.
- **Не замерены** `channel_write_response`, `fee_actual` и почасовой агрегат NO_OP.
- **Метки времени завышают размер** (ревью шага 20). Генератор кладёт на клиента ~20 строк на единицу записи, а в работе их сотни;
  соседние метки времени случайны и сжимаются до ~6 из 8 байт. В работе строки одной единицы идут подряд по ключу сортировки, и
  разница соседних меток меньше. Поэтому байты на строку, вероятно, завышены.
- **PG/CH ×25 для решения — не одинаковые строки.** В PostgreSQL решение несёт слепок объяснения r80.1, в ClickHouse решение
  выгружается без него. Отношение показывает выгоду слоя, а не только сжатие.
