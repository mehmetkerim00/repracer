# Возможности каналов (ChannelCapability)

Статус: 2026-09-14 · шаг 3 · первая география — Германия [Р-7]; цель — Release 1.0 [Р-11].
Это **данные**: при появлении схемы (шаг 3) таблица станет исходником для справочника `ChannelCapability`
(см. [domain-model.md §3.5](domain-model.md)).

## Легенда

| Метка | Значение |
|---|---|
| **[Р-n]** | Принятое решение ([decisions.md](decisions.md)) — окончательно |
| **[ТЗ]** | Из исходных требований проекта |
| **(проверить)** | Не подтверждено. Значение по памяти о публичной документации или предположение. Использовать нельзя, пока не проверено |
| **?** | Неизвестно совсем |

Пока значение лимита не подтверждено, Dispatcher применяет **консервативную политику** (раздел 4) — цифры не додумываются.

---

## 1. Компактная матрица

| Канал · поле | Операция записи | Write scope | Пакет | Лимит запросов | Лимит правок на объект | Подтверждение применения | Обратимо | Вебхуки | p95 |
|---|---|---|---|---|---|---|---|---|---|
| **Amazon · PRICE** | Listings Items API 2021-08-01 **`patchListingsItem`**, атрибут **`purchasable_offer`**: оффер выбирается селекторами `marketplace_id`, `currency`, `audience` (по умолчанию `ALL`); цена — `our_price[].schedule[].value_with_tax`, **десятичная сумма в основных единицах** (в примере `7.0` USD), а не целые центы **[док]**. `JSON_LISTINGS_FEED` не используется для цены: сообщения фида **не обрабатываются в порядке следования** — это нарушает Р-24 **[док]** (OQ-32 закрыт) | аккаунт + `marketplace_id` + seller SKU **[Р-1]**; `A1PA6795UKMFR9` — DE, `ATVPDKIKX0DER` — US; эндпоинты: EU — Европа (в т. ч. DE), NA — США **[док]** | **1 SKU на вызов** **[док]**; фид — до 25 000 сообщений, рекомендуемый минимум 1 500, 5 фидов за 5 минут — только для начальной загрузки **[док]** | `patchListingsItem`: **5 rps, burst 5 на пару «аккаунт продавца × приложение» и 500 rps на приложение — общие для всех тенантов** **[док]**; планы могут быть динамическими, `x-amzn-RateLimit-Limit` возвращается не всегда **[док]** | Не документирован (проверить, A-04) | Синхронная проверка и приём в каталог при вызове; ошибки после приёма — `getListingsItem` с `includedData=issues` и уведомление `LISTINGS_ITEM_ISSUES_CHANGE` **[док]**; время до видимости цены — (проверить, A-06) | Да | **Да:** доставка — SQS или EventBridge **[док]**; `ANY_OFFER_CHANGED` — изменения топ-20 предложений по состоянию, Buy Box и внешней цены, только для товаров с активным предложением продавца, фильтры `marketplaceIds` и `aggregationSettings` **[док]**; `PRICING_HEALTH` — оффер выбыл из Featured Offer **[док]** | < 60 с **[Р-8]** |
| **Amazon · QUANTITY** | `patchListingsItem`, атрибут **`fulfillment_availability`** — `[{"fulfillment_channel_code": "DEFAULT", "quantity": N}]`, операция `merge` меняет только `quantity` **[док]** | аккаунт + регион EU + seller SKU **[Р-1]**. Косвенно по документации: у `fulfillment_availability` нет селектора `marketplace_id` (у `purchasable_offer` он есть), уведомление `LISTINGS_ITEM_MFN_QUANTITY_CHANGE` несёт `SellerId`, `FulfillmentChannelCode`, `Sku`, `Quantity` без витрины **[док]**; прямого утверждения «одно значение на все витрины EU» нет — (проверить, A-01, OQ-50) | Как PRICE **[док]** | Как PRICE **[док]** | Не документирован (проверить, A-04) | Приём как у PRICE **[док]**; изменение доступного количества — уведомление `LISTINGS_ITEM_MFN_QUANTITY_CHANGE` (при обновлении продавцом, новых и отменённых заказах) **[док]**; обратное чтение `getListingsItem` **[док]** | Да, но меняет все витрины EU **[Р-1]** (проверить, A-01) | Да — `LISTINGS_ITEM_MFN_QUANTITY_CHANGE` **[док]** | < 60 с **[Р-8]** |
| **eBay · PRICE** | Inventory API `bulkUpdatePriceQuantity` (проверить; альтернатива `updateOffer`) **[Р-2]** | аккаунт + `EBAY_DE` + `offerId` (проверить) | До 25 (проверить) | Низкие дефолтные до Growth Check **[ТЗ]**; числа ? | **250 правок листинга в календарный день [Р-2]**; бюджет на ItemID, общий для полей и вариаций (проверить) | Синхронный статус по элементу в ответе; применение — readback `getOffer` (проверить) | Да, но расходует дневной бюджет | ? — Notification API для своих офферов (проверить) | < 5 мин **[Р-8]** |
| **eBay · QUANTITY** | `bulkUpdatePriceQuantity`: `shipToLocationAvailability.quantity` товара или `availableQuantity` оффера (проверить, OQ-47) | аккаунт + inventory SKU (уровень — проверить) | До 25 (проверить) | Как PRICE | **250/день [Р-2]**, общий бюджет с PRICE (проверить) | Синхронный статус; readback `getInventoryItem` (проверить) | Условно: `quantity = 0` может завершить листинг без out-of-stock control (проверить) | ? | < 5 мин **[Р-8]** |
| **Kaufland · PRICE** | `PATCH /v2/units/{id_unit}?storefront=<de\|at>`, поле **`listing_price`** — целые центы валюты витрины (EUR для `de` и `at`), от 1 цента до 1 млн EUR; пакетно — `POST /v2/units/bulk?storefront=…`, тело `[{"id_unit", "unit_data": {"listing_price"}}]` **[док]**. Поле `minimum_price` не пишем никогда **[Р-12]** | аккаунт + витрина + `id_unit` **[Р-35]**; коды витрин: Германия — **`de`**, Австрия — **`at`** **[док]** | `units/bulk`: **≤ 150 unit** в запросе, без дублей, одна витрина на запрос; нарушение — отказ всего запроса (400); ответ **207 Multi-Status** со статусом по каждому unit **[док]** | **111 запросов в секунду на продавца**, общий на все эндпоинты; превышение — **429** **[док]** | Не документирован (проверить) | Синхронно: `PATCH` → 200 с объектом unit, `bulk` → 207 с объектом по unit; применение — событие `item_unit_changed` (в payload `listing_price`, итоговая `price`, `date_lastchange`) и обратное чтение `GET /v2/units/{id_unit}`; видимость — `is_live`, причины — `POST /v2/units/status` (≤ 20 unit) **[док]** | Да | **Да:** `item_unit_changed`, `item_unit_live` / `item_unit_not_live`, `buy_box_changed` (⚠ этих событий нет в перечне `event_name` спецификации 2.44.0 — K-18; `buy_box_changed` — ранний доступ, Р-45); подписка `POST /v2/subscriptions?storefront=` (`callback_url` ≤ 255, `fallback_email`, `event_name`), проверка адреса GET `mode=subscribe&challenge`, подпись `Shop-Signature`, повторы ~12 ч, затем подписка отключается **[док]** | < 2 мин **[Р-8]** |
| **Kaufland · QUANTITY** | `PATCH /v2/units/{id_unit}` или `POST /v2/units/bulk`, поле **`amount`** (0…99 999) **[док]** | **аккаунт + `id_offer`, общая для витрин [Р-35]:** unit на разных витринах с одинаковым `id_offer` имеют общие количество и склад; порядок применения между витринами не гарантирован (сказано для файлов импорта) **[док]**. Запись идёт через один unit-носитель `id_offer`; подтверждение читает все unit с этим `id_offer` на витринах аккаунта (KFL_C14, K-06). Без `id_offer` синхронизация остатка не включается (CHECK 0027) | ≤ 150, как PRICE **[док]** | 111 rps на продавца **[док]** | Не документирован (проверить) | Синхронно (200 / 207); `item_unit_changed`, `item_unit_out_of_stock` при `amount = 0`; обратное чтение `GET /v2/units?id_offer=…&storefront=…` по каждой витрине **[док]** | Да; `amount = 0` → unit не в продаже (причина `stock_update_needed`), восстанавливается записью остатка **[док]** | Да (как PRICE) **[док]** | < 2 мин **[Р-8]** |
| **Kaufland · CHANNEL_MIN_PRICE** (только режим Smart Pricing) | **До ответа поддержки не пишется ни у кого [Р-41].** Поле **`minimum_price`** (центы) в `PATCH /v2/units/{id_unit}`, `POST /v2/units/bulk`, `POST /v2/units`, файлах инвентаря: «если задано, цена автоматически корректируется» — **включает Smart Pricing** **[док, Р-12]** | как PRICE **[Р-35]** | ≤ 150 **[док]** | 111 rps **[док]** | Не документирован (проверить) | Синхронно; `item_unit_changed` содержит `minimum_price` и итоговую `price`, которую выставил Smart Pricing **[док]** | **Как выключить Smart Pricing — не описано:** в схеме `minimum_price` ≥ 1, `null` не разрешён (проверить, вопрос K-07) | Да | < 2 мин **[Р-8]** |
| **Otto · PRICE** | Products API, эндпоинт цен (версию — проверить) | аккаунт + SKU (Otto — только DE) | ? | ~10 req/s **[ТЗ]**; на партнёра или приложение (проверить) | ? | Возможна асинхронная обработка с ID задачи (проверить); readback цен (проверить) | Да | ? | < 5 мин **[Р-8]** |
| **Otto · QUANTITY** | Availability / quantities API (проверить) | аккаунт + SKU | ? | ~10 req/s **[ТЗ]** | ? | ? | Да | ? | < 5 мин **[Р-8]** |

---

## 2. Дополнительные атрибуты

| Атрибут | Amazon | eBay | Kaufland | Otto |
|---|---|---|---|---|
| Условная запись (If-Match / версия) | В модели `patchListingsItem` 2021-08-01 нет ни заголовка If-Match, ни поля версии **[док]** | Не известна (проверить) | **Нет** в спецификации 2.44.0 (ни If-Match, ни поля версии) **[док]** | ? |
| Канал сам уменьшает остаток при заказе | Да для MFN: при заказе Amazon вычитает заказанные единицы из доступного количества, при отмене возвращает **[док]** | Да (проверить) | ? | ? |
| Предусловие записи | Роль SP-API без restricted **[Р-4]** | Листинг под Inventory API **[Р-2]** | unit существует на витрине; `listing_price` > 0 в каждом `PATCH`/`bulk` (формулировка документации; адаптер пишет остаток без цены и эскалирует отказ — K-13, KFL_C03); SaaS — аккаунт технологического партнёра **[док]** | ? |
| Класс данных наблюдений | `AMAZON_INFO`, ≤ 18 мес **[Р-3]** | `CHANNEL_INFO`, ≤ 18 мес | `CHANNEL_INFO`, ≤ 18 мес | `CHANNEL_INFO`, ≤ 18 мес |
| Аутентификация | LWA OAuth | OAuth2 user token | HMAC-SHA256 от `METHOD\nURI\nBODY\nTIMESTAMP`; заголовки `Shop-Client-Key`, `Shop-Timestamp` (±5 мин), `Shop-Signature`, `User-Agent`; SaaS дополнительно `Shop-Partner-Client-Key`, `Shop-Partner-Signature` **[док]**. Кодировка: текст — base64, примеры и тестовый вектор — hex; hex проверен на векторе (K-01) | OAuth2 client credentials, токен 60 мин **[ТЗ]** |
| Единица лимитирования запросов | Пара «аккаунт продавца × приложение» и отдельный лимит на приложение (общий для всех тенантов) — у `patchListingsItem` 5 и 500 rps **[док]** | Частично приложение — общая для тенантов (проверить) | Продавец, 111 rps на все эндпоинты **[док]**; для технологического партнёра — на продавца или на партнёра (проверить, K-04); адаптер держит клиентский бюджет ниже лимита (KFL_C01) | ? (OQ-36) |
| Цена в DE/AT | Поле цены называется `value_with_tax` и в примерах для US (USD); включён ли НДС для amazon.de, из документации не следует — (проверить, A-02, OQ-102) | GROSS (проверить) | GROSS (проверить, K-12); НДС задаётся `vat_indicator` unit, ставки — `GET /v2/info/vat-indicators` **[док]** | GROSS (проверить) |
| Источник данных о конкурентах | `ANY_OFFER_CHANGED` (топ-20 по состоянию, только при активном своём предложении) **[док]**; `getCompetitiveSummary` (пакет) 0.033 rps, burst 1 **[док]**; `getItemOffers` 0.5 rps, burst 1 **[док]** | ? (OQ-38) | **Основной [Р-36]:** событие `buy_box_changed` — топ-10 предложений товара по рангу Buy Box, суммы целыми в минимальных единицах, `target_price` в своём предложении при `lost`/`changed`, идентификаторы видны только у своих предложений; с задержкой (debounce), best-effort, только `new` **[док]**; и `GET /v2/buybox?id_product&storefront&condition` — топ-10 по рангу, синхронно, все состояния; `price` и `shipping_rate` — number «в валюте витрины» (единицы — проверить, K-17) **[док]**. **Сверка:** `POST /v2/reports/competitors-comparer?storefront=` → асинхронный CSV с минимальной ценой нового и б/у предложения, статус и ссылка — `GET /v2/reports/{id_report}` **[док]**; частота обновления и запросов — (проверить, K-09). Полнота по источникам — [ADR-0007](adr/0007-competitor-data-completeness.md) | ? (OQ-38) |

---

## 3. Следствия для Dispatcher и модели

### Amazon
- **QUANTITY:** все офферы SKU в маркетплейсах EU — один `WriteScope`; значение считается и пишется один раз.
  Даже при работе только с DE запись меняет остаток SKU на .fr/.it/.es и других маркетплейсах, где SKU активен.
  Перед включением синхронизации это показывается продавцу и подтверждается (INV-11).
- **Гонка с заказом:** Amazon уменьшает остаток сам. Запись, посчитанная до получения `ORDER_CHANGE`, перезапишет
  уменьшение. Смягчение: буфер канала, приоритет обработки заказов перед пересчётом остатка, обнаружение через наблюдение.
- **p95 < 60 с** достижим только до точки `ACCEPTED`; время применения внутри Amazon мы не контролируем (OQ-48).
- **Шаг 20, по снимку SP-API:** лимит `patchListingsItem` — 5 rps на продавца и **500 rps на приложение, общий для всех тенантов**:
  при 100 одновременно активных продавцах пиковая сумма их лимитов (500) упирается в лимит приложения — ограничитель запросов
  адаптера нужен на двух уровнях (продавец и приложение), а распределение по тенантам — честное, без агрегирования их данных
  (AUP). Фид `JSON_LISTINGS_FEED` сообщения не упорядочивает — цена и остаток пишутся только `patchListingsItem` (Р-24, OQ-32).
  Суммы Amazon — десятичные в основных единицах: адаптер переводит целые центы (инвариант 7) без потери точности.
- **`minimum_seller_allowed_price` не пишется никогда [Р-111]** — это запрет, а не ожидание ответа A-05: по A-05 поле — вероятный
  включатель собственного автоматического ценообразования Amazon, аналог Kaufland `minimum_price` [Р-12]. База отклоняет поле порта
  CHANNEL_MIN_PRICE у Amazon (0077), порт перечисляет поле в `NEVER_WRITTEN_CHANNEL_ATTRIBUTES`. `maximum_seller_allowed_price` —
  (проверить) по A-05; поля порта для него нет.

### eBay
- **250 правок/день на листинг [Р-2]** при целевой задержке < 5 мин: непрерывный репрайсинг раз в 5 минут даёт
  288 правок в сутки — **больше лимита** даже без записей остатка. Поэтому:
  - резерв `quantity_reserve` в `EditBudget` недоступен для цены;
  - минимальный интервал между записями цены на листинг — параметр, выводимый из остатка бюджета и времени до конца дня;
  - при исчерпании — только уменьшения остатка из резерва; цена ждёт нового дня;
  - `unaccounted_margin` на правки продавца в Seller Hub, которых мы не видим.
- Мульти-вариационный листинг делит один бюджет на все вариации (проверить) — частота репрайсинга на вариацию ниже.
- `quantity = 0` без включённого out-of-stock control может завершить листинг (проверить) → предполётная проверка C11.

### Kaufland (первый канал)
- **Запись:** `PATCH /v2/units/{id_unit}` для одной единицы, `POST /v2/units/bulk` — до 150 unit **одной витрины**; поля `listing_price`,
  `amount`, у Smart Pricing — `minimum_price`. Каждый запрос требует `listing_price` > 0 (формулировка документации — проверить,
  допускается ли `bulk` только с `amount`, K-13). Частичный успех пакета — 207 с результатом по каждому unit.
- **Лимит:** 111 rps на продавца, общий на все эндпоинты. Дневной лимит правок объекта не документирован.
- **Единица записи остатка — аккаунт + `id_offer` [Р-35]:** связанные одинаковым `id_offer` unit разных витрин делят `amount`
  и склад, поэтому единица записи QUANTITY одна на `id_offer`, а PRICE — по витрине и unit. Шаблоны справочника и CHECK — миграция 0027;
  общее правило «область записи остатка шире области записи цены» — [ADR-0002, Р-37](adr/0002-write-scope-per-field.md#дополнение-р-37-область-записи-остатка-шире-области-записи-цены).
- **Подтверждение применения:** синхронный ответ с объектом unit + событие `item_unit_changed` с полным объектом unit + обратное
  чтение. Для Smart Pricing фактическая цена покупателя — поле `price`, а не `listing_price`.
- **Вебхуки:** подписка на событие и витрину; в теле уведомления нет идентификатора продавца, поэтому адрес вебхука должен
  однозначно указывать аккаунт: один адрес на аккаунт с непрозрачным ротируемым токеном в пути; подпись проверяется всегда,
  токен аутентификацией не считается [Р-40, Р-31]. Ответ 2xx за 5 секунд, иначе повтор (от 1 до 60 минут, ~12 ч),
  затем подписка отключается и события теряются — нужна сверка.
- **Конкуренты [Р-36]:** основной источник — `buy_box_changed` и `GET /v2/buybox` (топ-10 по рангу); `competitors-comparer` — только
  сверка. `buy_box_changed` приходит с задержкой и под нагрузкой теряется, поэтому `GET /buybox` — запасной опрос; допустимая частота
  опроса — K-10. Что это значит для стратегий — [ADR-0007](adr/0007-competitor-data-completeness.md) [Р-39].
- **Ранний доступ и опрос [Р-45, Р-47]:** `buy_box_changed` выдаётся аккаунт-менеджером; до выдачи адаптер не пробует подписку, а основной
  источник — опрос `GET /v2/buybox` по ярусам волатильности: горячий ярус — с интервалом целевой задержки (Р-8), длинный хвост — раз в сутки.
  Сколько товаров помещается в горячий ярус, определяет бюджет запросов (K-04, K-10).
- **Уведомления с данными и без [Р-46]:** выбор по факту доставки. Без payload адаптер отдаёт `RESOURCE_CHANGED` с запросом из `resource`
  (`/buybox?id_product=…&condition=…` → опрос конкурентов, `/units/{id}` → обратное чтение), правило KFL_C19.
- **Потеря подписки [Р-48]:** после ~12 ч недоступности Kaufland отключает подписку и пишет на `fallback_email` — это ящик платформы с
  мониторингом. Восстановление — PATCH `is_active = true`, затем обязательный догон: заказы с момента потери, наши unit, конкуренты по ярусам,
  отчёт сверки. События за время отключения канал не повторяет.
- **Проверка входов [Р-42]:** снимки из `buy_box_changed` и `/buybox` проходят проверку правдоподобия до стратегии; ошибка единиц
  (K-17) ловится правилом «ровно ×100 / ×0.01» ([ADR-0008](adr/0008-price-decision-path.md)).
- **Комиссии:** в API есть `POST /v2/info/commission-rates/lookup` (до 50 EAN), но по Р-32 комиссии берутся из тарифных таблиц.
- **Технологический партнёр:** SaaS обязан зарегистрироваться как партнёр и подписывать запросы двумя парами ключей.
- **Адаптер (шаг 6):** [packages/kaufland-adapter](../packages/kaufland-adapter/). Всё, что документация не подтверждает, адаптер делает
  консервативно и пишет в журнал код правила — раздел 8. Контрактные тесты без сети — [tests/contract](../tests/contract/).

### Otto
- ~10 req/s **[ТЗ]**; если лимит на приложение, а не на партнёра — делится между тенантами (OQ-36).
- Централизованное обновление токенов (60 мин).

---

## 4. Консервативная политика для неподтверждённых значений

Применяется к каждому атрибуту со статусом `TO_VERIFY` или `UNKNOWN`:

| Атрибут | Поведение до проверки |
|---|---|
| `rate_limit` | Один параллельный запрос на `(channel_account, operation)`; экспоненциальный backoff с джиттером на 429/5xx; учёт заголовков лимита, если канал их отдаёт |
| `batch_max_items` | 1 |
| `object_edit_limit` | Если лимит может существовать — считаем все попытки, включая неуспешные |
| `confirmation_methods` | Запись не получает `APPLIED` без readback; readback по расписанию |
| `apply_grace_period` | Расхождение не объявляется до наблюдения после grace; `ASK_HUMAN` вместо `REASSERT` |
| `reversible` | Считаем необратимой: требуется подтверждение человека |
| `webhooks` | Считаем отсутствующими: сверка по расписанию |

---

## 5. План проверки

Результат каждой проверки записывается в `verification` соответствующего атрибута: статус, ссылка на источник, дата.

| Что проверить | Как | Где |
|---|---|---|
| Amazon: лимиты `patchListingsItem`, `getListingsItem`, Feeds | Документация по каждой операции + фактические заголовки `x-amzn-RateLimit-Limit` на тестовом аккаунте продавца (статическая песочница реальных лимитов не показывает — проверить) | Developer Central, тестовый seller-аккаунт |
| Amazon: атрибуты `purchasable_offer`, `fulfillment_availability` для DE | Product Type Definitions API для типа товара; `patchListingsItem` с `mode=VALIDATION_PREVIEW` (проверить наличие режима) | Тестовый аккаунт |
| Amazon: нотификации для цены/остатка/заказов | Перечень `notificationType` в Notifications API; тестовая подписка и SQS | Тестовый аккаунт |
| Amazon: задержка применения | Эксперимент: запись → опрос `getListingsItem` → распределение времени до видимости (≥ 100 замеров) | Тестовый SKU |
| Amazon: работает ли глобальный остаток EU так, как в Р-1, на неоднородных SKU | Эксперимент на SKU, активном в 2+ маркетплейсах | Тестовый аккаунт |
| Amazon: закрыто по снимку `vendor/amazon/sp-api-models/2026-09-16` и страницам документации (SOURCE.md). Осталось: вопросы A-01…A-08 (раздел 9) | Обращение в поддержку разработчиков SP-API + тестовый аккаунт продавца | Developer Support |
| eBay: снимок спецификации не сохранён — документация eBay отвечает 403 на автоматическую загрузку (vendor/ebay/sell-inventory-api/NOT-SNAPSHOTTED.md); все значения eBay — (проверить), вопросы E-01…E-06 (раздел 9) | Загрузка спецификации владельцем аккаунта разработчика eBay | eBay developer portal |
| eBay: лимиты вызовов приложения и пользователя | Developer Analytics API `getRateLimits` / `getUserRateLimits` | Ключи приложения |
| eBay: `bulkUpdatePriceQuantity` — размер пакета, уровень количества, формат ответа | Документация + Sandbox | Sandbox |
| eBay: область бюджета 250/день (вариации, оба поля, часовой пояс дня — отдельно для EBAY_DE и EBAY_US: до подтверждения запись с бюджетом правок отклоняет БД [Р-65], учёт неуспешных попыток и самой миграции) | Документация + обращение в eBay Developer Support; эксперимент в Sandbox, если лимит там воспроизводится | Developer Support |
| eBay: out-of-stock control и поведение при `quantity = 0` | Документация + Sandbox | Sandbox |
| eBay: вебхуки для собственных офферов и заказов | Notification API topics; Platform Notifications Trading API для мигрированных листингов | Документация |
| Kaufland: закрыто по документации 2.44.0 (снимок `vendor/kaufland/seller-api-v2/2026-09-14`). Осталось: вопросы K-01…K-18 (раздел 7) | Запрос в поддержку партнёров + тестовый аккаунт технологического партнёра | Kaufland partner support |
| Otto: эндпоинты цен и количества, пакеты, асинхронность, единица лимита, вебхуки/подписки, модель авторизации SaaS | Портал разработчика Otto Market + запрос в поддержку партнёров; песочница (наличие — проверить) | Otto developer portal |
| Все каналы: условная запись | Документация на заголовки If-Match/ETag или поля версии | Документация |
| Все каналы: GROSS в DE | Документация + сравнение значения, записанного через API, со страницей оффера | Тестовые офферы |

Технический инструмент: `SyncJob(CAPABILITY_PROBE)` — только read-only вызовы, записывает заголовки лимитов и
время ответа по аккаунту, не пишет в каналы.

---

## 6. Готовность канала к Release 1.0 [Р-11, Р-56]

Продукт не выпускается, пока не готовы три канала Release 1.0: **Kaufland** (витрины `de`, `at` [Р-26]), **Amazon** и **eBay** —
каждый на витринах ЕС и США [Р-56]. Otto и Walmart добавляются после первых клиентов, по факту спроса; их строки в матрице
остаются для плана. Канал готов, когда одновременно:

| Критерий | Как подтверждается |
|---|---|
| Витрины канала описаны | Строки `platform.marketplace` (идентификатор, страна, валюта, база цены, налоговый режим [Р-57, Р-58], часовой пояс суток [Р-62]) подтверждены документацией со ссылкой; сейчас у Amazon и eBay — (проверить) |
| Закрыты все «(проверить)» по каналу в этом документе | Каждый атрибут строк канала в `platform.channel_capability.verification` имеет статус `VERIFIED` или `DECIDED` с источником и датой |
| Работает запись | Запись PRICE и QUANTITY на тестовом аккаунте проходит путь `PENDING → DISPATCHED → ACCEPTED` |
| Работает обратное чтение | Для каждого поля есть рабочий способ `READBACK` или `REPORT` |
| Есть подтверждение применения | Запись переходит в `APPLIED` по наблюдению; измерено распределение задержки применения |
| Соблюдены бюджеты правок | Для каналов с лимитом правок на объект: попытки учитываются до отправки, превышение отклоняется БД (`edit_budget`); для каналов без документированного лимита — подтверждено, что лимита нет |

---

## 7. Вопросы в поддержку Kaufland (Seller API v2, спецификация 2.44.0)

| № | Вопрос | Почему важно |
|---|---|---|
| K-01 | Раздел Signing Requests: текст говорит «SHA-256 HMAC in base64 encoding», а примеры PHP/Python/Java и тестовый вектор дают hex. Какая кодировка обязательна для `Shop-Signature` и `Shop-Partner-Signature`? | Подпись всех запросов |
| K-02 | Подпись push-уведомлений: какие значение метода, URI (с query-строкой или без) и тело участвуют в подписи? Для технологического партнёра уведомление подписывается секретом продавца или партнёра? | Проверка подлинности вебхуков |
| K-03 | Время ответа на уведомление: на странице указаны и 15 секунд (все push-запросы), и 5 секунд (подтверждение уведомления). Какое значение действует? | Настройка приёмника |
| K-04 | Лимит 111 запросов в секунду «на продавца»: для технологического партнёра он считается на аккаунт продавца, на партнёра или на пару? Есть ли отдельные лимиты у `GET /buybox` и `POST /units/status`? Есть ли суточные лимиты? | Лимитер и ёмкость на тенанта |
| K-05 | Есть ли ограничение частоты изменений одного unit (число правок `listing_price` / `amount` в час или сутки)? | Бюджет правок (у eBay — 250/день) |
| K-06 | Связанные `id_offer` unit на `de` и `at`: `amount` на второй витрине обновляется в той же транзакции, что и `PATCH` на первой, или асинхронно? Приходит ли `item_unit_changed` по обеим витринам? Гарантирован ли порядок при двух REST-обновлениях `amount` на разных витринах? | Р-35: подтверждение остатка (KFL_C14) |
| K-07 | Как выключить Smart Pricing у unit после записи `minimum_price` (в схеме `minimum_price` ≥ 1, `null` не разрешён)? Сохраняется ли `minimum_price`, если в `PATCH` его не передавать? | Р-12, Р-41: выход из режима Smart Pricing |
| K-08 | Под Smart Pricing: приходит ли `item_unit_changed`, когда Smart Pricing меняет `price`? С какой частотой Smart Pricing меняет цену? | Omnibus: наблюдаемая цена в режиме Smart Pricing |
| K-09 | `POST /reports/competitors-comparer?storefront=at`: цены какой витрины в отчёте (описание говорит «kaufland.de»)? Как часто обновляются данные отчёта и как часто разрешено его запрашивать? Цены с доставкой и НДС? | Р-36: сверка конкурентов |
| K-10 | `buy_box_changed`: типичная задержка из-за debounce и доля потерянных уведомлений при высокой нагрузке? Разрешено ли регулярно опрашивать `GET /buybox` и с какой частотой? | Р-8: p95 < 2 мин |
| K-11 | Уменьшает ли Kaufland `amount` unit при оформлении заказа (`order_unit_new`)? В какой момент и приходит ли `item_unit_changed`? | Резервации и гонка остатка |
| K-12 | `listing_price` и `minimum_price` для `de` и `at` — цены с НДС (брутто)? Включают ли доставку? | Floor, Omnibus |
| K-13 | «You have to specify a listing price greater than zero» для `PATCH /units/{id_unit}` и `POST /units/bulk`: обязан ли запрос, меняющий только `amount`, содержать `listing_price`? | Раздельные записи цены и остатка (ADR-0002) |
| K-14 | Идемпотентность: как обрабатывается повтор идентичного `PATCH` после тайм-аута? Есть ли защита от дубля для `POST /units`? | Повторы при неизвестном исходе |
| K-15 | Какое поле фиксирует момент применения цены: `date_lastchange_iso` меняется при изменении `listing_price` и при изменениях Smart Pricing? Точность? | Подтверждение применения и Omnibus |
| K-16 | Уведомление не содержит идентификатора продавца. Для технологического партнёра с многими продавцами — рекомендуемый способ сопоставления: отдельный `callback_url` на продавца? Есть ли ограничение числа подписок? | Р-31: резолв тенанта входящего события |
| K-17 | `GET /buybox`: `price` и `shipping_rate` описаны как number (double) «в валюте витрины». Это основные единицы (17.95 EUR) или центы? В `buy_box_changed` те же суммы — целые в минимальных единицах | Цены конкурентов из опроса (KFL_C06) |
| K-18 | Перечень `event_name` в спецификации 2.44.0 (`POST /subscriptions`) не содержит `item_unit_*` и `buy_box_changed`, которые описаны на странице push-notifications. Можно ли подписаться на них через API и для каких витрин? | Р-36: основной источник конкурентов; быстрый путь подтверждения записи (KFL_C17) |

---

## 8. Консервативные правила адаптера Kaufland

Источник истины — [packages/kaufland-adapter/src/conservative.ts](../packages/kaufland-adapter/src/conservative.ts). Каждое срабатывание
пишется в журнал с кодом правила и номером вопроса. Сценарий — фикстура в [tests/contract/fixtures/kaufland](../tests/contract/fixtures/kaufland/),
где правило срабатывает (тест требует, чтобы сработало каждое правило).

| Код | Вопрос | Поведение сейчас | Что меняется после ответа | Сценарий |
|---|---|---|---|---|
| KFL_C01 | K-04 | Клиентский бюджет: 25 rps на продавца и общий 100 rps на партнёра; при нехватке запрос не отправляется, `RATE_LIMITED` с `retryAt` | Лимит на продавца — поднять бюджет продавца, убрать общий; на партнёра — общий бюджет основной и делится между тенантами (OQ-78) | `budget-exhausted` |
| KFL_C02 | K-14 | Запись (PATCH, bulk) не повторяется транспортом после тайм-аута и 5xx: `OUTCOME_UNKNOWN`, ядро делает обратное чтение | Повтор идентичного PATCH безопасен — разрешить транспортный повтор записей | `dispatch-timeout-unknown-outcome`, `dispatch-error-classification` |
| KFL_C03 | K-13 | Остаток пишется без `listing_price`; отказ из-за `listing_price` — `PRECONDITION_FAILED`, человек | Цена обязательна — остаток пишется с последней подтверждённой ценой, решение в ADR | `dispatch-bulk-207-partial` |
| KFL_C04 | K-07 | `CHANNEL_MIN_PRICE` отклоняется всегда [Р-41] | После проверки гипотезы нейтрализации на тестовом аккаунте и нового решения | `plan-stale-version` |
| KFL_C05 | K-08 | `minimum_price` > 0 у unit — Smart Pricing включён вне системы: CRITICAL | Уточнить, какие изменения цены Smart Pricing сопровождаются событием | `inbound-events-and-verification` |
| KFL_C06 | K-17 | `price` из `GET /buybox` — основные единицы, округление до центов | Если центы — убрать умножение на 100 | `read-competitors-buybox` |
| KFL_C07 | K-02 | Подпись уведомления: POST, полный URL, сырое тело, `Shop-Timestamp`, секрет продавца; иначе 401 и алерт | Заменить вариант подписи на подтверждённый | `webhook-bad-signature` |
| KFL_C08 | K-15 | Подтверждение сравнивает значение: несовпадение в окне — `PENDING`, после окна — `NOT_APPLIED` | Надёжный `date_lastchange_iso` — различать «не применено» и «перезаписано позже» | `plan-stale-version`, `confirm-shared-quantity` |
| KFL_C09 | — | Ответ 207 без строки по unit — `OUTCOME_UNKNOWN` | — | `dispatch-bulk-207-partial` |
| KFL_C10 | K-12 | Цены DE и AT — брутто; `NET` отклоняется | Если нетто — пересчитать базу в описании канала и в Gate | `plan-stale-version` |
| KFL_C11 | K-05 | Лимит правок не объявлен; 422 action-not-allowed на запись — человек и алерт | Лимит есть — объявить `EditBudgetRule` | `dispatch-error-classification` |
| KFL_C12 | K-03 | Уведомление старше 13 ч или из будущего больше 5 мин — 401 | Сузить окно, если время подписи ≠ время события | `webhook-bad-signature` |
| KFL_C13 | K-11 | Заказ — `RESOURCE_CHANGED`, остаток подтверждается чтением | Канал сам уменьшает `amount` — ожидаемое уменьшение не расхождение | `webhook-bad-signature` |
| KFL_C14 | K-06 | Подтверждение остатка читает все unit `id_offer` и требует совпадения | Распространение синхронное — достаточно носителя | `confirm-shared-quantity` |
| KFL_C15 | — | Неизвестное событие — `RESOURCE_CHANGED` без данных | — | `inbound-events-and-verification` |
| KFL_C16 | K-04 | `POST /units/status` — только для `is_live = false`, порциями по 20 | Регулярные проверки, если лимит позволяет | `confirm-shared-quantity` |
| KFL_C17 | K-18 | Подписка на `item_unit_*`/`buy_box_changed` пробуется; отказ — подписка неактивна, алерт, опрос вместо событий | Доступны — убрать запасной опрос; недоступны — пересмотреть Р-36 | `subscriptions-ensure` |
| KFL_C18 | — | Своё предложение в `/buybox` и `buy_box_changed` — по наличию `id_unit` | — | `read-competitors-buybox`, `inbound-events-and-verification` |
| KFL_C19 | — | Уведомление без payload — `RESOURCE_CHANGED` с идентичностью из `resource`: конкуренты — опрос `/buybox`, unit — обратное чтение (Р-46) | — | `pipeline-notification-without-payload` |

---

## 9. Вопросы в поддержку Amazon SP-API и eBay (шаг 20)

По снимку SP-API (`vendor/amazon/sp-api-models/2026-09-16`) и страницам документации из 26 ячеек Amazon в разделах 1–2 пометку
**[док]** получили 14. Из них в 3 осталась часть (проверить): write scope QUANTITY, «Обратимо» QUANTITY, подтверждение PRICE. Ещё
12 ячеек без [док]: без данных — «Лимит правок» PRICE и QUANTITY и «Цена в DE/AT»; остальные — решения и требования проекта или
без пометки. Утверждения о лимите 500 rps на приложение, отсутствии порядка в фиде, `value_with_tax`, `purchasable_offer`,
`minimum_seller_allowed_price` и уведомлениях взяты со страниц документации. В репозитории — их адреса и SHA-256, текст не хранится
(как у Kaufland); в моделях снимка этих утверждений нет. У eBay снимка нет: 18 из 26 ячеек — (проверить), [док] — ни одной.

| № | Вопрос | Почему важно | Открытый вопрос |
|---|---|---|---|
| A-01 | Количество MFN в `fulfillment_availability` (код `DEFAULT`) — одно значение на все витрины региона EU, где активен SKU, или на витрину? У атрибута нет селектора `marketplace_id`, уведомление `LISTINGS_ITEM_MFN_QUANTITY_CHANGE` витрину не несёт, но прямого утверждения нет | Р-1 и область записи остатка (Р-37); INV-11 — подтверждение продавца | OQ-50 |
| A-02 | `our_price.value_with_tax` для amazon.de — цена с НДС (брутто)? Для amazon.com поле называется так же — цена без sales tax? | Режим налога витрины [Р-58], пол маржи [Р-83] | OQ-102 |
| A-03 | По какой границе суток (часовой пояс) Amazon относит цену и продажи amazon.com и amazon.de к календарному дню? В документации отчётов даты — в UTC | История цен витрины [Р-62, Р-65]: для amazon.com пояс не задан — день не закрывается | OQ-112 |
| A-04 | Есть ли ограничение числа изменений цены или количества одного SKU за час или сутки, кроме лимита запросов? | Бюджет правок (у eBay — 250/день) | — |
| A-05 | Что делает запись `minimum_seller_allowed_price` / `maximum_seller_allowed_price`: включает автоматическое ценообразование Amazon, скрывает оффер вне порога или только ограничивает ручные изменения? | Аналог Kaufland `minimum_price` [Р-12]. **`minimum_seller_allowed_price` не пишется никогда [Р-111]** — ответ запрет не снимает; `maximum_seller_allowed_price` до ответа не пишется | — |
| A-06 | Типичное и предельное время от приёма `patchListingsItem` до видимости цены на витрине и в `getListingsItem` | Подтверждение применения [Р-64], p95 < 60 с [Р-8] | OQ-39, OQ-48 |
| A-07 | Burst `getListingsItem`: модель 2021-08-01 указывает 10, страница лимитов — 5. Какое значение действует? | Лимитер обратного чтения | — |
| A-08 | `ANY_OFFER_CHANGED`: задержка доставки и поведение `aggregationSettings` при частых изменениях; приходит ли уведомление, если своё предложение неактивно из-за остатка 0? | Достаточность источника конкурентов [Р-39] | OQ-31 |
| E-01 | **[Р-112] адаптер eBay заблокирован до получения снимка.** Снимок спецификации Sell Inventory API и страниц лимитов: документация отвечает 403 на загрузку без браузера. Официальный способ получить OpenAPI-файл для хранения в репозитории? | Все значения eBay | OQ-34 |
| E-02 | Бюджет 250 правок в день: единица (ItemID листинга, SKU, вариация), учитываются ли неуспешные попытки и сама миграция, **часовой пояс «календарного дня» отдельно для EBAY_DE и EBAY_US** | Бюджет правок [Р-2, Р-19]; БД отклоняет запись с бюджетом без подтверждённого пояса [Р-65] | OQ-47, OQ-112 |
| E-03 | `bulkUpdatePriceQuantity`: предельный размер пакета, частичный успех, уровень количества (товар или оффер) | Пакеты и подтверждение | OQ-47 |
| E-04 | Лимиты вызовов Inventory API для приложения и пользователя до и после Application Growth Check; какие из них общие для всех продавцов приложения | Лимитер, изоляция тенантов | OQ-35 |
| E-05 | Налоговый режим цены EBAY_DE (брутто) и EBAY_US (без sales tax) в `pricingSummary.price` | [Р-58] | OQ-102 |
| E-06 | `quantity = 0` у мигрированного листинга без out-of-stock control — завершает листинг? | Предполётная проверка C11 | — |

## 10. Открытые вопросы как параметры симулятора (шаг 21) [Р-113]

Неизвестное поведение канала задаётся параметром модели, а не угадывается в коде. Параметры, их значения по умолчанию и альтернативы —
`tests/contract/src/simulator/params.ts`; сценарии с вариантами — `tests/contract/fixtures/kaufland-sim/`; что показал каждый
вариант — [evidence/step21-simulator.md](evidence/step21-simulator.md).

| Канал | Вопросы → параметры | Не параметры (нечем задать) |
|---|---|---|
| Kaufland | K-04 (область лимита, нагрузка других продавцов), K-05 (лимит правок unit и ответ), K-06 (распространение остатка), K-10 (доступ, debounce, потери `buy_box_changed`), K-11 (уменьшение остатка при заказе), K-12 (брутто или нетто), K-13 (остаток без `listing_price`), K-14 (повтор записи, тайм-ауты), K-15 (задержка применения, `date_lastchange_iso`), K-17 (единицы цены `/buybox`) | K-01–K-03 (подпись и время ответа — свойства адаптера и вебхука, проверяются стендом), K-07–K-09 (Smart Pricing и отчёт сравнения — не пишем и не моделируем), K-16, K-18 (подписки не моделируются) |
| Amazon | A-01 (остаток на регион или витрину), A-04 (лимит правок SKU), A-06 (время применения, принято без применения), A-07 (burst обратного чтения), A-08 (задержка и потери `ANY_OFFER_CHANGED`); документированные 5/500 rps записи и 0.033 rps `getCompetitiveSummary` | A-02, A-03 (налог и граница суток — справочник витрин, не поведение канала), A-05 (запрет Р-111) |
| eBay | — | E-01…E-06: снимка спецификации нет, адаптер и модель заблокированы [Р-112] |

