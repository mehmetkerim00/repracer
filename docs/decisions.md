# Реестр принятых решений

Статус: **окончательные** · Р-1…Р-10 — шаг 2, Р-11…Р-19 — шаг 3, Р-20…Р-26 — шаг 4, Р-27…Р-34 — шаг 5, Р-35…Р-41 — шаг 6, Р-42…Р-48 — шаг 7, Р-49…Р-55 — шаг 8, Р-56…Р-59 — шаг 9, Р-60…Р-64 — шаг 10 (2026-09-14), Р-65…Р-67 — шаг 11 (2026-09-15).
Решение меняется только новым явным решением владельца продукта с записью в этот реестр.
Ссылки в документах: `[Р-n]`.

| № | Решение | Следствия в модели |
|---|---|---|
| Р-1 | **Amazon EU: остаток MFN — глобальное значение на SKU в регионе**, меняется во всех маркетплейсах сразу, побеждает последняя запись. **Цена — отдельно на каждый маркетплейс.** Единица записи разная для разных полей одного оффера. | `WriteScope` на поле; [ADR-0002](adr/0002-write-scope-per-field.md). Запись остатка для amazon.de меняет остаток SKU во всех маркетплейсах EU. |
| Р-2 | **eBay: только Inventory API.** Листинги, созданные вне него, требуют `bulkMigrateListing` (1–5 за вызов). **Миграция необратима:** после неё не работают Revise/Relist/Verify из Trading API, продавец навсегда теряет управление настройками Best Offer, требованиями к покупателям, благотворительностью и шаблоном оформления. Аукционы не мигрируются. **Лимит: 250 правок одного листинга в календарный день.** **Автомиграция запрещена**: предполётная проверка и явное согласие. | `EditBudget`, `ListingMigrationCheck`, `MigrationConsent`; [onboarding-ebay-migration.md](onboarding-ebay-migration.md). |
| Р-3 | **История цен делится надвое:** наши собственные цены — данные тенанта; данные, прочитанные из SP-API, — Amazon Information, не более 18 месяцев. *(Уточнено Р-20, Р-21: сырьё наших цен — 90 дней в PostgreSQL, затем архив; суточная свёртка — вечно.)* | `PriceHistory` (только свои цены, не удаляется) + `ChannelObservation` (наблюдения, срок по классу данных). |
| Р-4 | **PII не запрашиваем. Restricted-роли не берём.** | Нет хранилища PII. Для каналов, отдающих PII по умолчанию, — вычищение на входе. |
| Р-5 | **Floor: побеждает самый строгий (максимум). Абсолютный `min_price` обязателен**, без него репрайсинг не включается. | `GuardRail`, условие включения `pricing_enabled`. |
| Р-6 | **Мы не владелец остатка. Общий пул + буфер на канал.** *(Уточнено Р-15: «не владелец» относится к режиму внешнего источника; «общий пул + буфер на канал» действует во всех режимах.)* | `StockPool` — зеркало внешнего источника; `StockAllocation` = буфер канала; режим выделенных долей удалён. |
| Р-7 | *(Уточнено Р-56: Release 1.0 — Kaufland, Amazon, eBay; Otto и Walmart — после первых клиентов.)* **Порядок каналов: Kaufland → Amazon → eBay → Otto.** Первая география — Германия. *(Формулировка «MVP» удалена решением Р-11; география Release 1.0 после Р-11 и Р-14 — OQ-57.)* *(География Release 1.0 — Германия и Австрия, Р-26.)* | Порядок разработки каналов. |
| Р-8 | **Целевые задержки p95:** Amazon < 60 с, Kaufland < 2 мин, eBay и Otto < 5 мин. | `ChannelCapability.target_latency_p95`; точки измерения — OQ-48. |
| Р-9 | **Один пользователь может состоять в нескольких тенантах.** | `User` глобален; `Membership`. |
| Р-10 | **Модели на данных SP-API не обучаем вообще** (включая модели одного тенанта и автоподбор параметров стратегий). | INV-08. |

## Шаг 3

| № | Решение | Следствия в модели и схеме |
|---|---|---|
| Р-11 | *(Уточнено Р-56: «все четыре» — три канала Release 1.0: Kaufland, Amazon, eBay.)* **Слово MVP из проекта убрано. Цель — Release 1.0, полный продукт.** Каналы пишутся последовательно, но продукт не выпускается, пока не готовы все четыре. **Канал готов**, когда: закрыты все «(проверить)» по нему; работают запись и обратное чтение; есть подтверждение применения; соблюдены бюджеты правок. | Критерий готовности канала — в [channel-capabilities.md](channel-capabilities.md). |
| Р-12 | **Kaufland: указание `minimum_price` в API автоматически включает Smart Pricing Kaufland. Мы никогда туда не пишем. Floor держим у себя.** Исключение — явный режим тенанта «использовать Smart Pricing», при котором наш движок для оффера полностью отключается. **Режимы взаимоисключающие, смешение запрещено.** | `write_scope.pricing_mode ∈ {OFF, ENGINE, KAUFLAND_SMART_PRICING}`; поле записи `CHANNEL_MIN_PRICE` разрешено только в режиме Smart Pricing; intent/decision/PRICE-записи — только в режиме ENGINE. |
| Р-13 | ~~**Источник конкурентных данных Kaufland — `competitorsComparer`.**~~ *(Исправлено Р-36: основной источник — `buy_box_changed` и `GET /buybox`; CSV `competitors-comparer` — только сверка.)* | См. Р-36. |
| Р-14 | **Kaufland write scope: аккаунт + витрина + unit. Витрин несколько при одной регистрации.** *(Исправлено Р-35: так — только для цены; единица записи остатка — аккаунт + `id_offer`, общая для витрин.)* | Шаблон ключа PRICE; QUANTITY — см. Р-35. |
| Р-15 | **Остаток — три режима: внутренний пул / внешний источник через Inbound API / зеркало ERP. В Release 1.0 — первые два.** *(Жизненный цикл резервации — Р-25.)* | `stock_source.mode`; журнал движений для внутреннего пула; зеркало ERP запрещено ограничением схемы до отдельной миграции. |
| Р-16 | *(Уточнено Р-60: база региона выбирается по месту клиента, а не по витрине.)* **Изоляция: общая схема + RLS + `tenant_id` везде + партиционирование по тенанту на больших таблицах. Раздельные базы — только по регионам EU/US.** | [ADR-0003](adr/0003-tenant-isolation.md). |
| Р-17 | **Ответ SP-API на нашу запись — Amazon Information. Бессрочно храним только `write_id`, статус, время.** *(Место хранения — Р-20: завершённые записи уходят в ClickHouse и архив.)* | Ответы каналов — в `channel_data.channel_write_response` (18 мес). |
| Р-18 | *(Расширено Р-43: `max_price` существует на тех же уровнях и обязателен наравне с `min_price`.)* **`min_price` существует только на уровне товара и единицы записи. Уровня тенанта нет.** | Отдельная таблица `min_price` с двумя уровнями; в `guardrail` абсолютного пола нет. |
| Р-19 | **Бюджет eBay 250/день считается на листинг; учитываются все попытки, включая неуспешные.** | Счётчик попыток в `edit_budget` увеличивается до отправки, CHECK не даёт превысить лимит. |

## Шаг 4

| № | Решение | Следствия в модели и схеме |
|---|---|---|
| Р-20 | **Трёхслойное хранение.** Горячий слой PostgreSQL — только данные для текущего решения: `price_history` за 90 дней, незавершённые `channel_write`, актуальное состояние. Аналитический слой ClickHouse — `channel_observation`, `competitor_snapshot`, завершённые `channel_write`. Архив — Parquet в объектном хранилище, префикс по тенанту, удаление = удаление префикса. | [ADR-0004](adr/0004-storage-tiering.md); миграции 0015–0023; `schemas/clickhouse/`. |
| Р-21 | **Суточная свёртка `price_history`** (минимум, максимум, первая и последняя цена за день по единице записи) **хранится вечно и является юридическим доказательством для Omnibus.** Сырьё — 90 дней, затем архив. *(Уточнено Р-29: свёртка неизменяема, исправления — строками-поправками.)* | `tenant_data.price_daily`, ведётся триггером, закрытый день неизменяем. |
| Р-22 | **Инварианты, RLS и append-only остаются в PostgreSQL. ClickHouse — только чтение для аналитики, симулятора и бэктеста. Решение о цене никогда не читает ClickHouse синхронно.** | Всё, что нужно Price Gate и стратегии, имеет горячую проекцию в PostgreSQL (`competitor_state`, `observed_price_daily`, `observed_channel_state`). |
| Р-23 | **Изоляция тенантов в ClickHouse и архиве обязательна и описана отдельно:** доступ только через слой, подставляющий `tenant_id`; никаких общих выгрузок. | [tenant-isolation-analytics.md](tenant-isolation-analytics.md). |
| Р-24 | **Брокер — Kafka-совместимый, ключ партиции — `write_scope_id`. Порядок внутри единицы записи не может нарушаться шиной.** | [ADR-0005](adr/0005-message-broker.md); `outbox_event.scope_seq`. |
| Р-25 | **Резервация: `CREATED → CONFIRMED_BY_SOURCE → CONSUMED \| RELEASED`.** Подтверждение — только явным вызовом источника с внешним идентификатором заказа. TTL 24 часа, затем `RELEASED` с алертом. *(Уточнено Р-30: подтверждённая резервация старше 14 дней не освобождается, а поднимает алерт.)* | Миграция 0020; заменяет статусы резервации шага 3. |
| Р-26 | *(Уточнено Р-56 и Р-57: Германия и Австрия — ограничение только Kaufland, а не продукта; мультивалютность активна, EUR и USD.)* **География Release 1.0 — Германия и Австрия.** Мультивалютность остаётся в схеме, но не активируется. **Согласия на миграцию eBay при закрытии тенанта не удаляются: хранятся 3 года отдельно, с минимумом PII.** | CHECK «только EUR» (снимается отдельной миграцией); схема `legal`. |

## Шаг 5

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-27 | *(Уточнено Р-38: «целиком, всегда» — для ядра intent; входы с данными канала — 18 месяцев.)* **Три класса `price_intent`:** `CHANGED` — хранится целиком, всегда; `REJECTED_BY_GATE` — хранится целиком, всегда; `NO_OP` — сырьём 7 дней, затем схлопывание в почасовой агрегат (количество, причина, диапазон входных значений). | Меняет Р-20/Р-28 для intent: срок зависит от класса. ⚠ «Всегда» для входов с данными SP-API противоречит DPP (≤ 18 мес) — OQ-71. Миграция — шаг 6. |
| Р-28 | **Буфер в PostgreSQL: `price_intent` — 3 дня, `price_decision` — 30 дней, `observed_price_daily` — 45 дней.** | Политики хранения 0017 и 0016 меняются (решение — 3 → 30 дней, наблюдения — 60 → 45 дней). Миграция — шаг 6. |
| Р-29 | **Суточная свёртка неизменяема.** Исправление — добавочная строка-поправка со ссылкой и причиной; итог — представление. | `price_daily` строится по закрытому дню, не расширяется в течение дня; таблица поправок + представление итога; снимает запрет исправлений (OQ-66). Миграция — шаг 6. |
| Р-30 | **Подтверждённая резервация старше 14 дней не освобождается, а поднимает алерт.** | Задание по `CONFIRMED_BY_SOURCE` и `confirmed_at`; алерт через outbox; отвечает на часть OQ-64. |
| Р-31 | **Адаптер не выбирает тенанта.** Тенант приходит в сообщении и сверяется с `channel_account`. Несовпадение — отказ и алерт. | Порт `ChannelAdapter`: каждая команда несёт `tenantId` и `channelAccountId`; [ADR-0006](adr/0006-channel-port.md). |
| Р-32 | **Комиссии каналов — версионируемые тарифные таблицы в репозитории, не API.** | `fee_estimate.source = FEE_SCHEDULE`; эндпоинт Kaufland `/info/commission-rates/lookup` не используется для расчёта (допустим только для сверки таблицы вручную). Закрывает OQ-17. |
| Р-33 | **Стек: TypeScript для сервисов и фронтенда, React. Второй язык не вводится без ADR.** | npm workspaces в корне; `packages/*` на TypeScript. |
| Р-34 | *(Реализовано шагом 10: ретранслятор с advisory lock, порядок внутри единицы — по scope_seq; миграция 0038.)* **Ретранслятор outbox — собственный поллер с advisory lock. Debezium не используем.** | Порядок по `outbox_event.scope_seq`; закрывает часть OQ-65. |

## Шаг 6

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-35 | **Kaufland: единица записи остатка — аккаунт + `id_offer` (общая для витрин). Единица записи цены — аккаунт + витрина + unit.** Р-14 исправлен. | Шаблоны справочника: PRICE `[channel_account, marketplace, external_unit_id]`, QUANTITY `[channel_account, external_offer_id]` (миграция 0027); синхронизация остатка Kaufland требует `id_offer`. |
| Р-36 | *(Уточнено Р-45: `buy_box_changed` — ранний доступ; до его получения основной источник — опрос `GET /buybox`.)* **Источник конкурентов Kaufland — `buy_box_changed` и `GET /buybox` как основной; CSV `competitors-comparer` — только для сверки.** Р-13 исправлен. | Источники `KAUFLAND_BUY_BOX_CHANGED`, `KAUFLAND_BUYBOX` (0027); [ADR-0007](adr/0007-competitor-data-completeness.md). |
| Р-37 | **Общее правило (ADR-0002): область записи остатка шире области записи цены. Проверять это первым делом при подключении каждого нового канала.** | [ADR-0002, дополнение](adr/0002-write-scope-per-field.md#дополнение-р-37-область-записи-остатка-шире-области-записи-цены). |
| Р-38 | **`price_intent`: ядро хранится вечно, входы с данными канала — 18 месяцев. Следствие: окно бэктеста — максимум 18 месяцев; это заявленное ограничение продукта.** | `tenant_data.price_intent_core` (горячо 30 дней → архив навсегда); входы — ClickHouse, TTL 18 мес (0024). Закрывает OQ-71. |
| Р-39 | **Каждая стратегия объявляет требуемую полноту конкурентных данных. Стратегия недоступна на канале, который её не обеспечивает, и интерфейс объясняет причину.** Реализация — позже, решение фиксируется сейчас. | [ADR-0007](adr/0007-competitor-data-completeness.md); `CompetitorSnapshot.completeness` в порте. |
| Р-40 | **Вебхуки: один адрес на аккаунт продавца с непрозрачным токеном в пути. Токен — ротируемый секрет. Подпись проверяется всегда, токен аутентификацией не считается.** | `KauflandAdapter.handleInbound`; закрывает OQ-76. |
| Р-41 | **Smart Pricing до ответа поддержки не включается ни у кого.** Гипотеза нейтрализации (`minimum_price` = целевая цена) подлежит проверке на тестовом аккаунте, в код до подтверждения не попадает. | Адаптер отклоняет `CHANNEL_MIN_PRICE`; наблюдение `minimum_price` > 0 у unit — алерт. |

## Шаг 7

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-42 | *(Уточнено Р-49–Р-52: основные якоря — величины, которые система не меняет сама; наша цена — только предупреждение; различитель сдвига — разброс коэффициента; остановка блокирует только цены из данных конкурентов и снимается автоматически по выборке.)* **Слой проверки правдоподобия входных данных до движка стратегий. Испорченный снимок не должен превращаться в формально корректное решение.** Правила: отклонение от нашей цены более чем в N раз; выход за историческое распределение единицы записи за 30 дней; одновременный сдвиг многих снимков в одну сторону — остановка канала; соотношение ровно 100 или 0.01 — отдельный класс тревоги. | `packages/input-sanity`: `ACCEPT` \| `REJECT{reason}` \| `HALT_CHANNEL{reason}`; таблицы `rejected_competitor_snapshot`, `pricing_halt`, `competitor_price_daily`, `competitor_move` (0030); остановку снимает только человек; [ADR-0008](adr/0008-price-decision-path.md). |
| Р-43 | **`max_price` обязателен наравне с `min_price`. Без обоих репрайсинг не включается. Уровни — те же, что у `min_price` (товар и единица записи).** Р-18 расширяется. | `tenant_data.max_price`, `effective_max_price`; включение ENGINE без обеих границ или при min > max отклоняет БД (0030); потолок через `guardrail` запрещён; `validateRepricingEnablement` в `price-gate`. |
| Р-44 | **Price Gate проверяет нижнюю и верхнюю границу. Отклонённые по верхней границе решения хранятся так же, как отклонённые по нижней (класс `REJECTED_BY_GATE`).** | `price_decision.rejection_reason`, `max_price_ids`; выход за границу — отказ, не округление (CHECK `price_decision_no_bound_clamp`); тройная проверка: Gate, вставка решения, создание и отправка записи; невычислимая граница — `BOUND_UNRESOLVABLE` и CRITICAL; ClickHouse `040_step7.sql`. |
| Р-45 | **Kaufland `buy_box_changed` — ранний доступ, запрашивается у аккаунт-менеджера. До его получения Р-36 не активна.** | Адаптер: `buyBoxChangedAccess` (по умолчанию `NOT_GRANTED`) — подписка не пробуется; описание источника — `EARLY_ACCESS`; стратегии с конкурентами доступны через опрос `GET /buybox`. |
| Р-46 | **Адаптер обязан работать при обеих гипотезах о payload уведомления: с данными и без. Выбор — по факту доставки.** | Без payload — `RESOURCE_CHANGED` с идентичностью и `competitorQuery` из `resource` (KFL_C19); путь решения опрашивает `/buybox`, unit — обратное чтение. |
| Р-47 | **Опрос конкурентов ярусный по волатильности. Целевая задержка относится к горячему ярусу, длинный хвост — раз в сутки. Это заявленное ограничение продукта.** | `planPollingTiers`: горячий ярус — интервал целевой задержки Р-8, тёплый — час, холодный — сутки; при нехватке бюджета понижаются наименее волатильные; бюджет — вход (K-04). |
| Р-48 | **Потеря подписки: автовосстановление плюс обязательный догон состояния опросом. `fallback_email` — наш адрес с мониторингом.** | `ensureSubscriptions` включает отключённые подписки; `planSubscriptionCatchUp`: подписки → заказы с запасом назад → наши unit → конкуренты по ярусам → отчёт сверки; адрес — конфигурация платформы, не тенанта. |

## Шаг 8

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-49 | **Якоря проверки входов меняются.** Основные, в порядке применения: 1) себестоимость — цена конкурента ниже 1/3 или выше ×20 себестоимости неправдоподобна; 2) внутренняя согласованность снимка — одно предложение резко отличается от остальных; 3) тот же EAN на другом канале того же тенанта; 4) историческое распределение единицы записи. **Наша собственная цена — вспомогательный якорь: расхождение с ней даёт предупреждение, но не отклоняет снимок.** Причина: анкерить проверку на величине, которую система сама меняет, — замкнутый круг (ловушка OQ-90, слепое пятно холодного старта). | `packages/input-sanity`, набор правил `r49.1`: коды `PRICE_BELOW_COST_ANCHOR`, `PRICE_ABOVE_COST_ANCHOR`, `SNAPSHOT_INTERNAL_OUTLIER`, `CROSS_CHANNEL_MISMATCH`, `OUTSIDE_HISTORY_BAND`; без единого основного якоря — `NO_PLAUSIBILITY_ANCHOR` (класс `ANCHOR_MISSING`, fail-closed); предупреждения `OWN_PRICE_DEVIATION`, `INTERNAL_OUTLIER_IGNORED`; ×100 / ×0.01 сверяются с якорями и последним принятым значением, не с нашей ценой; `competitor_state.gtin` (0032); сценарии `pipeline-oq90-own-price-trap`, `pipeline-cold-start-cost-anchor`. |
| Р-50 | **Различитель «наш сбой против рыночного события» — разброс коэффициента сдвига.** Почти нулевой разброс при массовом сдвиге — ошибка разбора, остановка. Заметный разброс или сдвиг от одного продавца — рыночное событие, без остановки. | Порог — стандартное отклонение ln-коэффициента ≤ 0.005 (конфигурация); `competitor_move.seller_ref` (0032); предупреждения `MARKET_SHIFT_DISPERSED`, `MARKET_SHIFT_SINGLE_SELLER`; `assessShift` для выборки при проверке остановки; сценарий `pipeline-market-shift-dispersed`. |
| Р-51 | **Остановка канала блокирует только цены, выведенные из конкурентных данных.** Фиксированная цена, целевая маржа и ручная цена продолжают работать. | `price_intent.competitor_derived` (генерируемый из `rule_code`), копия в `price_decision`; триггеры одобрения и отправки проверяют остановку только для таких цен (0032); Gate — `isCompetitorDerived`. |
| Р-52 | **Остановка имеет автоматический выход:** по истечении окна берётся свежая независимая выборка; прошла проверки — работа возобновляется с записью в журнал. **Ручное снятие — без ограничения числа попыток, с обязательной заметкой.** | `pricing_halt.review_window`, `next_review_at`, `released_kind`; журнал `channel_data.pricing_halt_review` (append-only); снятие без записи журнала отклоняет триггер (0032); `pipeline.reviewHalts`, `releaseHaltManually`; сценарии `pipeline-halt-auto-release`, `pipeline-halt-review-failed-manual-release`. |
| Р-53 | *(Уточнено Р-58: НДС — режим расчёта витрин ЕС; в США — sales tax, цена витрины нетто.)* **Ставка НДС объявляется продавцом на товаре. По умолчанию — 19% (DE) и 20% (AT). Сопоставление категорий не строим. Цены витрин считаем брутто до ответа на K-12.** | `platform.vat_rate_default`, `tenant_data.product_vat_rate`, `tenant_data.effective_vat_rate_bp` (0032); ставка «неизвестна» больше не возникает — закрывает OQ-16. |
| Р-54 | **Intent, решение и запись цены — одна транзакция.** Границы читаются с закреплённой версией; изменение версии между снимком и записью — откат и пересчёт. | `PricingStore.commitDecision`; версия — последние строки `min_price`/`max_price` на обоих уровнях, перечитывается под `FOR SHARE` товара (изменение границ ENGINE-единицы берёт `FOR UPDATE` в отложенной проверке); код `BOUNDS_VERSION_CHANGED`; сценарий `pipeline-bounds-version-changed`, конкурентный тест в `packages/pricing-store-pg/test`. |
| Р-55 | **Отклонение снимка по устареванию поднимает предупреждение. Расхождение из-за правки продавца в кабинете канала — DivergenceCase, а не отклонение снимка.** | `SNAPSHOT_TOO_OLD` — алерт `COMPETITOR_SNAPSHOT_REJECTED`; наше предложение в снимке дальше ×1.5 от известных нам цен — `SELF_OFFER_DIVERGENCE` и `divergence_case` с причиной `EXTERNAL_CHANGE`; сценарий `pipeline-divergence-and-stale`. |

## Шаг 9

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-56 | **Release 1.0 — три канала: Kaufland, Amazon, eBay. Amazon и eBay покрывают и ЕС, и США. Otto и Walmart добавляются после первых клиентов, по факту спроса.** Р-26 (Германия и Австрия) — ограничение только для Kaufland, а не для продукта. Р-7 и Р-11 уточнены. | Справочник витрин `platform.marketplace` (0034): Kaufland `de`/`at`, Amazon DE и US, eBay DE и US; идентификаторы и режимы Amazon и eBay — (проверить) при адаптерах; критерии готовности — [channel-capabilities §6](channel-capabilities.md) для трёх каналов; адаптер Kaufland остаётся EUR и брутто. |
| Р-57 | *(Уточнено Р-61: себестоимость хранится в валюте возникновения и переводится при расчёте по курсу ЕЦБ.)* **Мультивалютность активируется до адаптера Amazon. Валюта — свойство единицы записи. USD и EUR поддерживаются наравне.** | CHECK «только EUR» заменены на `IN ('EUR','USD')` в 7 таблицах; валюта, база цены и налоговый режим единицы записи сверяются со справочником витрины (`offer_mapping_scope_guard`); себестоимость выбирается в валюте единицы записи, конвертации нет (OQ-99); проекция `competitor_state` хранит валюту и базу (0034). |
| Р-58 | **Налоговая модель обобщена: НДС (ЕС) и sales tax (США) — разные режимы расчёта цены.** В США цена на витрине нетто, налог добавляется при покупке; в ЕС — брутто. Режим влияет на расчёт маржи и пола. | `TaxTreatment` в `pricing-model`: `VAT_INCLUDED` (брутто, НДС внутри цены) и `SALES_TAX_EXCLUDED` (нетто, налог вне цены); `write_scope.tax_regime` ⇔ `price_basis` (CHECK); ставка НДС на товаре — только для стран с НДС; причина `PRICE_BASIS_MISMATCH`; сценарий `pipeline-tax-regimes-margin-floor`: себестоимость 10.00, комиссия 15 %, маржа 20 % — пол 19.15 EUR брутто и 15.39 USD нетто. |
| Р-59 | **Одна оценка цены — не больше 3 транзакций** (в шаге 8 — 10–12). | Порт `PricingStore`: `loadEvaluationContext` (один запрос) → `commitEvaluation` (снимок, движение, intent, решение и запись — одна транзакция с закреплённой версией границ и остановки) → `recordDispatch`; без изменения цены — 2 транзакции; [замер шага 9](benchmarks/step9-decision-path.md). Закрывает OQ-92. |

## Шаг 10

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-60 | **Регион хранения определяется местом клиента, а не местом витрины. Тенант живёт в одной базе, витрины — любые. Перенос тенанта между регионами — отдельная процедура, а не побочный эффект подключения витрины.** Р-16 уточнён. Закрывает OQ-101. | `tenant.data_region` неизменяем (`tenant_region_guard`); ни аккаунт канала, ни связка оффера, ни справочник витрин регион не ограничивают — проверка схемы 0039; EU-тенант с amazon.com хранится в базе EU; процедура переноса — [tenant-region-transfer.md](tenant-region-transfer.md). |
| Р-61 | **Себестоимость хранится в валюте возникновения. Конвертация — при расчёте, по дневному курсу ЕЦБ, зафиксированному на момент решения. Курс сохраняется вместе с решением ради объяснимости.** Закрывает OQ-99. | `platform.fx_rate` (неизменяем, загружает только `repracer_fx_loader`, 0037); «на момент решения» — последний курс, загруженный системой до решения, не старше 6 дней (иначе fail-closed: пол маржи не вычисляется, причина `FX_RATE_UNAVAILABLE`/`FX_RATE_STALE`); себестоимость переводится с округлением вверх; `price_decision.fx` — откуда, куда, курс, дата, сумма до и после; решение по переведённой себестоимости без курса отклоняет БД; загрузчик `@repracer/fx-rates`; сценарий `pipeline-fx-usd-floor-eur-cost` — 10.00 EUR → 11.56 USD → пол 17.79 USD. |
| Р-62 | *(Уточнено Р-65: пояс — по витрине со статусом проверки и источником; общего значения для США нет, America/Los_Angeles снято.)* **Граница суток для истории цен — свойство витрины, не константа Europe/Berlin. Часовой пояс переносится в справочник каналов.** Закрывает OQ-100. | `platform.marketplace.time_zone` (DE — Europe/Berlin, AT — Europe/Vienna, US — America/Los_Angeles (проверить)); `price_daily.day_tz` и `observed_price_daily.day_tz` сверяются с поясом витрины единицы; закрытие дней и удаление секций сырья — по каждому поясу; `price_day_close` — ключ (пояс, день) (0037); описание витрин адаптера Kaufland совпадает со справочником. |
| Р-63 | **Якорь «тот же EAN на другом канале» при разной валюте либо конвертирует, либо отклоняет с явной причиной. Молчаливый пропуск запрещён.** Закрывает OQ-103. | `input-sanity`: ссылка в другой валюте переводится по курсу ЕЦБ (к ближайшему центу), параметры отказа `CROSS_CHANNEL_MISMATCH` содержат курс; без курса — предупреждение `CROSS_CHANNEL_FX_UNAVAILABLE` с каналом, валютой и причиной, проверка якоря — SKIPPED с пояснением; сценарии `pipeline-fx-cross-channel-anchor-converted`, `-no-rate`. |
| Р-64 | **Записи в очереди отправляются диспетчером с упорядочиванием по `write_scope_id`. Запись не может остаться PENDING бесследно: либо отправлена, либо вытеснена новой версией с записью причины.** | `@repracer/write-dispatcher`; `channel_write.end_reason`, `superseded_by_write_id`, `last_error_code`, `next_attempt_at`; событие `scope.write.v1` «единица свободна, есть ждущая запись» — в той же транзакции отложенным триггером; обход `maintenance.due_write_scopes` (только идентификаторы и сроки) (0036); временная ошибка — повтор со сроком, неизвестный итог — сверка обратным чтением; до исправления терялись 19–35 из 40 (наблюдение пяти прогонов шага 10; тест с шага 15 утверждает только, что потеря есть, > 0) последних цен (`write-queue.pg.test.ts`); сценарии `pipeline-write-queue-second-not-lost`, `pipeline-write-superseded-with-reason`; [ADR-0009](adr/0009-write-dispatcher-and-broker.md). |

## Шаг 11

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-65 | **Часовой пояс — свойство витрины в справочнике каналов. Общее значение для США не подставляется. Amazon US и eBay US помечены (проверить) по отдельности: от границы суток зависит бюджет 250 правок eBay.** Р-62 уточнён. | `platform.marketplace.time_zone_status` (`CONFIRMED` / `TO_VERIFY`) и `time_zone_source` — у каждой витрины свои; у amazon.com и EBAY_US пояс не задан, у каждой — своя пометка; America/Los_Angeles из 0037 снят (0040). Подтверждённая витрина обязана иметь пояс (CHECK). Запись с бюджетом правок принимается только при подтверждённом поясе витрины и `budget_day`, равном текущему местному дню витрины (`aa_channel_write_budget_day_tz`). Сутки витрины без пояса не закрываются; секции сырья `price_history` с её строками не удаляются (`maintenance.price_history_in_unknown_time_zone`). Проверка схемы — 0041. Все витрины, включая DE и AT, остаются `TO_VERIFY`, пока граница суток не сверена с документацией канала (OQ-112). |
| Р-66 | **Клиент Kafka — confluent-kafka-javascript, не kafkajs.** | `@confluentinc/kafka-javascript` 1.10.1 (librdkafka), слой совместимости с KafkaJS: настройки в блоке `kafkaJS`. Продюсер идемпотентный, acks=all, одна пачка в полёте. Потребитель фиксирует смещения обработанных сообщений периодически — (проверить) на стенде. Внешний API `@repracer/broker` не изменился. На брокере не прогонялся (OQ-114). OQ-110 закрыт заменой клиента. |
| Р-67 | **Интерфейс строится до подключения каналов. Источник данных — стенд и фикстуры.** | `apps/console` (React 19, Vite 8) — пять экранов: товары, «почему эта цена», отклонённые решения, границы с разбивкой пола до цента, остановка цен. Модели экранов — `@repracer/console-model`, чистые функции над состоянием стенда. Сервер стенда прогоняет сценарии `tests/contract/fixtures` на хранилище в памяти и слушает только 127.0.0.1. Нет данных — экран показывает пробел, а не значение. Ручная остановка хранит автора и заметку и не снимается проверкой выборкой (0040). [ADR-0010](adr/0010-seller-console-on-stand.md). |

## Шаг 12

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-68 | **Решение хранит неизменяемый слепок объяснения:** сработавшие якоря, правила проверки входов с числами, версия набора правил, версия и параметры стратегии, проверки Gate. Слепок не содержит данных канала, поэтому не является Amazon Information и живёт вечно вместе с ядром решения. **Ссылка на полный снимок хранится отдельно и истекает через 18 месяцев** [Р-38]. Причина: экран «почему эта цена» на рабочих данных терял три шага из пяти. Закрывает OQ-120. | `DecisionExplanation` формата `r68.1` (`pricing-model/explanation.ts`); `price_decision.explanation` и копия триггером ядра в `price_intent_core.explanation`; параметры класса `CHANNEL` вырезаются в `withheld`, БД отклоняет слепок с их ключами (`security.channel_param_keys()` совпадает с `CHANNEL_PARAM_KEYS` — тест); `channel_data.price_decision_snapshot_ref` (18 мес) — в той же транзакции; `competitor_state.sanity_summary` — итог проверки принятого снимка для пересчёта (0042). Оценка — по-прежнему 3 транзакции [Р-59]. |
| Р-69 | **Два разных действия.** Kill switch (человек) останавливает все изменения цен без исключений, включая фиксированные и маржинальные. Остановка канала (система, испорченные данные) — только цены, выведенные из данных конкурентов [Р-51]. Закрывает OQ-116. | `tenant_data.price_stop` — остановка человеком: проверка Gate `PRICE_STOP` первой (HELD, `PRICING_STOPPED`), триггеры одобрения решения и создания и отправки записи; `pricing_halt` — только система (`CHANNEL_MASS_SHIFT`, CHECK `pricing_halt_system_only`); остановка входит в версию контекста решения (0042); сценарий `pipeline-kill-switch-tenant-stop`. |
| Р-70 | **Остановка тенанта — самостоятельный объект, а не набор остановок аккаунтов.** Аккаунт, подключённый при действующей остановке тенанта, не работает. Закрывает OQ-117. | Область `TENANT` без аккаунта; `tenant_data.pricing_stop_for` находит её для любой единицы записи; одна действующая остановка на область (`price_stop_active_uq`); тест `step12.pg.test.ts` подключает аккаунт после остановки. |
| Р-71 | **Каждый параметр с суммой несёт валюту. Интерфейс валюту не домысливает.** | `validateReason` (реестр причин); `security.jsonb_amounts_have_currency` в CHECK решения, ядра, intent, записи и отклонённого снимка (0042); сумма без валюты на экране — проблема объяснимости. |
| Р-72 | **Движок отдаёт только коды и параметры. Тексты — в словаре интерфейса, DE и EN. Английских строк в движке нет.** Закрывает OQ-121. | `REASON_PARAMS` (вид и класс источника каждого параметра); пояснения правил — `SanityNoteCode`; `explain`/`EXPLANATIONS` удалены, логи — коды; словарь `@repracer/console-model` `i18n/de.ts`, `i18n/en.ts` — полнота проверяет компилятор и тест. |
| Р-73 | **«Опасное изменение» — отклонённое Gate, где отклонение от границы больше 10 %. Остальное — «скорректировано».** Закрывает OQ-118. | `bound_deviation_bp = ceil(|предложено − граница| × 10000 / граница)` в решении и ядре, сверяется триггером пола; `dangerous` — генерируемый столбец (> 1000); `DANGEROUS_DEVIATION_BP`, `classifyBoundIntervention`; экран «остановленные изменения». |

## Шаг 13

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-74 | **Слепок объяснения — по классам intent (продолжение Р-27).** CHANGED — полный слепок, вечно; REJECTED_BY_GATE — полный слепок, вечно; **NO_OP — слепка нет, только код причины.** ~~Причина: слепок вырос в 4 раза на вечно хранимых таблицах и частично вернул рост, убранный на шаге 4.~~ **Обоснование исправлено Р-82:** NO_OP в вечное ядро не попадал никогда (Р-27, триггер `price_decision_record_core` пишет в ядро только CHANGED и REJECTED_BY_GATE), поэтому довод о вечных таблицах был неверен. Решение в силе ради горячего буфера решений: NO_OP — большинство потока, без его слепка буфер `price_decision` за 30 дней меньше на 44 % (замер шага 13). | `price_decision.no_change_reason` (код причины движка); CHECK `price_decision_explanation_by_class`: у NO_OP нет слепка, ссылок на справочник и ссылки на снимок, у остальных — есть (0044); пайплайн и хранилище в памяти не строят слепок для NO_OP; экран «почему эта цена» для NO_OP показывает код причины и пробел `NO_OP_NOT_EXPLAINED`. |
| Р-75 | **Повторяющиеся части слепка — в справочниках, слепок ссылается, а не дублирует:** версии наборов правил и параметры стратегий. | `platform.explanation_ruleset` — неизменяемый справочник: набор правил проверки входов `r49.1` (порядок правил, пороги по кодам) и профиль Gate `g74.1` (порядок проверок); решение и ядро ссылаются на него столбцами `sanity_ruleset`, `gate_profile` с внешним ключом; параметры стратегии — из `tenant_data.pricing_strategy` по версии (FK из ядра); слепок формата `r74.1` хранит только непрошедшую проверку Gate, правила не «прошли без пояснения» и пороги, отличные от набора (0044); совпадение справочника в коде и в БД проверяет тест. |
| Р-76 | **Остановки пишутся в `audit_event`, а не только в `price_stop`.** Остановка цен — действие человека с последствиями для денег, она обязана быть в общем журнале аудита. | Триггеры `zb_price_stop_audit`, `zb_pricing_halt_audit`, `zb_pricing_halt_review_audit` (0045): остановка и возобновление человеком — автор, пользователь, роль в момент действия, заметка, область; системная остановка и её снятие — актор SYSTEM; без события аудита остановку не записать. |
| Р-77 | **Тип стратегии доступен там, где решается, показывать ли предупреждение. Косвенный вывод по наличию минимальной маржи запрещён.** Закрывает OQ-127. | Стратегия хранится на единице записи независимо от режима: снято `write_scope_check3`, добавлены `write_scope_engine_has_strategy` (движок без стратегии не включается) и `write_scope_strategy_not_smart_pricing` [Р-12] (0045); включение без стратегии — отказ `STRATEGY_MISSING`; `MARGIN_WITHOUT_COST` всегда несёт `strategyType` и явный `requiredBy` (STRATEGY, MIN_MARGIN). |

## Шаг 14

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-78 | **Собственная аутентификация отменяется.** Вход, MFA, сброс пароля, приглашения — у внешнего поставщика identity; у нас — только сопоставление внешнего пользователя с членствами и ролями. Причина: 6 из 16 находок ревьюера шага 13 — про вход, впереди MFA, восстановление, приглашения, SSO; это не наш предмет и не наше отличие. Пароли и сессии удаляются, а не чинятся. | Поставщик — ZITADEL Cloud, регион EU ([ADR-0013](adr/0013-external-identity-provider.md)); 0048 удаляет `user_credential`, `user_session` и шесть функций входа, добавляет `platform.external_identity (issuer, subject)` и `security.resolve_external_identity`; `@repracer/identity` — проверка токена OIDC (RS256/ES256, JWKS) и сопоставление; стенд — имитатор поставщика; находки 1, 3, 9, 14, 16 закрыты удалением кода. |
| Р-79 | **Справочник правил и версии стратегий архивируются вместе с ядром intent.** Объяснение обязано оставаться самодостаточным после закрытия тенанта. | `exportCoreArchive` (`@repracer/analytics-export`): архив тенанта — строки ядра и ровно те версии стратегий и наборы правил, на которые они ссылаются; проверка — развернуть каждое объяснение из прочитанного обратно архива без базы; CHECK `partition_export_core_archive_self_contained` (0049): подтверждённый экспорт ядра в ARCHIVE — только со справочниками; экспортёр читает стратегии и справочник. |
| Р-80 | **Ядро уменьшается двумя способами сразу:** убрать из слепка данные, уже хранимые столбцами, и включить сжатие значения в секциях. | Формат `r80.1`: итог, причина и границы Gate, отклонение, профиль и набор правил, стратегия, правило, триггер, предложенная цена — только столбцы; копию отклоняет CHECK `*_explanation_no_column_copies`; столбцы intent в решении заполняет триггер из intent (0049). Сжатие: jsonb — lz4 с хранением MAIN во всех секциях, `toast_tuple_target` у листовых секций (порог в `retention_policy`, `ensure_partitions`). **Ограничение, найденное замером:** PostgreSQL сжимает значение, только если строка длиннее порога TOAST (~2 КБ, задан при сборке); `toast_tuple_target` его не снижает — обычная строка решения и ядра не сжимается (OQ-140). |
| Р-81 | **Почасовой агрегат NO_OP содержит код причины, а не только правило.** Иначе после 7 дней сырья вопрос «почему цена не менялась» остаётся без ответа навсегда. | `price_intent_noop.no_change_reason` и ключ `price_intent_noop_hourly (…, hour, no_change_reason, rule_code, trigger_type)` (`schemas/clickhouse/060_step14.sql`, проверка 6 в `099_verify.sql`); выгрузка берёт код из `price_decision.no_change_reason`, без кода строку не выгружает. Закрывает OQ-136. |
| Р-82 | **Исправление Р-74.** Заявленная причина была неверной: NO_OP никогда не попадал в вечное ядро (Р-27). Решение остаётся в силе ради горячего буфера (−44 %). | Обоснование Р-74 переписано; схема и код не меняются. |

## Шаг 15

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-83 | **ГЛАВНОЕ. Перед КАЖДОЙ отправкой цены в канал границы вычисляются заново и цена сверяется с ними — включая пол маржи, не только абсолютный `min_price`. Проверка исполняется на пути через PostgreSQL. Несовпадение — запись не уходит, алерт.** Причина: ретроспективное ревью показало, что «тройная проверка» Р-44 на деле одинарная — пол маржи при отправке не проверялся нигде (C4), `FINAL_RECHECK` тавтологичен (A4), тестов не было (A1). | `tenant_data.effective_price_floor` и `assert_price_floor` (0051): min_price и пол маржи по текущим себестоимости, комиссии, курсу ЕЦБ и ставке НДС, момент — время транзакции; вызываются триггерами записи при создании и при каждом переводе в DISPATCHED (первая попытка и повторы); пол не вычисляется — отказ. Отказ — `WRITE_BLOCKED_BY_BOUND_RECHECK` (FLOOR или FLOOR_UNRESOLVABLE) и алерт `PRICE_WRITE_NOT_SENT`; при фиксации — `BELOW_MARGIN_FLOOR`. Хранилище в памяти повторяет проверку. Тест `write-recheck.pg.test.ts`: себестоимость, курс, ставка НДС, min_price, повтор, фиксация; падал до исправления ([до](evidence/step15-r83-before.log), [после](evidence/step15-r83-after.log)); арифметика БД совпадает с кодом на 3000 случайных входов. |
| Р-84 | **Пропуск теста — провал сборки.** Тесты не пропускаются молча из-за отсутствия переменной окружения; CI гоняет всё на чистой PostgreSQL. Причина: «всё зелёное» 14 шагов подряд означало «зелёное при этих переменных локально». | Тесты БД без `REPRACER_PG_URL` падают при загрузке; `scripts/test-all.mjs` — сборка красная при любом skipped, todo, cancelled; `scripts/db/prepare.sh` — миграции, шаблон изолированных баз, смоук-тесты схемы `tests/db` (раньше вне репозитория); `.github/workflows/ci.yml` — PostgreSQL 16 и Redpanda. До шага 15 без базы молча пропускались 53 теста из 189. |
| Р-85 | **Вечный слепок не позволяет ВОССТАНОВИТЬ данные канала.** Производные величины (`targetMinor` и подобные), из которых выводится цена конкурента, к вечному хранению не допускаются. | Класс параметра `CHANNEL_DERIVED` (цели стратегий по рынку, неограниченная цель при упоре в границу, разница в мёртвой зоне) и производные от предложенной цены у цены из данных конкурентов (`proposedMinor`, `deviationBp`, `stepBp`, `amountMinor`) — вырезаются из слепка; у отклонённой цены из данных конкурентов ядро не хранит предложенную цену и отклонение, «опасное» — флагом; БД отклоняет слепок с производным ключом, существующие строки очищены (0052). Перебор-доказательство — `r85.test.ts` ([до](evidence/step15-r85-before.log)). Остаток: опубликованная цена и подрез стратегии — OQ-142. |
| Р-86 | **Сжатие ядра внутри PostgreSQL отменяется.** Ядро живёт окном ~2 месяца и уходит в архив, где сжимается gzip. Комментарии и правила о пороге TOAST исправить. | 0054 возвращает хранение по умолчанию во всех секциях, убирает порог из `retention_policy` и `ensure_partitions`; ложное утверждение в 0049 исправлено; правило 40 заменено (0056). Закрывает OQ-140. |
| Р-87 | **Parquet пишет ClickHouse своим экспортом. Новая библиотека не вводится.** | Архив ядра остаётся проверяемым JSON в gzip на тенанта (`core-archive.r79.1`); столбцовый Parquet — экспорт ClickHouse, когда слой будет развёрнут (OQ-114). Закрывает OQ-137. |
| Р-88 | **MFA обязателен для снятия остановки тенанта, массового импорта себестоимости и границ, смены ролей; на обычные изменения цен не требуется. Привязка identity создаётся ТОЛЬКО через приглашение.** | Второй фактор сессии — `app.auth_mfa` по `amr` токена (`hasSecondFactor`); БД требует его для снятия остановки тенанта и смены роли (0053); приглашение участника — владелец или администратор со вторым фактором; `platform.identity_invitation`, приём — `security.accept_identity_invitation`, у приложения нет вставки в `external_identity`; владельца нового тенанта регистрирует роль онбординга. Массового импорта в продукте нет — требование будет проверяться при его появлении (OQ-144). Закрывает OQ-138, OQ-139. |


## Шаг 16

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-89 | **Любой тест, не входящий в сборку, считается несуществующим. Проверка «все тестовые файлы репозитория запускаются сборкой» — часть CI.** Причина: 17 тестов шага 15, включая проверку главного инварианта Р-83, запускались вручную; откат защиты прошёл бы зелёным. | `scripts/check-test-inclusion.mjs`: каждый `*.test.ts` совпадает с аргументом скрипта `test` своего пакета или `test:repo` корня; файл, названный в скрипте, существует. Красная сборка до запуска тестов — в `scripts/test-all.mjs` и отдельным шагом CI. Поведение проверки — `scripts/test/check-test-inclusion.test.ts` на синтетическом репозитории с забытым файлом. Тесты `pricing-store-pg` — маской `test/*.pg.test.ts`. На шаге 16 вне сборки было 4 файла из 33 (17 тестов шага 15). |
| Р-90 | **Роли подключения к БД разделяются.** Роль пути решения — без права вставлять `audit_event`, менять членства, ставить признаки второго фактора и роли; административная роль — только для административного сервиса. База не может защитить от скомпрометированного приложения при единой роли — это записано в ADR честно, без обещаний, которые база не выполняет. | 0058: `repracer_app` (путь решения, `svc_app`, `svc_dispatcher`) лишён вставки в `audit.audit_event`, `tenant`, `app_user`, `membership`, `price_stop`, изменения членств и пользователей, функций приглашения, создания тенанта и сопоставления входа; `security.current_user_id()` и `session_mfa()` принимают `app.user_id` и `app.auth_mfa` только у сессии члена `repracer_admin`. `repracer_admin` (консоль) — права приложения, остановки человеком, роль и отзыв участника, приглашения; `repracer_provisioning` — только `security.provision_tenant`; `repracer_authenticator` — сопоставление входа и приём приглашения; события аудита пишут только функции триггеров роли `repracer_audit_writer`. Находки 2, 3, 5, 9–13 ревью шага 15 закрыты там же. Что база не обещает — [ADR-0016](adr/0016-role-separation-build-completeness.md), OQ-149. |
| Р-91 | **OQ-142: величина подреза в параметрах стратегии в вечном ядре НЕ хранится.** Хранится тип стратегии и ссылка на версию. Величина живёт в таблице с 18-месячным сроком. Юридическая оценка для Amazon — до подключения канала. | 0059: `channel_data.pricing_strategy_undercut` (класс CHANNEL, удаление через 18 месяцев после замены версии); CHECK `pricing_strategy_undercut_not_eternal`; стратегия по рынку без подреза не создаётся; существующие версии перенесены. В слепке `undercutMinor` — класс `CHANNEL_DERIVED`; экран берёт подрез из версии, пока он хранится. Архивы до 0059 — OQ-150; единица, закреплённая за заменённой версией, — OQ-151. Закрывает OQ-142. |
| Р-92 | **Утраченные тексты открытых вопросов не восстанавливаются по памяти.** Помечаются как утраченные с шагом возникновения и восстанавливаются, когда всплывут в работе. Восстановленное «по смыслу» опаснее отсутствующего: выглядит настоящим. | `docs/open-questions.md`: раздел «Утраченные тексты (Р-92)» (OQ-20, OQ-69, OQ-104…109); OQ-102 и OQ-114 — только тема от владельца; строки, записанные на шаге 15 по отчёту, помечены «не исходный текст». |
| Р-93 | **Проверки правил не сверяют имена функций в теле триггера и имена ключей. Правило проверяет ПОВЕДЕНИЕ либо не существует.** | Последняя проверка схемы (0061): свойства каталога, от которых зависит поведение (RLS, привилегии, реестр, сроки), и поведенческие проверки в откатываемой подтранзакции — начиная с Р-83 (отправка ниже пола маржи отклоняется); правила, сверявшие текст функций и имена ограничений, удалены с указанием поведенческого теста, который их заменяет (`migrations/README.md`). Перевод правила 29 на поведение нашёл дефект: проверка валюты сумм в БД (Р-71) не работала с шага 12 — исправлено 0060. |

## Шаг 17

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-94 | **Тест защиты обязан проверять ПРИЧИНУ отказа, а не сам факт отказа.** Тест, который остаётся зелёным после удаления своей защиты — потому что срабатывает другая, — считается несуществующим. Причина: ревьюер удалил триггер журнала снятия остановки, три набора тестов остались зелёными; таблица соответствия Р-93 ничего не доказывала. | `pg_temp.expect_fail(метка, запрос, причина)` в `tests/db/*.sql` сверяет SQLSTATE или текст сообщения и отличает «отказа не было» от «отказ по другой причине»; тесты TypeScript сверяют текст отказа (`assert.match`), а не только факт исключения. Проверка, которую нельзя довести до её причины (другая защита срабатывает раньше), не пишется: вместо неё — запись в документации, почему правило недостижимо. |
| Р-95 | **Мутационная проверка схемы — часть сборки.** Скрипт по одной снимает защиты (триггер, ограничение, право, проверку в теле функции) и убеждается, что падает именно заявленная проверка с заявленной причиной. Аналог мутационной проверки фикстур шага 6. | `scripts/db/mutation-check.mjs` и каталог `tests/db/mutations.mjs`: строка — инвариант, список мутаций и список проверок, которые обязаны упасть. Контрольный прогон без мутаций обязан быть зелёным. Непойманная мутация — красная сборка. Каталог покрывает таблицу соответствия Р-93 и защиты шага 17. **Уточнение шага 29 [Р-137]:** проверка идёт в ПОЛНОМ прогоне CI — при слиянии в main и по запросу, а не на каждом коммите: одна она занимает около получаса. Снятая защита теперь видна при слиянии, а не сразу (OQ-203). |
| Р-96 | **Роль пути решения получает ТОЛЬКО то, что нужно для вычисления и записи цены — списком разрешённого, а не перечнем запретов.** Всё остальное (согласия eBay, Smart Pricing, границы, себестоимость, стратегии, аккаунты, товары, поправки) — административной роли. Причина: перечень запретов был неполон, путь решения мог создать согласие на НЕОБРАТИМУЮ миграцию eBay. | `security.decision_path_allowed_privileges()` — единственный источник прав `repracer_app`; всё прочее отзывается (0062). Проверка схемы сверяет права в обе стороны, включая столбцовые и функции SECURITY DEFINER. Блокировку товара при фиксации решения и проверку выборки остановки путь решения получает функциями базы, а не правами на таблицы (0065, 0063). |
| Р-97 | **Компрометация административного сервиса — полная компрометация тенантов, к которым у него есть доступ.** Смягчений в ADR нет. Из этого следует: административные операции выполняются ТОЛЬКО по явному действию человека (пользователь сессии обязателен), фонового администрирования нет, каждая административная запись — в журнале аудита. | Триггеры `security.require_person_for_admin_write()` и `security.audit_admin_write()` на каждой таблице, запись в которую есть у административной роли и нет у пути решения (0066): без пользователя сессии и без действующего членства в тенанте строки запись отклоняется, состоявшаяся — пишется в `audit.audit_event` как `admin_change.*`. Право менять `platform.app_user` (в том числе признак второго фактора) у административной роли отозвано. |
| Р-98 | **Перепривязка внешнего входа — только по новому приглашению владельца тенанта.** Автоматической перепривязки нет. | `security.invite_relink` (владелец, второй фактор) и приём приглашения новым входом того же поставщика: прежняя привязка отзывается (`platform.external_identity_revocation`, неизменяемая), отозванный вход никого не сопоставляет и приглашений не принимает; выпуск приглашения и перепривязка — в журнале аудита (0066). Закрывает OQ-148. |

## Шаг 18

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-99 | **Мутация считается пойманной ТОЛЬКО если упала своя проверка данной строки каталога.** Падение соседней проверки — непойманная мутация. Причина: мутационная проверка страдала той же болезнью, которую лечит; цифра «не поймана 1 из 83» доверия не заслуживала. | Каталог `tests/db/mutations.mjs`: у каждой мутации — свои проверки (`own`); у проверки теста и проверки схемы — обязательная причина (сообщение утверждения или шаблон отказа), сверяемая с текстом провала без названия теста. Раннер различает «поймана», «упала только соседняя проверка» и «не поймана». Первый прогон по этим правилам: 17 непойманных из 98 ([docs/evidence/step18-mutation-first.md](evidence/step18-mutation-first.md)). |
| Р-100 | **Права выдаются ПО СТОЛБЦАМ там, где таблица содержит и рабочие, и административные поля.** Право на всю таблицу — только когда все её столбцы рабочие. | Путь решения: `pricing_halt` и `divergence_case` — вставка только рабочих столбцов (0068). Страж административной записи срабатывает на изменение административных столбцов (у UPDATE — список столбцов в аргументах триггера) и проверяет роль участника по объявленному действию таблицы (`security.admin_write_action`), а не только членство. Путь решения только блокирует действующую единицу записи; удержание и снятие блокировки — действия человека. |
| Р-101 | **Согласие на миграцию eBay: только владелец, только со вторым фактором, только от своего имени.** Пользователь сессии обязан совпадать с автором согласия. Причина: действие необратимо, после него продавец навсегда теряет управление настройками Best Offer. | Страж `migration_consent_guard` (0068) срабатывает раньше общего стража роли, чтобы каждая его причина была достижимой [Р-94]; признак MFA из профиля пользователя больше не принимается — только второй фактор сессии. |
| Р-102 | **Третья роль `svc_stock` для синхронизации остатка: только остатки и резервации, без цен и без аудита.** | `repracer_stock` со своим списком разрешённого (`security.stock_path_allowed_privileges()`, 0069); проверка схемы сверяет права в обе стороны; смоук `tests/db/smoke_stock.sql`. Закрывает OQ-152. |
| Р-103 | **Каждая append-only таблица имеет хотя бы одну строку в смоук-мире**, иначе проверка неизменяемости попыткой изменения ничего не проверяет. | `tests/db/smoke_append_only.sql`: пустые таблицы получают синтетическую строку в откатываемой транзакции, затем изменение строки каждой из 32 append-only таблиц обязано отклоняться триггером неизменяемости; таблица без строки — красная сборка. Закрывает OQ-155. |


## Шаг 19

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-104 | **Исключений в мутационной проверке не бывает.** Защита, которую ловит только соседняя проверка, получает собственную проверку. Принятые исключения через месяц превращаются в список, который никто не помнит. | Для каждой непойманной мутации честного прогона — либо своя проверка (сценарий, в котором соседняя защита не срабатывает раньше), либо защита удаляется как дубль с указанием, какая защита её держит: `channel_write_end_explained`, проверка владельца в стражах согласия, отдельная проверка платформенного тенанта, отдельная проверка «не административный сервис» в страже ручного intent, проверка вида элемента массива слепка (0072). Раннер честный: отказ смоук-проверки по другой причине — не поимка, метка сверяется целиком; у проверки теста — точная метка утверждения и фактический результат «без защиты» (`Missing expected rejection`, значение `actual`), сообщение утверждения само по себе поимкой не считается (ревью шага 19, находка 1). |
| Р-105 | **OQ-152 возвращается в открытые и закрывается так: `svc_stock` пишет в `channel_write` только поле QUANTITY.** Право ограничено видом поля, а не таблицей. | 0072: список разрешённого роли остатков получает запись в канал, её историю, бюджет правок, состояние синхронизации, событие outbox и чтение справочников; политики строк роли остатков на `channel_write`, `channel_write_history`, `write_scope`, `write_scope_sync_state` — только поле QUANTITY. Проверка схемы 0073 сверяет права роли по столбцам и условие поля в политиках; смоук `smoke_stock.sql` — запись остатка проходит, цены не видны, история цены отклоняется политикой. |
| Р-106 | **Шаг 19 — последний шаг линии безопасности.** Всё, что не закрыто, записывается в отдельный документ как принятый риск с указанием последствий. Дальше — каналы. | [accepted-risks.md](accepted-risks.md): что не защищено, при каком условии проявится, последствия, почему принято — без смягчений. |
| Р-107 | **Ручной intent вставляет только человек.** Путь решения не может создать «ручную цену владельца». | 0072: страж `channel_data.price_intent_manual_guard` — intent с видом MANUAL, правилом MANUAL или автором-участником принимается только от пользователя сессии административного сервиса, от его имени и с правом `ENABLE_REPRICING`; у пути решения пользователя сессии нет (Р-90) — отказ. Функция исполняется правами роли аудита. |

## Шаг 20

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-108 | **Каталог мутаций задним числом не расширяется. Но КАЖДАЯ НОВАЯ защита попадает в каталог при создании — это часть определения готовности.** Непокрытые на конец шага 19 защиты остаются в принятых рисках. | `scripts/db/check-catalog-coverage.mjs` (шаг CI после подготовки базы): триггер или CHECK-ограничение схем приложения без строки в `tests/db/mutations.mjs` и вне базового списка `tests/db/uncatalogued-baseline.txt` — красная сборка. Защита засчитывается, только если строка каталога называет её имя вместе с таблицей или мутирует тело функции, принадлежащей одному этому триггеру (ревью шага 20: прежний метод засчитывал одноимённые обобщённые триггеры других таблиц); базовый список по этому методу — 725 защит на конец шага 19 (238 триггеров, 487 CHECK), содержимое закреплено SHA-256 в скрипте. Поведение проверки — `scripts/test/check-catalog-coverage.test.ts`. Первое применение — защиты Р-109 (строка каталога `Р-109`, 5 из 5 мутаций пойманы). |
| Р-109 | **Риск 12б исключается из принятых и закрывается ДО первой строки кода миграции eBay.** Элемент согласия сверяется с предполётной проверкой по существу. Миграция необратима: продавец навсегда теряет управление настройками Best Offer — это сломанный аккаунт клиента. | 0074: элемент согласия принимается, только если вердикт согласия равен вердикту проверки, проверка — последняя для листинга и не старше **24 часов** на момент согласия, принятые потери поимённо равны потерям проверки (находка LOSS несёт имя потери — ограничение `listing_migration_check_findings_shape`). Срок 24 часа выбран нами: документация eBay срока не задаёт; изменение — решением владельца. |
| Р-110 | **Линия безопасности закрыта.** Новые находки ревьюера, не относящиеся к текущему шагу, идут в `accepted-risks.md` или в открытые вопросы и не порождают отдельных шагов. | Раздел ревьюера шага — в `docs/evidence/stepNN-review.md`; находка текущего шага исправляется в шаге или записывается риском. |

## Шаг 21

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-111 | **Поле Amazon `minimum_seller_allowed_price` не пишется НИКОГДА — как и `minimum_price` у Kaufland [Р-12]. Это запрет, а не пометка «проверить».** По A-05 поле — вероятный аналог включателя собственного автоматического ценообразования площадки. Две системы, меняющие одну цену, — отказ, который клиент припишет нам. | База: поле порта CHANNEL_MIN_PRICE есть только у возможностей Kaufland (`channel_capability_channel_min_price_only_kaufland`), режим Smart Pricing — только у единиц записи Kaufland (`write_scope_smart_pricing_only_kaufland`), запись поля — только в этом режиме (триггер `channel_write`); у Amazon поле порта CHANNEL_MIN_PRICE отклоняется базой на трёх уровнях (0077, смоук-проверки, строка каталога мутаций «Р-111»); третий уровень проверен смоуком на единице Kaufland, а тело запроса канала база не видит — атрибуты в теле проверяет контрактный стенд Amazon (ревью шага 21, находка 6). **Причина исправлена Р-114.** Порт: `NEVER_WRITTEN_CHANNEL_ATTRIBUTES`, `isNeverWritten` (`packages/channel-port`); модель Amazon симулятора отказывает при планировании; симулятор Kaufland считает запись `minimum_price` нарушением. Исключение Kaufland (явный режим Smart Pricing [Р-12]) остаётся и не включается до ответа поддержки [Р-41] |
| Р-112 | **Снимок спецификации канала — обязательное условие адаптера. Для eBay снимок недоступен без ключей разработчика — адаптер eBay заблокирован до регистрации.** | `vendor/ebay/sell-inventory-api/NOT-SNAPSHOTTED.md` остаётся причиной; кода адаптера eBay нет; симулятор модели eBay не строит — поведение без снимка задавать нечем (E-01); channel-capabilities §9 |
| Р-113 | **Симулятор строится на контрактном стенде шага 6: тот же формат сценариев, канал с состоянием вместо записанных ответов.** | `tests/contract/src/simulator/`: `SimulatedKauflandChannel` реализует тот же `ChannelBehaviour`, что `ScriptedChannel`; сценарий задаёт `world.channelModel` вместо `exchanges`, шаги `channelRun`/`channelDeliver`/`channelOrder`, `expect.channel` и `variants` — ответы на открытые вопросы K-nn как параметры модели; модель Amazon — на уровне порта (адаптера Amazon нет). Бэктест — тот же путь решения на записанной истории; ADR-0021 |

## Шаг 22

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-114 | **Исправление Р-111: причина запрета была неверной.** `minimum_seller_allowed_price` и `maximum_seller_allowed_price` сами по себе репрайсер Amazon не включают — его включает отдельный атрибут `automated_pricing_merchandising_rule_plan` с идентификатором правила. Запрет на запись границ остаётся по другой причине: они создают второй набор границ на стороне канала, и базовая цена обязана лежать между ними, иначе Amazon возвращает ошибку. Наши границы должны быть единственными. `automated_pricing_merchandising_rule_plan` добавлен в список «не пишется никогда» — он включает чужой репрайсер. | Решение владельца; страница manage-purchasable-offer называет атрибут только «Automated pricing rule configuration», включение репрайсера и ошибку вне границ документация не описывает. Порт: `NEVER_WRITTEN_CHANNEL_ATTRIBUTES` с причиной `CHANNEL_REPRICER` или `SECOND_BOUNDS`, `neverWrittenAttributes(channel)`; адаптер Amazon пишет только `our_price` и `fulfillment_availability`; контрактный стенд отклоняет любой запрос, в теле которого есть запрещённый атрибут. Комментарии ограничений 0077 — по Р-114 (0080) |
| Р-115 | **Обнаружение чужого репрайсера.** Если в атрибутах оффера есть привязка к правилу автоматического ценообразования, наш движок для этой единицы не работает, продавец получает предупреждение. Состояние читается `getListingsItem`: изменения правил асинхронны. | Адаптер Amazon: чтение перед каждой записью цены (AMZ_C03) — при привязке PATCH не отправляется, отказ `CHANNEL_REPRICER_ACTIVE` (REQUIRES_HUMAN), при границах канала — `CHANNEL_BOUNDS_PRESENT`; обратное чтение отказывает тем же кодом. Ядро: отказ записи — единица BLOCKED; отказ сверки с классом REQUIRES_HUMAN — единица BLOCKED сразу, без часа повторов (`planReconciliationTransition`); CRITICAL-алерт `PRICE_WRITE_SCOPE_BLOCKED` с кодом; причина HELD несёт действие продавца `DISABLE_CHANNEL_REPRICER` / `REMOVE_CHANNEL_BOUNDS` (DE/EN). Периодической проверки офферов без записи нет (OQ-163) |
| Р-116 | **Риск 19 переводится из принятых в задачи.** После записи цены обратное чтение сравнивает применённую цену с отправленной; расхождение, равное ставке налога, — признак неверной базы цены, остановка канала. Это тихий отказ того же класса, ради которого строилась проверка входов, только на выходе. | `priceBasisMismatch` (pricing-model): расхождение ровно на ставку НДС товара, допуск 1 цент на округление (проверить). Диспетчер после APPLIED сверки и синхронного применения, путь решения после синхронной первой отправки — `checkPriceBasis` хранилища; прочитанная цена в остановке не хранится. Системная остановка витрины `CHANNEL_PRICE_BASIS_MISMATCH` блокирует ЛЮБУЮ цену, не только из данных конкурентов (0080: `ac_price_decision_basis_halt_guard`, `bc_channel_write_basis_halt_guard`; Gate и двойник в памяти — так же), выборкой не снимается, снимает человек со вторым фактором; CRITICAL-алерт `PRICING_PRICE_BASIS_MISMATCH`. Расширяет Р-51/Р-69 (остановка системы — не только цены из данных конкурентов) — OQ-162. Без ставки (sales tax, США) проверка не выполняется |
| Р-117 | **Отчёт «границы остановили N опасных изменений» считает не отклонения Gate (их в норме ноль), а случаи, когда стратегия хотела уйти за границу и была ею удержана.** Формулировка: «пол удержал цену N раз, без него вы продали бы на X дешевле». | `dangerousReport` (console-model): удержание полом — эпизод подряд идущих оценок единицы с `CAPPED_AT_MIN_PRICE` или `TARGET_OUTSIDE_BOUNDS_HOLD` с целью ниже пола; X — сумма по эпизодам наибольшей разницы (удержанная цена − цель стратегии) по валютам. Цель выведена из цены конкурента и живёт только в горячем intent 3 дня [Р-85, Р-28]: старше видны только удержания, сдвинувшие цену на пол, без суммы; удержания без изменения цены — не видны (пробел `FLOOR_HOLD_TARGET_WINDOW`). Отклонения Gate больше 10 % [Р-73] — второй раздел отчёта |

## Шаг 23

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-118 | **Третий вид остановки — остановка по недоверию каналу.** Держит ВСЕ цены, включая фиксированные и маржинальные. Снимается только человеком. Основание: сломана трансляция цены в канал, а не входные данные — фиксированная цена пройдёт по тому же сломанному пути. Это не расширение Р-51 и не kill switch (Р-69), а отдельный вид с собственной причиной. Сюда относится неверная база цены (Р-116) и всё, где мы не понимаем, что канал делает с нашими числами. | Решение владельца; закрывает OQ-162. База (0082): таблица `channel_data.channel_distrust` (причина `PRICE_BASIS_MISMATCH`, одно действующее недоверие на витрину и причину) — отдельно от `pricing_halt` (система, только цены из данных конкурентов, Р-51) и `price_stop` (человек, все цены, Р-69); защиты 0080 удалены, `pricing_halt` снова только массовый сдвиг. Держит одобрение, создание и отправку записи PRICE и CHANNEL_MIN_PRICE (OQ-166); ставит только путь решения и диспетчер (право INSERT по столбцам, человек не создаёт); снимает участник с правом `RELEASE_CHANNEL_DISTRUST` (владелец, администратор), от своего имени, со вторым фактором и заметкой; создание и снятие — в `audit_event`. Gate: проверка `CHANNEL_DISTRUST` после SCOPE, профиль `g118.1`; причина `CHANNEL_DISTRUSTED` (DE/EN); алерты `PRICING_CHANNEL_DISTRUSTED`, `PRICING_CHANNEL_TRUST_RESTORED`. Консоль: три вида остановки рядом (что держит, кто ставит, как снимается), раздел недоверия со снятием; ADR-0023 |
| Р-119 | **Автоматическое снятие остановки по свежей выборке [Р-52] на Amazon неприменимо:** опроса конкурентов нет, выборку взять неоткуда. Это свойство канала, а не пробел. | Решение владельца; закрывает OQ-164. `ChannelDescriptor.haltRelease`: Kaufland — `SAMPLE`, Amazon — `MANUAL_ONLY`; справочник базы `platform.channel_behaviour` (0082) — то же значение, сверяет тест; `review_halt_by_sample` отвечает `MANUAL_ONLY`, путь решения пишет `HALT_RELEASE_MANUAL_ONLY` и не планирует проверку; экран остановок называет способ снятия по каналу; channel-capabilities §12 |
| Р-120 | **Чужой репрайсер проверяется при ОБНАРУЖЕНИИ оффера, а не только перед записью.** Продавец видит список товаров с активным Automate Pricing до того, как назначит им стратегию. | Решение владельца; закрывает часть OQ-163 (частота обхода не выбрана). `pipeline.discoverOffers`: `searchListingsItems` с атрибутами, наблюдения — `channel_data.offer_channel_pricing` (18 мес, источник `DISCOVERY` / `PRE_WRITE_READ` / `READBACK`), WARNING `OFFERS_WITH_CHANNEL_PRICING`; наблюдение пишет и блокировка единицы по отказу чтения перед записью или обратного чтения. База отклоняет назначение стратегии и включение ENGINE офферу с последним наблюдением правила или границ канала (`write_scope_strategy_guard`, привязка — `offer_mapping_channel_pricing_guard`, 0082) и стратегии, для которой канал не даёт данных конкурентов (`channel_data.strategy_unmet`, OQ-166). Консоль: список офферов с ценообразованием канала на экране стратегий, такие офферы не выбираются, сохранение отклоняется с причиной |


## Шаг 24

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-121 | **Ярусный опрос [Р-47] — не только способ экономить квоту, но ОБЯЗАТЕЛЬНЫЙ дублирующий контур уведомлений.** Пропуск уведомления канал не выдаёт: номеров последовательности нет (риск 21). Единственная защита от молчаливой потери — независимая сверка опросом. Это свойство архитектуры, а не оптимизация. | Решение владельца; закрывает OQ-157. Опрос сверяется с последним принятым состоянием товара ДО обработки снимка (`pollCompetitors(…, { reconcile })`, `reconciliation.ts`): сравнивается то, о чём канал обязан уведомить, — Buy Box, если его дают и уведомление, и опрос (Kaufland), иначе наименьшая цена конкурента (Amazon). Расхождение — проверка потери со сроком (`channel_data.notification_loss_check`, 0088; совпадение база отклоняет); вердикт ставит только функция базы `review_notification_loss`: снимок уведомления того же товара, новее прежнего состояния и не позже срока — «задержка», иначе «подозрение на потерю» и один CRITICAL-алерт `NOTIFICATION_LOSS_SUSPECTED` на проверку. Доставка каждого снимка — в журнале (`PUSH`, `PUSH_FETCH`, `POLL`, `SAMPLE`). Kaufland — сверка ярусного опроса `GET /buybox`; Amazon — `getCompetitiveSummary` по кругу (до 20 ASIN за вызов, 0.033 rps), снимок только для сверки (`AMAZON_COMPETITIVE_SUMMARY`, роль RECONCILIATION, AMZ_C11). Срок по умолчанию — 15 минут: задержка уведомлений не документирована (K-10, A-08); ложные тревоги, если debounce канала дольше срока (замер на симуляторе). Вызывающий включает сверку, когда подписка на уведомления активна; расписания нет (OQ-174) |
| Р-122 | **Снимки конкурентов пишутся в ClickHouse с первого дня работы с любым живым каналом.** История не добирается задним числом: без неё бэктест навсегда остаётся синтетическим. | Решение владельца; закрывает OQ-156. Путь решения пишет полный снимок порта в транзитный журнал `channel_data.competitor_snapshot_log` (0086) в той же транзакции, что проекцию снимка, при любом вердикте проверки входов — испорченные снимки тоже история [Р-38, Р-42]; идентификатор снимка — тот же, что в ссылке решения. Выгрузка суток `exportCompetitorSnapshotsDay` — в `repracer_analytics.competitor_snapshot` с вердиктом и доставкой (090), проверка числа строк; строка, которую отклонили бы ограничения ClickHouse, пропускается с причиной. Секция журнала удаляется через 3 дня только после подтверждённой выгрузки, принудительно — через 14. В ClickHouse — 18 месяцев как данные канала [Р-3]. Объём на клиента — [docs/benchmarks/step24-competitor-snapshot-volume.md](benchmarks/step24-competitor-snapshot-volume.md) |
| Р-123 | **Комплаенс-модуль Omnibus строится на суточной свёртке цен [Р-21], которая хранится вечно ради него. Правило: при объявлении скидки указанная прежняя цена не может быть выше минимальной цены этого оффера за последние 30 дней, отдельно по каждому каналу и по часовому поясу витрины [Р-62].** | Решение владельца. Оффер — единица записи цены (аккаунт + витрина + оффер, Р-14): отдельно по каналу и витрине. Окно — 30 суток витрины до суток начала скидки и цены суток начала до начала скидки (ревью шага 24); начало — не раньше текущих суток витрины, скидка в будущем — «окно не завершено»; цена, действовавшая к началу окна, — последнее изменение до него; закрытые сутки — `price_daily_effective` (с исправлениями, Р-29), открытые — сырьё `price_history` (0087). История короче окна — «не проверить», а не «верно»; пояс витрины не подтверждён — «не проверить» [Р-65]. База вычисляет проверку при объявлении (`discount_announcement`, append-only, административная запись с аудитом) и отклоняет прежнюю цену выше наименьшей; код (`omnibusLowestPriorPrice`, pricing-model) и база совпадают (тест). Консоль: проверка «эта скидка нарушит правило» до записи, отчёт по объявленным скидкам с повторной проверкой по текущей истории, выгрузка доказательной истории цен CSV с SHA-256. Чего модуль не проверяет — на экране и в ADR-0024 |

## Шаг 25

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-124 | **Комплаенс-модуль НЕ гарантирует соответствие.** Он проверяет по известной нам истории и ОБЯЗАН показывать её глубину: «видим N дней истории по этому офферу, проверка достоверна / неполная». История содержит только наши записи: цены, выставленные в кабинете канала или другим инструментом, не видны, поэтому модуль может подтвердить скидку, которая нарушает правило (риск 27). При подключении оффера история цен запрашивается у канала, если он её отдаёт; иначе полнота отсчитывается с даты подключения. | Решение владельца. `omnibus_lowest_prior_price` (0091) и `omnibusLowestPriorPrice` отдают начало видимой истории (подключение единицы записи цены или первая известная цена), число суток истории к началу скидки и число цен, выставленных мимо нас и замеченных сверкой (кейсы расхождения Р-55): такие цены входят в наименьшую цену окна, проверка с ними — `EXTERNAL_CHANGES` («не проверить»). «Достоверна» — только статус OK, и тогда — по нашей истории. Объявление скидки хранит глубину. Экран: первой строкой «модуль не гарантирует соответствие», глубина на проверке, у каждого объявления и по каждому офферу, всегда — чего модуль не видит. История цен от канала: `ChannelDescriptor.priceHistory` — у Kaufland Seller API 2.44.0 и моделей SP-API 2026-09-16 операции истории цен нет (проверяет тест по снимкам) — отсчёт с подключения |
| Р-125 | **Бэктест по умолчанию считает ВЫБОРКУ — товары, дающие основную выручку; полный каталог — отдельным фоновым заданием.** 6,5 часа на стратегию для 10 000 товаров — неработающая функция, а не медленная. | Решение владельца. `selectBacktestSample` / `runSampleBacktest`: товары по убыванию оценённой выручки до 80 % выручки, не больше 200 и не меньше 10; источник оценки (`ORDER_LINES` или допущение `PRICE_ASSUMPTION`), покрытие выручки и «выборку ограничил предел» — в отчёте. `runCatalogJob`: полный каталог партиями товаров с контрольной точкой, прерванное задание продолжается с первой незавершённой партии; массовый сдвиг [Р-42] внутри партии — ложь бэктеста в отчёте. Замер — docs/benchmarks/step25-backtest-sample.md |
| Р-126 | **Периодические работы запускает отдельный процесс-планировщик.** Без него Р-122 не выполняется: журнал снимков удаляется через 14 дней без выгрузки, и история, ради которой всё писалось, пропадает. | Решение владельца; закрывает OQ-174. `services/scheduler`: состояние работ в базе (0090) — срок следующего запуска и аренда (перехват и освобождение чужой действующей аренды отклоняет база, долгий запуск её продлевает), журнал запусков с отставанием и числом обработанных объектов; суточная выгрузка выполняет каждый пропущенный слот по очереди и повторяет невыгруженные сутки из отставания, остальные работы — один запуск за все пропущенные; провал работы оставляет слот, после трёх подряд — CRITICAL; отставание — WARNING и CRITICAL по порогам работы. Работы — docs/evidence/step25-scheduler.md. Остановка планировщика на сутки историю не теряет (тест на PostgreSQL и ClickHouse в CI); история теряется, только если выгрузка суток проваливается 14 суток подряд, — с алертом провала с первых суток и алертом принудительного удаления |

## Шаг 26

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-127 | **Работоспособность планировщика контролируется ИЗВНЕ.** Планировщик регулярно отмечается во внешнем сервисе; отсутствие отметки даёт письмо владельцу, а не запись в его собственный журнал. Причина: все алерты отставания генерирует сам планировщик, а упавший процесс о себе не сообщит (риск 29). | `packages/service-runtime/src/heartbeat.ts`: отметка «жив» после каждого успешного такта и «сбой» после провального, не чаще раза в минуту (лимит сервиса — 5 отметок в минуту [док]); адрес отметки — секрет развёртывания, в журнал не пишется; без адреса процесс не стартует (кроме явного `REPRACER_SCHEDULER_HEARTBEAT=off` для стенда). Протокол — ping-адрес Healthchecks.io (`vendor/healthchecks/2026-09-17/SOURCE.md`); период, допуск и письмо владельцу настраиваются в сервисе. Проверка — модель «dead man's switch» в `services/scheduler/test/process.test.ts`: пока процесс работает, писем нет; остановленный процесс даёт письмо в пределах периода и допуска. Аккаунта сервиса нет — см. [docs/evidence/step26-external-control.md](evidence/step26-external-control.md) |
| Р-128 | **Периодические механизмы проверяются прогоном В ЖИВОМ РЕЖИМЕ, а не вызовом функции.** Тест обязан утверждать наблюдаемое поведение за период: сколько раз опрошен горячий товар, сколько холодный, не выпал ли товар без движения цены. Причина: ярусы опроса [Р-47] не работали девять шагов — третий случай одного типа после инварианта границ и проверки валюты сумм; общее у всех — код тестировался, но не исполнялся в реальном режиме. | `tests/contract/src/scheduler-live.pg.test.ts`: цикл процесса планировщика (`runScheduler`) работает виртуальные сутки на PostgreSQL с симулятором Kaufland и моделью порта Amazon; между тактами — приёмник уведомлений и диспетчер записей. Утверждаются числа запросов к каналу по каждому товару и строки базы, а не вызовы функций. Каждая работа каталога `JOB_CATALOG` обязана иметь утверждение о наблюдаемом поведении — иначе сборка красная. Находки первого прогона — [docs/evidence/step26-live-mode.md](evidence/step26-live-mode.md) |
| Р-129 | **Развёртывание планировщика — часть репозитория, а не устная инструкция.** Один экземпляр, перезапуск при падении, конфигурация, метрики. | `deploy/scheduler/compose.yaml` (закреплённые версии образов, `restart: unless-stopped`, один экземпляр, проверка работоспособности, секреты файлами), `deploy/scheduler/README.md`, `services/scheduler/src/main.ts` — точка входа: конфигурация из окружения и файлов секретов, каталог аккаунтов из базы, учётные данные каналов из каталога секретов, журнал и алерты строками JSON; `metrics.ts` — метрики Prometheus и `/healthz` без идентификаторов тенантов |
| Р-130 | **Расширение Р-128: живым прогоном проверяется ВЕСЬ фон, а не только периодические работы.** Диспетчер записей, приёмник уведомлений, ретранслятор outbox, выгрузка в ClickHouse и удаление по сроку проверяются прогоном настоящих циклов над моделями канала, очереди, брокера и аналитического слоя; утверждается наблюдаемое (запросы к каналу, строки базы, сообщения очередей, запросы к слою), а не вызовы функций. Причина: шаг 26 проверил живым режимом только планировщик, а стыки процессов модульные тесты не видят. | `tests/contract/src/background-live.pg.test.ts` — пять механизмов, у каждого своё утверждение (механизм без утверждения — красная сборка); модель ClickHouse по HTTP `tests/contract/src/live/fake-clickhouse.ts` понимает ровно то, что шлёт выгрузка, и сообщает о непонятых запросах. Найдено прогоном: суточная выгрузка не повторяется внутри суток (OQ-193). Найдено ревью того же шага: первая редакция прогона проверяла только путь отказа выгрузки, а два её утверждения были тавтологиями — правило «механизм утверждён» без проверки самих утверждений не спасает |
| Р-131 | **Без объявленной себестоимости репрайсинг НЕ ВКЛЮЧАЕТСЯ** (не «включается с предупреждением»). Продавец видит требование при попытке включить стратегию. Причина: у товара без себестоимости, истории и того же EAN на другом канале нет ни одного якоря проверки входов [Р-49] — ошибка канала в сто раз проходит незамеченной именно там, где у продавца нет ощущения нормальной цены (OQ-186, живой режим шага 26); себестоимость и так обязательна для пола маржи [Р-5, Р-83]. | Переход единицы записи в `ENGINE` без действующей себестоимости отклоняет база (0098, `zw_write_scope_cost_required_guard`); причина `COST_REQUIRED` с причиной отсутствия (нет профиля, нет оценки комиссии, нет курса) — `packages/pricing-model/src/reasons.ts`, словарь DE/EN; экран включения показывает препятствие, а не предупреждение с обходом; предупреждение `MARGIN_WITHOUT_COST` остаётся рядом как объяснение, но подтверждением больше не обходится |
| Р-132 | **Провалившаяся фоновая работа повторяется с растущей паузой, а не каждый такт.** Пауза удваивается с каждым провалом подряд, минимум — период самой работы, предел — сутки. Причина: процесс, который просыпается к ближайшему сроку, видел слот провалившейся работы в прошлом и повторял её в цикле; для работы с лимитом канала это означало запрос в секунду (живой режим шага 26, находка 6). | `services/scheduler/src/scheduler.ts`: `retryDelaySeconds(interval, consecutiveFailures)`, предел `RETRY_BACKOFF_CAP_SECONDS` = 86 400; число провалов подряд хранится в базе (`maintenance.scheduled_job.consecutive_failures`), поэтому перезапуск процесса паузу не сбрасывает |
| Р-133 | **Уточнение Р-132: «пауза не меньше периода работы» — только для работ, которые ходят во внешние каналы с лимитами.** Внутренние работы (выгрузка в ClickHouse, удаление по сроку, закрытие суток, секции) повторяются с растущей паузой от минуты до часа. Причина: по Р-132 суточная выгрузка после провала ждала сутки, и при отказе аналитического слоя история снимков терялась за 14 попыток вместо сотен (OQ-193, живой прогон шага 27). | Вид повтора — свойство работы и хранится в базе (`maintenance.scheduled_job.retry_kind`, 0104): CHANNEL — пауза от периода работы, потолок сутки; INTERNAL — от минуты, потолок час. `retryDelaySeconds(interval, failures, kind)` в `services/scheduler`; INTERNAL у `analytics-export-day`, `price-days-close`, `partitions`, `retention` |
| Р-134 | **Массовый импорт себестоимости — часть онбординга, а не отдельная функция на будущее.** Причина: без себестоимости репрайсинг не включается [Р-131], а в тестовых данных её не было у 76 из 101 включённого оффера; руками столько не завести — продавец не дойдёт до конца настройки. Импорт обязан: разбирать типичные выгрузки (CSV, XLSX), сопоставлять колонки с подсказками, показывать предпросмотр до применения, отдельно перечислять строки, которые не сопоставились, и не применять ничего частично. | `packages/cost-import` — разбор CSV и XLSX без внешних зависимостей (XLSX — ZIP с XML, распаковка `node:zlib`), подсказки колонок по заголовкам и значениям, предпросмотр с отпечатком; база (0103): пакет `tenant_data.cost_import` с числом строк и отпечатком, строки импорта живут только в транзакции своего пакета, отложенная проверка «принесено ровно объявленное»; экран `apps/console/src/screens/CostImport.tsx` показывает, что применится, что нет и почему, сколько офферов останутся без себестоимости, и даёт исправить сопоставление колонок. Неоднозначное число и комиссия меньше процента без знака процента — отдельные причины, а не догадка |
| Р-135 | **Массовый импорт себестоимости и границ требует второго фактора, и разбиение на отдельные транзакции его не обходит** (OQ-144, риск 17). | Пакет импорта без второго фактора база не создаёт, а строки импорта без пакета не существуют (0103) — раздробить импорт на транзакции нельзя. Для правок мимо импорта — окно: больше пяти предложений за десять минут без второго фактора отклоняются (`mass_change_window_requires_mfa`). Считаются только правки БЕЗ второго фактора: база записывает признак `created_with_mfa` у каждой версии цены и себестоимости. Ключ окна — предложение, а не строка. По ревью шага: у версии себестоимости время создания равно времени транзакции (иначе окно обходилось датой задним числом), а гардрейл уровня тенанта или аккаунта, меняющий пол маржи у всех предложений, требует второго фактора прямо |
| Р-136 | **Расширение Р-130: всё, к чему прикасается продавец, проверяется живым прогоном ЧЕРЕЗ КОНСОЛЬ, а не вызовом хранилища.** Причина: импорт себестоимости не проходил через консоль (тело запроса 64 КиБ ≈ 2 180 строк, клиент падал на файле в сотни килобайт), а живой прогон этого не видел — он звал хранилище напрямую. Проверялся не тот путь, которым пойдёт продавец. | `apps/console/test/console-live.pg.test.ts`: 10 000 предложений, настоящая PostgreSQL, те же HTTP-запросы, что у браузера; у каждой операции утверждается время и размер ответа (предел экрана — 10 с и 8 МБ). Прогон нашёл семь операций, не выдержавших объёма ([docs/evidence/step29-console-live.md](evidence/step29-console-live.md)) |
| Р-137 | **CI делится на быстрый и полный. Быстрый на каждый коммит: типы, юнит-тесты, PostgreSQL. Полный при слиянии в main: мутации, брокер, ClickHouse, развёртывание. Задания параллельные.** Причина: прогон отменился по лимиту 40 минут, и разбор опечатки стоил три четверти часа. | `.github/workflows/ci.yml`: задания `fast` и `full` без зависимостей друг от друга; `node scripts/test-all.mjs --scope=fast|full`; что откладывается до полного прогона — поимённо в `scripts/test-scopes.mjs` с причиной, а не по имени файла. Отложенный тест не «пропускается» [Р-84]: быстрый прогон печатает, что именно отложено |
| Р-138 | **Импорт не переводит валюты и не угадывает единицы. Комиссия из файла продавца помечается источником «от продавца» и не смешивается с тарифной таблицей; при расхождении с фактическими выплатами показываются оба числа.** | Источник `SELLER_DECLARED` в `channel_data.fee_estimate` (0106): версии тарифа у него не бывает, у `FEE_SCHEDULE` она обязательна. Обе оценки живут рядом; пол маржи база считает по БОЛЬШЕЙ действующей оценке — заниженная комиссия опускала бы пол [Р-83]. Экран границ показывает обе с источниками и помечает ту, по которой посчитан пол. Неоднозначное число и доля комиссии без знака процента — отдельные причины в списке непримененных [Р-134] |

## Шаг 30

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-139 | **Массовые операции продавца выполняются ФОНОВЫМ ЗАДАНИЕМ с видимым прогрессом, а не синхронным запросом.** Относится к применению импорта себестоимости, правке границ каталога, назначению стратегии каталогу и выгрузке доказательной истории. Причина: применение импорта занимало 8 с на машине разработчика и 19 с на раннере CI; на медленной базе это упрётся в таймаут прокси, и продавец увидит ошибку при том, что изменения прошли. Задание создаётся, показывает ход, переживает перезагрузку страницы, применяется целиком или никак [Р-134] и возобновляется после падения процесса. | `tenant_data.bulk_job` и `tenant_data.bulk_job_artifact` (0108): вид, параметры, ход отдельными столбцами, аренда, число попыток, итог; ход пишется ОТДЕЛЬНОЙ транзакцией, поэтому переживает откат работы. Второй фактор предъявляется при СОЗДАНИИ задания: `security.second_factor_present(kinds)` принимает второй фактор сессии ИЛИ выполняющееся задание того же тенанта, созданное со вторым фактором, чей вид совпадает с тем, что пишется. Роли разделены [Р-90]: создаёт задание человек административной ролью (автор и аудит), ведёт его роль `repracer_bulk_worker` (аренда, ход, итог) — иначе каждый тик хода был бы строкой административного аудита. Исполнитель — отдельный процесс (`packages/bulk-jobs`, `apps/console/server/bulk-worker.ts`); аренда продлевается, пока идёт работа. Экран хода и история заданий — `packages/console-model/src/jobs.ts`, `apps/console/src/screens/Jobs.tsx`; задание видно только своему тенанту. Живой прогон и сценарий убитого процесса — [docs/evidence/step30-bulk-jobs-live.md](evidence/step30-bulk-jobs-live.md) |
| Р-140 | **Выбор предложений в консоли не переживает листание неявно. «Выбрать всё» означает весь каталог по текущему фильтру, а не показанную страницу; выбор отдельных строк сбрасывается при смене фильтра и явно показывается числом.** | Экраны границ и стратегий: «весь каталог (N)» — отдельный выбор, который раскрывает сервер (`all: true`, Р-136); страница выбирается своей кнопкой и названа страницей с её числом; строка «Выбрано: N из M» есть всегда, как и «Ничего не выбрано»; смена страницы сбрасывает выбор отдельных строк, и это сказано на экране до того, как случится. Проверяется тем, что читает продавец (`apps/console/test/console.test.ts`) |
| Р-141 | **Пол по большей комиссии выбирается по РЕЗУЛЬТАТУ на конкретной цене, а не по сравнению ставок.** «0 % + 5 €» дороже «10 %» на цене ниже 50 €. | Уточняет Р-138. `tenant_data.effective_price_floor` перебирает ВСЕ действующие оценки комиссии, считает цену минимальной маржи по каждой и берёт наибольшую — сравнение ставок было лексикографическим и опускало пол (находка 8 ревью шага 29). Проверка — `packages/pricing-store-pg/test/cost-import.pg.test.ts` со случаем, где оценка с фиксированной частью дороже оценки с большей ставкой; мутация «брать последнюю оценку перебора» ловится ею |

## Шаг 31

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-142 | **Уточнение Р-136: «через консоль» означает «КАК БРАУЗЕР».** Живой прогон не выдаёт себе заголовков, которых браузер не пошлёт: переход по ссылке, скачивание файла и открытие в новой вкладке идут без `Authorization`. Причина: файл доказательства Omnibus скачать было нельзя — ссылка не несёт токен, продавец получал 401 вместо CSV; OQ-202 объявили закрытым, а работать это не могло, и прогон не увидел, потому что слал заголовок сам. | Перенос байтов файла вынесен из работы с окном (`fetchFile` в `apps/console/src/api.ts`), и живой прогон зовёт ИМЕННО ЕГО — послать заголовок, которого не послала бы страница, он больше не может. Рядом — проверка перехода по ссылке без заголовка: файл по ней не отдаётся (401), то есть он и не лежит открыто. Структурное правило в `console.test.ts`: ни один экран не ведёт ссылкой на адрес API — правило проверено на той самой ссылке, из-за которой сломалось скачивание. Всё, что продавец уносит файлом, проходит один путь: фоновое задание готовит файл, консольный клиент забирает его запросом с токеном |
| Р-143 | **Второй фактор относится к ВИДУ операции, а не к факту выполнения задания.** Задание, которому второй фактор не нужен, не открывает окно для операций, которым он нужен, и его записи не считаются подтверждёнными. Причина: выгрузка доказательства открывала изменение пола маржи у всех предложений тенанта. | `security.second_factor_present(kinds)` требует три условия сразу: вид задания назван вызывающим стражем, аренда жива, второй фактор предъявлен при СОЗДАНИИ задания. Виды, которым второй фактор не нужен, названы одним списком и в базе (`security.read_only_job_kinds`), и в коде (`READ_ONLY_JOB_KINDS`). Правило проверки схемы (0112) не даёт поставить такой вид в список стража — то есть ошибка шага 30 больше не повторяется молча. Записи задания помечаются подтверждёнными тем же правилом, по которому база пропускает запись (`mark_created_with_mfa`): иначе окно массовой правки [Р-135] считает своими ровно те правки, которые человек только что подтвердил |
| Р-144 | **Рутинная операция не наследует требования массовой. Правка одного предложения второго фактора не требует.** | Второй фактор при СОЗДАНИИ задания обязателен только у импорта себестоимости — он массовый всегда [Р-134]. У правки границ объём заранее неизвестен: его знает запрос, и он же отвечает 403 до создания задания, если предложений больше одного; последнее слово за базой (`bounds_mass_edit_requires_mfa`). Признак «создано со вторым фактором» у задания стал несущим и получил свою проверку: задание границ, созданное без второго фактора, массовую правку не открывает. Проверяется живым прогоном на настоящем каталоге — вход одним паролем получает отказ на каталог и применяет одно предложение [OQ-209] |

## Шаг 32

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-145 | **Выгрузка файла имеет ОДИН обязательный путь.** Новый обработчик не может собрать файл по-своему обычным способом: хранилище выгрузки не входит в его тип. Обход типа ловит правило, а свойства результата проверяет база — каждый рубеж закрывает своё, и ни один не закрывает всё (уточнено по находке 3 ревью шага 32). Причина: выгрузка ленты собрала файл синхронно и квадратично — тот же дефект, который шаг только что починил для доказательства, внесённый заново новым обработчиком того же шага. Когда два обработчика решают одну задачу по-разному, второй делает её хуже | Обработчик задания ОБЪЯВЛЯЕТ файл (имя, заголовок, строки) и не может собрать из них байты: хранилище выгрузки не входит в тип его хранилища (`BulkJobStore`). Строки в файл превращает исполнитель (`packages/bulk-jobs/src/index.ts`) — он же режет файл на части, отдаёт цикл событий, считает контрольную сумму и приводит имя к безопасному виду. Правило «файл собирается ровно в одном месте» смотрит на весь код репозитория (`apps/console/test/console.test.ts`). В базе — три стража файла задания (0113): контрольная сумма считается по содержимому, файл кладётся только к своему идущему заданию, и только у вида задания из `security.file_producing_job_kinds()` |
| Р-146 | **Находка, пойманная мутацией или ревьюером, закрывается правилом там, где это возможно, а не только исправлением.** Мутация ловит случайно, правило — всегда | Разбор находок шагов 29–31 (`docs/evidence/step32-rules.md`): что закрыто правилом, что закрыто исправлением и почему. Новые правила — уровня репозитория (`scripts/test/repo-rules.test.ts`) и уровня консоли (`apps/console/test/console.test.ts`); у каждого названа находка, из которой оно выросло, и есть положительные и отрицательные контроли [Р-94] |
| Р-147 | **Итог массового задания не входит в список заданий.** Список отдаёт состояние и ход, итог запрашивается отдельно. Причина: итог с 10 000 правок уходил в список, десятки мегабайт при пределе экрана 8 МБ, и экран опрашивает его раз в секунду | `bulkJobsView` не несёт итога вовсе, `bulkJobView` отдаёт только то, что показывает экран (`result.view`); полный итог берётся отдельным запросом по одному заданию |

## Шаг 33

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-148 | **Логи и доказательства в репозитории не содержат путей файловой системы машины разработчика, имён пользователей и внутренних адресов.** Репозиторий публичный | Из шести логов `docs/evidence` убраны домашний каталог разработчика и рабочий каталог сессии (406 строк); пути заменены на `<repo>/`, `<home>/`, `<tmp>` — доказательства остались читаемыми. Правило в `scripts/test/repo-rules.test.ts` отклоняет такие строки у новых доказательств и ловит заодно адреса частных сетей; путь раннера сборки (`/home/runner/...`) разрешён намеренно — он одинаков у всех и ничего не раскрывает; правило смотрит весь репозиторий, а не только `docs/`. **Решение относится к рабочему дереву, а не к истории git:** прежние коммиты эти пути сохраняют, и на публичном GitHub они видны в просмотре коммита (находка 8 ревью шага 33). Вычистить их можно только перезаписью всех 101 коммита — это меняет SHA всех веток и требует отдельного решения владельца; секретов там нет, поэтому шаг 33 на это не пошёл. Шаг 34 записал это принятым риском 35: перезапись ломает привязку прогонов CI к коммитам |

## Шаг 34

| № | Решение | Следствия в модели, схеме и коде |
|---|---|---|
| Р-149 | **Онбординг — направляемый путь, а не набор экранов.** Шаги: тенант → подключение канала → импорт себестоимости [Р-134] → границы → стратегия с предпросмотром → включение. Каждый шаг завершается проверяемым состоянием; прогресс хранится в базе; путь возобновляется с места остановки. Пропустить шаг себестоимости нельзя [Р-131] — можно только сузить набор офферов | Состояние шагов ВЫВОДИТСЯ из данных функцией `tenant_data.onboarding_status` (0115), а не ставится галочкой: шаг завершён, потому что состояние проверяемо. Хранится только то, чего вывести нельзя, — `tenant_data.onboarding_progress`: сужение набора и последний шаг, запись человека с аудитом. «Себестоимость готова» — то же, чего требует включение (`write_scope_cost_ready`: себестоимость И полная оценка комиссии); живой прогон показал, что иначе путь говорит «готово» и упирается на последнем шаге. Включение набора — задание `REPRICING_ENABLE` со своим правом `ENABLE_REPRICING` [Р-143]; отказанные предложения называются на экране поимённо с причиной. Экран `Onboarding.tsx` — первый в консоли |
| Р-150 | **Канал, для которого нет доступов, показывает честное состояние «ожидает доступа» с перечнем того, что нужно (ключи, партнёрство), а не ошибку и не пустоту** | Схема запрещала «аккаунт без ключей»: CHECK требовал `credentials_ref` у всего, что не DISCONNECTED. Добавлено состояние `AWAITING_ACCESS` и столбец `access_blockers` (0115): ждущий обязан НАЗВАТЬ, чего ждёт, кодами из `security.channel_access_blockers()` — `PARTNER_REGISTRATION` (OQ-75), `DEVELOPER_KEYS` (OQ-112), `NOTIFICATION_QUEUE` (OQ-167), `SELLER_AUTHORIZATION`; активный без ключей по-прежнему невозможен. Экран пути и список миров показывают состояние и перечень; операция, которой нужен канал, у тенанта без канала отвечает `409 NO_CHANNEL`, а не 500 |
| Р-151 | **Демо-тенант: отдельный тенант на симуляторе шага 21 вместо живого канала.** Данные синтетические, но путь — настоящий: те же экраны, тот же движок, те же решения с объяснениями. Помечен как демо везде, где показываются деньги. Служит двум целям: показ продавцу и живой прогон онбординга целиком | Демо — тенант вида CUSTOMER с признаком `tenant.demo` (0115), а не новый вид: два десятка стражей (вход, очистка, аудит) видят его как клиента. Сценарий (`tests/contract/src/live/demo-world.ts`): 200 предложений, у каждого три конкурента — дрейф (`RANDOM_WALK`), подрезчик (`UNDERCUT_SELF`), волны каждые два часа (`SCHEDULE`). Метка — в списке миров и баннером на КАЖДОМ экране мира. Стенд поднимает демо по `REPRACER_DEMO=on`, время в нём идёт (виртуальная минута в секунду). Демо живёт в настоящем, а не в прошлом: данные, внесённые через консоль, действуют с настоящего момента |
