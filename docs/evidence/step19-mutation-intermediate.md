| Строка | Инвариант | Мутация | Поймана своей проверкой | Свои проверки мутации |
|---|---|---|---|---|
| 12 | Р-43, Р-44: потолок при создании и отправке записи | `DROP TRIGGER ba_channel_write_ceiling_insert ON tenant_data.channel_write` | да | ✗ smoke «write created above lowered max_price (Р-44, check 2)» |
| 12 | Р-43, Р-44: потолок при создании и отправке записи | `DROP TRIGGER ba_channel_write_ceiling_dispatch ON tenant_data.channel_write` | да | ✗ smoke «dispatch above lowered max_price (Р-44, check 3)» |
| 13 | Р-44: без округления до границы, причина отказа обязательна | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_no_bound_clamp` | да | ✗ smoke «clamp to floor instead of rejection (Р-44)» |
| 13 | Р-44: без округления до границы, причина отказа обязательна | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_rejection_reason_iff` | да | ✗ smoke «REJECTED without a reason» |
| 14 | Р-43: потолок не в guardrail | `ALTER TABLE tenant_data.guardrail DROP CONSTRAINT guardrail_ceiling_moved_to_max_price` | да | ✗ smoke «guardrail carrying a ceiling (moved to max_price, Р-43)» |
| 16 | Р-52: снятие остановки только с записью журнала | `DROP TRIGGER c_pricing_halt_release_journal ON channel_data.pricing_halt` | да | ✗ smoke «manual release without a journal record (Р-52)» |
| 17 | Р-51: признак цены по конкурентам копируется в решение | `DROP TRIGGER aa_price_decision_copy_derivation ON channel_data.price_decision` | да | ✗ smoke «competitor_derived is not derived from rule_code» |
| 18 | Р-57: валюты EUR и USD | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_supported_currency` | да | ✗ smoke «currency outside EUR and USD» |
| 19 | Р-58: налоговый режим единицы записи цены | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_tax_regime_for_price` | да | ✓ smoke «net price basis with VAT regime (Р-58)»<br>✗ smoke «price write scope without tax regime (Р-58)» |
| 20, 21 | OQ-98: отказ хранит параметры причины | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_rejection_explained` | да | ✗ smoke «rejection without its reason parameters (OQ-98)»<br>✓ store.pg.test.ts «OQ-93, OQ-94, OQ-98» /OQ-98: a rejection without its reason parameters is refused/ |
| 22 | Р-64: завершение записи — с причиной; ждущая запись объявляется событием | `ALTER TABLE tenant_data.channel_write_history DROP CONSTRAINT channel_write_history_end_explained` | да | ✗ smoke «write discarded without a reason (Р-64)»<br>✗ smoke «write history ended without a reason (Р-64)» |
| 22 | Р-64: завершение записи — с причиной; ждущая запись объявляется событием | `DROP TRIGGER zz_channel_write_announce_dispatch ON tenant_data.channel_write` | да | ✗ write-queue.pg.test.ts «control — without the dispatcher» /has no scope.write.v1 event/ |
| 23 | Р-61: курс ЕЦБ неизменяем; курс — в решении с переводом себестоимости | `DROP TRIGGER zz_fx_rate_immutable ON platform.fx_rate` | да | ✗ smoke «ECB rate is immutable (Р-61)» |
| 23 | Р-61: курс ЕЦБ неизменяем; курс — в решении с переводом себестоимости | `DROP TRIGGER aa_price_decision_fx_recorded ON channel_data.price_decision` | да | ✗ fx-day-boundary.pg.test.ts «Р-61 in the database» /Р-61: a decision on a converted cost without its exchange rate is refused/ |
| 23 | Р-61: курс ЕЦБ неизменяем; курс — в решении с переводом себестоимости | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_fx_shape` | да | ✗ smoke «decision exchange rate without its fields (Р-61)» |
| 24, 26 | Р-62, Р-65: граница суток — пояс витрины | `ALTER TABLE platform.marketplace DROP CONSTRAINT marketplace_time_zone_known_if_confirmed` | да | ✗ smoke «US storefront confirmed without a time zone» |
| 24, 26 | Р-62, Р-65: граница суток — пояс витрины | `DROP TRIGGER aa_channel_write_budget_day_tz ON tenant_data.channel_write` | да | ✗ smoke «budgeted write while the storefront day boundary is unconfirmed (Р-65)»<br>✗ smoke «budget day is not the current storefront day (Р-65)» |
| 24, 26 | Р-62, Р-65: граница суток — пояс витрины | `DROP TRIGGER aa_price_daily_day_tz ON tenant_data.price_daily` | да | ✗ fx-day-boundary.pg.test.ts «Р-65: a US storefront» /Р-65: a price day of a US storefront is not closed in/ |
| 25 | Р-60: тенант в базе своего региона | `DROP TRIGGER tenant_region_guard ON tenant_data.tenant` | да | ✗ smoke «tenant in wrong region DB (Р-60)» |
| 27 | Р-69, Р-70: остановки человеком и системные | `ALTER TABLE channel_data.pricing_halt DROP CONSTRAINT pricing_halt_system_only` | да | ✗ step12.pg.test.ts «Р-69: a system halt is only for broken channel data» /Р-69: a person cannot create a system halt/ |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER aa_price_stop_role_guard ON tenant_data.price_stop` | да | ✗ step12.pg.test.ts «Р-69, Р-70, OQ-125 in the database» /OQ-129: a viewer does not stop pricing\|OQ-125: an operator does not resume a tenant stop/ |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER ab_price_decision_stop_guard ON channel_data.price_decision` | да | ✗ smoke «approval while the tenant is stopped by a person (Р-69)» |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER bb_channel_write_stop_guard ON tenant_data.channel_write` | да | ✗ smoke «dispatch while the tenant is stopped by a person (Р-69)» |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER ca_pricing_halt_release_role_guard ON channel_data.pricing_halt` | да | ✗ smoke «manual halt release without a second factor (finding 12, Р-88)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_explanation_by_class` | да | ✗ smoke «NO_OP decision with an explanation (Р-74)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_no_change_reason_code` | да | ✗ smoke «NO_OP decision with an unknown no-change reason (Р-74)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `DROP TRIGGER a0_price_decision_intent_columns ON channel_data.price_decision` | **нет** (упала только соседняя проверка) | ✓ smoke «decision intent columns are taken from the client, not from the intent (Р-80)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `DROP TRIGGER a_price_decision_snapshot_ref_guard ON channel_data.price_decision_snapshot_ref` | да | ✗ step14.pg.test.ts «finding 10, Р-80» /finding 10: a snapshot reference of a NO_OP decision is refused/ |
| 29 | Р-71: сумма с валютой | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_amounts_have_currency` | да | ✗ smoke «rejection parameters with an amount without its currency (Р-71)» |
| 29 | Р-71: сумма с валютой | `ALTER TABLE tenant_data.channel_write DROP CONSTRAINT channel_write_end_params_currency` | да | ✗ smoke «write ended with an amount without its currency (Р-71)» |
| 29 | Р-71: сумма с валютой | `ALTER TABLE channel_data.rejected_competitor_snapshot DROP CONSTRAINT rejected_competitor_snapshot_details_cur` | да | ✗ smoke «rejected snapshot details: an amount without its currency (Р-71)» |
| 30 | Р-73: «опасное» согласовано с отклонением | `ALTER TABLE tenant_data.price_intent_core DROP CONSTRAINT price_intent_core_dangerous_consistent` | да | ✗ smoke «eternal core: dangerous flag against the deviation (Р-73)» |
| 31, 37, 38 | OQ-125, OQ-129, находка 4: матрица прав остановок, автор — пользователь сессии | `CREATE OR REPLACE FUNCTION security.pricing_permission(p_role text, p_action text) RETURNS boolean LANGUAGE sq` | да | ✗ step14.pg.test.ts «finding 2» /the matrix is not trivially all-true\|×/ |
| 31, 37, 38 | OQ-125, OQ-129, находка 4: матрица прав остановок, автор — пользователь сессии | `tenant_data.price_stop_role_guard(): «IF security.current_user_id() IS NULL OR u IS DIST…» → «IF false THEN…»` | да | ✗ step14.pg.test.ts «finding 4» /finding 4: a stop is not written in the name of another membership/ |
| 33 | Р-75: справочник объяснения неизменяем | `DROP TRIGGER explanation_ruleset_immutable ON platform.explanation_ruleset` | да | ✗ smoke «explanation ruleset is immutable (Р-75)» |
| 34 | Р-76: остановки и снятия — в журнале аудита | `DROP TRIGGER zb_price_stop_audit ON tenant_data.price_stop` | да | ✗ role-separation.pg.test.ts «the audit log is still written» /pricing.stop_created/ |
| 34 | Р-76: остановки и снятия — в журнале аудита | `DROP TRIGGER zb_pricing_halt_audit ON channel_data.pricing_halt` | да | ✗ smoke «the system halt is not in the audit log» |
| 34 | Р-76: остановки и снятия — в журнале аудита | `DROP TRIGGER zb_pricing_halt_review_audit ON channel_data.pricing_halt_review` | да | ✗ smoke «the manual release is not in the audit log with its author» |
| 35 | Р-77: движок без стратегии не включается | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_engine_has_strategy` | да | ✗ smoke «ENGINE without a strategy (Р-77)» |
| 41 | Р-79: подтверждённый архив ядра — со справочниками | `ALTER TABLE maintenance.partition_export DROP CONSTRAINT partition_export_core_archive_self_contained` | да | ✗ step14.pg.test.ts «Р-79» /Р-79: a verified archive of the core without the explanation dictionary is refused/ |
| 42 | Р-83: пол с полом маржи перед каждой отправкой | `tenant_data.channel_write_before_update(): «NEW.floor_at_dispatch_minor := tenant_data.assert_…» → «NEW.floor_at_dispatch_minor :=…»` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /below the recomputed margin floor was dispatched/<br>✗ write-recheck.pg.test.ts «the unit cost rose between decision and dispatch» /the write reached the channel/ |
| 43 | Р-85, Р-91, находка 15: из вечного ядра не выводится значение канала | `ALTER TABLE tenant_data.price_intent_core DROP CONSTRAINT price_intent_core_competitor_rejection_not_kept` | да | ✗ smoke «eternal core: a competitor-derived rejection keeps its proposed price (Р-85)» |
| 43 | Р-85, Р-91, находка 15: из вечного ядра не выводится значение канала | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_explanation_keys_declared` | **нет** (упала только соседняя проверка) | ✓ verify 0073_verify_schema_invariants_v16.sql /undeclared reason parameter was stored/<br>✗ другая причина smoke «decision explanation with an undeclared reason parameter (finding 15)» |
| 43 | Р-85, Р-91, находка 15: из вечного ядра не выводится значение канала | `ALTER TABLE tenant_data.price_intent_core DROP CONSTRAINT price_intent_core_explanation_keys_declared` | да | ✗ smoke «eternal core: an undeclared reason parameter in the explanation (finding 15)» |
| 44 | Находки 1–3, Р-88: журнал проверок, автоматическое снятие, второй фактор | `DROP TRIGGER a_pricing_halt_review_guard ON channel_data.pricing_halt_review` | да | ✗ smoke «automatic review recorded in a user session (finding 3)» |
| 44 | Находки 1–3, Р-88: журнал проверок, автоматическое снятие, второй фактор | `DROP TRIGGER zc_pricing_halt_review_released_in_tx ON channel_data.pricing_halt_review` | да | ✗ audit-guards.pg.test.ts «finding 1» /a release record without the release fails at commit/ |
| 44 | Находки 1–3, Р-88: журнал проверок, автоматическое снятие, второй фактор | `tenant_data.price_stop_role_guard(): «AND NOT security.session_mfa() THEN…» → «AND false THEN…»` | да | ✗ audit-guards.pg.test.ts «Р-88: releasing the tenant stop needs a second factor» /Р-88: the tenant stop is not released without a second factor/ |
| 45 | C2: день бюджета при повторе — текущий день витрины | `DROP TRIGGER ab_channel_write_budget_day_retry ON tenant_data.channel_write` | да | ✗ smoke «retry after midnight is charged to today, whose budget is exhausted (C2, Р-19)»<br>✗ smoke «retry while the storefront day boundary is unconfirmed (C2, Р-65)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON tenant_data.migration_consent TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.migration_consent: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path creates an eBay migration consent (Р-96, Р-2)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT UPDATE ON tenant_data.tenant TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.tenant: UPDATE of the decision path does not match the allow list/<br>✗ smoke «path opts the tenant into Kaufland Smart Pricing (Р-96, Р-12, Р-41)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON tenant_data.min_price TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.min_price: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path lowers min_price (Р-96, Р-5)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON tenant_data.cost_profile TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.cost_profile: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path changes the unit cost (Р-96, Р-83)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT UPDATE ON channel_data.pricing_halt TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_data\.pricing_halt: UPDATE of the decision path does not match the allow list/<br>✗ smoke «path schedules the review of a halt (finding 2 ревью шага 16)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON channel_data.pricing_halt_review TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_data\.pricing_halt_review: INSERT of the decision path does not match the allow list/<br>✗ smoke «path writes an automatic halt review (finding 2 ревью шага 16)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON audit.audit_event TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /audit\.audit_event: INSERT of the decision path does not match the allow list/<br>✗ smoke «path forges an audit event (finding 5, Р-90)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT EXECUTE ON FUNCTION security.invite_member(uuid, text, text, bytea, interval) TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /security\.invite_member.*SECURITY DEFINER function executable by the decision path/<br>✗ другая причина smoke «path invites a member (Р-90)» |
| Р-96 | путь решения — только вычисление и запись цены | `CREATE OR REPLACE FUNCTION tenant_data.lock_decision_products(p_tenant_id uuid, p_write_scope_ids uuid[]) RETU` | да | ✗ store.pg.test.ts «Р-54: a concurrent bound change serialises» /decision commit must wait for the bound change/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF failed > 0 THEN…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /SAMPLE_FAILED: one failed observation fails the review/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF accepted < required THEN…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /NO_SAMPLE: fewer accepted products than the sample requires/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF v_at < h.next_review_at THEN…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /NOT_DUE: the review window has not elapsed/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «v_at      timestamptz := least(p_at, now());…» → «v_at      timestamptz := p_at;…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /NOT_DUE: the review window has not elapsed and a moment in the future does not shorten it/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «AND sm.observed_at <= v_at;…» → «;…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /NO_SAMPLE: an observation after the review moment does not count/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «AND om.channel_product_ref = sm.channel_product_re…» → «))…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /NO_SAMPLE: a stale observation and a product outside the storefront do not count/ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF p_tenant_id IS DISTINCT FROM security.current_t…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» /the review of a halt runs only in the context of its own tenant/ |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE tenant_data.min_price DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.min_price» |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE channel_data.price_decision DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only channel_data.price_decision» |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE channel_data.price_intent DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only channel_data.price_intent» |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE tenant_data.pricing_strategy DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.pricing_strategy» |
| находка 4 | неизменяемость проверяется попыткой изменения | `CREATE OR REPLACE FUNCTION security.forbid_mutation() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN coal` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /forbid_mutation does not refuse an UPDATE/<br>✗ smoke «append-only tenant_data.min_price» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `DROP TRIGGER a0_admin_write_person_insert ON tenant_data.product` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.product: administrative INSERT without the person guard/<br>✗ другая причина smoke «administrative change without a person (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `DROP TRIGGER zz_admin_write_audit_insert ON tenant_data.product` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.product: administrative INSERT is not written to the audit log/<br>✗ smoke «an administrative change is not in the audit log (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `DROP TRIGGER a0_admin_write_person_insert ON channel_data.pricing_halt_review` | да | ✗ smoke «the administrative service records an automatic review without a person (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `security.require_person_for_admin_write(): «IF r IS NULL OR (action <> 'OWN_GUARD' AND NOT sec…» → «IF r IS NOT NULL AND action <>…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «administrative change by a user outside the tenant (Р-97)»<br>✗ другая причина smoke «administrative change in the platform tenant (step 17 finding 9)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `security.audit_admin_write(): «IF NOT security.admin_session() THEN…» → «IF true THEN…»` | да | ✗ smoke «an administrative change is not in the audit log (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `security.admin_session(): «pg_has_role(session_user, 'repracer_admin', 'MEMBE…» → «false…»` | да | ✗ smoke «administrative change without a person (Р-97)» |
| находка 5 | создание тенанта не присоединяет существующего пользователя мимо приглашения | `security.provision_tenant(uuid,text,text,jsonb): «IF m ->> 'role' IS DISTINCT FROM 'OWNER' THEN…» → «IF false THEN…»` | да | ✗ smoke «existing user attached as a member without an invitation (step 16 finding 5)» |
| находка 5 | создание тенанта не присоединяет существующего пользователя мимо приглашения | `security.provision_tenant(uuid,text,text,jsonb): «IF lower(trim(m ->> 'email')) IS DISTINCT FROM exi…» → «IF false THEN…»` | да | ✗ smoke «existing user provisioned as owner under another email (step 16 finding 5)» |
| находка 5 | создание тенанта не присоединяет существующего пользователя мимо приглашения | `security.provision_tenant(uuid,text,text,jsonb): «IF NOT EXISTS (SELECT 1 FROM platform.external_ide…» → «IF false AND NOT EXISTS (SELEC…»` | да | ✗ smoke «existing user who never signed in provisioned as owner (step 16 finding 5)» |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.accept_identity_invitation(bytea,text,text,text,boolean): «IF NOT inv.relink THEN…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» /a relink needs a relink invitation\|an ordinary invitation does not relink/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.accept_identity_invitation(bytea,text,text,text,boolean): «IF EXISTS (SELECT 1 FROM platform.external_identit…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» /was unlinked\|the revoked sign-in accepts nothing/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.invite_relink(uuid,uuid,bytea,interval): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» /a relink invitation without a second factor/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.invite_relink(uuid,uuid,bytea,interval): «AND m.status = 'ACTIVE' AND m.role = 'OWNER') THEN…» → «AND m.status = 'ACTIVE') THEN…»` | да | ✗ identity.pg.test.ts «Р-98» /only an active owner relinks/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.invite_relink(uuid,uuid,bytea,interval): «IF NOT EXISTS (SELECT 1 FROM tenant_data.membershi…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» /a relink is issued only to a member of the tenant/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `DROP TRIGGER a_external_identity_one_active ON platform.external_identity` | да | ✗ identity.pg.test.ts «step 17 finding 3» /the second acceptance waits for the first\|two active sign-ins of one provider/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `platform.external_identity_one_active(): «PERFORM pg_advisory_xact_lock(hashtextextended('pl…» → «…»` | да | ✗ identity.pg.test.ts «step 17 finding 3» /the second acceptance waits for the first/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.resolve_external_identity(text,text): «AND NOT EXISTS (SELECT 1 FROM platform.external_id…» → «…»` | да | ✗ identity.pg.test.ts «Р-98» /the revoked sign-in resolves nobody/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `DROP TRIGGER zz_identity_relinked_audit ON platform.identity_invitation` | да | ✗ identity.pg.test.ts «Р-98» /the invitation and the relink are audited/ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `DROP TRIGGER zz_identity_relink_invited_audit ON platform.identity_invitation` | да | ✗ identity.pg.test.ts «Р-98» /the invitation and the relink are audited/ |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `channel_data.pricing_strategy_undercut_guard(): «IF EXISTS (SELECT 1 FROM tenant_data.write_scope s…» → «IF false AND EXISTS (SELECT 1 …»` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» /the undercut of a pinned version does not start expiring/ |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `DROP TRIGGER b_write_scope_pins_live_strategy_version ON tenant_data.write_scope` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» /a scope is not pinned back/ |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `DROP TRIGGER zb_write_scope_release_strategy_version ON tenant_data.write_scope` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» /the released version starts its 18 months/ |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `tenant_data.pricing_strategy_supersede_undercut(): «AND s.status <> 'RETIRED')…» → «AND false)…»` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» /OQ-151: a new version is created while the old one is still pinned/ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'number' AND v::text ~ '^-?[0-9]+$';…» → «RETURN t = 'number';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /an amount that is not minor units/ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND s IN ('EUR', 'USD');…» → «RETURN t = 'string';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a currency that is not an ISO code\|a currency outside EUR and USD/ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND (NOT spec ? 'v' OR (spec -…» → «RETURN t = 'string';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /an enum value outside the registry/ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'number' AND v::text ~ '^-?[0-9]+$';…» → «RETURN t IN ('number', 'string…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /an amount as a string/ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.explanation_field_kinds(): «"$.format":{"k":"enum","v":["r80.1"]}…» → «"$.format":{"k":"code"}…»` | **нет** (упала только соседняя проверка) | ✗ другая причина undercut-eternal.pg.test.ts «finding 15» /another explanation format/ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.explanation_node_declared(jsonb,text,boolean): «OR (w.value #>> '{}') !~ '^[A-Za-z][A-Za-z0-9_]{0,…» → «…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a withheld name that is not an identifier/ |
| Р-101 | согласие на миграцию eBay — только владелец, от своего имени, со вторым фактором | `tenant_data.migration_consent_guard(): «AND (security.current_user_id() IS NULL OR NEW.use…» → «AND false THEN…»` | да | ✗ smoke «eBay consent in the name of another owner (Р-101)» |
| Р-101 | согласие на миграцию eBay — только владелец, от своего имени, со вторым фактором | `tenant_data.migration_consent_guard(): «AND NOT security.session_mfa() THEN…» → «AND false THEN…»` | да | ✗ smoke «eBay consent by the owner without a second factor (Р-101)» |
| Р-100 | административная запись проверяет роль и административные столбцы | `security.require_person_for_admin_write(): «IF r IS NULL OR (action <> 'OWN_GUARD' AND NOT sec…» → «IF r IS NULL THEN…»` | да | ✗ smoke «an operator lowers min_price (Р-100)»<br>✗ smoke «eBay consent by an admin in their own name (Р-101)»<br>✗ smoke «write scope status changed by a viewer (step 18 finding 1)» |
| Р-100 | административная запись проверяет роль и административные столбцы | `DROP TRIGGER a0_admin_write_person_update ON tenant_data.write_scope` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.write_scope: administrative UPDATE without the person guard/<br>✗ другая причина smoke «pricing mode changed without a person (step 17 finding 1)» |
| Р-100 | административная запись проверяет роль и административные столбцы | `security.admin_write_action(text): «('tenant_data.min_price', 'MANAGE_PRICING'), …» → «…»` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.min_price: no administrative action is declared/ |
| находка 4 (шаг 17) | путь решения не пишет действия человека в свои таблицы | `GRANT INSERT ON channel_data.pricing_halt TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_data\.pricing_halt: INSERT of the decision path does not match the allow list/<br>✗ smoke «path inserts a halt released by a person (step 17 finding 4)» |
| находка 4 (шаг 17) | путь решения не пишет действия человека в свои таблицы | `GRANT UPDATE ON channel_data.divergence_case TO repracer_app` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_data\.divergence_case: UPDATE of the decision path does not match the allow list/<br>✗ smoke «path resolves a divergence case for a person (step 17 finding 4)» |
| находка 4 (шаг 17) | путь решения не пишет действия человека в свои таблицы | `DROP TRIGGER a1_write_scope_path_status_guard ON tenant_data.write_scope` | да | ✗ smoke «path unblocks a write scope (step 17 finding 4)»<br>✗ smoke «path holds a write scope (step 17 finding 4)» |
| находка 5 (шаг 17) | срок проверки остановки переносится со вторым фактором и в аудите | `DROP TRIGGER a1_pricing_halt_review_schedule_guard ON channel_data.pricing_halt` | да | ✗ smoke «the review of a halt moved without a second factor (step 17 finding 5)» |
| находка 5 (шаг 17) | срок проверки остановки переносится со вторым фактором и в аудите | `DROP TRIGGER zz_admin_write_audit_update ON channel_data.pricing_halt` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_data\.pricing_halt: administrative UPDATE is not written to the audit log/<br>✗ smoke «the moved review of a halt is not in the audit log (step 17 finding 5)» |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.explanation_node_declared(jsonb,text,boolean): «ELSIF NOT security.param_value_valid(v, fields -> …» → «ELSIF false THEN…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a free text in strategy.currentMinor/ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND s IN ('EUR', 'USD');…» → «RETURN t = 'string' AND s ~ '^…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a currency outside EUR and USD/ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «PERFORM s::timestamptz;…» → «NULL;…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /an invalid moment in the price stop context/ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «BETWEEN 0 AND 1000;…» → «IS NOT NULL;…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a ratio outside its range/ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'number' AND v::text ~ '^[0-9]+$';…» → «RETURN t = 'number';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a negative count/ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND s ~ '^[A-Za-z0-9_.:@-]{1,1…» → «RETURN t = 'string';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» /a free text as an id/ |
| Р-102 | роль остатков — только остатки и резервации | `GRANT SELECT ON tenant_data.min_price TO repracer_stock` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.min_price: SELECT of the stock role does not match its allow list/<br>✗ smoke «stock role reads prices (Р-102)» |
| Р-102 | роль остатков — только остатки и резервации | `GRANT UPDATE ON tenant_data.write_scope TO repracer_stock` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.write_scope: UPDATE of the stock role does not match its allow list/ |
| Р-102 | роль остатков — только остатки и резервации | `GRANT UPDATE (pricing_mode) ON tenant_data.write_scope TO repracer_stock` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.write_scope\.pricing_mode: UPDATE of the stock role is not in its allow list/<br>✗ другая причина smoke «stock role changes the pricing of a write scope (Р-102)» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE tenant_data.guardrail DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.guardrail» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE channel_data.pricing_halt_review DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only channel_data.pricing_halt_review» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE platform.external_identity_revocation DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only platform.external_identity_revocation» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE tenant_data.price_daily_correction DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.price_daily_correction» |
| OQ-153 | привязки входа удаляются только у отключённого пользователя | `maintenance.purge_user_identities(uuid): «IF NOT EXISTS (SELECT 1 FROM platform.app_user u W…» → «IF false THEN…»` | да | ✗ smoke «sign-ins of an active user are purged (OQ-153)» |
| находка 1 (шаг 18) | в сессии административного сервиса любое изменение строки — под стражем человека и в аудите | `security.require_person_for_admin_write(): «  u := security.current_user_id();
  IF u IS NULL …» → «  IF TG_OP = 'UPDATE' AND NOT …»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «write scope status changed by the administrative service without a person (step 18 finding 1)» |
| находка 1 (шаг 18) | в сессии административного сервиса любое изменение строки — под стражем человека и в аудите | `security.audit_admin_write(): «    -- Находка 1 ревью шага 18: в журнал попадает …» → «    IF NOT cols && TG_ARGV THE…»` | да | ✗ smoke «a status change of a write scope by a person is not in the audit log (step 18 finding 1)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF c.user_id IS NULL OR c.user_id IS DISTINCT FROM…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «listing added to a consent by another member (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «listing added to a consent without a second factor (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF c.given_at <> now() THEN…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «listing added to a consent given in an earlier transaction (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF NOT EXISTS (SELECT 1 FROM channel_data.listing_…» → «IF false AND EXISTS (SELECT 1 …»` | да | ✗ smoke «listing added to a consent without a preflight check (Р-101, Р-2)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_revocation_guard(): «IF mem.membership_id IS NULL OR NEW.revoked_by_mem…» → «IF false THEN…»` | да | ✗ smoke «consent revoked in the name of another member (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_revocation_guard(): «IF mem.role <> 'OWNER' THEN…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «consent revoked by an admin (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_revocation_guard(): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | да | ✗ smoke «consent revoked without a second factor (Р-101)» |
| Р-107 (находка 3, шаг 18) | ручной intent — только человек в административном сервисе, от своего имени, с правом менять цену | `channel_data.price_intent_manual_guard(): «IF mem.membership_id IS NULL OR NEW.created_by_mem…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «path creates a manual price intent (Р-107)»<br>✗ другая причина smoke «manual price intent in the name of another member (Р-107)» |
| Р-107 (находка 3, шаг 18) | ручной intent — только человек в административном сервисе, от своего имени, с правом менять цену | `channel_data.price_intent_manual_guard(): «IF NOT security.pricing_permission(mem.role, 'ENAB…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «manual price intent by a viewer (Р-107)» |
| Р-107 (находка 3, шаг 18) | ручной intent — только человек в административном сервисе, от своего имени, с правом менять цену | `channel_data.price_intent_manual_guard(): «NOT (NEW.trigger_type = 'MANUAL' OR NEW.rule_code …» → «NOT (NEW.trigger_type = 'MANUA…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «manual price intent in the name of another member (Р-107)»<br>✗ другая причина smoke «manual price intent by a viewer (Р-107)» |
| находка 4 (шаг 18) | системную остановку ставит проверка входов пути решения, а не человек | `channel_data.pricing_halt_insert_guard(): «IF security.admin_session() THEN…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина smoke «system halt created by a person (step 18 finding 4)» |
| находка 4 (шаг 18) | системную остановку ставит проверка входов пути решения, а не человек | `DROP TRIGGER a00_pricing_halt_insert_guard ON channel_data.pricing_halt; CREATE TRIGGER a00_pricing_halt_inser` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /pricing_halt: trigger a00_pricing_halt_insert_guard has a WHEN condition that is not in the list/<br>✗ другая причина smoke «system halt created by a person (step 18 finding 4)» |
| находка 6 (шаг 18) | одна действующая привязка входа — и при изоляции строже READ COMMITTED | `platform.external_identity_one_active(): «IF current_setting('transaction_isolation') <> 're…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «step 18 finding 6» /step 18 finding 6: a link in REPEATABLE READ is refused/ |
| находка 6 (шаг 18) | одна действующая привязка входа — и при изоляции строже READ COMMITTED | `ALTER FUNCTION platform.external_identity_one_active() SECURITY INVOKER` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /platform\.external_identity_one_active\(\): owned by repracer_resolver but not SECURITY DEFINER/ |
| находка 5 (шаг 18) | привязки входа удаляет только удаление привязок отключённого пользователя | `GRANT DELETE ON platform.external_identity_revocation TO repracer_retention` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /role repracer_retention may delete sign-in links/<br>✗ smoke «retention role deletes sign-in revocations directly (step 18 finding 5)» |
| находка 5 (шаг 18) | привязки входа удаляет только удаление привязок отключённого пользователя | `GRANT DELETE ON platform.external_identity TO repracer_retention` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /role repracer_retention may delete sign-in links/<br>✗ smoke «retention role deletes sign-in links directly (step 18 finding 5)» |
| находка 5 (шаг 18) | привязки входа удаляет только удаление привязок отключённого пользователя | `security.forbid_mutation(): «IF TG_OP = 'DELETE' AND current_user IN ('repracer…» → «IF TG_OP = 'DELETE' THEN…»` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /security\.forbid_mutation does not refuse a DELETE/ |
| находка 7 (шаг 18) | скаляр на месте узла слепка — отказ | `security.explanation_node_declared(jsonb,text,boolean): «IF jsonb_typeof(node) NOT IN ('object', 'array') T…» → «IF false THEN…»` | **нет** (упала только соседняя проверка) | ✗ другая причина undercut-eternal.pg.test.ts «finding 15» /step 18 finding 7: a scalar among the strategy steps is refused\|step 18 finding 7: a scalar among the sanity checks is refused\|step 18 finding 7: a scalar explanation is refused/ |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON tenant_data.channel_write_history USING (tenant_id = security.current_tenant_id` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_write_history: policy stock_quantity of the stock role is not limited to the QUANTITY field/<br>✗ другая причина smoke «stock role writes price history of a channel write (Р-105)» |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON tenant_data.channel_write USING (tenant_id = security.current_tenant_id()) WITH` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /channel_write: policy stock_quantity of the stock role is not limited to the QUANTITY field/<br>✗ smoke «the stock role sees price writes or does not see quantity writes (Р-105)» |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON tenant_data.write_scope USING (tenant_id = security.current_tenant_id())` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /write_scope: policy stock_quantity of the stock role is not limited to the QUANTITY field/<br>✗ smoke «the stock role sees price writes or does not see quantity writes (Р-105)» |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `GRANT UPDATE (pricing_mode) ON tenant_data.write_scope TO repracer_stock` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /tenant_data\.write_scope\.pricing_mode: UPDATE of the stock role is not in its allow list/ |
| шаг 19: атрибуты функций защит | функция служебной роли исполняется её правами | `ALTER FUNCTION security.audit_admin_write() SECURITY INVOKER` | да | ✗ verify 0073_verify_schema_invariants_v16.sql /security\.audit_admin_write\(\): owned by repracer_audit_writer but not SECURITY DEFINER/<br>✓ smoke «an administrative change is not in the audit log (Р-97)» |

Строк с мутациями: 50; ложных (хотя бы одна мутация не поймана заявленными проверками): **9** — 28, 32, 39, 40; 43; Р-97; находка 6; находка 1 (шаг 18); Р-101 (находка 2, шаг 18); Р-107 (находка 3, шаг 18); находка 4 (шаг 18); находка 7 (шаг 18).
Мутаций: 147; не поймано своей проверкой: **14** (из них упала только соседняя проверка строки: 14).
Не мутировались: 1–11 (свойства каталога остались в проверке схемы (0067)); 15 (данные справочника НДС остались в проверке схемы (0067)); 36 (правило проверяло отсутствие объектов входа).
✗ — проверка упала на мутации (поймала), ✓ — осталась зелёной.
