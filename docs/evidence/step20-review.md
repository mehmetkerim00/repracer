# Ревью шага 20

**Ревьюер:** отдельный агент. **Предмет:** ветка `step20-infra` после `7896274`, ревью выполнено по коммиту `c9c0fa0`.

**Ограничения ревьюера:**
- рабочие базы не трогать;
- `prepare.sh`, мутационную проверку, сборку и смоук-файлы не запускать;
- роли не менять;
- файлы и git не менять.

Эксперименты шли в собственной копии `review20_a`, после работы она удалена. Нарушений ограничений нет. Правило Р-110: находки,
относящиеся к шагу, исправлены в шаге; остальное записано в принятые риски.

| № | Серьёзность | Находка | Итог |
|---|---|---|---|
| 1 | Критическая | Ни один прогон `ci.yml` не был зелёным, а документы писали «проверено в CI». Цифры порядка — из временного workflow | **Исправлено.** Разбор трёх красных прогонов: мутация Р-101 не находила текст после 0074; NO_OP не выгружался; тест порядка терял последнюю цену. Первый зелёный прогон — [35146073530](https://github.com/mehmetkerim00/repracer/actions/runs/35146073530), после исправлений ревью — [35147325267](https://github.com/mehmetkerim00/repracer/actions/runs/35147325267): 276 из 276, мутаций 157 из 157, покрытие каталога 725 |
| 2 | Высокая | INV-12 обходился проверкой с датой в будущем: она «последняя» навсегда | **Исправлено** (0074): момент проверки не позже часов базы; смоук-проверка и строка каталога |
| 3 | Высокая | «Не старше 24 ч» обходилось той же будущей датой | **Исправлено** тем же стражем |
| 4 | Высокая | При старте миграции потери не сверялись: согласие на READY запускало миграцию после перепроверки READY_WITH_LOSSES | **Исправлено** (0074): старт сверяет снимок, вердикт и поимённые потери всех последних проверок после согласия; смоук-проверки INELIGIBLE и новой потери, две строки каталога |
| 5 | Средняя | Равные `checked_at` — «последняя» не определена | **Исправлено:** элемент согласия при равном моменте — отказ; старт миграции требует совпадения всех проверок с наибольшим моментом |
| 6 | Средняя | Проверка покрытия каталога засчитывала защиту по одному имени без таблицы и по общей мутированной функции; базовый список мог расти; ветки внутри функций и другие виды защит не видны | **Исправлено частично.** Нужны имя вместе с таблицей или мутация функции единственного триггера; базовый список пересчитан строгим методом (725 вместо 601) и закреплён SHA-256. Ветки функций, RLS, права, UNIQUE, FK, EXCLUDE — [accepted-risks.md](../accepted-risks.md), п. 13а |
| 7 | Средняя | Замер сжатия считал `price_intent` и `price_decision` вместе с NO_OP, который в ClickHouse туда не попадает | **Исправлено в документе:** ~62–70 ГБ при доле NO_OP шага 14, 88–95 ГБ без неё; добавлены ограничения (метки времени, неравные строки PG/CH) |
| 8 | Средняя | Идемпотентность доказана только с `FINAL`, проверка агрегата условная | **Частично.** Сырые строки после повтора выводятся в журнал; проверка агрегата срабатывает, если день не изменился между выгрузками (в зелёном прогоне сработала), иначе пишется в журнал. Двойной счёт агрегата найден CI и исправлен окном дедупликации (070) |
| 9 | Средняя | Сопоставление `OUT_OF_ORDER` с повторными доставками может простить одновременную обработку партиции; контроль тривиален для сверки | **Не исправлено** — ограничение записано в [step20-broker-order.md](step20-broker-order.md); риск — [accepted-risks.md](../accepted-risks.md), п. 1 |
| 10 | Низкая | 518 вызовов канала на 480 решений — мир доказательства не изолирован | **Не исправлено**, записано в доказательстве |
| 11 | Низкая | Утверждения [док] о фидах, лимите приложения, `value_with_tax` опираются на несохранённый текст страниц | **Уточнено:** OQ-32 и channel-capabilities §9 прямо говорят, что опора — страница с SHA-256, текст не хранится, в моделях этого нет |
| 12 | Низкая | Разбивка ячеек Amazon неточна | **Исправлено:** 14 с [док], из них 3 с остатком (проверить); 12 без [док] |
| 13 | Низкая | Ссылка на временный прогон как на задание CI; «пять дефектов»; устаревшая строка в рисках; находки READY с BLOCKER и LOSS с пустым именем | **Исправлено:** ссылка уточнена, восемь дефектов, строка рисков обновлена; вердикт согласован с находками (`listing_migration_check_verdict_matches_findings`), имя потери — код |
| 14 | Низкая | Округление стоимости незамеренных таблиц | Пересчитано вместе с п. 7 |

## Не подтвердились утверждения шага 20 (на момент ревью)

- «Порядок за брокером и выгрузка проверены в CI» — зелёного `ci.yml` не было. Теперь есть: 35146073530.
- «Риск 12б закрыт» — обходился тремя способами (п. 2–5). Закрыт после исправлений.
- «Каждая новая защита — в каталоге» — в общем виде неверно. Теперь верно для триггеров и CHECK, остальное — риск 13а.
- «88–95 ГБ у верхней границы» — модель строк не соответствовала хранению NO_OP.
- «OQ-32 закрыт по документации» — опора на несохранённую страницу; формулировка уточнена.

## Подтвердилось

- **Порядок за брокером:** шаг 19 обработал ноль сообщений из-за журнала в чужой базе; исправление корректно, рабочий воркер не
  затронут. Результаты с ключом и без ключа воспроизводятся в разных прогонах.
- **ClickHouse:** DDL 050 исправлен, 060 и 070 применяются, `099_verify` зелёный.
- **Замер сжатия:** арифметика таблицы сходится (79,2 ГБ; 9,50 €).
- **Р-109 и Р-108:** все мутации строки Р-109 пойманы своими проверками; базовый список соответствовал методу шага 19.
- **Снимок SP-API:** SHA-256 файлов — 17 из 17; лимиты моделей (`patchListingsItem` 5/5, `getListingsItem` 5/10,
  `getCompetitiveSummary` 0.033/1) и отсутствие витрины в `LISTINGS_ITEM_MFN_QUANTITY_CHANGE` — по файлам; 0075 меняет только источник.
- **eBay:** 403 зафиксирован в прогоне CI.
- **0076:** копия 0073.
