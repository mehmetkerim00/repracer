# Ревью шага 22

**Ревьюер:** отдельный агент. **Предмет:** `3beea54..2ffc011` (шаг 22, ветка `step22`).

**Ограничения ревьюера:**
- рабочие базы не трогать;
- `prepare.sh`, мутационную проверку, сборку и смоук-файлы не запускать;
- роли, файлы и git не менять.

Опыты шли в собственной копии `review22_a` (из шаблона), после работы она удалена. Нарушений ограничений нет.

**Что ревьюер прогнал сам:**
- стенд Amazon — 36 из 36;
- пути решения Amazon на своей базе — 18 из 18;
- юнит-тесты затронутых пакетов — 35 из 35;
- `typecheck` — без ошибок.

Обходов остановки CHANNEL_PRICE_BASIS_MISMATCH в базе опыты не нашли.

Находки относятся к шагу 22 и исправлены в нём же [Р-110]. Итог — колонка «Итог»; исправления — коммит после 2ffc011.

| № | Серьёзность | Находка | Итог |
|---|---|---|---|
| 1 | Средняя | Р-116 не проверялся на первой отправке пути решения: синхронный канал (Kaufland) применяет запись сразу, и сверки диспетчером нет | Исправлено: путь решения сверяет базу цены после синхронного применения (`PricingStore.checkPriceBasis`); тест `pricing-pipeline/src/price-basis.test.ts` |
| 2 | Средняя | Любой отказ сверки класса REQUIRES_HUMAN сразу блокировал единицу, включая 429 сервера токенов LWA (он классифицировался как AUTH_INVALID) | Исправлено: сразу блокируют только `CHANNEL_REPRICER_ACTIVE` и `CHANNEL_BOUNDS_PRESENT`; 429 LWA — RATE_LIMITED; тест в `transitions.test.ts` |
| 3 | Средняя | Обрыв ограничителя: при свободной доле приложения меньше 1/5 burst становился 0, запросы не проходили вовсе | Исправлено: пока у приложения есть свободная скорость, burst не меньше 1; тест «свободно 19 из 100 — проходит около 19 в секунду» |
| 4 | Средняя | Р-117: цена на полу давала удержание на каждой оценке, сумма росла с числом оценок (12 оценок — «€36.60» вместо €3.05) | Исправлено: удержание — эпизод подряд идущих оценок единицы, сумма — наибольшая за эпизод; тест |
| 5 | Средняя (опытом не подтверждено) | Ложная остановка у Kaufland: если `price` unit включает доставку, 20.99 + 3.99 совпадает с 20.99 × 1.19; при Smart Pricing — TAX_REMOVED | Принятый риск 20, вопрос K-12 дополнен; до ответа остановку снимает человек |
| 6 | Низкая | Р-116 не видит сумму другой базы, сохранённую каналом (прочитанное значение ≠ отправленному → NOT_APPLIED, повторы, без остановки) | Принятый риск 19 дополнен |
| 7 | Низкая | Р-117: удержания без изменения цены (NO_OP) старше 3 дней не видны вовсе, а документы говорили «считаются без суммы»; комментарии «пол − цель» против кода «удержанная цена − цель» | Исправлены комментарии, ADR-0022 и Р-117 |
| 8 | Низкая | Стенд пропускал запрещённый атрибут в пути операции PATCH (`/attributes/minimum_seller_allowed_price`) | Исправлено: проверяются и ключ, и путь |
| 9 | Низкая | Двойник в памяти требовал профиль себестоимости для ставки НДС, PostgreSQL брал ставку страны по умолчанию | Исправлено: память берёт ставку витрины по умолчанию, как `effective_vat_rate_bp` |
| 10 | Низкая | Цена покупателя из канала (`observedMinor`, класс CHANNEL) хранилась в `pricing_halt.details` без срока | Исправлено: в остановке только наши значения; тест |
| 11 | Низкая | Строка каталога «Р-116» не покрывала ветку создания записи; страж не держит CHANNEL_MIN_PRICE; доступность стратегии [Р-39] проверяет только сервер стенда | Ветка INSERT: смоук и мутация в строке «Р-116». Остальное — OQ-166 |
| 12 | Низкая | Транспорт повторяет 429 до 3 раз мимо двухуровневого бюджета и без учёта крайнего срока вызова | Принятый риск 15 дополнен |
| 13 | Низкая (опытом не подтверждено) | Код прежней временной ошибки оставался у принятой записи и мог попасть в причину HELD | Исправлено: при переходе в ACCEPTED код очищается (оба хранилища) |

## Не подтвердились утверждения шага 22

- «Проверка после синхронного применения» — на пути решения её не было (находка 1).
- «Удержание старше считается, но без суммы» — для NO_OP не считалось (находка 7).
- «Стенд отклоняет любой запрос с запрещённым атрибутом» — путь PATCH пропускался (находка 8).
- «Мутации Р-116 4 из 4», «379 из 381» — ревьюеру запускать запрещено. `step22-mutation.md` в коммите 2ffc011 отсутствовал; добавлен
  вместе с исправлениями. CI 2ffc011 зелёный: 383 из 383, мутаций 170, не поймано 0.

## Подтвердилось

- **Остановка Р-116 в базе держится:**
  - путь решения не одобряет фиксированную цену и не создаёт запись при действующей остановке, в том числе с `marketplace NULL`;
  - прямое изменение `pricing_halt` — нет права;
  - `review_halt_by_sample` → NOT_ACTIVE;
  - административный сервис системную остановку не создаёт, ручное снятие — со вторым фактором.
- **Находка 1 ревью шага 21 закрыта:** превью через точку сохранения на каждое предложение не срабатывает на 0078 и ничего не
  оставляет.
- **Находки 2–4 и 7 ревью шага 21 закрыты.**
- **Секреты:** тело ошибки LWA не читается, токен только в заголовке, ключ кэша — SHA-256.
- **Р-114 в адаптере:** тело PATCH строится только из `our_price` или `fulfillment_availability`.
- **Бюджет:** списание атомарно, отрицательных токенов нет.
