# Шаг 24: ревью

Дата: 2026-09-17. Ревьюер — отдельный агент, диапазон `6ce5bc4..7a2c2cf`, только чтение кода и проверочные прогоны чистых функций; базы
стенда, подготовку, мутационную проверку и файлы репозитория не трогал. 14 находок, все — в пределах шага 24.

| № | Важность | Находка | Итог |
|---|---|---|---|
| 1 | высокая | Omnibus: начало скидки не проверялось — задним числом окно уходило в прошлое с более высокой ценой; скидка в будущем получала «верно» | **Исправлено** (0087): начало не раньше текущих суток витрины (`STARTS_BEFORE_TODAY`), скидка позже момента проверки — `WINDOW_OPEN`; смоук-проверки и строки каталога Р-123; тесты кода, базы и консоли |
| 2 | средняя | История цен пишется при ACCEPTED: в окно Omnibus входит неприменённая цена; двойник — только при APPLIED | Расхождение двойника **не подтвердилось** (двойник пишет при ACCEPTED); последствие — отказ законной скидки, строже реальности — **риск 28, OQ-180** |
| 3 | средняя | Сверка Kaufland открывала проверку для состояний без уведомлений (б/у) — ложные CRITICAL | **Исправлено**: сверяются только состояния, которые покрывает источник уведомлений канала (`UNCOVERED`) |
| 4 | средняя | Сверка — лишнее полное чтение контекста и отдельная транзакция (Р-59); исключение обрывало пакет | **Исправлено**: проверка — из того же контекста и в той же транзакции, что снимок; снимок только для сверки — лёгкое чтение состояния и одна транзакция, сбой товара ловится (`RECONCILIATION_SNAPSHOT_FAILED`) |
| 5 | средняя | Выгрузка снимков: пропущенные строки терялись при удалении секции; утверждение о `partition_export` неверно | **Исправлено**: сутки с пропусками не отмечаются проверенными; комментарий исправлен; разбор пропусков — **OQ-181** |
| 6 | средняя | Нет индекса под проверку потерь; выгрузка суток целиком в память | **Исправлено**: индекс журнала по товару аккаунта и времени получения (0086), выгрузка частями по 2 000 строк; замер объёма журнала в PostgreSQL — OQ-181 |
| 7 | средняя/низкая | `poll_snapshot_id` не ссылался на снимок журнала | **Исправлено**: идентификатор проверки — идентификатор снимка в журнале |
| 8 | низкая/средняя | При сбое фиксации уведомление считалось записанным, сообщение очереди удалялось | **Исправлено**: сбой фиксации снимка из уведомления — исключение, сообщение остаётся для повторной доставки |
| 9 | низкая/средняя | Круг сверки Amazon по часам пропускал товары при редком вызове | **Исправлено**: окно по номеру вызова (`cycle`), 80 товаров — за 4 вызова подряд (тест) |
| 10 | низкая | Каталог не покрывал совпадение товара и условие «новее прежнего состояния»; проверка тенанта дублировала политику строк | **Исправлено**: условие совпадения — одно сравнение строк, две новые смоук-проверки и мутации; проверка тенанта удалена [Р-104] |
| 11 | низкая | Маршруты сверки и объявления выполнялись при PUT/DELETE; дата 2026-13-01 давала 500 | **Исправлено**: 405 до маршрутов, строгая календарная дата; тест консоли |
| 12 | низкая | CSV доказательной истории: формулы электронной таблицы, `\r` | **Исправлено**: апостроф перед `= + - @`, кавычки при `\r`; тест. Хеш выгрузки — для получателя файла, в аудит не пишется (чтение) |
| 13 | низкая | ADR: «пояс не подтверждён — не проверить», а пояса TO_VERIFY используются; доказательство сверки не оговаривает K-19 | **Исправлено** в ADR-0024 и docs/evidence/step24-notification-loss.md |
| 14 | низкая | Цены суток начала скидки до её начала не входили в окно | **Исправлено** (0087 и код): входят; смоук-проверка и мутация |

## После исправлений

- Мутационная проверка строк Р-121 и Р-123 — docs/evidence/step24-mutation.md.
- Найдено исправлениями: лёгкое чтение состояния на PostgreSQL искало состояние товара в регистре порта (`new` вместо `NEW`) — сценарий
  `amazon/pipeline/notification-loss-rotation` на PostgreSQL упал, двойник в памяти проходил; исправлено.
- Замер на модели Kaufland после исправлений повторён — числа не изменились.
