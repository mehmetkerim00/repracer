# Шаг 25: ревью

Дата: 2026-09-17. Ревьюер — отдельный агент, диапазон `e757ee6..c2c14bb`. Он читал код и проверял схему на своей базе `review25_b`
(миграции 0001–0093, после проверки база удалена). Базы стенда, подготовку, тесты, мутационную проверку и файлы репозитория не трогал.

- **Первый запуск ревьюера** остановился без отчёта. Его база `review25_a` удалена, ревью запущено заново.
- **Второй запуск** дал 10 находок, все в пределах шага 25. Девять исправлены, одна исправлена частично, остаток — риск 31 и OQ-182.

| № | Важность | Находка | Итог |
|---|---|---|---|
| 1 | высокая | Срок вызова адаптера считался от начала такта + 50 с. Работы такта идут по очереди, поэтому у второго и следующих аккаунтов срок истекал до старта — все запросы TIMEOUT. Отказы не давали ни провала, ни алерта: опрос [Р-47, Р-121] молча не выполнялся | **Исправлено**: срок — от начала запуска работы, для опроса — `maxQueries / бюджет + 60 с`. Доля отказов (канал и обработка) выше 20 % — `COMPETITOR_POLL_FAILURES`; в `items` — только опрошенные без отказа. Тест scheduler.test.ts |
| 2 | средняя/высокая | Движением считался `move_bp <> 0`, а цена без изменений даёт 10 000 — каждый опрос делал товар «волатильным», ярусы не работали | **Исправлено** в обоих хранилищах: `move_bp <> 10000`. В фикстуру `pipeline-tiered-poll-due` добавлен шаг через 2 часа: товары со сдвигом — тёплый ярус, без сдвига — холодный. **Этот шаг нашёл второй дефект:** хранилище в памяти сравнивало товар движения («ref\|condition») с одним ref и не находило ни одного движения — в памяти все товары были холодными, а прежняя фикстура это не проверяла. Исправлено; память и PostgreSQL дают одинаковый результат |
| 3 | средняя | Отметку «цена не применена» (риск 28) путь решения мог вставить сам, для любой цены: применённая цена пропала бы из суточной свёртки и окна Omnibus | **Исправлено** (0091): страж `price_history_not_applied_guard` — отметка только для цены записи с итогом NOT_APPLIED по полю PRICE той же единицы. Смоук-проверка и строка каталога |
| 4 | средняя | Повторная выгрузка непроверенных суток шла по всем группам таблиц. Секция другой группы к тому времени удалена по сроку — ложный CRITICAL «секция удалена» каждый день | **Исправлено**: отставание считается по группам (решения, записи, снимки); повторяется только непроверенная группа |
| 5 | средняя | Опоздавшие строки после проверки: секция не удалялась по сроку и не выгружалась повторно, через 14 суток её сносило принудительное удаление — без алерта. Срок работ — по часам процесса | **Исправлено**: `ROWS_CHANGED` в отставании (число строк ≠ выгруженному) и алерт `ANALYTICS_PARTITION_FORCE_DROPPED` работы удаления. **Не исправлено**: сравнение срока с часами процесса — риск 31 |
| 6 | средняя | Аренда не продлевалась (запуск дольше аренды выполнялся дважды); `LEASE_LOST` обрывал весь такт; страж обходился двумя UPDATE (освободить, затем занять) | **Исправлено**: продление каждую треть срока; потеря аренды ловится по работе (`SCHEDULER_LEASE_LOST`), остальные работы выполняются; освободить действующую аренду может только владелец, заявивший себя в транзакции (0090). Смоук-проверка и мутация; тесты scheduler.test.ts и scheduler.pg.test.ts. **Новый тест нашёл дефект исправления:** продление не чаще раза в секунду при аренде 1 с приходило к её истечению — нижняя граница снижена до 200 мс |
| 7 | средняя/низкая | Постоянный сбой выгрузки одних суток держал слот: следующие сутки не выгружались и через 14 суток удалялись | **Исправлено**: провал суток и группы не проваливает работу — `ANALYTICS_EXPORT_FAILED`, сутки повторяются из отставания, слот продвигается; отставание старше 72 ч — `ANALYTICS_EXPORT_BACKLOG` CRITICAL. Тест history-survives-stop переписан под новый порядок алертов |
| 8 | низкая/средняя | Ошибка обработки одного снимка обрывала пакет опроса: ни один товар не отмечался опрошенным, каждые 60 с тот же пакет заново | **Исправлено**: ошибка ловится по снимку (`processingFailed`), опрошенными отмечаются все, кроме отказов адаптера |
| 9 | низкая | Пропуски выгрузки разбирала та же роль, что их пишет; `EXPORTED_AFTER_FIX` с ClickHouse не сверяется | **Частично**: разбор — отдельная роль `repracer_export_triage` (0092), у выгрузки права на разбор нет (тест clickhouse-export.pg, инструмент `scripts/ops/snapshot-export-skips.mts`). Сверка с ClickHouse и учётная запись оператора — OQ-182 |
| 10 | низкая | Мелочи: начало истории в базе учитывало отмеченные неприменёнными цены; внешние изменения — без фильтра по причине; строки состояния удалённых работ копились; пропуски выгрузки не удалялись при удалении данных тенанта; уровень отставания — в памяти; у процесса нет точки входа | **Исправлено**: начало истории без отмеченных (0091), фильтр `EXTERNAL_CHANGE`, очистка состояния неактивных работ, удаление пропусков при очистке тенанта (0092). **Не исправлено**: уровень в памяти — риск 31; точка входа — развёртывание, docs/evidence/step25-scheduler.md |

## После исправлений

- Локально: 452 из 455 тестов. Не прошли 3 теста, которым нужны ClickHouse и брокер: они на машине разработчика не запускаются, их прогон — CI.
- CI 35241578889 нашёл дефект исправления находки 5: алерт `ANALYTICS_PARTITION_FORCE_DROPPED` называл секции числами — журнал удаления
  хранит OID удалённой секции, а не имя. Алерт теперь называет таблицу и сутки секции; тест history-survives-stop проверяет сутки D6.
- Мутационная проверка — docs/evidence/step25-mutation.md.
- Итоговый прогон CI — в строке статуса шага 25 в CLAUDE.md.
