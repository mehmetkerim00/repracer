# Шаг 26: ревью

Дата: 2026-09-18. Ревьюер — отдельный агент, диапазон `fbe9d07..8f9aef0`. Он читал код и применял миграции на своей базе `review26_a`
(после проверки удалил её). Базы стенда, подготовку, тесты, мутационную проверку и файлы репозитория не трогал.

13 находок. Все в пределах шага; исправлены все, кроме одной, оставленной риском 34 и OQ-192.

| № | Важность | Находка | Итог |
|---|---|---|---|
| 1 | высокая | Тест OQ-180 считал сутки витрины сдвигом UTC на +2 часа (летнее время): с 28 октября принятие и применение попадали в одни сутки Берлина, и единственный прямой тест центрального механизма шага падал бы полгода | **Исправлено**: границу суток и сутки принятия и применения считает база (`AT TIME ZONE 'Europe/Berlin'`), моменты — от полуночи витрины; даты из базы берутся текстом (DATE в драйвере становится полуночью пояса процесса) |
| 2 | высокая | Смоук-метка с подстановкой (`… (OQ-180): % <> %`): раннер сверяет текст ошибки целиком, поэтому мутация окна Omnibus считалась пойманной, не будучи пойманной | **Исправлено**: значения — отдельной строкой `RAISE NOTICE`, текст исключения равен метке каталога. Это и показала мутационная проверка: 3 мутации OQ-180 из 7 не ловились своими проверками |
| 3 | высокая | `tenant_data.price_history_applied` объявлена `TENANT_CLOSURE_ONLY`, но не названа в `purge_tenant_data`: строки тенанта остаются в базе навсегда | **Исправлено**: таблица добавлена в очистку, и заведено правило проверки схемы «каждая таблица данных тенанта с этим методом названа в `purge_tenant_data`» со строкой каталога. **Правило сразу нашло вторую такую таблицу — `tenant_data.price_stop`** (шаг 12), она тоже добавлена |
| 4 | высокая | Позднее подтверждение применения: цена, уже учтённая в закрытых сутках, получала время применения и попадала в свёртку следующих суток второй раз | **Исправлено**: отметка не ставится цене закрытых суток (триггер и страж); остаток — риск 34, OQ-192 |
| 5 | средняя | Отметка наружу и `/healthz` считались по завершению такта: работа с арендой 2 часа выглядела смертью процесса — ложное письмо владельцу | **Исправлено**: живость — по началу итерации цикла (`onIterationStart`), отметка идёт таймером, а не только по концу такта |
| 6 | средняя | `restart: unless-stopped` не перезапускает контейнер по нездоровому healthcheck, а цикл ловит любые исключения — процесс не завершается никогда | **Исправлено**: после 10 неудавшихся тактов подряд процесс завершается (перезапуск делает развёртывание); формулировки в compose и README исправлены |
| 7 | средняя | Товар без конкурентов (мы единственный продавец) после исправления «движение — только по ценам конкурентов» не получал ни одного наблюдения и навсегда оставался в пробе яруса | **Исправлено**: снимок без конкурентов записывается как наблюдение «цена не двигалась» (10 000 б. п.) |
| 8 | средняя | Из-за того же — знаменатель окна массового сдвига [Р-50] уменьшался, порог остановки витрины смягчался без оценки ложных срабатываний | **Исправлено** тем же: строка наблюдения возвращается в окно |
| 9 | средняя | Частично отклонённое окно сверки Amazon считалось успешным (товары окна пропускались), а при постоянном отказе CRITICAL-алерт повторялся каждую минуту | **Исправлено**: любой отказ в окне — провал запуска (окно повторяется); `SCHEDULER_JOB_FAILING` поднимается один раз при переходе через порог |
| 10 | средняя | Условие по `coalesce(applied_at, accepted_at)` лишило `close_price_days` и `omnibus_raw_prices` отсечения секций `price_history`, а обход всей истории считался при каждом вызове | **Исправлено**: добавлено безопасное условие по `accepted_at` (применение не раньше принятия), обход всей истории — только когда закрытых суток ещё нет |
| 11 | средняя | Троттлинг отметки обходился сменой состояния: «мигающий» планировщик мог выбрать лимит сервиса (5 отметок в минуту), и отметка о сбое терялась | **Исправлено**: между любыми отметками — не меньше 12 секунд |
| 12 | низкая | Такт, в котором провалились ВСЕ работы, считался благополучным: снаружи процесс выглядел здоровым | **Исправлено**: `tickIsHealthy` — такт здоров, если хотя бы одна работа не провалилась |
| 13 | низкая | Мелочи: формулировка про имя файла секрета; утверждение в доказательстве о товаре без якоря; дублирующий внешний ключ учётной записи оператора; отсутствующая проверка платформенного тенанта у подтверждений; непокрытая сборка процесса; отсутствие модульных случаев пробы яруса; два подзапроса к движениям на каждого кандидата; пробуждение по работе, арендованной другим процессом; возможная утечка адреса отметки при неразбираемом URL; осиротевшие базы прежних прогонов | **Исправлено**: формулировки, внешний ключ снят как дубль [Р-104], проверка тенанта добавлена со смоуком и строкой каталога, `dueClockOf` и `tickIsHealthy` покрыты тестами, добавлены модульные случаи пробы, запрос сведён к одному проходу, арендованные другим процессом работы не будят цикл, разбор адреса отметки не показывает адрес, базы удалены |

## После исправлений

- Мутационная проверка — docs/evidence/step26-mutation.md.
- Ревьюер проверял коммит `8f9aef0`; часть исправлений мутационной проверки уже была в рабочем дереве, и он это отметил.
- Не проверялось ревьюером: живой прогон (3 минуты), мутационная проверка и смоуки прогоном, настоящий ClickHouse, настоящий внешний
  сервис контроля, `docker compose up`.
