| Строка | Инвариант | Мутация | Поймана своей проверкой | Свои проверки мутации |
|---|---|---|---|---|
| 12 | Р-43, Р-44: потолок при создании и отправке записи | `DROP TRIGGER ba_channel_write_ceiling_insert ON tenant_data.channel_write` | да | ✗ smoke «write created above lowered max_price (Р-44, check 2)» |
| 12 | Р-43, Р-44: потолок при создании и отправке записи | `DROP TRIGGER ba_channel_write_ceiling_dispatch ON tenant_data.channel_write` | да | ✗ smoke «dispatch above lowered max_price (Р-44, check 3)» |
| 13 | Р-44: без округления до границы, причина отказа обязательна | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_no_bound_clamp` | да | ✗ smoke «clamp to floor instead of rejection (Р-44)» |
| 13 | Р-44: без округления до границы, причина отказа обязательна | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_rejection_reason_iff` | да | ✗ smoke «REJECTED without a reason» |
| 14 | Р-43: потолок не в guardrail | `ALTER TABLE tenant_data.guardrail DROP CONSTRAINT guardrail_ceiling_moved_to_max_price` | да | ✗ smoke «guardrail carrying a ceiling (moved to max_price, Р-43)» |
| 16 | Р-52: снятие остановки только с записью журнала | `DROP TRIGGER c_pricing_halt_release_journal ON channel_data.pricing_halt` | да | ✗ smoke «manual release without a journal record (Р-52)» |
| 17 | Р-51: признак цены по конкурентам копируется в решение | `DROP TRIGGER aa_price_decision_copy_derivation ON channel_data.price_decision` | да | ✗ smoke «competitor_derived is not derived from rule_code» |
| 18 | Р-57: валюты EUR и USD | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_supported_currency` | да | ✗ smoke «currency outside EUR and USD» |
| 19 | Р-58: налоговый режим единицы записи цены | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_tax_regime_for_price` | да | ✓ smoke «net price basis with VAT regime (Р-58)»<br>✗ smoke «price write scope without tax regime (Р-58)» |
| 20, 21 | OQ-98: отказ хранит параметры причины | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_rejection_explained` | да | ✗ smoke «rejection without its reason parameters (OQ-98)»<br>✓ store.pg.test.ts «OQ-93, OQ-94, OQ-98» [OQ-98: a rejection without its reason parameters is refused] без защиты: resolved |
| 22 | Р-64: завершение записи — с причиной; ждущая запись объявляется событием | `ALTER TABLE tenant_data.channel_write_history DROP CONSTRAINT channel_write_history_end_explained` | да | ✗ smoke «write discarded without a reason (Р-64)»<br>✗ smoke «write history ended without a reason (Р-64)» |
| 22 | Р-64: завершение записи — с причиной; ждущая запись объявляется событием | `DROP TRIGGER zz_channel_write_announce_dispatch ON tenant_data.channel_write` | да | ✗ write-queue.pg.test.ts «control — without the dispatcher» [a PENDING write has no scope.write.v1 event: nobody would ever learn about it] без защиты: ^[1-9][0-9]*$ |
| 23 | Р-61: курс ЕЦБ неизменяем; курс — в решении с переводом себестоимости | `DROP TRIGGER zz_fx_rate_immutable ON platform.fx_rate` | да | ✗ smoke «ECB rate is immutable (Р-61)» |
| 23 | Р-61: курс ЕЦБ неизменяем; курс — в решении с переводом себестоимости | `DROP TRIGGER aa_price_decision_fx_recorded ON channel_data.price_decision` | да | ✗ fx-day-boundary.pg.test.ts «Р-61 in the database» [Р-61: a decision on a converted cost without its exchange rate is refused] без защиты: resolved |
| 23 | Р-61: курс ЕЦБ неизменяем; курс — в решении с переводом себестоимости | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_fx_shape` | да | ✗ smoke «decision exchange rate without its fields (Р-61)» |
| 24, 26 | Р-62, Р-65: граница суток — пояс витрины | `ALTER TABLE platform.marketplace DROP CONSTRAINT marketplace_time_zone_known_if_confirmed` | да | ✗ smoke «US storefront confirmed without a time zone» |
| 24, 26 | Р-62, Р-65: граница суток — пояс витрины | `DROP TRIGGER aa_channel_write_budget_day_tz ON tenant_data.channel_write` | да | ✗ smoke «budgeted write while the storefront day boundary is unconfirmed (Р-65)»<br>✗ smoke «budget day is not the current storefront day (Р-65)» |
| 24, 26 | Р-62, Р-65: граница суток — пояс витрины | `DROP TRIGGER aa_price_daily_day_tz ON tenant_data.price_daily` | да | ✗ fx-day-boundary.pg.test.ts «Р-65: a US storefront» [Р-65: a price day of a US storefront is not closed in Europe/Berlin \| Р-65: a price day of a US storefront is not closed in America/Los_Angeles] без защиты: resolved |
| 25 | Р-60: тенант в базе своего региона | `DROP TRIGGER tenant_region_guard ON tenant_data.tenant` | да | ✗ smoke «tenant in wrong region DB (Р-60)» |
| 27 | Р-69, Р-70: остановки человеком и системные | `ALTER TABLE channel_data.pricing_halt DROP CONSTRAINT pricing_halt_system_only` | да | ✗ step12.pg.test.ts «Р-69: a system halt is only for broken channel data» [Р-69: a person cannot create a system halt] без защиты: resolved |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER aa_price_stop_role_guard ON tenant_data.price_stop` | да | ✗ step12.pg.test.ts «Р-69, Р-70, OQ-125 in the database» [OQ-129: a viewer does not stop pricing \| OQ-125: an operator does not resume a tenant stop] без защиты: ^(STOPPED\|RELEASED)$ |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER ab_price_decision_stop_guard ON channel_data.price_decision` | да | ✗ smoke «approval while the tenant is stopped by a person (Р-69)» |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER bb_channel_write_stop_guard ON tenant_data.channel_write` | да | ✗ smoke «dispatch while the tenant is stopped by a person (Р-69)» |
| 27 | Р-69, Р-70: остановки человеком и системные | `DROP TRIGGER ca_pricing_halt_release_role_guard ON channel_data.pricing_halt` | да | ✗ smoke «manual halt release without a second factor (finding 12, Р-88)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_explanation_by_class` | да | ✗ smoke «NO_OP decision with an explanation (Р-74)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_no_change_reason_code` | да | ✗ smoke «NO_OP decision with an unknown no-change reason (Р-74)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `DROP TRIGGER a0_price_decision_intent_columns ON channel_data.price_decision` | да | ✗ smoke «decision intent columns are taken from the client, not from the intent (Р-80)» |
| 28, 32, 39, 40 | Р-68, Р-74, Р-80: слепок по классам, код причины NO_OP, столбцы intent в решении, NO_OP без ссылки на снимок | `DROP TRIGGER a_price_decision_snapshot_ref_guard ON channel_data.price_decision_snapshot_ref` | да | ✗ step14.pg.test.ts «finding 10, Р-80» [finding 10: a snapshot reference of a NO_OP decision is refused] без защиты: "command":"INSERT" |
| 29 | Р-71: сумма с валютой | `ALTER TABLE channel_data.price_decision DROP CONSTRAINT price_decision_amounts_have_currency` | да | ✗ smoke «rejection parameters with an amount without its currency (Р-71)» |
| 29 | Р-71: сумма с валютой | `ALTER TABLE tenant_data.channel_write DROP CONSTRAINT channel_write_end_params_currency` | да | ✗ smoke «write ended with an amount without its currency (Р-71)» |
| 29 | Р-71: сумма с валютой | `ALTER TABLE channel_data.rejected_competitor_snapshot DROP CONSTRAINT rejected_competitor_snapshot_details_cur` | да | ✗ smoke «rejected snapshot details: an amount without its currency (Р-71)» |
| 30 | Р-73: «опасное» согласовано с отклонением | `ALTER TABLE tenant_data.price_intent_core DROP CONSTRAINT price_intent_core_dangerous_consistent` | да | ✗ smoke «eternal core: dangerous flag against the deviation (Р-73)» |
| 31, 37, 38 | OQ-125, OQ-129, находка 4: матрица прав остановок, автор — пользователь сессии | `CREATE OR REPLACE FUNCTION security.pricing_permission(p_role text, p_action text) RETURNS boolean LANGUAGE sq` | да | ✗ step14.pg.test.ts «finding 2» [/[A-Z_]+ × [A-Z_]+/] без защиты: ^true$ |
| 31, 37, 38 | OQ-125, OQ-129, находка 4: матрица прав остановок, автор — пользователь сессии | `tenant_data.price_stop_role_guard(): «IF security.current_user_id() IS NULL OR u IS DIST…» → «IF false THEN…»` | да | ✗ step14.pg.test.ts «finding 4» [finding 4: a stop is not written in the name of another membership] без защиты: ^STOPPED$ |
| 33 | Р-75: справочник объяснения неизменяем | `DROP TRIGGER explanation_ruleset_immutable ON platform.explanation_ruleset` | да | ✗ smoke «explanation ruleset is immutable (Р-75)» |
| 34 | Р-76: остановки и снятия — в журнале аудита | `DROP TRIGGER zb_price_stop_audit ON tenant_data.price_stop` | да | ✗ role-separation.pg.test.ts «the audit log is still written» [Р-76: the stop is written to the audit log by the trigger with the session user as author] без защиты: \+ \[\] |
| 34 | Р-76: остановки и снятия — в журнале аудита | `DROP TRIGGER zb_pricing_halt_audit ON channel_data.pricing_halt` | да | ✗ smoke «the system halt is not in the audit log» |
| 34 | Р-76: остановки и снятия — в журнале аудита | `DROP TRIGGER zb_pricing_halt_review_audit ON channel_data.pricing_halt_review` | да | ✗ smoke «the manual release is not in the audit log with its author» |
| 35 | Р-77: движок без стратегии не включается | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_engine_has_strategy` | да | ✗ smoke «ENGINE without a strategy (Р-77)» |
| 41 | Р-79: подтверждённый архив ядра — со справочниками | `ALTER TABLE maintenance.partition_export DROP CONSTRAINT partition_export_core_archive_self_contained` | да | ✗ step14.pg.test.ts «Р-79» [Р-79: a verified archive of the core without the explanation dictionary is refused] без защиты: "command":"INSERT" |
| 42 | Р-83: пол с полом маржи перед каждой отправкой | `tenant_data.channel_write_before_update(): «NEW.floor_at_dispatch_minor := tenant_data.assert_…» → «NEW.floor_at_dispatch_minor :=…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /below the recomputed margin floor was dispatched/<br>✗ write-recheck.pg.test.ts «the unit cost rose between decision and dispatch» [/cost: the write reached the channel: .*/] без защиты: ^false$ |
| 43 | Р-85, Р-91, находка 15: из вечного ядра не выводится значение канала | `ALTER TABLE tenant_data.price_intent_core DROP CONSTRAINT price_intent_core_competitor_rejection_not_kept` | да | ✗ smoke «eternal core: a competitor-derived rejection keeps its proposed price (Р-85)» |
| 43 | Р-85, Р-91, находка 15: из вечного ядра не выводится значение канала | `ALTER TABLE tenant_data.price_intent_core DROP CONSTRAINT price_intent_core_explanation_keys_declared` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /undeclared reason parameter was stored/<br>✗ smoke «eternal core: an undeclared reason parameter in the explanation (finding 15)»<br>✗ smoke «decision explanation with an undeclared reason parameter (finding 15)» |
| 44 | Находки 1–3, Р-88: журнал проверок, автоматическое снятие, второй фактор | `DROP TRIGGER a_pricing_halt_review_guard ON channel_data.pricing_halt_review` | да | ✗ smoke «automatic review recorded in a user session (finding 3)» |
| 44 | Находки 1–3, Р-88: журнал проверок, автоматическое снятие, второй фактор | `DROP TRIGGER zc_pricing_halt_review_released_in_tx ON channel_data.pricing_halt_review` | да | ✗ audit-guards.pg.test.ts «finding 1» [a release record without the release fails at commit] без защиты: ^accepted$ |
| 44 | Находки 1–3, Р-88: журнал проверок, автоматическое снятие, второй фактор | `tenant_data.price_stop_role_guard(): «AND NOT security.session_mfa() THEN…» → «AND false THEN…»` | да | ✗ audit-guards.pg.test.ts «Р-88: releasing the tenant stop needs a second factor» [Р-88: the tenant stop is not released without a second factor] без защиты: ^RELEASED$ |
| 45 | C2: день бюджета при повторе — текущий день витрины | `DROP TRIGGER ab_channel_write_budget_day_retry ON tenant_data.channel_write` | да | ✗ smoke «retry after midnight is charged to today, whose budget is exhausted (C2, Р-19)»<br>✗ smoke «retry while the storefront day boundary is unconfirmed (C2, Р-65)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON tenant_data.migration_consent TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.migration_consent: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path creates an eBay migration consent (Р-96, Р-2)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT UPDATE ON tenant_data.tenant TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.tenant: UPDATE of the decision path does not match the allow list/<br>✗ smoke «path opts the tenant into Kaufland Smart Pricing (Р-96, Р-12, Р-41)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON tenant_data.min_price TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.min_price: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path lowers min_price (Р-96, Р-5)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON tenant_data.cost_profile TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.cost_profile: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path changes the unit cost (Р-96, Р-83)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT UPDATE ON channel_data.pricing_halt TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.pricing_halt: UPDATE of the decision path does not match the allow list/<br>✗ smoke «path schedules the review of a halt (finding 2 ревью шага 16)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON channel_data.pricing_halt_review TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.pricing_halt_review: INSERT of the decision path does not match the allow list/<br>✗ smoke «path writes an automatic halt review (finding 2 ревью шага 16)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT INSERT ON audit.audit_event TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /audit\.audit_event: INSERT of the decision path does not match the allow list/<br>✗ smoke «path forges an audit event (finding 5, Р-90)» |
| Р-96 | путь решения — только вычисление и запись цены | `GRANT EXECUTE ON FUNCTION security.invite_member(uuid, text, text, bytea, interval) TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /security\.invite_member.*SECURITY DEFINER function executable by the decision path/<br>✗ другая причина smoke «path invites a member (Р-90)» |
| Р-96 | путь решения — только вычисление и запись цены | `CREATE OR REPLACE FUNCTION tenant_data.lock_decision_products(p_tenant_id uuid, p_write_scope_ids uuid[]) RETU` | да | ✗ store.pg.test.ts «Р-54: a concurrent bound change serialises» [decision commit must wait for the bound change] без защиты: ^true$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF failed > 0 THEN…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [SAMPLE_FAILED: one failed observation fails the review] без защиты: ^RELEASED$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF accepted < required THEN…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [NO_SAMPLE: fewer accepted products than the sample requires] без защиты: ^RELEASED$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «sm.recorded_at >= h.next_review_at AND …» → «…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [NO_SAMPLE: an observation recorded before the review window elapsed does not count] без защиты: ^RELEASED$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «v_at      timestamptz := least(p_at, now());…» → «v_at      timestamptz := p_at;…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [finding 3: the next review is scheduled from the database clock, not from a moment in the future] без защиты: ^false$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «AND sm.observed_at <= v_at;…» → «;…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [NO_SAMPLE: an observation after the review moment does not count] без защиты: ^RELEASED$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «AND om.channel_product_ref = sm.channel_product_re…» → «))…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [NO_SAMPLE: a stale observation and a product outside the storefront do not count] без защиты: ^RELEASED$ |
| находка 2 | автоматическое снятие — итог базы по выборке, не запись пути решения | `channel_data.review_halt_by_sample(uuid,uuid,timestamptz): «IF p_tenant_id IS DISTINCT FROM security.current_t…» → «IF false THEN…»` | да | ✗ audit-guards.pg.test.ts «finding 3» [the review of a halt runs only in the context of its own tenant] без защиты: ^accepted$ |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE tenant_data.min_price DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.min_price» |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE channel_data.price_decision DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only channel_data.price_decision» |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE channel_data.price_intent DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only channel_data.price_intent» |
| находка 4 | неизменяемость проверяется попыткой изменения | `ALTER TABLE tenant_data.pricing_strategy DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.pricing_strategy» |
| находка 4 | неизменяемость проверяется попыткой изменения | `CREATE OR REPLACE FUNCTION security.forbid_mutation() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN coal` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /forbid_mutation does not refuse an UPDATE/<br>✗ smoke «append-only tenant_data.min_price» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `DROP TRIGGER a0_admin_write_person_insert ON tenant_data.product` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.product: administrative INSERT without the person guard/<br>✗ другая причина smoke «administrative change without a person (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `DROP TRIGGER zz_admin_write_audit_insert ON tenant_data.product` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.product: administrative INSERT is not written to the audit log/<br>✗ smoke «an administrative change is not in the audit log (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `DROP TRIGGER a0_admin_write_person_insert ON channel_data.pricing_halt_review` | да | ✗ smoke «the administrative service records an automatic review without a person (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `ALTER TABLE audit.audit_event DROP CONSTRAINT audit_event_check1` | да | ✗ smoke «administrative change by a user outside the tenant (Р-97)»<br>✗ smoke «administrative change in the platform tenant (step 17 finding 9)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `security.audit_admin_write(): «IF NOT security.admin_session() THEN…» → «IF true THEN…»` | да | ✗ smoke «an administrative change is not in the audit log (Р-97)» |
| Р-97 | административная запись — только по действию человека и вся в аудите | `security.admin_session(): «pg_has_role(session_user, 'repracer_admin', 'MEMBE…» → «false…»` | да | ✗ smoke «administrative change without a person (Р-97)» |
| находка 5 | создание тенанта не присоединяет существующего пользователя мимо приглашения | `security.provision_tenant(uuid,text,text,jsonb): «IF m ->> 'role' IS DISTINCT FROM 'OWNER' THEN…» → «IF false THEN…»` | да | ✗ smoke «existing user attached as a member without an invitation (step 16 finding 5)» |
| находка 5 | создание тенанта не присоединяет существующего пользователя мимо приглашения | `security.provision_tenant(uuid,text,text,jsonb): «IF lower(trim(m ->> 'email')) IS DISTINCT FROM exi…» → «IF false THEN…»` | да | ✗ smoke «existing user provisioned as owner under another email (step 16 finding 5)» |
| находка 5 | создание тенанта не присоединяет существующего пользователя мимо приглашения | `security.provision_tenant(uuid,text,text,jsonb): «IF NOT EXISTS (SELECT 1 FROM platform.external_ide…» → «IF false AND NOT EXISTS (SELEC…»` | да | ✗ smoke «existing user who never signed in provisioned as owner (step 16 finding 5)» |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.accept_identity_invitation(bytea,text,text,text,boolean): «IF NOT inv.relink THEN…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» [an ordinary invitation does not relink] без защиты: resolved |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.accept_identity_invitation(bytea,text,text,text,boolean): «IF EXISTS (SELECT 1 FROM platform.external_identit…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» [the revoked sign-in accepts nothing] без защиты: resolved |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.invite_relink(uuid,uuid,bytea,interval): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» [Р-98: a relink invitation without a second factor] без защиты: resolved |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.invite_relink(uuid,uuid,bytea,interval): «AND m.status = 'ACTIVE' AND m.role = 'OWNER') THEN…» → «AND m.status = 'ACTIVE') THEN…»` | да | ✗ identity.pg.test.ts «Р-98» [Р-98: only an active owner relinks] без защиты: resolved |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.invite_relink(uuid,uuid,bytea,interval): «IF NOT EXISTS (SELECT 1 FROM tenant_data.membershi…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «Р-98» [Р-98: a relink is issued only to a member of the tenant] без защиты: resolved |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `DROP TRIGGER a_external_identity_one_active ON platform.external_identity` | да | ✗ identity.pg.test.ts «step 17 finding 3» [Р-98: the second acceptance waits for the first \| /Р-98: two active sign-ins of one provider for one user: ([0-9a-f-]{36})/] без защиты: ^(true\|[0-9a-f-]{36})$ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `platform.external_identity_one_active(): «PERFORM pg_advisory_xact_lock(hashtextextended('pl…» → «…»` | да | ✗ identity.pg.test.ts «step 17 finding 3» [Р-98: the second acceptance waits for the first] без защиты: ^true$ |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `security.resolve_external_identity(text,text): «AND NOT EXISTS (SELECT 1 FROM platform.external_id…» → «…»` | да | ✗ identity.pg.test.ts «Р-98» [the revoked sign-in resolves nobody] без защиты: userId |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `DROP TRIGGER zz_identity_relinked_audit ON platform.identity_invitation` | да | ✗ identity.pg.test.ts «Р-98» [Р-98: the invitation and the relink are audited] без защиты: ^(?!.*identity\.relinked\b) |
| Р-98 | перепривязка входа — только приглашением владельца, прежний вход отозван | `DROP TRIGGER zz_identity_relink_invited_audit ON platform.identity_invitation` | да | ✗ identity.pg.test.ts «Р-98» [Р-98: the invitation and the relink are audited] без защиты: ^(?!.*identity\.relink_invited) |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `channel_data.pricing_strategy_undercut_guard(): «IF EXISTS (SELECT 1 FROM tenant_data.write_scope s…» → «IF false AND EXISTS (SELECT 1 …»` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» [OQ-151: the undercut of a pinned version does not start expiring] без защиты: ^accepted$ |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `DROP TRIGGER b_write_scope_pins_live_strategy_version ON tenant_data.write_scope` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» [OQ-151: a scope is not pinned back to a version whose undercut expires] без защиты: ^accepted$ |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `DROP TRIGGER zb_write_scope_release_strategy_version ON tenant_data.write_scope` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» [the released version starts its 18 months] без защиты: 0: version: 1 superseded: false |
| OQ-151 | подрез закреплённой версии стратегии не начинает срок хранения | `tenant_data.pricing_strategy_supersede_undercut(): «AND s.status <> 'RETIRED')…» → «AND false)…»` | да | ✗ undercut-eternal.pg.test.ts «Р-91: the strategy version keeps its type» [OQ-151: a new version is created while the old one is still pinned] без защиты: still pinned by a write scope |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'number' AND v::text ~ '^-?[0-9]+$';…» → «RETURN t = 'number';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [an amount that is not minor units] без защиты: ^true$ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND s IN ('EUR', 'USD');…» → «RETURN t = 'string';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a currency that is not an ISO code \| a currency outside EUR and USD] без защиты: ^true$ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND (NOT spec ? 'v' OR (spec -…» → «RETURN t = 'string';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [an enum value outside the registry] без защиты: ^true$ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'number' AND v::text ~ '^-?[0-9]+$';…» → «RETURN t IN ('number', 'string…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [an amount as a string] без защиты: ^true$ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.explanation_field_kinds(): «"$.format":{"k":"enum","v":["r80.1"]}…» → «"$.format":{"k":"code"}…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [finding 6: an explanation format shaped as a code other than r80.1 is refused] без защиты: ^true$ |
| находка 6 | слепок проверяется по видам и значениям параметров, а не только по именам ключей | `security.explanation_node_declared(jsonb,text,boolean): «OR (w.value #>> '{}') !~ '^[A-Za-z][A-Za-z0-9_]{0,…» → «…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a withheld name that is not an identifier] без защиты: ^true$ |
| Р-101 | согласие на миграцию eBay — только владелец, от своего имени, со вторым фактором | `tenant_data.migration_consent_guard(): «AND (security.current_user_id() IS NULL OR NEW.use…» → «AND false THEN…»` | да | ✗ smoke «eBay consent in the name of another owner (Р-101)» |
| Р-101 | согласие на миграцию eBay — только владелец, от своего имени, со вторым фактором | `tenant_data.migration_consent_guard(): «AND NOT security.session_mfa() THEN…» → «AND false THEN…»` | да | ✗ smoke «eBay consent by the owner without a second factor (Р-101)» |
| Р-100 | административная запись проверяет роль и административные столбцы | `security.require_person_for_admin_write(): «IF action <> 'OWN_GUARD' AND r IS NOT NULL AND NOT…» → «IF false THEN…»` | да | ✗ smoke «an operator lowers min_price (Р-100)»<br>✗ smoke «eBay consent by an admin in their own name (Р-101)»<br>✗ smoke «write scope status changed by a viewer (step 18 finding 1)»<br>✗ smoke «consent revoked by an admin (Р-101)» |
| Р-100 | административная запись проверяет роль и административные столбцы | `DROP TRIGGER a0_admin_write_person_update ON tenant_data.write_scope` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.write_scope: administrative UPDATE without the person guard/<br>✗ другая причина smoke «pricing mode changed without a person (step 17 finding 1)» |
| Р-100 | административная запись проверяет роль и административные столбцы | `security.admin_write_action(text): «('tenant_data.min_price', 'MANAGE_PRICING'), …» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.min_price: no administrative action is declared/ |
| находка 4 (шаг 17) | путь решения не пишет действия человека в свои таблицы | `GRANT INSERT ON channel_data.pricing_halt TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.pricing_halt: INSERT of the decision path does not match the allow list/<br>✗ smoke «path inserts a halt released by a person (step 17 finding 4)» |
| находка 4 (шаг 17) | путь решения не пишет действия человека в свои таблицы | `GRANT UPDATE ON channel_data.divergence_case TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.divergence_case: UPDATE of the decision path does not match the allow list/<br>✗ smoke «path resolves a divergence case for a person (step 17 finding 4)» |
| находка 4 (шаг 17) | путь решения не пишет действия человека в свои таблицы | `DROP TRIGGER a1_write_scope_path_status_guard ON tenant_data.write_scope` | да | ✗ smoke «path unblocks a write scope (step 17 finding 4)»<br>✗ smoke «path holds a write scope (step 17 finding 4)» |
| находка 5 (шаг 17) | срок проверки остановки переносится со вторым фактором и в аудите | `DROP TRIGGER a1_pricing_halt_review_schedule_guard ON channel_data.pricing_halt` | да | ✗ smoke «the review of a halt moved without a second factor (step 17 finding 5)» |
| находка 5 (шаг 17) | срок проверки остановки переносится со вторым фактором и в аудите | `DROP TRIGGER zz_admin_write_audit_update ON channel_data.pricing_halt` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.pricing_halt: administrative UPDATE is not written to the audit log/<br>✗ smoke «the moved review of a halt is not in the audit log (step 17 finding 5)» |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.explanation_node_declared(jsonb,text,boolean): «ELSIF NOT security.param_value_valid(v, fields -> …» → «ELSIF false THEN…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a free text in strategy.currentMinor] без защиты: ^true$ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND s IN ('EUR', 'USD');…» → «RETURN t = 'string' AND s ~ '^…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a currency outside EUR and USD] без защиты: ^true$ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «PERFORM s::timestamptz;…» → «NULL;…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [an invalid moment in the price stop context] без защиты: ^true$ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «BETWEEN 0 AND 1000;…» → «IS NOT NULL;…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a ratio outside its range] без защиты: ^true$ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'number' AND v::text ~ '^[0-9]+$';…» → «RETURN t = 'number';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a negative count] без защиты: ^true$ |
| находка 8 (шаг 17) | виды значений у каждого скалярного поля слепка | `security.param_value_valid(jsonb,jsonb): «RETURN t = 'string' AND s ~ '^[A-Za-z0-9_.:@-]{1,1…» → «RETURN t = 'string';…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [a free text as an id] без защиты: ^true$ |
| Р-102 | роль остатков — только остатки и резервации | `GRANT SELECT ON tenant_data.min_price TO repracer_stock` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.min_price: SELECT of the stock role does not match its allow list/<br>✗ smoke «stock role reads prices (Р-102)» |
| Р-102 | роль остатков — только остатки и резервации | `GRANT UPDATE ON tenant_data.write_scope TO repracer_stock` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.write_scope: UPDATE of the stock role does not match its allow list/ |
| Р-102 | роль остатков — только остатки и резервации | `GRANT UPDATE (pricing_mode) ON tenant_data.write_scope TO repracer_stock` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.write_scope\.pricing_mode: UPDATE of the stock role is not in its allow list/<br>✗ другая причина smoke «stock role changes the pricing of a write scope (Р-102)» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE tenant_data.guardrail DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.guardrail» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE channel_data.pricing_halt_review DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only channel_data.pricing_halt_review» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE platform.external_identity_revocation DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only platform.external_identity_revocation» |
| Р-103 | неизменяемость таблиц, у которых раньше не было строк, проверяется попыткой изменения | `ALTER TABLE tenant_data.price_daily_correction DISABLE TRIGGER zz_append_only` | да | ✗ smoke «append-only tenant_data.price_daily_correction» |
| OQ-153 | привязки входа удаляются только у отключённого пользователя | `maintenance.purge_user_identities(uuid): «IF NOT EXISTS (SELECT 1 FROM platform.app_user u W…» → «IF false THEN…»` | да | ✗ smoke «sign-ins of an active user are purged (OQ-153)» |
| находка 1 (шаг 18) | в сессии административного сервиса любое изменение строки — под стражем человека и в аудите | `security.require_person_for_admin_write(): «IF u IS NULL THEN…» → «IF false THEN…»` | да | ✗ smoke «the administrative service records an automatic review without a person (Р-97)» |
| находка 1 (шаг 18) | в сессии административного сервиса любое изменение строки — под стражем человека и в аудите | `security.audit_admin_write(): «    -- Находка 1 ревью шага 18: в журнал попадает …» → «    IF NOT cols && TG_ARGV THE…»` | да | ✗ smoke «a status change of a write scope by a person is not in the audit log (step 18 finding 1)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF c.user_id IS NULL OR c.user_id IS DISTINCT FROM…» → «IF false THEN…»` | да | ✗ smoke «listing added to a consent by another owner (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | да | ✗ smoke «listing added to a consent without a second factor (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_item_guard(): «IF c.given_at <> now() THEN…» → «IF false THEN…»` | да | ✗ smoke «listing added to a consent given in an earlier transaction (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_revocation_guard(): «IF mem.membership_id IS NULL OR NEW.revoked_by_mem…» → «IF false THEN…»` | да | ✗ smoke «consent revoked in the name of another owner (Р-101)» |
| Р-101 (находка 2, шаг 18) | листинг в согласие добавляет владелец, давший его, со вторым фактором, в той же транзакции, по предполётной проверке; отзыв — владелец от своего имени со вторым фактором | `tenant_data.migration_consent_revocation_guard(): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | да | ✗ smoke «consent revoked without a second factor (Р-101)» |
| Р-107 (находка 3, шаг 18) | ручной intent — только человек в административном сервисе, от своего имени, с правом менять цену | `channel_data.price_intent_manual_guard(): «IF mem.membership_id IS NULL OR NEW.created_by_mem…» → «IF false THEN…»` | да | ✗ smoke «manual price intent in the name of another member (Р-107)» |
| Р-107 (находка 3, шаг 18) | ручной intent — только человек в административном сервисе, от своего имени, с правом менять цену | `channel_data.price_intent_manual_guard(): «IF NOT security.pricing_permission(mem.role, 'ENAB…» → «IF false THEN…»` | да | ✗ smoke «manual price intent by a viewer (Р-107)» |
| Р-107 (находка 3, шаг 18) | ручной intent — только человек в административном сервисе, от своего имени, с правом менять цену | `channel_data.price_intent_manual_guard(): «NOT (NEW.trigger_type = 'MANUAL' OR coalesce(NEW.r…» → «NOT (NEW.trigger_type = 'MANUA…»` | да | ✗ smoke «path creates a manual price intent of the owner (Р-107)» |
| находка 4 (шаг 18) | системную остановку ставит проверка входов пути решения, а не человек | `channel_data.pricing_halt_insert_guard(): «IF security.admin_session() THEN…» → «IF false THEN…»` | да | ✗ smoke «system halt created by a person (step 18 finding 4)» |
| находка 4 (шаг 18) | системную остановку ставит проверка входов пути решения, а не человек | `channel_data.pricing_halt_sample_insert_guard(): «IF security.admin_session() THEN…» → «IF false THEN…»` | да | ✗ smoke «halt sample observation recorded by a viewer in the administrative service (step 19 review finding 3)» |
| находка 4 (шаг 18) | системную остановку ставит проверка входов пути решения, а не человек | `DROP TRIGGER a00_pricing_halt_insert_guard ON channel_data.pricing_halt; CREATE TRIGGER a00_pricing_halt_inser` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /pricing_halt: trigger a00_pricing_halt_insert_guard has a WHEN condition that is not in the list/<br>✗ smoke «system halt created by a person (step 18 finding 4)» |
| находка 6 (шаг 18) | одна действующая привязка входа — и при изоляции строже READ COMMITTED | `platform.external_identity_one_active(): «IF current_setting('transaction_isolation') <> 're…» → «IF false THEN…»` | да | ✗ identity.pg.test.ts «step 18 finding 6» [step 18 finding 6: a link in REPEATABLE READ is refused] без защиты: ^linked$ |
| находка 6 (шаг 18) | одна действующая привязка входа — и при изоляции строже READ COMMITTED | `ALTER FUNCTION platform.external_identity_one_active() SECURITY INVOKER` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /platform\.external_identity_one_active\(\): owned by repracer_resolver but not SECURITY DEFINER/ |
| находка 5 (шаг 18) | привязки входа удаляет только удаление привязок отключённого пользователя | `GRANT DELETE ON platform.external_identity_revocation TO repracer_retention` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /role repracer_retention may delete sign-in links/<br>✗ smoke «retention role deletes sign-in revocations directly (step 18 finding 5)» |
| находка 5 (шаг 18) | привязки входа удаляет только удаление привязок отключённого пользователя | `GRANT DELETE ON platform.external_identity TO repracer_retention` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /role repracer_retention may delete sign-in links/<br>✗ smoke «retention role deletes sign-in links directly (step 18 finding 5)» |
| находка 5 (шаг 18) | привязки входа удаляет только удаление привязок отключённого пользователя | `security.forbid_mutation(): «IF TG_OP = 'DELETE' AND current_user IN ('repracer…» → «IF TG_OP = 'DELETE' THEN…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /security\.forbid_mutation does not refuse a DELETE/ |
| находка 7 (шаг 18) | скаляр на месте узла слепка — отказ | `security.explanation_node_declared(jsonb,text,boolean): «IF jsonb_typeof(node) NOT IN ('object', 'array') T…» → «IF false THEN…»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [step 18 finding 7: a scalar among the sanity checks is refused as a declared-keys violation] без защиты: ^error: cannot call jsonb_each |
| находка 7 (шаг 18) | скаляр на месте узла слепка — отказ | `security.explanation_node_declared(jsonb,text,boolean): «IF NOT security.explanation_node_declared(v, path …» → «IF jsonb_typeof(v) = 'object' …»` | да | ✗ undercut-eternal.pg.test.ts «finding 15» [step 18 finding 7: a scalar among the strategy steps is refused \| step 18 finding 7: a scalar among the sanity checks is refused] без защиты: ^true$ |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON tenant_data.channel_write_history USING (tenant_id = security.current_tenant_id` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_write_history: policy stock_quantity of the stock role is not limited to the QUANTITY field/<br>✗ smoke «stock role writes price history of a channel write (Р-105)» |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON tenant_data.channel_write USING (tenant_id = security.current_tenant_id()) WITH` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_write: policy stock_quantity of the stock role is not limited to the QUANTITY field/<br>✗ smoke «the stock role sees price writes or does not see quantity writes (Р-105)» |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON tenant_data.write_scope USING (tenant_id = security.current_tenant_id())` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /write_scope: policy stock_quantity of the stock role is not limited to the QUANTITY field/<br>✗ smoke «the stock role sees price writes or does not see quantity writes (Р-105)» |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `GRANT UPDATE (pricing_mode) ON tenant_data.write_scope TO repracer_stock` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.write_scope\.pricing_mode: UPDATE of the stock role is not in its allow list/ |
| Р-105 | роль остатков пишет в канал только поле QUANTITY | `ALTER POLICY stock_quantity ON channel_data.write_submission USING (tenant_id = security.current_tenant_id())` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /write_submission: policy stock_quantity of the stock role is not limited to the QUANTITY field/ |
| шаг 19: атрибуты функций защит | функция служебной роли исполняется её правами | `ALTER FUNCTION security.audit_admin_write() SECURITY INVOKER` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /security\.audit_admin_write\(\): owned by repracer_audit_writer but not SECURITY DEFINER/<br>✓ smoke «an administrative change is not in the audit log (Р-97)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.migration_consent_item_guard(): «IF lc.verdict IS NULL OR lc.verdict IS DISTINCT FR…» → «IF false THEN…»` | да | ✗ smoke «consent to a listing whose preflight verdict is INELIGIBLE (Р-109)»<br>✗ smoke «listing added to a consent without a preflight check (Р-101, Р-2)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.migration_consent_item_guard(): «AND l.listing_migration_check_id <> NEW.listing_mi…» → «AND false) THEN…»` | да | ✗ smoke «consent to a superseded preflight check (Р-109)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.migration_consent_item_guard(): «IF lc.checked_at < now() - interval '24 hours' THE…» → «IF false THEN…»` | да | ✗ smoke «consent to a preflight check older than 24 hours (Р-109)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.migration_consent_item_guard(): «IF losses IS DISTINCT FROM (SELECT…» → «IF false AND losses IS DISTINC…»` | да | ✗ smoke «consent acknowledging other losses than the preflight check found (Р-109)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `ALTER TABLE channel_data.listing_migration_check DROP CONSTRAINT listing_migration_check_findings_shape` | да | ✗ smoke «preflight LOSS finding without the name of the loss (Р-109)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `DROP TRIGGER a00_listing_migration_check_not_future ON channel_data.listing_migration_check` | да | ✗ smoke «preflight check dated in the future (Р-109, step 20 review)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `ALTER TABLE channel_data.listing_migration_check DROP CONSTRAINT listing_migration_check_verdict_matches_findi` | да | ✗ smoke «preflight verdict READY with a blocking finding (Р-109, step 20 review)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.migration_consent_item_guard(): «AND l.listing_migration_check_id <> NEW.listing_mi…» → «AND l.checked_at > lc.checked_…»` | да | ✗ smoke «consent to one of two preflight checks recorded at the same moment (Р-109, step 20 review)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.offer_mapping_migration_guard(): «OR latest.verdict IS DISTINCT FROM i.verdict_at_co…» → «…»` | да | ✗ smoke «migration started after the fresh check became INELIGIBLE (INV-12, step 20 review)» |
| Р-109 | элемент согласия eBay — по вердикту, последней и свежей предполётной проверке, с поимённо принятыми потерями | `tenant_data.offer_mapping_migration_guard(): «OR channel_data.migration_check_losses(latest.find…» → «…»` | да | ✗ smoke «migration started with a loss the owner did not acknowledge (INV-12, Р-109, step 20 review)» |
| Р-111 | собственный пол цены канала (Kaufland minimum_price, Amazon minimum_seller_allowed_price) не пишется: поле — только у Kaufland и только в явном режиме Smart Pricing | `ALTER TABLE platform.channel_capability DROP CONSTRAINT channel_capability_channel_min_price_only_kaufland` | да | ✗ smoke «Amazon capability for the channel repricer floor (Р-111)» |
| Р-111 | собственный пол цены канала (Kaufland minimum_price, Amazon minimum_seller_allowed_price) не пишется: поле — только у Kaufland и только в явном режиме Smart Pricing | `ALTER TABLE tenant_data.write_scope DROP CONSTRAINT write_scope_smart_pricing_only_kaufland` | да | ✗ smoke «Amazon write scope in Smart Pricing mode (Р-111)» |
| Р-111 | собственный пол цены канала (Kaufland minimum_price, Amazon minimum_seller_allowed_price) не пишется: поле — только у Kaufland и только в явном режиме Smart Pricing | `tenant_data.channel_write_before_insert(): «IF s.field <> 'PRICE' OR s.pricing_mode <> 'KAUFLA…» → «IF false THEN…»` | да | ✗ smoke «CHANNEL_MIN_PRICE write in ENGINE mode (Р-12)» |
| Р-88/OQ-144 | границы больше чем одного предложения одной транзакцией административного сервиса — только со вторым фактором; версия границы — время транзакции | `DROP TRIGGER zc_min_price_mass_edit_requires_mfa ON tenant_data.min_price` | да | ✗ smoke «min_price of two offers in one transaction without a second factor (Р-88)» |
| Р-88/OQ-144 | границы больше чем одного предложения одной транзакцией административного сервиса — только со вторым фактором; версия границы — время транзакции | `DROP TRIGGER zc_max_price_mass_edit_requires_mfa ON tenant_data.max_price` | да | ✗ smoke «max_price of two offers in one transaction without a second factor (Р-88)» |
| Р-88/OQ-144 | границы больше чем одного предложения одной транзакцией административного сервиса — только со вторым фактором; версия границы — время транзакции | `tenant_data.bounds_mass_edit_requires_mfa(): «IF offers > 1 THEN…» → «IF false THEN…»` | да | ✗ smoke «min_price of two offers in one transaction without a second factor (Р-88)»<br>✗ smoke «max_price of two offers in one transaction without a second factor (Р-88)» |
| Р-88/OQ-144 | границы больше чем одного предложения одной транзакцией административного сервиса — только со вторым фактором; версия границы — время транзакции | `tenant_data.bounds_mass_edit_requires_mfa(): «WHERE b.tenant_id = NEW.tenant_id AND b.created_at…» → «WHERE false
  ) edited;…»` | да | ✗ smoke «min_price of one offer and max_price of another in one transaction without a second factor (Р-88)» |
| Р-88/OQ-144 | границы больше чем одного предложения одной транзакцией административного сервиса — только со вторым фактором; версия границы — время транзакции | `tenant_data.row_in_current_transaction(xid): «= 'in progress'…» → «= 'committed'…»` | да | ✗ smoke «min_price of two offers in one transaction without a second factor (Р-88)»<br>✗ smoke «max_price of two offers in one transaction without a second factor (Р-88)» |
| Р-88/OQ-144 | границы больше чем одного предложения одной транзакцией административного сервиса — только со вторым фактором; версия границы — время транзакции | `tenant_data.bounds_mass_edit_requires_mfa(): «IF NEW.created_at IS DISTINCT FROM now() THEN…» → «IF false THEN…»` | да | ✗ smoke «backdated bound version from the administrative service (Р-88)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER ac_price_decision_distrust_guard ON channel_data.price_decision` | да | ✗ smoke «fixed-price approval while the channel is distrusted (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER bc_channel_write_distrust_guard ON tenant_data.channel_write` | да | ✗ smoke «dispatch of a fixed-price write while the channel is distrusted (Р-118)»<br>✗ smoke «fixed-price write created while the channel is distrusted (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `ALTER TABLE channel_data.channel_distrust DROP CONSTRAINT channel_distrust_details_check` | да | ✗ smoke «buyer price read from the channel kept in a distrust (Р-3, Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `ALTER TABLE channel_data.channel_distrust DROP CONSTRAINT channel_distrust_reason_known` | да | ✗ smoke «channel distrust with an unknown reason (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `ALTER TABLE channel_data.channel_distrust DROP CONSTRAINT channel_distrust_release_by_person` | да | ✗ smoke «channel distrust released with a note shorter than 10 characters (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `GRANT UPDATE ON channel_data.channel_distrust TO repracer_admin` | да | ✗ smoke «channel distrust reason changed (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER zb_channel_distrust_audit ON channel_data.channel_distrust` | да | ✗ smoke «a channel distrust is created without an audit event (Р-76, Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `channel_data.channel_distrust_audit(): «IF OLD.released_at IS NOT NULL OR NEW.released_at …» → «IF true THEN…»` | да | ✗ smoke «a channel distrust is released without an audit event with its author and note (Р-76, Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER a0_admin_write_person_insert ON channel_data.channel_distrust` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.channel_distrust: administrative INSERT without the person guard/ |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER a0_admin_write_person_update ON channel_data.channel_distrust` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.channel_distrust: administrative UPDATE without the person guard/ |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER zz_admin_write_audit_insert ON channel_data.channel_distrust` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.channel_distrust: administrative INSERT is not written to the audit log/ |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER zz_admin_write_audit_update ON channel_data.channel_distrust` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.channel_distrust: administrative UPDATE is not written to the audit log/ |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `tenant_data.channel_write_distrust_guard(): «IF NEW.field NOT IN ('PRICE', 'CHANNEL_MIN_PRICE')…» → «IF NEW.field NOT IN ('PRICE', …»` | да | ✗ smoke «fixed-price write created while the channel is distrusted (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `tenant_data.channel_write_distrust_guard(): «IF NEW.field NOT IN ('PRICE', 'CHANNEL_MIN_PRICE')…» → «IF NEW.field NOT IN ('PRICE')…»` | да | ✗ smoke «channel price floor write created while the channel is distrusted (Р-118, OQ-166)»<br>✗ smoke «dispatch of a channel price floor write while the channel is distrusted (Р-118, OQ-166)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `channel_data.channel_distrust_for(uuid,uuid): «AND d.released_at IS NULL…» → «AND false…»` | да | ✗ smoke «fixed-price approval while the channel is distrusted (Р-118)»<br>✗ smoke «dispatch of a fixed-price write while the channel is distrusted (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER a00_channel_distrust_insert_guard ON channel_data.channel_distrust` | да | ✗ smoke «a person creates a channel distrust (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `DROP TRIGGER ca_channel_distrust_release_guard ON channel_data.channel_distrust` | да | ✗ smoke «channel distrust released without a second factor (Р-118)»<br>✗ smoke «channel distrust released in the name of another member (Р-118)»<br>✗ smoke «channel distrust released twice (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `channel_data.channel_distrust_release_guard(): «IF security.current_user_id() IS NULL OR u IS DIST…» → «IF false THEN…»` | да | ✗ smoke «channel distrust released in the name of another member (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `channel_data.channel_distrust_release_guard(): «IF NOT security.session_mfa() THEN…» → «IF false THEN…»` | да | ✗ smoke «channel distrust released without a second factor (Р-118)» |
| Р-118 | остановка по недоверию каналу держит любую цену и порог цены канала, ставит её система, снимает только человек с правом, от своего имени, со вторым фактором | `channel_data.channel_distrust_release_guard(): «IF OLD.released_at IS NOT NULL THEN…» → «IF false THEN…»` | да | ✗ smoke «channel distrust released twice (Р-118)» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `DROP TRIGGER a2_write_scope_strategy_guard ON tenant_data.write_scope` | да | ✗ smoke «strategy unavailable on the channel assigned to an offer (Р-39, OQ-166)»<br>✗ smoke «strategy assigned to an offer with the channel repricer active (Р-120)» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `tenant_data.write_scope_strategy_guard(): «IF unmet <> '{}'::jsonb THEN…» → «IF false THEN…»` | да | ✗ smoke «strategy unavailable on the channel assigned to an offer (Р-39, OQ-166)» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `DROP TRIGGER zz_no_truncate ON channel_data.offer_channel_pricing` | да | ✗ smoke «truncate channel_data.offer_channel_pricing» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `tenant_data.write_scope_strategy_guard(): «IF pricing IS NOT NULL THEN…» → «IF false THEN…»` | да | ✗ smoke «strategy assigned to an offer with the channel repricer active (Р-120)» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `channel_data.strategy_unmet(text,jsonb): «u := u \|\| 'COMPLETENESS'::text;…» → «NULL;…»` | да | ✗ smoke «strategy unavailable on the channel assigned to an offer (Р-39, OQ-166)» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `ALTER TABLE channel_data.offer_channel_pricing DROP CONSTRAINT offer_channel_pricing_source_known` | да | ✗ smoke «channel pricing observation from an unknown source (Р-120)» |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `tenant_data.write_scope_strategy_guard(): «OR NEW.pricing_mode <> 'ENGINE')…» → «OR false)…»` | да | ✗ channel-pricing-guard.pg.test.ts «finding 1 [Р-120]» [finding 1: switching repricing off is not refused by channel-owned pricing] без защиты: channel-owned pricing |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `DROP TRIGGER a2_offer_mapping_channel_pricing_guard ON tenant_data.offer_mapping` | да | ✗ channel-pricing-guard.pg.test.ts «finding 3 [Р-120]» [finding 3: an offer with channel-owned pricing is mapped to a strategy write scope] без защиты: resolved |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `tenant_data.offer_mapping_channel_pricing_guard(): «IF pricing IS NOT NULL THEN…» → «IF false THEN…»` | да | ✗ channel-pricing-guard.pg.test.ts «finding 3 [Р-120]» [finding 3: an offer with channel-owned pricing is mapped to a strategy write scope] без защиты: resolved |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `channel_data.review_halt_by_sample(uuid,uuid,timestamp with time zone): «RETURN 'MANUAL_ONLY';…» → «NULL;…»` | да | ✗ channel-reference.pg.test.ts «Р-119: a storefront halt on Amazon» [Р-119: a clean sample does not release a halt on a channel without competitor polling] без защиты: ^(?!MANUAL_ONLY$).+ |
| Р-39/OQ-166, Р-120 | стратегия назначается, только если канал даёт нужные ей данные конкурентов и у предложения нет собственного ценообразования канала | `DROP TRIGGER zz_append_only ON channel_data.offer_channel_pricing` | да | ✗ smoke «append-only channel_data.offer_channel_pricing» |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `ALTER POLICY inbound_router_resolve ON tenant_data.channel_account USING (true)` | да | ✗ inbound.pg.test.ts «step 23: the receiver role routes» [a disconnected account receives no notifications] без защиты: .+ |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `GRANT EXECUTE ON FUNCTION security.resolve_amazon_seller(text, text) TO repracer_app` | да | ✗ inbound.pg.test.ts «step 23: the receiver role routes» [the decision path role may not search accounts across tenants] без защиты: resolved |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `ALTER TABLE channel_data.offer_pricing_health DROP CONSTRAINT offer_pricing_health_threshold_money` | да | ✗ smoke «pricing health threshold without its currency (Р-71)» |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `DROP TRIGGER zz_append_only ON channel_data.inbound_notification` | да | ✗ smoke «append-only channel_data.inbound_notification» |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `DROP TRIGGER zz_no_truncate ON channel_data.inbound_notification` | да | ✗ smoke «truncate channel_data.inbound_notification» |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `DROP TRIGGER zz_append_only ON channel_data.offer_pricing_health` | да | ✗ smoke «append-only channel_data.offer_pricing_health» |
| Шаг 23 A (приёмник уведомлений) | журнал обработанных уведомлений и состояния PRICING_HEALTH не переписываются и не очищаются целиком | `DROP TRIGGER zz_no_truncate ON channel_data.offer_pricing_health` | да | ✗ smoke «truncate channel_data.offer_pricing_health» |
| OQ-172 | остановка человеком держит и порог цены канала CHANNEL_MIN_PRICE — создание и отправку | `tenant_data.channel_write_stop_guard(): «IF NEW.field NOT IN ('PRICE', 'CHANNEL_MIN_PRICE')…» → «IF NEW.field NOT IN ('PRICE')…»` | да | ✗ smoke «channel price floor write created while pricing is stopped by a person (Р-69, OQ-172)»<br>✗ smoke «dispatch of a channel price floor write while pricing is stopped by a person (Р-69, OQ-172)» |
| Р-122 | журнал полных снимков конкурентов не переписывается и не очищается целиком; вердикт и форма снимка известны | `DROP TRIGGER zz_append_only ON channel_data.competitor_snapshot_log` | да | ✗ smoke «append-only channel_data.competitor_snapshot_log» |
| Р-122 | журнал полных снимков конкурентов не переписывается и не очищается целиком; вердикт и форма снимка известны | `DROP TRIGGER zz_no_truncate ON channel_data.competitor_snapshot_log` | да | ✗ smoke «truncate channel_data.competitor_snapshot_log» |
| Р-122 | журнал полных снимков конкурентов не переписывается и не очищается целиком; вердикт и форма снимка известны | `ALTER TABLE channel_data.competitor_snapshot_log DROP CONSTRAINT competitor_snapshot_log_verdict_known` | да | ✗ smoke «competitor snapshot log with an unknown sanity verdict (Р-122)» |
| Р-122 | журнал полных снимков конкурентов не переписывается и не очищается целиком; вердикт и форма снимка известны | `ALTER TABLE channel_data.competitor_snapshot_log DROP CONSTRAINT competitor_snapshot_log_snapshot_object` | да | ✗ smoke «competitor snapshot log whose snapshot is not an object (Р-122)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `DROP TRIGGER a1_discount_announcement_guard ON tenant_data.discount_announcement` | да | ✗ smoke «discount announced with a prior price above the lowest price of 30 days (Omnibus, Р-123)»<br>✗ smoke «discount announced in a currency other than the currency of the offer (Р-71, Р-123)»<br>✗ smoke «the Omnibus check of a discount announcement is not computed by the database (Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `tenant_data.discount_announcement_guard(): «IF p.lowest_minor IS NOT NULL AND NEW.reference_pr…» → «IF false THEN…»` | да | ✗ smoke «discount announced with a prior price above the lowest price of 30 days (Omnibus, Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `DROP TRIGGER zz_no_truncate ON tenant_data.discount_announcement` | да | ✗ smoke «truncate tenant_data.discount_announcement» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `DROP TRIGGER a0_admin_write_person_insert ON tenant_data.discount_announcement` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.discount_announcement: administrative INSERT without the person guard/ |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `DROP TRIGGER zz_admin_write_audit_insert ON tenant_data.discount_announcement` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.discount_announcement: administrative INSERT is not written to the audit log/ |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `tenant_data.discount_announcement_guard(): «IF scope IS NULL OR scope.currency IS DISTINCT FRO…» → «IF false THEN…»` | да | ✗ smoke «discount announced in a currency other than the currency of the offer (Р-71, Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «AND d.price_day < wfrom…» → «AND false…»` | да | ✗ smoke «the Omnibus check of a discount announcement is not computed by the database (Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `tenant_data.discount_announcement_guard(): «IF NEW.starts_at < (…» → «IF false AND NEW.starts_at < (…»` | да | ✗ smoke «discount announced to start before the current storefront day (Omnibus, Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «CASE WHEN p_starts_at > now() THEN 'WINDOW_OPEN'…» → «CASE WHEN false THEN 'WINDOW_O…»` | да | ✗ smoke «a discount starting later is confirmed before its window closes (Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «AND coalesce(ap.applied_at, h.accepted_at) >= to_t…» → «AND false…»` | да | ✗ smoke «the prices of the discount day before its start are not in the window (Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `ALTER TABLE tenant_data.discount_announcement DROP CONSTRAINT discount_announcement_prices` | да | ✗ smoke «discount whose sale price is not below the prior price (Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `ALTER TABLE tenant_data.discount_announcement DROP CONSTRAINT discount_announcement_period` | да | ✗ smoke «discount ending before it starts (Р-123)» |
| Р-123 | объявленная прежняя цена скидки не выше наименьшей цены оффера за 30 суток витрины; проверку вычисляет база; объявления не переписываются | `DROP TRIGGER zz_append_only ON tenant_data.discount_announcement` | да | ✗ smoke «append-only tenant_data.discount_announcement» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `ALTER TABLE channel_data.competitor_snapshot_log DROP CONSTRAINT competitor_snapshot_log_delivery_known` | да | ✗ smoke «competitor snapshot log with an unknown delivery (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `ALTER TABLE channel_data.notification_loss_check DROP CONSTRAINT notification_loss_check_diverged` | да | ✗ smoke «notification loss check without a divergence (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `channel_data.review_notification_loss(uuid,uuid,timestamp with time zone): «AND l.delivery IN ('PUSH', 'PUSH_FETCH') AND l.obs…» → «AND l.delivery IN ('PUSH', 'PU…»` | да | ✗ smoke «a notification observed before the held state resolves a loss check (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `GRANT INSERT ON channel_data.notification_loss_verdict TO repracer_admin` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.notification_loss_verdict: administrative INSERT without the person guard/ |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `GRANT INSERT ON channel_data.notification_loss_verdict TO repracer_app` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.notification_loss_verdict: INSERT of the decision path does not match the allow list/<br>✗ другая причина smoke «path sets a notification loss verdict itself (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `DROP TRIGGER zz_append_only ON channel_data.notification_loss_check` | да | ✗ smoke «append-only channel_data.notification_loss_check» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `DROP TRIGGER zz_no_truncate ON channel_data.notification_loss_check` | да | ✗ smoke «truncate channel_data.notification_loss_check» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `DROP TRIGGER zz_append_only ON channel_data.notification_loss_verdict` | да | ✗ smoke «append-only channel_data.notification_loss_verdict» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `DROP TRIGGER zz_no_truncate ON channel_data.notification_loss_verdict` | да | ✗ smoke «truncate channel_data.notification_loss_verdict» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `ALTER TABLE channel_data.notification_loss_check DROP CONSTRAINT notification_loss_check_order` | да | ✗ smoke «notification loss check due before the poll (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `ALTER TABLE channel_data.notification_loss_check DROP CONSTRAINT notification_loss_check_compared_known` | да | ✗ smoke «notification loss check comparing an unknown value (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `ALTER TABLE channel_data.notification_loss_verdict DROP CONSTRAINT notification_loss_verdict_evidence` | да | ✗ smoke «notification loss verdict delayed without the notification snapshot (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `ALTER TABLE channel_data.notification_loss_verdict DROP CONSTRAINT notification_loss_verdict_known` | да | ✗ smoke «notification loss verdict of an unknown kind (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `channel_data.review_notification_loss(uuid,uuid,timestamp with time zone): «AND l.received_at <= d.due_at…» → «…»` | да | ✗ smoke «a notification after the due time resolves a loss check (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `channel_data.review_notification_loss(uuid,uuid,timestamp with time zone): «AND l.delivery IN ('PUSH', 'PUSH_FETCH') AND…» → «AND…»` | да | ✗ smoke «a polled snapshot counts as a delivered notification (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `channel_data.review_notification_loss(uuid,uuid,timestamp with time zone): «AND c.due_at <= v_at…» → «…»` | да | ✗ smoke «a loss check is decided before its due time (Р-121)» |
| Р-121 | сверка опросом: проверка потери уведомления — только при расхождении и со сроком после опроса; вердикт ставит база и только по снимку уведомления того же товара до срока | `channel_data.review_notification_loss(uuid,uuid,timestamp with time zone): «AND (l.channel_account_id, l.marketplace, l.channe…» → «…»` | да | ✗ smoke «a notification of another product resolves a loss check (Р-121)» |
| OQ-169 | единице записи назначается только действующая версия стратегии | `tenant_data.write_scope_strategy_guard(): «AND status IS DISTINCT FROM 'ACTIVE' THEN…» → «AND false THEN…»` | да | ✗ smoke «a draft strategy version assigned to an offer (OQ-169)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `DROP TRIGGER a_scheduled_job_lease_guard ON maintenance.scheduled_job` | да | ✗ smoke «a scheduled job taken over while another scheduler holds its lease (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `maintenance.scheduled_job_lease_guard(): «AND OLD.lease_until > now()…» → «AND false…»` | да | ✗ smoke «a scheduled job taken over while another scheduler holds its lease (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job_run DROP CONSTRAINT scheduled_job_run_order` | да | ✗ smoke «a scheduler run finished before it started (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `DROP TRIGGER zz_append_only ON maintenance.scheduled_job_run` | да | ✗ smoke «append-only maintenance.scheduled_job_run» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `DROP TRIGGER zz_no_truncate ON maintenance.scheduled_job_run` | да | ✗ smoke «truncate maintenance.scheduled_job_run» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `maintenance.scheduled_job_lease_guard(): «AND current_setting('repracer.scheduler_owner', tr…» → «AND false THEN…»` | да | ✗ smoke «an active scheduled job lease released by another scheduler (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_key_format` | да | ✗ smoke «a scheduled job key outside the job and account format (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_catch_up_known` | да | ✗ smoke «a scheduled job with an unknown catch-up rule (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_interval_positive` | да | ✗ smoke «a scheduled job without a positive interval (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_scope_pair` | да | ✗ smoke «a scheduled job scoped to a tenant without an account (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_lease_pair` | да | ✗ smoke «a scheduled job lease without its end (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_outcome_known` | да | ✗ smoke «a scheduled job with an unknown last outcome (Р-126)» |
| Р-126 | периодическую работу выполняет один планировщик — аренда; состояние и журнал запусков известной формы; журнал неизменяем | `ALTER TABLE maintenance.scheduled_job_run DROP CONSTRAINT scheduled_job_run_outcome_known` | да | ✗ smoke «a scheduler run with an unknown outcome (Р-126)» |
| Р-124 | проверка Omnibus показывает глубину видимой истории и цены, выставленные мимо нас; такие цены — в окне, проверка с ними не достоверна | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «WHEN external_n > 0 THEN 'EXTERNAL_CHANGES'…» → «WHEN false THEN 'EXTERNAL_CHAN…»` | да | ✗ smoke «a price set outside repracer does not make the Omnibus check unverifiable (Р-124)» |
| Р-124 | проверка Omnibus показывает глубину видимой истории и цены, выставленные мимо нас; такие цены — в окне, проверка с ними не достоверна | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «AND c.opened_at >= from_ts AND c.opened_at < p_sta…» → «AND false…»` | да | ✗ smoke «a price set outside repracer does not make the Omnibus check unverifiable (Р-124)» |
| Р-124 | проверка Omnibus показывает глубину видимой истории и цены, выставленные мимо нас; такие цены — в окне, проверка с ними не достоверна | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «SELECT least(…» → «SELECT greatest(…»` | да | ✗ smoke «the history depth of an offer is not counted from its first known price (Р-124)» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `DROP TRIGGER a_price_history_not_applied_guard ON tenant_data.price_history_not_applied` | да | ✗ smoke «a price of an applied write marked as not applied (risk 28)» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `tenant_data.price_history_not_applied_guard(): «AND w.final_status = 'NOT_APPLIED'…» → «…»` | да | ✗ smoke «a price of a write the channel applied marked as not applied (risk 28)» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `DROP TRIGGER b_price_history_mark_not_applied ON tenant_data.channel_write_history` | да | ✗ smoke «a price write the channel did not apply is not marked (risk 28)» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `tenant_data.omnibus_raw_prices(uuid,uuid,text,timestamp with time zone): «na.price_history_id = h.price_history_id)…» → «na.price_history_id = h.price_…»` | да | ✗ smoke «a price the channel did not apply is in the Omnibus window (risk 28)» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `tenant_data.omnibus_lowest_prior_price(uuid,uuid,timestamp with time zone): «na.price_history_id = h.price_history_id)…» → «na.price_history_id = h.price_…»` | да | ✗ smoke «a price the channel did not apply is in the Omnibus window (risk 28)» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `maintenance.close_price_days(timestamp with time zone,integer): «na.price_history_id = h.price_history_id)…» → «na.price_history_id = h.price_…»` | да | ✗ omnibus-not-applied.pg.test.ts «risk 28» [risk 28: a price the channel did not apply is rolled up into the daily price] без защиты: ^900$ |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `DROP TRIGGER zz_append_only ON tenant_data.price_history_not_applied` | да | ✗ smoke «append-only tenant_data.price_history_not_applied» |
| риск 28 | цена, которую канал не применил, отмечается и не входит ни в суточную свёртку, ни в окно Omnibus | `DROP TRIGGER zz_no_truncate ON tenant_data.price_history_not_applied` | да | ✗ smoke «truncate tenant_data.price_history_not_applied» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `DROP TRIGGER a_partition_export_skip_guard ON maintenance.partition_export` | да | ✗ smoke «a partition with an unresolved skipped snapshot marked as verified (OQ-181)» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `maintenance.partition_export_skip_guard(): «IF open_skips > 0 THEN…» → «IF false THEN…»` | да | ✗ smoke «a partition with an unresolved skipped snapshot marked as verified (OQ-181)» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `ALTER TABLE maintenance.snapshot_export_skip DROP CONSTRAINT snapshot_export_skip_reason_known` | да | ✗ smoke «a skipped snapshot with an unknown reason (OQ-181)» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `ALTER TABLE maintenance.snapshot_export_skip_resolution DROP CONSTRAINT snapshot_export_skip_resolution_known` | да | ✗ smoke «a skipped snapshot resolution of an unknown kind (OQ-181)» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `ALTER TABLE maintenance.snapshot_export_skip_resolution DROP CONSTRAINT snapshot_export_skip_resolution_operat` | да | ✗ smoke «a skipped snapshot resolved without the operator (OQ-181)» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `ALTER TABLE maintenance.snapshot_export_skip_resolution DROP CONSTRAINT snapshot_export_skip_resolution_note` | да | ✗ smoke «a skipped snapshot resolved without a note (OQ-181)» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `DROP TRIGGER zz_append_only ON maintenance.snapshot_export_skip` | да | ✗ smoke «append-only maintenance.snapshot_export_skip» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `DROP TRIGGER zz_no_truncate ON maintenance.snapshot_export_skip` | да | ✗ smoke «truncate maintenance.snapshot_export_skip» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `DROP TRIGGER zz_append_only ON maintenance.snapshot_export_skip_resolution` | да | ✗ smoke «append-only maintenance.snapshot_export_skip_resolution» |
| OQ-181 | пропущенный выгрузкой снимок записан и разобран человеком с именем и заметкой; секция с неразобранным пропуском не отмечается проверенной | `DROP TRIGGER zz_no_truncate ON maintenance.snapshot_export_skip_resolution` | да | ✗ smoke «truncate maintenance.snapshot_export_skip_resolution» |
| риск 31 | уровень отставания работы планировщика хранится в базе: перезапуск и второй процесс алерт не повторяют | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_lag_level_known` | да | ✗ smoke «a scheduled job with an unknown lag level (риск 31)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `DROP TRIGGER a_snapshot_export_skip_resolution_guard ON maintenance.snapshot_export_skip_resolution` | да | ✗ smoke «a skipped snapshot resolved without a platform operator account (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `maintenance.snapshot_export_skip_resolution_guard(): «o.operator_id = NEW.operator_id AND o.active…» → «o.operator_id = NEW.operator_i…»` | да | ✗ smoke «a skipped snapshot resolved by a retired operator account (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `DROP TRIGGER zz_append_only ON maintenance.snapshot_export_skip_verification` | да | ✗ smoke «append-only maintenance.snapshot_export_skip_verification» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `DROP TRIGGER zz_no_truncate ON maintenance.snapshot_export_skip_verification` | да | ✗ smoke «truncate maintenance.snapshot_export_skip_verification» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `maintenance.snapshot_export_skip_resolution_guard(): «IF NOT NEW.mfa THEN…» → «IF false THEN…»` | да | ✗ smoke «a skipped snapshot resolved without the second factor (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `maintenance.partition_export_skip_guard(): «AND (r.resolution = 'LOSS_ACCEPTED'…» → «AND (true…»` | да | ✗ smoke «a partition verified on the operator word that the snapshot was exported after the fix (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `ALTER TABLE maintenance.snapshot_export_skip_verification DROP CONSTRAINT snapshot_export_skip_verification_ro` | да | ✗ smoke «a skip verification with no rows in ClickHouse (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `ALTER TABLE maintenance.snapshot_export_skip_verification DROP CONSTRAINT snapshot_export_skip_verification_pl` | да | ✗ smoke «a skip verification that belongs to a tenant (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `ALTER TABLE platform.platform_operator DROP CONSTRAINT platform_operator_platform_tenant` | да | ✗ smoke «a platform operator that belongs to a tenant (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `ALTER TABLE platform.platform_operator DROP CONSTRAINT platform_operator_issuer_url` | да | ✗ smoke «a platform operator without an identity provider (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `ALTER TABLE platform.platform_operator DROP CONSTRAINT platform_operator_subject_present` | да | ✗ smoke «a platform operator without a subject (OQ-182)» |
| OQ-182 | разбор пропущенного снимка — от действующей учётной записи оператора платформы со вторым фактором; «выгружен после исправления» — только со сверкой ClickHouse | `ALTER TABLE platform.platform_operator DROP CONSTRAINT platform_operator_name_present` | да | ✗ smoke «a platform operator without a name (OQ-182)» |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `DROP TRIGGER a_price_history_applied_guard ON tenant_data.price_history_applied` | да | ✗ smoke «a price of a write the channel did not confirm marked with a time of application (OQ-180)» |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `tenant_data.price_history_applied_guard(): «AND w.final_status = 'APPLIED'…» → «…»` | да | ✗ smoke «a price of a write the channel reported a time for but did not apply marked with a time of application (OQ-180)» |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `DROP TRIGGER b_price_history_mark_applied ON tenant_data.channel_write_history` | да | ✗ smoke «the time the channel applied a price is not recorded for its price history (OQ-180)» |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `tenant_data.omnibus_raw_prices(uuid,uuid,text,timestamp with time zone): «SELECT coalesce(ap.applied_at, h.accepted_at), h.a…» → «SELECT h.accepted_at, h.amount…»` | да | ✗ smoke «the Omnibus window uses the time the price was accepted, not applied (OQ-180)» |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `maintenance.close_price_days(timestamp with time zone,integer): «WHERE coalesce(ap.applied_at, h.accepted_at) >= da…» → «WHERE h.accepted_at >= day_sta…»` | да | ✗ omnibus-applied-time.pg.test.ts «OQ-180» [OQ-180: a price applied after midnight is rolled up into the day it was accepted] без защиты: ^0$ |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `maintenance.purge_tenant_data(uuid,boolean): «'tenant_data.price_history_applied', …» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.price_history_applied: tenant closure does not delete the table/ |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `DROP TRIGGER zz_append_only ON tenant_data.price_history_applied` | да | ✗ smoke «append-only tenant_data.price_history_applied» |
| OQ-180 | цена входит в окно Omnibus и в суточную свёртку по времени, когда канал её применил | `DROP TRIGGER zz_no_truncate ON tenant_data.price_history_applied` | да | ✗ smoke «truncate tenant_data.price_history_applied» |
| Р-131 | единица записи переходит в ENGINE только с объявленной себестоимостью товара | `DROP TRIGGER zw_write_scope_cost_required_guard ON tenant_data.write_scope` | да | ✗ smoke «ENGINE without the declared unit cost (Р-131)» |
| Р-131 | единица записи переходит в ENGINE только с объявленной себестоимостью товара | `tenant_data.write_scope_cost_required_guard(): «IF NEW.pricing_mode = 'ENGINE'…» → «IF false…»` | да | ✗ smoke «ENGINE without the declared unit cost (Р-131)» |
| Р-131 | единица записи переходит в ENGINE только с объявленной себестоимостью товара | `tenant_data.write_scope_has_cost(uuid,uuid,timestamp with time zone): «SELECT EXISTS (…» → «SELECT true OR EXISTS (…»` | да | ✗ smoke «ENGINE without the declared unit cost (Р-131)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `maintenance.correct_closed_price_days(timestamp with time zone): «AND c.closed_at < m.recorded_at…» → «AND false…»` | да | ✗ smoke «the closed day of acceptance is recomputed without the price that moved (Р-29, OQ-192)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `maintenance.correct_closed_price_days(timestamp with time zone): «CONTINUE WHEN eff.corrected_by = 'HUMAN';…» → «…»` | да | ✗ smoke «the recomputation does not touch a day a person corrected (Р-29, OQ-192)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `maintenance.price_day_rollup(uuid,uuid,text,date,text): «coalesce(ap.applied_at, h.accepted_at) >= (p_day::…» → «h.accepted_at >= (p_day::times…»` | да | ✗ smoke «the price is counted in the day the channel applied it (OQ-192)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `DROP TRIGGER a_price_daily_system_correction_chain ON tenant_data.price_daily_system_correction` | да | ✗ smoke «a system correction that does not continue the chain of the day (Р-29)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `ALTER TABLE tenant_data.price_daily_system_correction DROP CONSTRAINT price_daily_system_correction_shape` | да | ✗ smoke «a system correction of a day with prices but without the amounts (Р-29)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `ALTER TABLE tenant_data.price_daily_system_correction DROP CONSTRAINT price_daily_system_correction_bounds` | да | ✗ smoke «a system correction whose lowest price is above its highest (OQ-192)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `ALTER TABLE tenant_data.price_daily_system_correction DROP CONSTRAINT price_daily_system_correction_reason_che` | да | ✗ smoke «a system correction with an unknown reason (OQ-192)» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `DROP TRIGGER zz_append_only ON tenant_data.price_daily_system_correction` | да | ✗ smoke «append-only tenant_data.price_daily_system_correction» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `DROP TRIGGER zz_no_truncate ON tenant_data.price_daily_system_correction` | да | ✗ smoke «truncate tenant_data.price_daily_system_correction» |
| OQ-192 | подтверждение применения после закрытия суток переносит цену строкой-поправкой [Р-29]: свёртка не меняется, повтор пересчёта ничего не пишет | `maintenance.purge_tenant_data(uuid,boolean): «, 'tenant_data.price_daily_system_correction'…» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.price_daily_system_correction: tenant closure does not delete the table/ |
| шаг 27, F | закрытие тенанта удаляет и таблицы данных канала, которые раньше ждали срока | `maintenance.purge_tenant_channel_data(uuid): «'channel_data.pricing_halt_sample', …» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.pricing_halt_sample: tenant closure does not delete the table/ |
| шаг 27, F | закрытие тенанта удаляет и таблицы данных канала, которые раньше ждали срока | `maintenance.purge_tenant_channel_data(uuid): «'channel_data.price_decision_snapshot_ref', …» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.price_decision_snapshot_ref: tenant closure does not delete the table/ |
| шаг 27, F | закрытие тенанта удаляет и таблицы данных канала, которые раньше ждали срока | `maintenance.purge_tenant_channel_data(uuid): «'channel_data.pricing_strategy_undercut',…» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /channel_data\.pricing_strategy_undercut: tenant closure does not delete the table/ |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `DROP TRIGGER zd_cost_profile_import_guard ON tenant_data.cost_profile` | да | ✗ smoke «a cost import row that belongs to a batch of another transaction (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `tenant_data.cost_profile_import_guard(): «IF batch_xmin IS NULL OR NOT tenant_data.row_in_cu…» → «IF false THEN…»` | да | ✗ smoke «a cost import row that belongs to a batch of another transaction (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `DROP TRIGGER a0_admin_write_person_insert ON tenant_data.cost_import` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.cost_import: administrative INSERT without the person guard/ |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `DROP TRIGGER zz_admin_write_audit_insert ON tenant_data.cost_import` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.cost_import: administrative INSERT is not written to the audit log/ |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `DROP TRIGGER zz_append_only ON tenant_data.cost_import` | да | ✗ smoke «append-only tenant_data.cost_import» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_profile DROP CONSTRAINT cost_profile_import_batch_fk; DROP TRIGGER zz_no_truncate` | да | ✗ smoke «truncate tenant_data.cost_import» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `maintenance.purge_tenant_data(uuid,boolean): «, 'tenant_data.cost_import'…» → «…»` | да | ✗ verify 0105_verify_schema_invariants_v25.sql /tenant_data\.cost_import: tenant closure does not delete the table/ |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `DROP TRIGGER ze_cost_import_all_or_nothing ON tenant_data.cost_import` | да | ✗ smoke «a cost import that brings fewer rows than it declared (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `tenant_data.cost_import_all_or_nothing(): «IF brought <> NEW.row_count THEN…» → «IF false THEN…»` | да | ✗ smoke «a cost import that brings fewer rows than it declared (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_profile DROP CONSTRAINT cost_profile_import_source` | да | ✗ smoke «a cost row of source IMPORT without a batch (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_import DROP CONSTRAINT cost_import_source_format_check` | да | ✗ smoke «a cost import of an unknown file format (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_import DROP CONSTRAINT cost_import_source_name_check` | да | ✗ smoke «a cost import without a file name (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_import DROP CONSTRAINT cost_import_row_count_check` | да | ✗ smoke «a cost import of no rows (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_import DROP CONSTRAINT cost_import_fingerprint_check` | да | ✗ smoke «a cost import without the fingerprint of the preview (Р-134)» |
| Р-134 | массовый импорт себестоимости применяется целиком: строки живут только в транзакции своего пакета, пакет приносит ровно объявленное число строк, значения пакета не выдумываются | `ALTER TABLE tenant_data.cost_import DROP CONSTRAINT cost_import_skipped_rows_check` | да | ✗ smoke «a cost import with a negative number of skipped rows (Р-134)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER zc_cost_import_requires_mfa ON tenant_data.cost_import` | да | ✗ smoke «a cost import without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `tenant_data.cost_import_requires_mfa(): «IF security.session_mfa() THEN RETURN NULL; END IF…» → «…»` | да | ✗ smoke «a cost import batch and its row in one transaction are accepted (Р-134)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER a1_cost_profile_transaction_time ON tenant_data.cost_profile` | да | ✗ smoke «a backdated cost version from the administrative service (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `tenant_data.cost_version_is_transaction_time(): «IF NEW.created_at IS DISTINCT FROM now() THEN…» → «IF false THEN…»` | да | ✗ smoke «a backdated cost version from the administrative service (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER zg_guardrail_wide_scope_requires_mfa ON tenant_data.guardrail` | да | ✗ smoke «a tenant-wide guardrail without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `tenant_data.wide_guardrail_requires_mfa(): «IF NEW.scope_type IN ('TENANT', 'CHANNEL_ACCOUNT')…» → «IF false THEN…»` | да | ✗ smoke «a tenant-wide guardrail without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `tenant_data.mass_change_window_requires_mfa(): «SELECT 'PRODUCT:' \|\| coalesce(b.product_id, w.prod…» → «SELECT 'MIN:' \|\| b.min_price_i…»` | да | ✗ smoke «onboarding of offer 3 in the window is accepted without a second factor (Р-135)»<br>✗ smoke «cost and both bounds of a fifth offer in the window are accepted without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER zf_cost_profile_mass_window_requires_mfa ON tenant_data.cost_profile` | да | ✗ smoke «prices of more than five offers changed within ten minutes without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `tenant_data.mass_change_window_requires_mfa(): «IF offers > 5 THEN…» → «IF false THEN…»` | да | ✗ smoke «prices of more than five offers changed within ten minutes without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `tenant_data.mass_change_window_requires_mfa(): «window_start timestamptz := now() - interval '10 m…» → «window_start timestamptz := no…»` | да | ✗ smoke «min_price of a sixth offer within ten minutes without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER a_cost_profile_created_with_mfa ON tenant_data.cost_profile` | да | ✗ smoke «a manual cost edit after a second-factor mass change is still accepted (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER a_min_price_created_with_mfa ON tenant_data.min_price` | да | ✗ smoke «a manual min_price edit after a second-factor mass change is still accepted (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER a_max_price_created_with_mfa ON tenant_data.max_price` | да | ✗ smoke «a manual max_price edit after a second-factor mass change is still accepted (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER zf_min_price_mass_window_requires_mfa ON tenant_data.min_price` | да | ✗ smoke «min_price of a sixth offer within ten minutes without a second factor (Р-135)» |
| Р-135 | массовое изменение цен требует второго фактора, и разбиение на отдельные транзакции его не обходит (риск 17) | `DROP TRIGGER zf_max_price_mass_window_requires_mfa ON tenant_data.max_price` | да | ✗ smoke «max_price of a sixth offer within ten minutes without a second factor (Р-135)» |
| Р-133 | вид повтора работы хранится в базе: у внутренних работ пауза растёт от минуты, у работ канала — от периода работы | `ALTER TABLE maintenance.scheduled_job DROP CONSTRAINT scheduled_job_retry_kind_known` | да | ✗ smoke «a scheduled job with an unknown retry kind (Р-133)» |

Строк с мутациями: 74; ложных (хотя бы одна мутация не поймана заявленными проверками): **0** — —.
Мутаций: 342; не поймано своей проверкой: **0** (из них упала только соседняя проверка строки: 0).
Не мутировались: 1–11 (свойства каталога остались в проверке схемы (0067)); 15 (данные справочника НДС остались в проверке схемы (0067)); 36 (правило проверяло отсутствие объектов входа).
✗ — проверка упала на мутации (поймала), ✓ — осталась зелёной.
