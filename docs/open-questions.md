# Открытые вопросы

Статус: 2026-09-15, после шага 16. Нумерация OQ стабильна: закрытые номера не переиспользуются.

Приоритет:
- **S11** — блокировал шаг 11 (инфраструктура отложена решением владельца);
- **S12** — блокировал шаг 12 (закрыты решениями Р-68…Р-73);
- **S13** — блокировал шаг 13 (OQ-128 закрыт, OQ-114 перенесён в S14);
- **S14** — блокирует шаг 14;
- **B** — блокирует выпуск Release 1.0 или готовность канала [Р-11];
- **H** — важно до реализации соответствующей фичи.

> **Файл повреждён на шаге 15 (ошибка скрипта правки), открытые разделы восстановлены частично. Р-92 (шаг 16): утраченные тексты по памяти не восстанавливаются.**
> - Строки, полностью видные в журнале работы шагов 9–15, — последний вариант текста.
> - Остальные строки — из копии файла на шаге 8; **их текст и раздел не перепроверены** (часть вопросов могла быть закрыта решениями Р-57…Р-93 без записи в таблице закрытых).
> - Утраченные вопросы — в разделе «Утраченные тексты (Р-92)» с шагом возникновения; текст восстанавливается, когда вопрос всплывёт в работе.
> - OQ-102 и OQ-114 — только тема, которую назвал владелец на шаге 16; остальные колонки — (проверить).
> - OQ-141, OQ-143…147 записаны на шаге 15 после повреждения по отчёту шага 15: помечены «не исходный текст».
---

## Закрыто решениями

| № | Вопрос | Закрыт | Остаток |
|---|---|---|---|
| OQ-02 | Порядок каналов | Р-7 | — |
| OQ-03 | Остаток: правило «источник учёл заказ» | Р-25 | Детали переходов → OQ-64 |
| OQ-04 | Общий пул или доли | Р-6 | — |
| OQ-11 | Пользователь в нескольких тенантах | Р-9 | — |
| OQ-13 | Рынки ЕС | Р-7, Р-26 | — |
| OQ-14 | Целевая задержка | Р-8 | Точки измерения → OQ-48 |
| OQ-18 | Самый строгий или самый конкретный floor | Р-5 | — |
| OQ-19 | Fallback при невычислимом floor | Р-5 | — |
| OQ-21 | PriceHistory против DPP | Р-3 | — |
| OQ-23 | Нужны ли PII | Р-4 | — |
| OQ-29 | Обучение на данных одного тенанта | Р-10 (SP-API) | Другие каналы — H |
| OQ-33 | Остаток MFN Amazon EU | Р-1 | → OQ-50 |
| OQ-34 | eBay Inventory vs Trading API | Р-2 | — |
| OQ-37 | Kaufland: единица записи и витрины | Р-14, исправлено Р-35 | Ответы поддержки → OQ-37a |
| OQ-40 | Механизм изоляции тенантов | Р-16, ADR-0003 | → OQ-60 |
| OQ-41 | Брокер | Р-24, ADR-0005 | Поставщик, число партиций, топология → OQ-67 |
| OQ-43 | Объём аудита | Схема 0011 | — |
| OQ-44 | Срок снимков конкурентов | Р-20: ClickHouse, TTL 18 мес | — |
| OQ-45 | Ответ SP-API на запись | Р-17 | — |
| OQ-46 | Уровень min_price | Р-18 | — |
| OQ-51 | Срок данных других каналов | Шаг 3: 18 мес | → OQ-28 |
| OQ-53 | Резерв бюджета eBay | → OQ-47 | — |
| OQ-57 | География Release 1.0 | Р-26: Германия и Австрия, мультивалютность не активна | — |
| OQ-62 | Срок согласий eBay после закрытия тенанта | Р-26: 3 года в legal | — |
| OQ-17 | Комиссии Kaufland: API или таблица | Р-32: тарифные таблицы в репозитории | Формат таблиц → OQ-77 |
| OQ-63 | Порог перепартиционирования | Шаг 4: `price_history` — 8 подпартиций на месяц, число меняется для новых месяцев без миграции | — |
| OQ-66 | Исправления `price_history` при вечной `price_daily` | Р-29, миграция 0025: закрытый день неизменяем, поправки `price_daily_correction`, итог `price_daily_effective` | Юридическая оценка свёртки → OQ-68 |
| OQ-70 | Размер горячих окон intent/decision | Р-28, миграция 0024: intent 3 дня, decision 30 дней, `observed_price_daily` 45 дней | — |
| OQ-71 | «Всегда» для intent против DPP | Р-38, миграция 0024: ядро intent вечно (`price_intent_core` → архив), входы с данными канала — 18 мес; окно бэктеста ≤ 18 мес | — |
| OQ-72 | Остаток Kaufland связан через `id_offer` | Р-35, Р-37, миграция 0027 | Синхронность распространения → K-06 (OQ-37a) |
| OQ-73 | Источник конкурентов Kaufland | Р-36, ADR-0007, миграция 0027 | Подписка на события → OQ-80; единицы цены `/buybox` → K-17 |
| OQ-74 | Миграции под Р-27…Р-30 | Миграции 0024–0026 (0028 — исправление проверки режима, 0029 — проверка схемы) | NO_OP в ClickHouse — `030_step6.sql`, не прогонялся (OQ-67) |
| OQ-76 | Приём вебхуков Kaufland | Р-40; `KauflandAdapter.handleInbound` | Ingest-шлюз → OQ-79 |
| OQ-81 | Расписание опроса и сверки Kaufland | Р-47: ярусы по волатильности (`planPollingTiers`); Р-48: догон после потери подписки | Ёмкость горячего яруса → K-04, K-10 |
| OQ-16 | Ставки НДС DE и AT | Р-53, миграция 0032: ставка на товаре, по умолчанию DE 19 % / AT 20 %, категории не сопоставляются | Брутто цен витрин → K-12 |
| OQ-84 | Процесс снятия остановки канала | Р-52, миграция 0032: автоматический выход по свежей выборке, ручное снятие с заметкой без лимита попыток, журнал `pricing_halt_review` | Интерфейс и роли → OQ-97; выборка → OQ-96 |
| OQ-85 | Ставки НДС для цены целевой маржи и пола маржи | Р-53 | — |
| OQ-88 | Холодный старт проверки входов | Р-49: без истории якорь — себестоимость, тот же EAN на другом канале или согласованность снимка; без якорей — `NO_PLAUSIBILITY_ANCHOR` (сценарий `pipeline-cold-start-cost-anchor`) | Новый товар без себестоимости, EAN на другом канале и истории не оценивается, пока не появится якорь |
| OQ-89 | `PricingStore` на PostgreSQL | Шаг 8: `@repracer/pricing-store-pg`; intent, решение и запись — одна транзакция [Р-54]; все сценарии пути на стенде прогоняются на PostgreSQL | Производительность пути → OQ-92, OQ-93 |
| OQ-90 | Застревание при цене далеко от рынка | Р-49: наша цена — вспомогательный якорь, только предупреждение (сценарий `pipeline-oq90-own-price-trap`) | — |
| OQ-91 | Ложная остановка витрины одним конкурентом | Р-50: остановка только при почти нулевом разбросе коэффициента и не от одного продавца (сценарий `pipeline-market-shift-dispersed`) | Порог разброса → OQ-83 |
| OQ-92 | Транзакций на одну оценку снимка | Р-59: контекст — один запрос, снимок и решение — одна транзакция, итог отправки — третья; 2,3 транзакции на оценку, пик 185/с выдерживается ([замер шага 9](benchmarks/step9-decision-path.md)) | Потолок фиксации одного экземпляра → OQ-104 |
| OQ-93 | Окно массового сдвига на каждый снимок | Проекция `competitor_move_latest` (0034) обновляется в транзакции снимка; чтение окна не растёт с числом движений | Журнал `competitor_move` остаётся для выборки Р-50 |
| OQ-94 | Проекция `competitor_state` теряет часть снимка | 0034: `currency`, `price_basis`, `suggested_price_minor`; пересчёт по проекции видит подсказку канала | `lowest_landed_minor` — по-прежнему цена с доставкой |
| OQ-95 | Дата сценариев стенда и суточные партиции | Стенд сдвигает моменты сценария к реальному дню (`rebaseScenario`); `REPRACER_STAND_CLOCK` фиксирует часы | — |
| OQ-98 | Объяснение решения в PostgreSQL | 0034: `price_decision.reason_params` и `checks`; отказ без параметров причины отклоняет БД (`price_decision_rejection_explained`) | Экспорт в ClickHouse (`040_step7.sql`) параметров и проверок ещё не содержит — шаг 10 |
| OQ-99 | Себестоимость в другой валюте | Р-61, 0037: курс ЕЦБ на момент решения, перевод вверх, курс в решении; решение без курса отклоняет БД | Календарь TARGET и предел устаревания 6 дней → OQ-112 |
| OQ-100 | Граница суток истории цен для витрин США | Р-62, 0037: часовой пояс витрины в `platform.marketplace`, закрытие дней по поясу | Пояс витрин США — проверить → OQ-112 |
| OQ-101 | Регион данных и витрины США | Р-60: регион — место клиента; перенос — отдельная процедура ([tenant-region-transfer.md](tenant-region-transfer.md)) | Автоматизация переноса → OQ-111; требования каналов к размещению → OQ-102 |
| OQ-103 | Якорь «тот же EAN» между валютами | Р-63: перевод по курсу ЕЦБ или явное предупреждение `CROSS_CHANNEL_FX_UNAVAILABLE` | — |

---
| OQ-110 | Клиент Kafka: kafkajs 2.2.4 без выпусков с 2023 года | Р-66: `@confluentinc/kafka-javascript` 1.10.1, слой совместимости с KafkaJS | На брокере не прогонялся → OQ-114; периодическая фиксация смещений — проверить |
| OQ-116 | Что останавливает «стоп-кран» | Р-69: остановка человеком (`price_stop`) — все цены; системная остановка витрины — только цены по конкурентам; сценарий `pipeline-kill-switch-tenant-stop` | Рабочий вход и аудит → OQ-128 |
| OQ-117 | Остановка тенанта как объект | Р-70: область `TENANT` без аккаунта, проверяется Gate и триггерами решения и записи; аккаунт, подключённый позже, стоит (`step12.pg.test.ts`) | — |
| OQ-118 | «Опасное изменение» не определено | Р-73: отклонённое Gate с отклонением от границы > 10 %; `bound_deviation_bp` и `dangerous` в решении и ядре (0042) | — |
| OQ-120 | Решение не связано со снимком и проверкой входов в PostgreSQL | Р-68: слепок объяснения в решении и ядре, ссылка на снимок 18 мес в той же транзакции; экран на PostgreSQL показывает пять шагов и совпадает с памятью (`console.pg.test.ts`) | Размер слепка → OQ-126 |
| OQ-121 | Причины без полей для объяснения | Р-71, Р-72: параметры со схемой и валютой, коды пояснений вместо текста, словарь DE/EN; 17 кодов получили параметры | 4 кода ограничены → OQ-130 |
| OQ-125 | Кто может останавливать и возобновлять цены | Шаг 12: роль `OPERATOR`; остановить — владелец и оператор, возобновить тенант — владелец, аккаунт и витрину — владелец и оператор; проверяет БД (0042) | Вход и аудит → OQ-128; другие роли → OQ-129 |
| OQ-126 | Размер решения со слепком объяснения | Р-74, Р-75: слепок только у CHANGED и REJECTED_BY_GATE, справочник `platform.explanation_ruleset` и версии стратегий (0044); горячий буфер решений −44 %, ядро −8 % на строку ([замер шага 13](benchmarks/step13-explanation-size.md)) | Дальнейшее уменьшение ядра → OQ-131 |
| OQ-127 | Стратегия на PostgreSQL только у включённой единицы записи | Р-77: стратегия хранится независимо от режима, ENGINE без стратегии невозможен (0045); предупреждение несёт тип стратегии и явный `requiredBy` | — |
| OQ-128 | Рабочий вход и аудит остановок | Шаг 13: остановки в `audit_event` [Р-76] (0045). Шаг 14: собственный вход отменён [Р-78] — вход у внешнего поставщика identity ([ADR-0013](adr/0013-external-identity-provider.md)), у нас сопоставление `(issuer, subject)` с членствами (0048); автор действия — пользователь сессии (0048) | Подключение ZITADEL не прогонялось (нет сети); MFA для действий с ценами → OQ-138; привязка при приглашении → OQ-139 |
| OQ-131 | Размер вечного ядра intent | Шаг 14, Р-80: слепок без копий столбцов (`r80.1`, 0049); архив ядра самодостаточен [Р-79] ([замер шага 14](benchmarks/step14-explanation-size.md)) | Сжатие в секциях PostgreSQL не действует на строки короче порога TOAST → OQ-140; формат архива Parquet → OQ-137 |
| OQ-132 | Второй фактор (MFA) | Шаг 14, Р-78: MFA — у поставщика identity (TOTP, SMS, email, U2F/FIDO2, passkeys у ZITADEL) | Требовать MFA для действий с ценами → OQ-138 |
| OQ-134 | Восстановление пароля, приглашения, смена email | Шаг 14, Р-78: у поставщика identity (`CreateInviteCode`, сброс пароля) | Создание привязки при приёме приглашения → OQ-139 |
| OQ-136 | NO_OP не объясняется после 30 дней | Шаг 14, Р-81: код причины — в сырье `price_intent_noop` и в ключе почасового агрегата (`060_step14.sql`) | Числа (цель, порог) после 7 дней не хранятся — агрегат даёт причину, диапазон предложенных и опорных значений |
| OQ-137 | Формат архива ядра (Parquet) | Шаг 15, Р-87: Parquet пишет ClickHouse своим экспортом; библиотека не вводится | Экспорт ClickHouse не развёрнут (OQ-114); до него архив — проверяемый JSON в gzip |
| OQ-138 | MFA для действий с ценами | Шаг 15, Р-88: второй фактор обязателен для снятия остановки тенанта и смены роли (БД, 0053); обычные изменения цен — без него | Значения `amr` у ZITADEL — (проверить) на настоящем токене (OQ-141); массовый импорт → OQ-144 |
| OQ-139 | Привязка внешнего пользователя при приглашении | Шаг 15, Р-88: только приёмом приглашения (`security.accept_identity_invitation`, 0053), email приглашения = email токена | Отправка письма с токеном — поставщик (`CreateInviteCode`), интеграция не прогонялась (OQ-141) |
| OQ-140 | Сжатие ядра в секциях PostgreSQL | Шаг 15, Р-86: отменено (0054); вечная копия — архив, сжатый gzip | — |
| OQ-129 | Права остальных ролей на остановку | Шаг 13: остановить и возобновить аккаунт или витрину — владелец, администратор, оператор, менеджер цен; тенант — владелец и администратор; `security.pricing_permission` (0045) | Менеджер остатков и наблюдатель только смотрят; изменение — решением владельца |
| OQ-142 | Цена конкурента выводится из опубликованной цены и подреза стратегии | Шаг 16, Р-91: подрез не хранится в вечной версии стратегии и в слепке; живёт в channel_data.pricing_strategy_undercut 18 месяцев после замены версии (0059) | Юридическая оценка для Amazon — до подключения канала (Р-91); архивы до 0059 — OQ-150 |

## Из копии шага 8: блокировали шаг 9 (S9) — статус не перепроверен

| № | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|
| OQ-55 | Формат исходного файла справочника `channel_capability` | `KAUFLAND_DESCRIPTOR` и строки справочника 0027 должны совпадать | Генерировать строки справочника из `ChannelDescriptor` и сверять в CI |
| OQ-58 | Smart Pricing: как выключить после записи `minimum_price` (K-07) | Р-41: режим не включается ни у кого; гипотеза нейтрализации проверяется только на тестовом аккаунте | Проверить после OQ-75 |
| OQ-60 | Подписанный контекст тенанта | Адаптер — системный сервис многих тенантов | — |
| OQ-64 | Резервация: какое событие переводит в `CONSUMED` для каждого режима остатка; уменьшает ли Kaufland `amount` при заказе (K-11) | Адаптер отдаёт строки заказов (`readOrderLines`) и `RESOURCE_CHANGED` по `order_unit_*` | TTL — только для `CREATED`; старше 14 дней в `CONFIRMED_BY_SOURCE` — алерт (Р-30, 0026) |
| OQ-65 | Загрузка в ClickHouse и экспортёр партиций (ретранслятор outbox решён Р-34) | Адаптер публикует сырые данные в брокер; NO_OP-агрегат требует идемпотентного экспорта | Коннектор брокер → ClickHouse; экспортёр — задание на партицию с токеном дедупликации |
| OQ-67 | Инфраструктура слоёв: брокер, число партиций, ClickHouse | DDL ClickHouse (010–030) не прогонялся — **шаг 20: DDL 001–060 применён на ClickHouse 26.8.4.11, `099_verify.sql` — 0 строк; найден и исправлен дефект порядка `CODEC`/`DEFAULT` (050); сжатие замерено** | Поставщик, цены хранения и число партиций на 3 года — не выбраны |
| OQ-75 | **Регистрация технологического партнёра Kaufland** и тестовый аккаунт продавца | Без партнёрских ключей не работает ни один запрос SaaS; без тестового аккаунта нельзя записать реальные ответы в стенд, закрыть K-вопросы и Р-11. Контрактные тесты сейчас проверяют соответствие адаптера **нашему прочтению документации**, а не поведению Kaufland | Подать заявку сейчас; первые записи рекордера — сценарии 207, 429, подписка на `buy_box_changed` |
| OQ-77 | Формат и версионирование тарифных таблиц комиссий [Р-32] | Пол по марже для первого канала | `fees/<channel>/<yyyy-mm-dd>.yaml`, версия = дата файла |
| OQ-78 | **Общий бюджет запросов Kaufland для нескольких экземпляров адаптера**; распределение между тенантами, если лимит на партнёра (K-04) | Бюджет в памяти процесса: N экземпляров расходуют N × 25 rps. Если 111 rps — на партнёра, это потолок на всех продавцов платформы | Общее хранилище token bucket (ключи `kaufland:seller:<id>`, `kaufland:partner`) и справедливая очередь по тенантам; ёмкость — после K-04 |
| OQ-79 | **Ingest-шлюз вебхуков:** хранение и ротация токена [Р-40], внешний URL для проверки подписи, ответ 2xx за 5 с (K-03), передача в брокер | Подпись считается по полному URL (KFL_C07): если прокси меняет схему, хост или порт, все уведомления отклоняются | Шлюз передаёт адаптеру внешний URL из конфигурации, а не из заголовков; при ротации — период двух действующих токенов |
| OQ-80 | **`item_unit_*` и `buy_box_changed` нет в перечне `event_name` спецификации** (K-18); `buy_box_changed` — ранний доступ [Р-45] | До выдачи доступа основной источник конкурентов — опрос `/buybox` по ярусам (Р-47), подтверждение записи — обратное чтение | Запрос доступа — OQ-86; адаптер принимает уведомления с данными и без (Р-46) |
| OQ-83 | **Калибровка порогов проверки входов `r49.1`** (себестоимость 1/3…×20; согласованность ×5 при ≥ 3 предложениях; тот же EAN ×4; история 30 дней × 3; ×100 с допуском 2 %; массовый сдвиг ≥ 10 товаров ×1.15 за 15 мин с разбросом ln-коэффициента ≤ 0.005) на данных тестового аккаунта | Ложная остановка витрины блокирует цены из данных конкурентов; ложный REJECT оставляет цену устаревшей | После OQ-75: прогон записанных снимков через проверку входов без записи, доля REJECT и HALT по классам и якорям |
| OQ-86 | **Запрос раннего доступа к `buy_box_changed`** у аккаунт-менеджера Kaufland [Р-45] | До выдачи — только опрос; ёмкость горячего яруса ограничена бюджетом | Отправить вместе с OQ-75 |
| OQ-87 | **Ящик `fallback_email` платформы с мониторингом** и дежурство [Р-48] | Отключённая подписка без реакции — потерянные события | Общий ящик ops с пересылкой в систему алертов |
| OQ-96 | **Выборка для автоматического снятия остановки:** сейчас первые N = 5 товаров витрины со стратегиями по конкурентам в порядке `channel_product_ref` | Детерминированная выборка проверяет одни и те же товары; малый N пропускает частичный сбой разбора | Случайная выборка, стратифицированная по продавцам и ценовым диапазонам; N и окно — вместе с OQ-83 |
| OQ-97 | **Интерфейс снятия остановки и роли:** кто снимает (OWNER/ADMIN), что сверяет с каналом, где видит журнал проверок | Р-52 задаёт журнал и заметку, но не права и экран | Роль OWNER/ADMIN; экран остановки с журналом `pricing_halt_review` и чек-листом сверки |

## Две валюты и два налоговых режима (после шага 9)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-102 | — | **Идентификаторы и налоговые режимы витрин Amazon и eBay** _(восстановлена только тема — владелец, шаг 16, Р-92)_ | (проверить) | (проверить) — шаг 20: идентификаторы Amazon подтверждены документацией (`A1PA6795UKMFR9` DE, `ATVPDKIKX0DER` US, 0075); налоговый режим Amazon — A-02 (поле `value_with_tax` одинаково для DE и US); eBay — снимок не сохранён, E-01, E-05 |

## Инфраструктура, диспетчер и курсы (после шага 10)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-114 | S14 | **Стенд инфраструктуры и сеть Docker** _(восстановлена только тема — владелец, шаг 16, Р-92)_ | Брокер (Redpanda) и ClickHouse на стенде не прогонялись: Docker не скачивает образы (CLAUDE.md, шаг 10) | (проверить) — шаг 20: брокер и ClickHouse поднимаются в CI (GitHub Actions): порядок за брокером проверен, DDL ClickHouse применён, выгрузка и сжатие замерены (docs/benchmarks/step20-clickhouse-compression.md); локально Docker по-прежнему не скачивает образы |
| OQ-111 | B | **Автоматизация переноса тенанта между регионами** [Р-60] | Процедура описана, но не реализована; ручной перенос — риск потери суточной свёртки цен (Omnibus) | Скрипт выгрузки/загрузки с контрольными суммами по таблицам, прогон на одноразовых базах EU → US |
| OQ-112 | B | **Граница суток каждой витрины и календарь ЕЦБ:** все витрины — `TO_VERIFY` [Р-65]; у amazon.com и EBAY_US пояс не задан; предел устаревания курса 6 дней — по пасхальным закрытиям TARGET | Пока пояс EBAY_US не подтверждён, БД отклоняет записи с бюджетом правок — адаптер eBay US не сможет писать; сутки витрин без пояса не закрываются, их сырьё цен не удаляется; неверный предел курса — отказ пола маржи в праздники | Для каждой витрины отдельно сверить с документацией канала (eBay — граница календарного дня лимита 250 правок, Amazon — отчёты и уведомления) и перевести в `CONFIRMED` миграцией со ссылкой на источник; календарь TARGET — с сайта ЕЦБ — шаг 20: документация SP-API границу суток витрины не задаёт (даты отчётов — UTC) — A-03; eBay — E-02 |
| OQ-113 | H | **Подтверждение асинхронных записей (Amazon):** диспетчер ждёт `ACCEPTED → APPLIED` до `confirmationTimeoutMs`, сверяя обратным чтением | Для синхронного Kaufland единица освобождается сразу; для Amazon очередь стоит до подтверждения | При адаптере Amazon: подтверждение по отчёту обработки фида или уведомлению; параметры ожидания — по замеру |
| OQ-115 | H | **Параметры повторов и сверки диспетчера** (5 попыток, 2 с…5 мин, сверка через 30 с, в полёте 2 мин) | Допущение до замеров задержек каналов (Р-8); короткое окно — лишние обратные чтения, длинное — медленная очередь | Калибровать по записям тестового аккаунта (OQ-75) |

## Интерфейс: чего не хватает данных (после шага 11)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-119 | H | **Следующая проверка цены не хранится:** ярус опроса считается `planPollingTiers`, но не записывается | Статус «следующая проверка» на экране — «нет данных» | Хранить план опроса по товару: ярус, время следующего опроса, источник (уведомление или опрос) |
| OQ-122 | H | **Чем подтверждено применение цены:** не хранится, ответ ли это на запись или обратное чтение; время применения каналом не хранится | Шаг «подтвердил ли канал» показывает только статус и время принятия | `channel_write.confirmed_by` (`WRITE_RESPONSE` / `READ_BACK` / `NOTIFICATION`) и время подтверждения (без содержимого ответа, Р-17) |
| OQ-123 | H | **Название товара, SKU продавца, часовой пояс пользователя** | Товар на экране — unit и EAN; время — в UTC | Каталог товаров без PII (название, SKU) и пояс в настройках пользователя или тенанта |
| OQ-124 | H | **Сверка пола с калькулятором площадки:** у ставки комиссии нет версии тарифа и категории, себестоимость в контексте решения — одна сумма (OQ-77) | Продавец не может понять, по какому тарифу посчитан пол, если цифры разошлись с калькулятором | Тарифные таблицы с версией в решении [Р-32]; составляющие себестоимости в контексте решения |

## Объяснимость, остановки и роли (после шага 12)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-130 | H | **Четыре причины объяснимы не полностью** (`REASON_LIMITS`): имя продавца при сдвиге от одного продавца; текст ответа канала; что задержало истёкшее предложение; счётчики бюджета, когда исчерпание сообщает канал | Экран говорит, чего нет и почему; значение не домысливается | Задержка — время постановки в очередь и повторов в записи intent; счётчики — (проверить) заголовки лимитов канала; имени продавца нет в источнике, текст ответа не хранится по Р-17 |

## Размер, вход и аудит (после шага 13)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-133 | H | **Срок аудита остановок**: `audit_event` — 18 месяцев, `price_stop` — до закрытия тенанта | Через 18 месяцев в общем журнале нет события, а сама остановка с автором и заметкой — есть | Решение владельца: достаточно ли `price_stop` для истории, или события остановок хранятся дольше (отдельный класс аудита) |
| OQ-135 | H | **Включение репрайсинга не проверяется БД по роли**: `setPricingMode` не знает автора, право `ENABLE_REPRICING` проверяют сервер и хранилище в памяти | Код, минующий сервер, включит движок от имени приложения | Автор и роль в изменении режима единицы записи (столбец или журнал с триггером прав), как у остановок |

## Вход, архив и сжатие (после шага 14)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-141 | H | **Подключение ZITADEL не прогонялось** _(не исходный текст: записан на шаге 15 по отчёту шага 15)_: сети на стенде нет; путь `OIDC_ISSUER`/`OIDC_JWKS_URL` проверен только на локальном имитаторе | Реальные токены (набор утверждений, ротация ключей, `amr`) не сверены | После сети: проект, приложение OIDC, получатель, JWKS; тест с настоящим токеном; сверить облако и субобработчиков по DPA (ADR-0013) |

## Перепроверка, производные значения, CI (после шага 15)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-143 | H | **Подлинность выборки автоматического снятия остановки** _(не исходный текст: записан на шаге 15 по отчёту шага 15)_: БД проверяет, что снятие — действие системы по чистой выборке в момент снятия после окна, но не может проверить, что снимки выборки действительно запрошены у канала | Ошибка кода проверки выборки снимет остановку без настоящей выборки | Хранить идентификаторы снимков выборки (данные канала, 18 мес) и сверять с журналом запросов адаптера |
| OQ-144 | H | ~~**Массовый импорт себестоимости и границ со вторым фактором**~~ **Закрыт шагом 28 (0103):** импорт себестоимости есть [Р-134] и требует второго фактора [Р-135]; разбить его на транзакции нельзя — строки импорта живут только внутри транзакции своего пакета, а массовую правку мимо импорта ловит окно десяти минут | Онбординг с тысячами офферов проходим: без себестоимости репрайсинг не включается [Р-131] | Импорт границ отдельной выгрузкой пока не сделан: границы правятся массовым экраном шага 21 (тоже со вторым фактором) |
| OQ-145 | B | **Сценарий `memory-only` не проверяется на PostgreSQL** _(не исходный текст: записан на шаге 15 по отчёту шага 15)_: курсы — данные платформы, общая база их содержит | Путь «курса нет» на PostgreSQL не проверен | Прогонять такие сценарии в изолированной базе (`test/isolated-db.ts`) |
| OQ-146 | H | **Первый прогон CI в GitHub** _(не исходный текст: записан на шаге 15 по отчёту шага 15)_ | Шаг 16: удалённое хранилище создано (приватное), код отправлен; файл CI не отправлен — у токена gh нет права `workflow` | Владелец: `gh auth refresh -h github.com -s workflow`, отправка коммита CI, разбор первого прогона; параметры Redpanda в CI — (проверить) |
| OQ-147 | B | **`security.resolve_external_identity` доступна приложению без контекста** _(не исходный текст: записан на шаге 15 по отчёту шага 15)_: по известной паре (издатель, subject) она отдаёт членства пользователя | Код приложения с ошибкой может читать членства чужого пользователя | Вызывать только после проверки токена в одном месте (authenticator) и отдельной ролью входа без прочих прав |


## Роли подключения, подрез, CI (после шага 16)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-148 | H | **Повторная привязка пользователя к другому входу того же поставщика** не поддерживается (0058): приём приглашения отказывает, если у пользователя уже есть привязка этого издателя | Смена учётной записи у поставщика, слияние аккаунтов — только вручную | Процедура перепривязки со вторым фактором и событием аудита; решение владельца — **закрыт шагом 17 [Р-98]: перепривязка — приглашением владельца со вторым фактором, прежняя привязка отзывается (0066)** |
| OQ-149 | H | **Административный сервис сам заявляет пользователя сессии и второй фактор** (`app.user_id`, `app.auth_mfa`): база ограничивает, КТО может это заявить (Р-90), но токен поставщика не видит | Скомпрометированный административный сервис действует от имени любого участника своих тенантов | Подписанное утверждение сессии, проверяемое в БД (например, ключ проверки в функции входа), или отдельный сервис авторизации; ADR-0016 — **шаг 17 [Р-97]: записано прямо в ADR-0016 и ADR-0017; административная запись без пользователя сессии отклоняется, каждая — в аудите (0066). Сам вопрос доверия остаётся** |
| OQ-150 | H | **Архивы ядра, выгруженные до 0059, содержат подрез в версиях стратегий** (Р-91) | Вечная копия до шага 16 позволяет вывести цену конкурента из опубликованной цены | Архивы на стенде не выгружались в хранилище (OQ-114): проверить при первом развёртывании; при наличии — переписать с очисткой — **закрыт шагом 17: выгрузка архива и проверка при чтении обратно отказывают, если версия стратегии несёт подрез (`buildCoreArchiveBundle`, `verifyCoreArchiveBundle`); архивов не выгружалось** |
| OQ-151 | B | **Подрез версии стратегии удаляется через 18 месяцев после замены, даже если единица записи всё ещё закреплена за старой версией** | Оценка такой единицы — отказ «параметр стратегии не задан» (fail-closed), цена не меняется | Переводить единицы на новую версию при её создании или продлевать срок, пока версия закреплена; решение владельца — **закрыт шагом 17 (0066): срок подреза начинается только у заменённой версии, за которой не закреплена ни одна неснятая единица; закрепить единицу за версией с истекающим подрезом нельзя** |
| OQ-152 | H | **Фоновой синхронизации остатка не существует в модели ролей [Р-97]:** запись `stock_pool`, `stock_movement`, `reservation` и `sync_job` есть только у административной роли, а та без пользователя сессии ничего не пишет | Когда появится синхронизация остатка из внешнего источника (Р-6, Р-15), писать будет некому | Отдельная роль подключения со списком разрешённого (как `repracer_app`), собственный аудит с актором SYSTEM; решение владельца до шага синхронизации остатка — закрывался шагом 18 [Р-102]: роль repracer_stock (svc_stock) — только остатки и резервации, без цен и без аудита (0069); **возвращён в открытые шагом 19: без записи в `channel_write` роль не доставляет остаток в канал; закрыт шагом 19 [Р-105]: запись в канал — только поле QUANTITY, право ограничено видом поля политиками строк (0072)** |
| OQ-153 | B | **У `platform.external_identity_revocation` нет политики срока хранения** (как и у самой `platform.external_identity`): удалить отзыв значило бы вернуть к жизни прежний вход | Платформенные таблицы без срока — исключение из правила «новая таблица → retention_policy» | Либо срок хранения привязок и отзывов вместе с закрытием учётной записи пользователя, либо явное правило «привязки живут, пока жив пользователь»; решение владельца — **закрыт шагом 18: привязки и отзывы живут время жизни пользователя (метод USER_LIFETIME), удаляются вместе и только у отключённого пользователя (0069)** |
| OQ-154 | B | **Проверка «автоматическое снятие в сессии пользователя» недостижима [Р-94]:** раньше срабатывает Р-52 (нет записи проверки), а запись автоматической проверки в сессии человека невозможна по построению | Защита `pricing_halt_release_role_guard` (ветка AUTO) не проверяется поведением и не покрыта мутационной проверкой | Либо убрать ветку как недостижимую, либо построить сценарий, где запись проверки создана системой, а снятие — в сессии человека — **закрыт шагом 18: недостижимая ветка удалена (0068); проверка чистой выборки на момент автоматического снятия осталась** |
| OQ-155 | B | **Неизменяемость части таблиц не проверена поведением:** в смоук-мире нет строк в `audit.audit_event`, `pricing_halt_review`, `competitor_move`, `external_identity`, `cost_profile`, `guardrail` и других (список печатает `smoke_r65.sql`) | Мутационная проверка ловит снятие триггера только на таблицах со строками; на остальных держится свойство каталога (0067) | Дополнить смоук-мир строками этих таблиц либо признать свойство каталога достаточным для них явным решением — **закрыт шагом 18 [Р-103]: у каждой из 32 append-only таблиц в смоук-мире есть строка, изменение отклоняет триггер неизменяемости (tests/db/smoke_append_only.sql)** |

## Симулятор, бэктест, консоль (шаг 21)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-156 | H | ~~**Писателя `repracer_analytics.competitor_snapshot` нет:** снимки конкурентов попадают только в горячие проекции PostgreSQL (`competitor_state`, `competitor_price_daily` 45 дней)~~ **Закрыт Р-122 (шаг 24): журнал полных снимков в транзакции снимка (0086) и выгрузка суток `exportCompetitorSnapshotsDay` с проверкой числа строк; объём — docs/benchmarks/step24-competitor-snapshot-volume.md.** | Бэктест на реальной истории невозможен: загрузчик `readCompetitorHistory` проверен в CI на синтетических строках, данных нет | Потребитель снимков (брокер или выгрузка) с `competitorSnapshotRow`: валюта из снимка, а не DEFAULT 'EUR' столбца; тенант — из сообщения [Р-31] |
| OQ-157 | H | ~~**Периодический догон при доступе к `buy_box_changed`** (K-10, принятый риск 18): потерянное уведомление не догоняется~~ **Закрыт Р-121 (шаг 24): опрос — дублирующий контур, расхождение без уведомления до срока — подозрение на потерю с алертом (0088). Срок и расписание — OQ-174, OQ-175.** | Цена стоит часами при потере одного уведомления | Опрос по ярусам [Р-47] как страховка и при выданном доступе; частота — после ответа K-10 о доле потерь |
| OQ-158 | H | **Обнаружение нетто `listing_price`** (K-12, принятый риск 19) | Покупатель видит цену на 19 % выше, Buy Box не выигрывается, сверка молчит | Сравнение цены своего предложения в снимке (`seller_offer.prices.sales_price`) с последней записанной; расхождение ровно на ставку НДС — алерт; или ответ K-12 |
| OQ-159 | B | **Два снимка с одним моментом до секунды:** второй отклоняется как OUT_OF_ORDER (симулятор, `burst-rate-limit`) | При debounce 0 (K-10) настоящее изменение рынка в ту же секунду теряется | Порядок по `id_message` или по времени получения при равном `timestamp`; ответ K-10 о гарантиях момента |
| OQ-160 | B | **Лента цен и отчёт об опасных изменениях видят только горячие данные:** консоль не читает `price_history` (90 дней) и `price_daily`; у решений в ClickHouse нет отклонения от границы | Лента — от суток (записи) и 30 дней (решения); отчёт об опасных изменениях — не длиннее 30 дней [Р-28] | Чтение `price_history` консолью; `bound_deviation_bp` и класс вмешательства в выгрузке решения (ClickHouse, 18 мес) |
| OQ-161 | B | ~~**Бэктест на хранилище в памяти растёт нелинейно:** 26 352 снимка — ~50 с~~ **Закрыт шагом 24: индексы и отбор старого состояния в хранилище в памяти, поток истории — 26,4 млн снимков (1 000 товаров × 18 мес × раз в 30 минут) за 40 минут, 11 тыс. снимков/с; docs/benchmarks/step24-backtest-catalog.md.** | История реального продавца (сотни товаров × 18 мес) не пройдёт за разумное время | Носитель состояния бэктеста с индексами по единице записи или прогон по товарам независимо (решения одного товара не зависят от другого, кроме массового сдвига Р-42) |

## Адаптер Amazon (шаг 22)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-162 | B | ~~**Остановка витрины по неверной базе цены блокирует все цены** [Р-116].~~ **Закрыт Р-118 (шаг 23): отдельный вид остановки — недоверие каналу.** Это системная остановка, а Р-51 и Р-69 говорят, что системная остановка держит только цены из данных конкурентов | Фиксированная и маржинальная цены витрины стоят до ручного снятия; решение 0080 расширяет Р-51/Р-69 без явного решения владельца | Решение владельца: подтвердить расширение или выбрать другой объект (например, остановку витрины без автора-человека) |
| OQ-163 | H | **Чужой репрайсер у оффера без наших записей не обнаруживается** [Р-115]. Проверка идёт при чтении перед записью и при обратном чтении | Правило, привязанное в кабинете Amazon к офферу, цену которого мы не меняем, обнаружится только при следующей записи; продавец не получит предупреждения | Периодический обход офферов `searchListingsItems` с `attributes` (лимит 5/100 rps) или уведомление об изменении листинга — выбор источника и частоты. **Шаг 23 [Р-120]: обход офферов при обнаружении есть (`pipeline.discoverOffers`); частота и расписание обхода не выбраны — вопрос остаётся** |
| OQ-164 | H | ~~**Автоматическое снятие остановки витрины [Р-52] на Amazon недостижимо.**~~ **Закрыт Р-119 (шаг 23): свойство канала `MANUAL_ONLY`.** Независимой выборки нет: опрос конкурентов — `getCompetitiveSummary` 0.033 rps (AMZ_C07) | Остановка по массовому сдвигу на amazon.de снимается только вручную (сценарий `amazon/pipeline/halt-auto-release`) | Решение владельца: выборка по очереди `getCompetitiveSummary` на несколько товаров за окно, свежие `ANY_OFFER_CHANGED` как выборка или ручное снятие как правило Amazon |
| OQ-165 | H | ~~**Идентичность оффера Amazon расходится между путями:**~~ **Закрыт шагом 23: одна функция `offerIdentityOf` (channel-port) для пути решения, очереди записей и хранилища в памяти.** путь решения передаёт `externalUnitId = SKU`, очередь записей — `externalSku` и регион | Адаптер принимает оба варианта; третий путь с другой формой идентичности не будет замечен до отказа | Одна функция построения `OfferIdentity` из `offer_mapping` для пути решения и диспетчера |
| OQ-166 | H | ~~**Остановка по базе цены не держит CHANNEL_MIN_PRICE; доступность стратегии [Р-39] проверяет только сервер консоли**~~ **Закрыт шагом 23 (0082): недоверие держит PRICE и CHANNEL_MIN_PRICE; доступность стратегии проверяет база (`write_scope_strategy_guard`, `strategy_unmet`) и хранилище в памяти.** (ревью шага 22, находка 11) | Режим Smart Pricing при остановке может записать порог; другой клиент хранилища может назначить недоступную стратегию | Страж 0080 — на все поля цены; проверка доступности — в хранилище по описанию каналов аккаунта |

## Приём уведомлений, остановки, консоль (шаг 23)

| № | Приоритет | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|---|
| OQ-167 | H | **Приёмник уведомлений не проверен на живой очереди:** очередь SQS, политика для принципала SP-API, `maxReceiveCount` переадресации и задержка доставки (A-08, A-14) заданы только в коде и документации | Пороги тишины (30 мин) и опоздания (15 мин) — допущения; `maxReceiveCount` приёмника (5) может не совпасть с политикой очереди — тогда CRITICAL `NOTIFICATION_GIVING_UP` придёт не на последней попытке | Тестовый аккаунт AWS и регистрация приложения SP-API: подписка на `ANY_OFFER_CHANGED` и `PRICING_HEALTH`, замер задержек и повторов, `maxReceiveCount` из атрибутов очереди при запуске |
| OQ-168 | B | **`PRICING_HEALTH`: значения `issueType` и восстановление Featured Offer** (A-13) | Состояние на экране товаров показывается как пришло и не снимается, пока не придёт новое уведомление | Ответ поддержки SP-API; или срок жизни состояния на экране |
| OQ-169 | B | ~~**Стратегия: назначить существующую версию без новой и снять стратегию с оффера**~~ **Закрыт шагом 24: `assignStrategyVersion` и `unassignStrategy` хранилищ, база назначает только действующую версию (0085), консоль — назначение после превью и снятие при выключенном репрайсинге.** | Каждое сохранение создаёт версию; убрать стратегию с оффера из консоли нельзя (только выключить репрайсинг) | Операции хранилища «назначить версию» и «снять стратегию» со стражем базы и аудитом |
| OQ-170 | B | ~~**Автор и статус версии стратегии в консоли**~~ **Закрыт шагом 24: версии стратегии в состоянии консоли — автор, время, статус.** | Экран стратегий не показывает, кто и когда создал версию, и не отличает архивные версии | Чтение `created_by_membership_id`, `created_at`, `status` в состоянии консоли |
| OQ-172 | H | ~~**Kill switch [Р-69] не держит CHANNEL_MIN_PRICE** (ревью шага 23, находка вне шага В-1): `channel_write_stop_guard` (0042) пропускает поля кроме PRICE~~ **Закрыт шагом 24 (0085): страж остановки человеком — на PRICE и CHANNEL_MIN_PRICE, смоук-проверка и строка каталога.** | Порог цены канала Smart Pricing создаётся и отправляется при остановке человеком; сегодня не достигается — Smart Pricing не включается ни у кого [Р-41] | Страж 0042 — на PRICE и CHANNEL_MIN_PRICE, как у недоверия (0082), со своей смоук-проверкой |
| OQ-173 | B | ~~**Двойник хранилища в памяти однородный по каналу** (ревью шага 23, находка 15): доступность стратегии проверяется по источникам канала мира, а не единицы~~ **Закрыт шагом 24: источники конкурентов по каналу каждого аккаунта в посеве двойника.** | Сценарий с аккаунтами разных каналов в памяти может назначить стратегию, которую PostgreSQL отклонит | Источники конкурентов по каналу каждого аккаунта в посеве двойника |
| OQ-171 | H | ~~**Журнал уведомления записывается после доставки в путь, не в той же транзакции**~~ **Закрыт шагом 24: запись журнала уведомлений — первая в транзакции снимка или состояния PRICING_HEALTH; повтор — ничего не записано (`DUPLICATE_NOTIFICATION`); сценарий параллельных приёмников на памяти и PostgreSQL.** | Сбой между обработкой и записью журнала — уведомление обработается повторно (принятый риск 22) | Запись журнала в транзакции решения о цене — когда путь решения примет идентификатор уведомления в фиксацию |

## Продукт и скоуп

| № | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|
| OQ-01 | B | Размер целевого клиента | Оценки объёма опираются на 10 000 SKU |
| OQ-05 | H | FBA: только репрайсинг? | Офферы FBA вне остатка |
| OQ-06 | H | Предсобранные комплекты | У `BUNDLE` нет пула |
| OQ-07 | H | Вариации Amazon и eBay | Бюджет листинга eBay |
| OQ-08 | B | Создание листингов вне скоупа — подтвердить | Сценарий eBay |
| OQ-09 | H | Акционные и зачёркнутые цены | `price_type` в `price_history` и `price_daily` |
| OQ-10 | H | B2B-цены | Модель цены |
| OQ-12 | H | Ручное утверждение цен | `HELD`, `divergence_case` |
| OQ-38 | B | Источник конкурентов для eBay и Otto | Стратегии с конкурентами на этих каналах недоступны (ADR-0007) |
| OQ-47 | H | eBay: уровень количества, часовой пояс дня, резерв и запас бюджета | Канал №3; по Р-37 проверить первым — шаг 20: документация eBay недоступна для снимка (403) — E-01…E-03 |
| OQ-48 | B | Точки измерения SLA Р-8 | Столбцы времени есть в `channel_write` |
| OQ-49 | H | Политики расхождений по умолчанию | UX |
| OQ-50 | B | Amazon: запись остатка меняет весь EU | Скоуп и ответственность — шаг 20: косвенно по документации — у `fulfillment_availability` нет селектора витрины, уведомление `LISTINGS_ITEM_MFN_QUANTITY_CHANGE` витрину не несёт; прямого утверждения нет — вопрос A-01 |
| OQ-54 | B | Продавцы с другим репрайсером | `CONTESTED` |
| OQ-56 | B | Согласие eBay: свежесть MFA, срок, фраза | Онбординг eBay |
| OQ-82 | H | Каталог стратегий: объявление требуемой полноты и тексты объяснений недоступности [Р-39] | ADR-0007 фиксирует правило, реализация — позже |

## Экономика цены

| № | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|
| OQ-15 | H | Курсы валют | Мультивалютность в схеме не активна (Р-26) |

## Право и комплаенс

| № | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|
| OQ-22 | B | Закрытие тенанта: удалять ли вечную `price_daily`, поправки, ядро intent и архив наших цен; льготный период | Процедура отказывает без явного подтверждения |
| OQ-24 | H | Резидентность данных тенантов США | Отдельные слои в регионе US |
| OQ-25 | H | Omnibus: ответственность за «прежнюю цену» | `REFERENCE`-цена |
| OQ-26 | B | Amazon: тип приложения, роли, сроки проверки | Канал №2 |
| OQ-27 | H | Сроки удаления после отзыва авторизации | Отключение аккаунта |
| OQ-28 | H | Ограничения eBay, Kaufland, Otto на хранение данных | Возможно, срок < 18 мес |
| OQ-30 | H | eBay Marketplace Account Deletion | Условие API eBay |
| OQ-52 | B | Юридический текст согласия eBay | Договор |
| OQ-59 | B | Тенант с рынками ЕС и США | Один регион данных |
| OQ-61 | B | Хостинг PostgreSQL: роли без суперпользователя, планировщик, резервные копии ≤ 7 дней | Гарантия «≤ 18 месяцев» |
| OQ-68 | B | Признаёт ли юрист суточную свёртку (min, max, первая, последняя) с поправками доказательством Omnibus после удаления сырья из горячего слоя; граница дня Europe/Berlin для AT | Р-21, Р-29 |

## API каналов

| № | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|
| OQ-31 | B | Amazon: достаточно ли `ANY_OFFER_CHANGED` | Канал №2; полнота — ADR-0007 |
| OQ-32 | B | Amazon: `patchListingsItem` или фиды | Канал №2 — **закрыт шагом 20 по документации SP-API** (страница building-listings-management-workflows-guide; в репозитории — адрес и SHA-256 загруженного текста, сам текст не хранится, в моделях снимка этого утверждения нет): сообщения `JSON_LISTINGS_FEED` не обрабатываются в порядке следования, а цена и остаток требуют порядка [Р-24] — только `patchListingsItem` (5 rps на продавца, 500 на приложение); фид — только начальная загрузка (channel-capabilities §1) |
| OQ-35 | H | eBay: лимиты и Growth Check | Канал №3 |
| OQ-36 | H | Otto: авторизация SaaS, единица лимита | Канал №4 |
| OQ-39 | H | Подтверждение применения по каналам | Критерий готовности [Р-11] |

## Архитектура

| № | Вопрос | На что влияет | Как закрыть |
|---|---|---|---|
| OQ-42 | H | Хранилище секретов и ротация | `credentials_ref`; токены адресов вебхуков [Р-40] |

## Утраченные тексты (Р-92)

Текст утрачен на шаге 15 (повреждение файла). По памяти не восстанавливается; номер не переиспользуется. Когда вопрос всплывёт в работе — вернуть строку в свой раздел с пометкой, откуда взят текст.

| № | Шаг возникновения | Что известно без восстановления по памяти |
|---|---|---|
| OQ-20 | до шага 8 (в копии шага 8 отсутствует) | В журнале — неполная строка о ручном изменении цены продавцом в кабинете канала; возможно, закрыт Р-55 (DivergenceCase) — (проверить) |
| OQ-69 | до шага 8 (в копии шага 8 отсутствует) | — |
| OQ-104 | шаг 9 | — |
| OQ-105 | шаг 9 | — |
| OQ-106 | шаг 9 | — |
| OQ-107 | шаг 9 | — |
| OQ-108 | шаг 9 | — |
| OQ-109 | шаг 9 | — |

## Шаг 24

| № | Приоритет | Вопрос | Что блокирует | Как закрыть |
|---|---|---|---|---|
| OQ-174 | H | ~~**Нет процесса, который вызывает периодические работы по расписанию:** ярусный опрос и сверка [Р-47, Р-121], проверка потерь уведомлений, сверка Amazon по кругу, выгрузка суток снимков в ClickHouse [Р-122], закрытие суток цен [Р-21] — функции есть и проверены, вызывающего по часам нет~~ **Закрыт Р-126 (шаг 25): процесс-планировщик services/scheduler — аренда работ в базе (0090), пропущенные суточные слоты по очереди, алерты отставания; остановка на сутки историю не теряет (docs/evidence/step25-scheduler.md).** | Без расписания потеря уведомления не обнаруживается, журнал снимков удаляется принудительно через 14 дней без выгрузки (история теряется), суточная свёртка не закрывается | Планировщик в сервисе пути решения: расписание по аккаунту, одна работа на аккаунт (advisory lock), метрики пропусков; включение сверки — по состоянию подписки на уведомления |
| OQ-175 | H | **Срок уведомления сверки — допущение 15 минут** (K-10, A-08) | Debounce канала дольше срока — ложные тревоги (симулятор: 9 за 72 часа при debounce 20 минут); срок сильно больше — поздний алерт о потере | Замер задержки `buy_box_changed` и `ANY_OFFER_CHANGED` на тестовых аккаунтах; срок по каналу в описании канала. **Шаг 25: допущение помечено — алерт NOTIFICATION_LOSS_SUSPECTED несёт graceBasis «ASSUMPTION (OQ-175)»; вопрос остаётся до замера** |
| OQ-176 | H | **Сверка Amazon по кругу при неизвестном лимите приложения** (A-15): лимит приложения принят равным лимиту пары — 20 ASIN за 30 секунд на всё приложение | При многих клиентах круг по офферам одного клиента растягивается на сутки и дольше: потеря не обнаруживается днями | Ответ SP-API о лимите `getCompetitiveSummary` на уровне приложения; приоритет сверки по волатильности товара. **Шаг 25: планировщик делит темп вызова между всеми аккаунтами Amazon (30 с × число аккаунтов) и поднимает AMAZON_RECONCILIATION_CIRCLE_SLOW при круге дольше 24 ч; лимит приложения по-прежнему не документирован** |
| OQ-177 | B | **Kaufland: приходит ли `buy_box_changed` при изменении цены победителя без смены победителя** (K-19) | Сверка сравнивает цену Buy Box; если уведомления о такой смене нет — каждая такая смена даёт ложное подозрение на потерю | Ответ аккаунт-менеджера; при «не приходит» — сравнивать победителя, а не цену |
| OQ-178 | H | **Omnibus: цены вне нашей истории** [Р-123]: цены до подключения оффера и выставленные в кабинете канала или другим инструментом не видны | Окно, начавшееся до нашей первой цены, — «не проверить»; цена, выставленная мимо нас после подключения, в окно не попадает — проверка может сказать «верно» при нарушении | Сверка действующей цены канала при обнаружении и обратном чтении с записью в историю цен как внешней цены (наблюдение, не наша запись) |
| OQ-179 | B | **Omnibus: показ прежней цены каналом и исключения** [Р-123]: канал не возвращает показанную покупателю «прежнюю цену»; скидки канала (купоны, акции) и правовые исключения (скоропортящиеся товары, постепенное снижение) не моделируются | Модуль проверяет объявление, а не то, что увидел покупатель; юридическая оценка остаётся на продавце | Юридическая консультация по PAngV §11; атрибуты скидки Kaufland и Amazon в снимках спецификаций |
| OQ-180 | B | ~~**История цен по применённым, а не принятым записям** (риск 28, ревью шага 24, находка 2)~~ **Закрыт шагом 26 (0096): время применения цены каналом — `tenant_data.price_history_applied` (пишет триггер завершения записи, страж проверяет подтверждение); окно Omnibus и суточная свёртка считают цену по времени применения; проверка — omnibus-applied-time.pg.test.ts.** | Omnibus учитывал цену в сутках принятия: цена, применённая после полуночи витрины, попадала в чужие сутки | Цены записей без подтверждения применения по-прежнему считаются по времени принятия — лучшее известное приближение |
| OQ-181 | H | ~~**Разбор снимков, пропущенных выгрузкой в ClickHouse** (ревью шага 24, находка 5): сутки с пропусками не отмечаются проверенными и не удаляются по сроку, но принудительно удаляются через 14 дней~~ **Закрыт шагом 25 (0092): пропуски выгрузки записываются, разбираются оператором с именем и заметкой, секция с неразобранным пропуском не отмечается проверенной; снимок без цен выгружается с валютой витрины.** | Снимок без цен («конкурентов нет») или в валюте вне EUR/USD не попадёт в историю бэктеста; при пропусках — ручной разбор до принудительного удаления | Выгрузка снимка без цен (валюта из витрины) и алерт по суткам с пропусками; объём журнала в PostgreSQL и память выгрузки — замер |

## Шаг 25

| № | Приоритет | Вопрос | Что блокирует | Как закрыть |
|---|---|---|---|---|
| OQ-182 | B | **Учётной записи оператора платформы в модели нет** — ~~разбор пишет роль разбора с именем оператора текстом~~ **сужен шагом 26 (0095): разбор — от действующей учётной записи `platform.platform_operator` со вторым фактором, «выгружен после исправления» засчитывается только после сверки с ClickHouse.** Осталось: учётные записи операторов заводит владелец вручную, административного интерфейса и журнала действий операторов нет; второй фактор заявляет инструмент разбора (как административный сервис, Р-97) | Действие оператора привязано к учётной записи, но не к проверенному входу: подтверждения второго фактора у базы нет | Вход операторов платформы у поставщика identity [Р-78] и аудит действий операторов (OQ-191) |
| OQ-183 | H | ~~**Кто замечает, что процесс планировщика не работает** (риск 29)~~ **Закрыт Р-127 (шаг 26): процесс отмечается во внешнем сервисе после каждого такта, пропуск отметки дольше периода и допуска даёт письмо владельцу (docs/evidence/step26-external-control.md).** Аккаунт сервиса не подключён — OQ-188 | Упавший процесс о себе не сообщал | Внешняя проверка отставания работ по метрикам, а не только живости процесса — OQ-189 |
| OQ-184 | B | **Импорт истории цен от канала при подключении оффера** [Р-124]: ни Kaufland, ни Amazon её не отдают (снимки спецификаций) | Полнота проверки Omnibus первые 30 суток после подключения — всегда «неполная» | Метод порта истории цен и хранение внешних цен — когда канал (eBay, Otto, новая версия API) даст историю |
| OQ-185 | B | **Выручка для выборки бэктеста** [Р-125]: строки заказов не хранятся (данные с PII, Р-4), выборка строится по оценке `PRICE_ASSUMPTION` | Выборка по цене, а не по продажам: «основная выручка» — допущение | Агрегат выручки по товару без PII из строк заказов канала (количество × цена, без покупателя) в данных тенанта |
| OQ-186 | H | ~~**Новый товар без якоря проверки входов не оценивается никогда**~~ **Закрыт Р-131 (шаг 27): без объявленной себестоимости репрайсинг не включается — это проверяет база (0098), продавец видит требование при попытке включить стратегию.** Товар без себестоимости остаётся OFF: отказов по нему нет, потому что решений по нему нет | Замкнутый круг «нет якоря — нет принятых снимков — нет истории для якоря» разомкнут требованием, а не послаблением проверки входов | Остался вопрос для товаров, у которых себестоимость есть, а истории и того же EAN нет: якорь себестоимости их закрывает (проверка ×N от себестоимости) — новых вопросов не осталось |
| OQ-187 | B | **Сколько времени товар должен наблюдаться, чтобы волатильность считалась известной** (шаг 26, проба яруса): сейчас — сутки наблюдений, и до них товар опрашивается не реже раза в час | Допущение продукта: при каталоге 10 000 новых товаров проба — 2,8 запроса в секунду, это в бюджете 10 rps, но при больших каталогах понижение по бюджету заберёт пробу | Замер на живом канале: за какое время видна волатильность товара; связать с ограничением продукта Р-47 |
| OQ-188 | H | **Аккаунта внешнего сервиса контроля нет** [Р-127]: адрес отметки, период, допуск и получатель письма настраиваются в сервисе; по документации ответ 200 не подтверждает запись отметки | Неверно настроенная отметка обнаруживается только письмом при первом пропуске | Завести аккаунт (ЕС), настроить период 2 минуты и допуск 3 минуты, проверить письмо остановкой процесса; второй канал уведомления |
| OQ-189 | B | **Внешняя проверка видит только живость процесса** [Р-127]: отставание работ по-прежнему сообщает сам планировщик | Планировщик, который работает, но не успевает, снаружи выглядит здоровым | Экспорт метрик наружу и правило тревоги по `repracer_scheduler_job_last_lag_seconds`; или вторая проверка «выгрузка суток была сегодня» |
| OQ-190 | B | ~~**Развёртывания диспетчера записей и приёмника уведомлений в репозитории нет**~~ **Закрыт шагом 27:** точки входа `services/pricing-worker/src/main.ts` и `services/notification-receiver/src/main.ts`, развёртывания `deploy/worker` и `deploy/notification-receiver`, общий слой процессов `packages/service-runtime` (секреты файлами, `/healthz`, метрики) | Процессы, без которых цены не уходят в канал, разворачиваются так же, как планировщик | Живой очереди SQS по-прежнему нет (OQ-167): приёмник проверен на модели очереди, не на аккаунте Amazon |
| OQ-191 | B | **Действия операторов платформы не в аудите** (OQ-182): разбор пропуска пишется в свою таблицу, `audit_event` — про действия участников тенанта | Действия оператора платформы нельзя посмотреть вместе с остальными административными действиями | Журнал действий операторов платформы или расширение `audit_event` на платформенные действия |
| OQ-192 | B | ~~**Поправка закрытых суток при позднем подтверждении применения**~~ **Закрыт шагом 27 (0101):** отметка времени применения ставится всегда, а закрытые сутки пересчитывает `maintenance.correct_closed_price_days` строкой-поправкой [Р-29] — своя таблица `tenant_data.price_daily_system_correction` (у системного пересчёта нет человека-автора, Р-97), итог — представление `price_daily_effective`; сутки, из которых ушла единственная цена, становятся пустыми (изменений 0) | Доказательство Omnibus говорит о сутках, в которых покупатель видел цену; сама свёртка неизменяема | Поправка человека — последнее слово: пересчёт день, который правил человек, не трогает |
| OQ-193 | B | ~~**Провалившаяся выгрузка суток не повторяется внутри суток**~~ **Закрыт Р-133 (шаг 28, 0104):** вид повтора — свойство работы в базе; у внутренних работ (выгрузка, закрытие суток, секции, удаление по сроку) пауза растёт от минуты до часа, у работ канала — от периода работы | Отказ аналитического слоя на четверть часа стоит пяти попыток, а не одной | Осталось: порог, после которого повторы прекращаются и остаётся только алерт, не задан — работа повторяется до суток |
| OQ-194 | B | ~~**Кто замечает молчание диспетчера записей и приёмника**~~ **Закрыт шагом 28:** у обоих процессов есть отметка во внешнем сервисе [Р-127], как у планировщика; без адреса отметки процесс не стартует, пока её не выключили явно | Остановленный хост виден снаружи, а не только по ждущим записям | Аккаунта внешнего сервиса по-прежнему нет (OQ-188): отметка проверена только кодом и развёртыванием, не живым сервисом |
| OQ-195 | B | **Импорт не переводит валюты** [Р-61, Р-134]: строка, у которой валюта не совпадает с валютой оффера, перечисляется как несопоставленная | Продавец, у которого закупка в USD, а витрина в EUR, импортом себестоимость не заведёт — только руками | Решение владельца: переводить по курсу ЕЦБ на момент импорта (и хранить курс в профиле) либо принимать себестоимость в её валюте, как разрешает Р-61, и переводить при расчёте пола |
| OQ-196 | B | **Порог окна массовой правки — 5 предложений за 10 минут** [Р-135]: число выбрано как ограничение продукта, на живых продавцах не проверялось | Продавец, честно правящий десяток позиций подряд без второго фактора, получит отказ на шестой | Замер на первых клиентах: сколько предложений правят руками за раз; при необходимости — порог на тенанта или подсказка «войдите со вторым фактором» вместо отказа |

| OQ-197 | B | ~~**Комиссия из файла продавца помечается тарифом**~~ **Закрыт шагом 29 (0106, Р-138):** у неё свой источник `SELLER_DECLARED`, версии тарифа у него не бывает; обе оценки живут рядом, пол маржи считается по большей, экран границ показывает обе с источниками | Число из Excel продавца больше не выдаёт себя за тариф репозитория [Р-32] | Осталось: фактических выплат канала у нас нет, поэтому «расхождение с выплатами» показать не из чего — сверяются только объявленное и тарифное |
| ~~OQ-197~~ | — | **прежняя формулировка:** комиссия из файла продавца помечается тарифом (ревью шага 28, замечание 19): `channel_data.fee_estimate` знает три источника — `CHANNEL_API`, `FEE_SCHEDULE`, `CALIBRATED`; число, набранное продавцом в Excel, ложится в `FEE_SCHEDULE`, хотя Р-32 называет тарифом версионируемые таблицы репозитория | Оценка комиссии, из которой считается пол маржи, не отличима по источнику от тарифа, который вёл бы разработчик | Решение владельца: отдельный источник `SELLER_DECLARED` (миграция и порядок выбора в 0051) либо отказ принимать комиссию из файла вовсе. Пока: версия оценки называет пакет импорта (`seller-import:<id>`), срок — 90 дней |
| OQ-198 | B | **Отметка во внешнем сервисе не проверена живым запуском** (ревью шага 28, замечание 17): проверка развёртывания поднимает все три процесса с `REPRACER_*_HEARTBEAT=off`, потому что аккаунта сервиса нет (OQ-188) | Что процесс стартует С отметкой и что compose монтирует нужный файл, проверено только чтением текста | Завести аккаунт внешнего сервиса (OQ-188) и снять `off` в проверке развёртывания либо поднять в CI заглушку сервиса отметок |
| OQ-199 | B | **Объявленный предел импорта (200 000 строк) не измерен** (ревью шага 28, замечание 21): живой прогон снял время на 10 000 строк (12 с), а в разборе и применении есть квадратичные места (поиск дубля, поиск области оффера, по одной вставке на строку) | Продавец с каталогом в сотни тысяч позиций упрётся не в предел базы, а во время | Замер на 200 000 строк; при необходимости — индексы по ключам в памяти, пакетная вставка и снижение `row_count` до измеренного числа |
| OQ-200 | B | ~~**Кодировка файла не определяется**~~ **Закрыт шагом 29:** кодировка определяется по BOM и по содержимому (строгая проверка UTF-8, узнавание UTF-16 по нулевым байтам, иначе Windows-1252), показывается на экране рядом с разделителем, и её можно выбрать вручную — при неуверенности продавца спрашивают | Немецкая выгрузка в Windows-1252 разбирается, а не отвергается целиком | Осталось: список кодировок — четыре; экзотическое (ISO-8859-15, UTF-32) не поддержано и отвергается понятным отказом |
| ~~OQ-200~~ | — | **прежняя формулировка:** кодировка файла не определяется (ревью шага 28, замечание 22): `readTable` декодирует как UTF-8, экран показывает формат и разделитель, но не кодировку | Немецкая выгрузка в Windows-1252 с «€» разберётся как `COST_NOT_A_NUMBER` целиком, UTF-16 будет отвергнут как «не таблица» | Определять кодировку по BOM и по доле неверных последовательностей, показывать её на экране рядом с разделителем и давать выбрать вручную — как у колонок |
| OQ-201 | B | **Предпросмотр стратегии по выборке** [Р-136, Р-125]: на каталоге целевого клиента экран считает решение по 500 предложениям из 10 000, а назначается стратегия на все | Продавец видит поведение выборки, а не каждого предложения; непопавшее в выборку проверяет только база (0082) при записи | Замер на первых клиентах: достаточно ли выборки, чтобы заметить неудачную стратегию; при необходимости — фоновое задание с полным предпросмотром [Р-125] |
| OQ-202 | B | **Выгрузка доказательной истории ограничена 100 000 строк** [Р-136]: каталог целевого клиента за 30 суток — 300 000 строк (28 МБ) | Продавец, которому нужно доказательство по всему каталогу (например, по запросу проверяющего), получит отказ и должен выгружать частями | Решение владельца: потоковая выгрузка файлом (не JSON-ответом) или фоновая подготовка архива со ссылкой |
| OQ-203 | B | **Быстрый прогон CI не покрывает мутационную проверку [Р-95], брокер, ClickHouse и развёртывание** [Р-137]: три теста и четыре проверки идут только в полном прогоне | Снятая защита, поломка брокера или аналитического слоя видны не на коммите, а при слиянии в main | Следить за долей красных полных прогонов; если она вырастет, переносить проверки обратно в быстрый или поднимать инфраструктуру быстрее |
| OQ-204 | B | **Слабые утверждения в тестах, не исправленные шагом 29** (ревью тавтологий, задача E): `scheduler-live.pg.test.ts:156,174,186` — `jobOf` отдаёт заглушку `{runs: 0, failed: 0}`, поэтому «провалов нет» верно и когда работа не запускалась; `backtest.test.ts:35` — ранги берутся из фактического результата, `:70` — утверждается длина списка допущений; `console-step21.test.ts:44` — принимаются оба исхода превью; `console.test.ts:60-61` — `every` истинно на пустом массиве, `:159` — `balanced` считает сам продукт; `preview.test.ts:80` и `console-model.test.ts:52` — длина строки вместо её содержания; мета-тесты «у каждого механизма есть утверждение» (`background-live.pg.test.ts:357`, `scheduler-live.pg.test.ts:211`) проверяют наличие имени, а не наличие проверки | Такой тест зеленеет и тогда, когда проверяемого поведения нет [Р-94] | Разбирать по одному вместе с тем кодом, который они проверяют; мета-тесты — считать выполненные утверждения, а не зарегистрированные имена |
| OQ-205 | B | **Живой прогон консоли в быстром задании CI** [Р-136, Р-137]: он занимает около минуты и включает применение импорта 10 000 строк (8,3 с при пороге экрана 10 с) | Запас до красной сборки — 19 %: медленный раннер CI покрасит быстрый прогон без изменения кода | Замерить разброс на раннерах CI; либо поднять порог с объяснением, либо перенести живой прогон в полное задание, оставив в быстром его короткую версию |
