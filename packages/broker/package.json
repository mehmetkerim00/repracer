{
  "name": "@repracer/broker",
  "version": "0.1.0",
  "private": true,
  "description": "Kafka-compatible broker (Redpanda, Р-24, ADR-0005): idempotent keyed producer, per-partition sequential consumer, outbox relay from PostgreSQL in commit order per write scope.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@confluentinc/kafka-javascript": "1.10.1",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@types/pg": "8.23.1"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test test/*.test.ts"
  }
}
