{
  "name": "@repracer/console-model",
  "version": "0.1.0",
  "private": true,
  "description": "Seller console view models (Р-67): product list, decision trace, rejected decisions, bounds with the floor breakdown to the cent, kill switch plan. Pure functions over stand state; no invented fields — missing data is reported as a gap.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@repracer/pricing-model": "0.1.0",
    "@repracer/pricing-pipeline": "0.1.0",
    "@repracer/cost-import": "0.1.0",
    "@repracer/stock-sync": "0.1.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test src/*.test.ts"
  }
}
