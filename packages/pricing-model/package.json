{
  "name": "@repracer/pricing-model",
  "version": "0.1.0",
  "private": true,
  "description": "Shared pricing types, money and margin math, reason codes with explanations. Pure, no I/O.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@repracer/channel-port": "0.1.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test src/*.test.ts"
  }
}
