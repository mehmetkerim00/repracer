{
  "name": "@repracer/service-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Shared runtime of long-running processes (Р-129, OQ-190): configuration with file secrets, channel credentials, the account directory, the JSON log sink and the metrics/healthz endpoint.",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "dependencies": {
    "@repracer/channel-port": "0.1.0",
    "@repracer/pricing-store-pg": "0.1.0",
    "pg": "8.23.0"
  },
  "devDependencies": { "@types/pg": "8.23.1" },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/*.test.ts"
  }
}
