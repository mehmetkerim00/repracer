{
  "name": "@repracer/stock-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Stock synchronisation (Р-6, Р-152, Р-153): sources, published quantity over the shared pool with a per-channel buffer, reservations from channel orders, quantity writes and the stock screens' data.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@repracer/channel-port": "0.1.0",
    "@repracer/pricing-pipeline": "0.1.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/*.test.ts"
  }
}
