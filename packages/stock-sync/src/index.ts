/**
 * Синхронизация остатков (шаг 35) [Р-6, Р-15, Р-25, Р-152, Р-153].
 *
 * Мы НЕ владелец остатка [Р-6]: остаток приходит из источника продавца (файл → внутренний пул, или Inbound API), а в каждый
 * канал уходит ОБЩИЙ пул минус буфер канала. Что здесь: правило публикуемого количества (одно на все каналы, как в
 * docs/domain-model.md §3.27), порт хранилища остатков и конвейер «заказы канала → резервации → пересчёт → записи».
 * Расчёт не читает канал: ловушки каналов (остаток Amazon EU общий на регион, у Kaufland общий на id_offer) — свойство
 * единицы записи и показываются продавцу ДО включения [Р-153], а не обнаруживаются после.
 */
export * from './published.ts';
export * from './store.ts';
export * from './pipeline.ts';
export * from './memory-store.ts';
export * from './import.ts';
