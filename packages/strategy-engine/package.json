{
  "name": "@repracer/strategy-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Pricing strategies: accepted snapshot, cost and bounds in, PriceIntent with class and structured reason out. Pure functions, no I/O.",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "dependencies": { "@repracer/channel-port": "0.1.0", "@repracer/pricing-model": "0.1.0" },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test src/*.test.ts"
  }
}
